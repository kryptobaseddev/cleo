# v2026.4.110 Release Plan — Substrate + PSYCHE Wave 0

**Target tag**: v2026.4.110
**Release theme**: "Substrate for PSYCHE" — worktree-backend core integration + agent registry fix + Wave 0 PSYCHE research artifacts
**Baseline HEAD**: 290035c25 (v2026.4.109 shipped: T1150 T-MSR Wave 3+4 + ADR-054 migration system)
**Owner release window**: 2026-04-22 onward
**Decisions anchoring this release**: D023 (SDK-first), D024 (neurosurgery), D027 (scope), D028 (CORE/CLI boundary), **D029 (env-paths worktree canon — SUPERSEDES D022+D025)**, **D030 (native worktree impl — SUPERSEDES D026 worktrunk-wrap)**

> **2026-04-22 corrections absorbed**: (a) E_AGENT_NOT_FOUND unblocked — spawn operational. (b) D029 env-paths `~/.local/share/cleo/worktrees/<hash>/<id>/` replaces `.cleo/.trees/`. (c) Wave 0 children are real T1209/T1210/T1211. (d) **T1161 is NATIVE implementation, NOT worktrunk wrapper** (D030) — CLEO already ships complete native worktree stack (cant/worktree.ts + cleo-git-shim + branch-lock + sentient/baseline+merge + classify); worktrunk's two missing features (hooks, include-file) lifted as native additions. (e) All 5 Leads parallel from start.

> **Corrections landed 2026-04-22 (from parallel-agent coordination)**:
> - **E_AGENT_NOT_FOUND unblocked** — `cleo orchestrate spawn T1161 --tier 0` now returns fully-resolved prompt + auto-provisions worktree + classifies cleo-dev at 0.90 confidence. Lead B (T1210) loses critical-path urgency.
> - **Worktree canon = env-paths live path** `~/.local/share/cleo/worktrees/<projectHash>/<taskId>/` per D029. D025 `.cleo/.trees/` superseded.
> - **Wave 0 children now real task IDs**: T1209 (audit), T1210 (registry), T1211 (glossary). Dotted pseudo-IDs from original plan were rejected by task store — fixed.
> - **Revised dep graph**: Wave α (C+E docs) + Wave β (A+B code) fully parallel from start, Lead D last after A's SDK.

---

## Part 1 — Scope (what's IN, what's OUT)

### IN SCOPE for v2026.4.110

| ID | Title | Size | Worktree |
|----|-------|------|----------|
| T1140 | Meta-A: worktree-by-default spawn prompts | medium | `~/.local/share/cleo/worktrees/<hash>/T1140` |
| T1210 | packages/agents/ cleanup + peer_id SSoT | medium | `~/.local/share/cleo/worktrees/<hash>/T1210` |
| T1161 | Worktrunk core backend (`packages/worktree-backend/`) | large | `~/.local/share/cleo/worktrees/<hash>/T1161` |
| T1209 | PSYCHE source deep-audit → PSYCHE-SOURCE-NOTES.md | small | `~/.local/share/cleo/worktrees/<hash>/T1209` |
| T1211 | PSYCHE↔CLEO terminology glossary | small | `~/.local/share/cleo/worktrees/<hash>/T1211` |

**Five Lead-spawnable work items.** Can run in parallel once dependencies resolved.

### EXPLICITLY OUT OF SCOPE (deferred to later releases)

| Deferred to | Work |
|-------------|------|
| v2026.4.111 | Wave 1 (T1076 user_profile) + Wave 2 (T1081 peer_id) schema work in parallel |
| v2026.4.112 | Wave 3 (T1082 dialectic) + Wave 9 (T1149 Conduit A2A) |
| v2026.4.113 | Wave 4 (T1083 multi-pass) + Wave 5 (T1145 deriver queue) |
| v2026.4.114 | Wave 6 (T1146 dreamer) + Wave 7 (T1147 reconciler — absorbs T1139) |
| v2026.4.115 | Wave 8 (T1148 peer-card) |
| **v2026.5.0** | **"CLEO Sentient v1"** — integration consolidation + MCP adapter proof |

### Previously filed, now DONE — do NOT re-schedule

- T1141 Meta-B migration sanitizer — shipped ✅ (part of T1150 T-MSR)
- T1150 T-MSR Migration System Remediation — shipped ✅ (ADR-054 hybrid path A+)

### Out-of-band but may intersect

- **v2026.4.109** cut by parallel CLI-improvements agent (already merged to main). No rebase needed — I baseline against their HEAD.
- If the CLI agent continues into .110 with additional CLI-shaped work, coordinate via Conduit thread (pre-T1149) or GitHub issue comment.

---

## Part 2 — Lead Spawn Plan (worktree-isolated)

Per D022+D025+D026: **every Lead works in its own `.cleo/.trees/<slug>` worktree**. Orchestrator (me) NEVER touches main. Cherry-pick consolidation only after IVTR-green on each branch.

### Lead A — Native Worktree Backend SDK (T1161)

- **Worktree**: `~/.local/share/cleo/worktrees/<hash>/T1161`
- **Branch**: `feat/T1161-worktree-backend`
- **Persona**: cleo-dev (classify.ts auto-routes at 0.90 confidence)
- **Model**: sonnet
- **Scope per D030**: NATIVE implementation. ZERO worktrunk dependency. Formalize existing scattered worktree logic into one SDK surface + add two missing features (hooks + include-file) CLEO-natively.
- **Deliverables**:
  - `packages/worktree-backend/` new peer-to-core package per D028 (name kept — implementation is native)
  - SDK primitives: `createWorktree`, `destroyWorktree`, `listWorktrees`, `pruneWorktrees` — wrap existing `packages/cant/src/worktree.ts` + integrate `branch-lock.ts` + `cleo-git-shim` + `sentient/baseline.ts` + `sentient/merge.ts` + `classify.ts`
  - Declarative hooks framework (post-create, post-start) at `.cleo/worktree-hooks.json` OR CANT-declared in agent `.cant` files — lifted from worktrunk pattern, implemented native
  - `.cleo/worktree-include` glob pattern for auto-copying `.env*`, `.npmrc`, `.cleo/config.json` into new worktrees — lifted from worktrunk `.worktreeinclude` pattern, CLEO-owned
  - All paths resolved via `packages/core/src/paths.ts` + `platform-paths.ts` + `env-paths` per D026
  - Contracts types in `packages/contracts/src/operations/worktree.ts`
  - Deprecation-safe: existing callers of `packages/cant/src/worktree.ts` functions keep working via re-export shim
- **Baseline**: `pnpm install && pnpm run build && pnpm run test` green before agent handoff
- **Return**: commit sha, changed file list, manifest key_findings
- **Blocks**: Lead D (T1140 consumes this)

### Lead B — Agent Registry Fix (T1210)

- **Worktree**: `~/.local/share/cleo/worktrees/<hash>/T1210`
- **Branch**: `fix/T1144-agent-registry-ssot`
- **Persona**: cleo-dev
- **Model**: sonnet
- **Deliverables**:
  - Consolidate `packages/agents/cleo-subagent/cleo-subagent.cant` ↔ `packages/agents/seed-agents/cleo-subagent.cant` (ONE canonical path)
  - Define `PeerIdentity` type in `packages/contracts/src/peer.ts`
  - Wire `packages/cant/src/native-loader.ts` to produce `PeerIdentity[]` from seed-agents
  - Fix `E_AGENT_NOT_FOUND: cleo-db-lead not found` regression (verified present on HEAD 290035c25)
  - `cleo agents list` SDK-first (registry reachable via programmatic dispatch, not only CLI)
- **Return**: commit sha, changed file list, E2E test that `cleo orchestrate spawn T<dummy> --tier 1` now resolves without E_AGENT_NOT_FOUND
- **Blocks**: Lead D (T1140 spawn depends on registry working)

### Lead C — PSYCHE Source Audit (T1209)

- **Worktree**: `~/.local/share/cleo/worktrees/<hash>/T1209`
- **Branch**: `docs/T1144-psyche-source-notes`
- **Persona**: cleo-historian (research) OR cleo-subagent
- **Model**: haiku (research-heavy, read-only)
- **Deliverables**:
  - `.cleo/agent-outputs/T1075-psyche-integration-plan/PSYCHE-SOURCE-NOTES.md`
  - Per-file summary of `/mnt/projects/honcho/src/`: `models.py`, `schemas/internal.py`, `dialectic/{core,chat,prompts}.py`, `crud/{peers,peer_card,representation,session,workspace}.py`, `deriver/`, `dreamer/`, `reconciler/`, `llm/`
  - Explicit CLEO target mapping for each PSYCHE file
- **Baseline**: N/A (docs-only; no build needed)
- **Return**: manifest with files written, word count, key_findings summary
- **Blocks**: nothing — parallel

### Lead D — Worktree-Default Spawn Prompt (T1140)

- **Worktree**: `~/.local/share/cleo/worktrees/<hash>/T1140`
- **Branch**: `feat/T1140-worktree-default-spawn`
- **Persona**: cleo-prime (orchestrator-facing changes)
- **Model**: sonnet
- **Deliverables**:
  - `packages/core/src/orchestrate/spawn-prompt.ts` emits `## Worktree Setup (REQUIRED)` section by default — calling worktree-backend SDK (Lead A's work)
  - `--no-worktree` flag for explicit opt-out (logged to audit)
  - Baseline-validator gating: agent receives worktree path only after CANT-declared baseline green
  - Context-isolation text: "authorized only within {{ worktreePath }}"
  - Updates to CLEO-INJECTION.md to document the new default
- **Baseline**: `pnpm install && pnpm run build && pnpm run test` green
- **Return**: commit sha, test showing spawn prompt contains worktree section
- **Depends on**: Lead A (SDK) + Lead B (registry)

### Lead E — PSYCHE↔CLEO Glossary (T1211)

- **Worktree**: `~/.local/share/cleo/worktrees/<hash>/T1211`
- **Branch**: `docs/T1144-psyche-glossary`
- **Persona**: cleo-historian OR cleo-subagent
- **Model**: haiku
- **Deliverables**:
  - `.cleo/agent-outputs/T1075-psyche-integration-plan/GLOSSARY.md`
  - workspace ≡ project; peer ≡ CANT agent; session ≡ session; representation ≡ theory-of-mind (new `brain_representations` table); collection ≡ `brain_observations` grouping; document ≡ `brain_learnings`; message ≡ session turn (new `session_messages` table)
- **Return**: manifest with glossary written, cross-ref count
- **Blocks**: nothing — parallel

---

## Part 3 — Dependency Graph + Parallel Waves

```
    ALL IN PARALLEL FROM START (spawn is unblocked — D029):
    ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐
    │  Lead C     │  │  Lead E     │  │  Lead A     │  │  Lead B     │
    │ PSYCHE     │  │ Glossary    │  │ Worktrunk   │  │ Agent       │
    │ Audit       │  │             │  │ Backend     │  │ Registry    │
    │ (T1209)     │  │ (T1211)     │  │ (T1161)     │  │ (T1210)     │
    └──────┬──────┘  └──────┬──────┘  └─────┬───────┘  └──────┬──────┘
           │                │               │                 │
           │                │               └────────┬────────┘
           │                │                        │
           │                │               Only A must complete for D:
           │                │                        ▼
           │                │              ┌─────────────┐
           │                │              │  Lead D     │
           │                │              │ Worktree-   │
           │                │              │ Default     │
           │                │              │ Spawn       │
           │                │              │ (T1140)     │
           │                │              └──────┬──────┘
           │                │                     │
           └────────────────┴─────────────────────┘
                     All five complete → release cut
```

**Revised cherry-pick order (all parallel, ordered on merge)**: C → E → B → A → D (docs first even if code lands earlier — keeps main history telling a coherent story).

- Leads C + E can land immediately on completion (docs-only, no main regression risk)
- Lead B lands second (fixes registry regression cleanly before new consumers arrive)
- Lead A lands third (new `packages/worktree-backend/` package)
- Lead D lands fourth (consumes A's SDK; may require rebase if other agent's CLI work touched dispatch layer)

**Lead D-only dependency**: Lead D needs A's SDK published + B's registry clean. D can start in parallel once A has draft SDK surface (even pre-tests-green); D finalizes after A's IVTR-green.

---

## Part 4 — Coordination Protocol

### With the CLI-improvements agent (if still active)

- Read their latest commits via `git log --oneline main..origin/main` before each consolidation
- Their work touches `packages/cleo/src/` CLI domains; my Lead D also touches `packages/cleo/src/dispatch/` but at the orchestrate layer, not CLI command definitions — low conflict surface
- If conflict: serialize — let their commit land, rebase my Lead D worktree before cherry-pick
- **Pre-T1149 (no Conduit yet)**: communicate via GitHub issue comment on the active PR, or via dropping a note in `.cleo/agent-outputs/COORDINATION-V2026.4.110.md`

### Between Leads

- Each Lead reports back via `cleo manifest show <taskId>` after completion
- Leads A and B complete → I cherry-pick to main → rebase worktree substrate-spawn-default → dispatch Lead D
- Leads C and E are docs-only, cherry-pick immediately on completion

### Orchestrator (me) in main worktree — read-only rules

- NO source-code writes to main during .110 cycle
- Allowed: reading files, reading manifests, updating `.cleo/agent-outputs/*.md` planning docs (inside NEXT-SESSION-HANDOFF and this release plan)
- Allowed: `cleo` mutations (task updates, decisions, memory observations)
- Cherry-pick only after Lead returns with green-test evidence

---

## Part 5 — Release Cut Acceptance

v2026.4.110 cut CANNOT happen until ALL of these green:

1. ✅ All 5 Lead tasks complete (IVTR-green in their worktrees)
2. ✅ Cherry-picks landed in order (C → E → B → A → D) — main is linear
3. ✅ `pnpm biome ci .` repo-wide (strict mode)
4. ✅ `pnpm run build` root (full dep graph)
5. ✅ `pnpm run test` repo-wide (ZERO new failures vs .109 baseline)
6. ✅ `cleo check canon` passes (canon drift check)
7. ✅ `cleo admin smoke --provider claude` passes (spawn smoke test — worktree path populated)
8. ✅ `git diff v2026.4.109..HEAD --stat` matches cherry-pick plan (no stray changes)
9. ✅ CHANGELOG.md entry documents: worktrunk backend, worktree-default spawn, agent registry fix, Wave 0 research artifacts
10. ✅ Version bump across ALL packages to 2026.4.110 (sync per T1184 install-scenarios pattern)

Tag + push + npm publish flow per existing `cleo release ship` pattern. Do NOT reuse the .110 tag if aborted (D016 from earlier — never reuse tags).

---

## Part 6 — Post-Release Observations to Capture

When .110 is green, record these memory entries:

- **Observation**: How long did the 5-Lead-parallel pattern actually take vs estimate
- **Pattern**: Conflicts encountered between Lead D and main during rebase (informs T1149 Conduit A2A design)
- **Learning**: Any worktrunk integration surprises (informs T1161 follow-on work)
- **Decision (candidate)**: If `.cleo/.trees/` layout proves friction-free, promote D025 from technical to canonical

---

## Part 7 — Links

- Full PSYCHE integration plan: `.cleo/agent-outputs/T1075-psyche-integration-plan/PLAN.md`
- Prior session handoff: `.cleo/agent-outputs/NEXT-SESSION-HANDOFF.md`
- Master 4-pillar anchor: T1151 (under T942 sentient architecture)
- Migration system (done): ADR-054 hybrid path A+, T1150 T-MSR Wave 3+4
- 6-DB topology: `docs/architecture/DATABASE-ERDS.md`, `docs/adr/ADR-054-migration-system-hybrid-path-a-plus.md`

---

*This plan is authoritative for v2026.4.110. If scope is cut, it's cut by removing Leads D or E first (D because T1140 has a sane-but-ugly fallback to hand-written worktree sections; E because glossary can ship as .111). Leads A, B, C are non-negotiable.*
