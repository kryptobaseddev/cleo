{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "tasks.find response",
  "description": "Array of minimal task objects (id, title, status only)",
  "allOf": [
    {
      "$ref": "../common-success.schema.json"
    },
    {
      "properties": {
        "data": {
          "type": "object",
          "description": "Array of minimal task objects (id, title, status only)"
        }
      }
    }
  ]
}
