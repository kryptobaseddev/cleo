---
id: T10447
tasks: [T10447]
kind: docs
summary: "Record audited main branch protection verification for Saga T10431."
---

Adds a release note documenting the exact GitHub API commands and observed branch protection state for `kryptobaseddev/cleo` `main` during T10447.

Confirmed by API:

- strict required status checks are enabled;
- required contexts are `CI`, `Lockfile Check`, and `Contracts Dep Lint`;
- pull request review approvals required count is `0`;
- restrictions are `null`;
- admins are not enforced;
- force pushes and deletions are disabled;
- repository `allow_auto_merge=false`.

The branch protection response did not expose `merge_queue` or `merge_queue_enabled`, so the release note intentionally does not claim an API-confirmed merge queue value.
