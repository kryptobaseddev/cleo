---
id: T10302
tasks: [T10302]
kind: test
summary: integration regression — chokepoint auto-recovery on malformed brain.db
---

Adds `packages/core/src/store/__tests__/auto-recovery-chokepoint.test.ts` — the chokepoint-level regression test required by Epic T10286 (Saga T10281 SG-BRAIN-DB-RESILIENCE). Complements T10303's unit tests by driving the real `getBrainDb()` chokepoint against a project root containing a malformed brain.db fixture and asserting end-to-end recovery: corrupt file moved to `.cleo/quarantine/brain-malformed-<iso>/`, freshest valid snapshot restored, chokepoint retry succeeds, canonical `brain.auto-recovery` Pino warn() fires once. Four scenarios covered: system-snapshot restore, newest-first freshness ranking across multiple snapshots, no-snapshot failure surfacing, VACUUM-INTO snapshot fallback. Fixture matches the live T10260/T10265 incident signature (sqlite_schema B-tree page corruption → `ERR_SQLITE_ERROR errcode=11`). Suite runtime ~2.9s — stays in the default vitest matrix. Saga: T10281; Epic: T10286.
