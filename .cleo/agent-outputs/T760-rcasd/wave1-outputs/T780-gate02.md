# T780 GATE-02: Widen Task.acceptance to (string | AcceptanceGate)[]

**Status**: complete
**Date**: 2026-04-16
**Epic**: T768 / T760

## Summary

Widened `Task.acceptance` from `string[]` to `(string | AcceptanceGate)[]` across the
contracts and core packages. Zero schema migration — the `acceptance_json` column
already holds arbitrary JSON. All existing tests continue to pass.

## Files Modified (9 modified, 1 test file extended)

### packages/contracts/src/task.ts
- Added import of `AcceptanceGate` from `./acceptance-gate.js`
- Added `AcceptanceItem = string | AcceptanceGate` union type (exported)
- Widened `Task.acceptance?: AcceptanceItem[]` (was `string[]`)
- Widened `TaskCreate.acceptance?: AcceptanceItem[]` (was `string[]`)

### packages/contracts/src/task-record.ts
- Imported `AcceptanceItem` from `./task.js`
- Widened `TaskRecord.acceptance?: AcceptanceItem[]` (was `string[]`)

### packages/contracts/src/acceptance-gate-schema.ts
- Added `acceptanceItemSchema = z.union([z.string(), acceptanceGateSchema])`
- Added `acceptanceArraySchema = z.array(acceptanceItemSchema)` for full field validation
- Exported `AcceptanceItemInput` and `AcceptanceArrayInput` inferred types

### packages/contracts/src/index.ts
- Exported `AcceptanceItem` from `./task.js`
- Exported `acceptanceItemSchema`, `acceptanceArraySchema` from `./acceptance-gate-schema.js`
- Exported `AcceptanceItemInput`, `AcceptanceArrayInput` types

### packages/core/src/tasks/add.ts
- Imported `AcceptanceItem` from `@cleocode/contracts`
- Widened `AddTaskOptions.acceptance?: AcceptanceItem[]`
- Fixed `.trim()` map calls: `(a) => (typeof a === 'string' ? a.trim() : a)` in both
  the dry-run preview path and the live task creation path

### packages/core/src/tasks/update.ts
- Imported `AcceptanceItem` from `@cleocode/contracts`
- Widened `UpdateTaskOptions.acceptance?: AcceptanceItem[]`

### packages/core/src/tasks/enforcement.ts
- Imported `AcceptanceItem` from `@cleocode/contracts`
- Widened `AddTaskEnforcementOptions.acceptance?: AcceptanceItem[]`
- Widened `UpdateTaskEnforcementOptions.acceptance?: AcceptanceItem[]`
- Widened `AcceptanceEnforcement.checkMinimumCriteria(criteria: AcceptanceItem[], ...)`
- Widened `checkMin` function signature to `AcceptanceItem[]`

### packages/core/src/tasks/epic-enforcement.ts
- Imported `AcceptanceItem` from `@cleocode/contracts`
- Widened `validateEpicCreation` options: `acceptance?: AcceptanceItem[]`

### packages/core/src/store/converters.ts
- Imported `AcceptanceItem` from `@cleocode/contracts`
- Added type parameter: `safeParseJsonArray<AcceptanceItem>(row.acceptanceJson)`

## Contract Test Added

Added `acceptanceArraySchema — mixed (string | AcceptanceGate)[] (T780)` describe block
to `packages/contracts/src/__tests__/acceptance-gate.test.ts` with 6 tests:

1. Round-trips mixed array `["legacy 1", TestGate, "legacy 2"]` — asserts length=3,
   string pass-through, gate fully parsed, equality of each element
2. Accepts array of only legacy strings
3. Accepts array of only typed gates
4. Accepts empty array
5. Rejects element with invalid gate kind (unknown-kind object fails both branches)
6. Type-checks: parsed result assignable to `AcceptanceItem[]`

## Quality Gates

- `pnpm --filter @cleocode/contracts run build` — clean (zero type errors)
- `pnpm --filter @cleocode/core run build` — clean (zero type errors)
- `pnpm --filter @cleocode/contracts run test` — 91 passed (83 pre-existing + 8 new)
- Pre-existing failures: 1 unrelated flaky test (`temporal-supersession.test.ts`
  ENOTEMPTY race condition, also present on unmodified HEAD with 13 failures total)
- Existing test regression count: 0

## Guardrails Respected

- DB `acceptance_json` column unchanged — no migration
- `cleo verify --run` not implemented (T781)
- Migration heuristic not added (T782)
- No existing test modified; only 1 new describe block appended
