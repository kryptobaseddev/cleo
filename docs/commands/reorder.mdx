---
title: "cleo reorder"
description: "Change task position within sibling group (move to position, before/after, top/bottom)"
icon: "sort"
---

{/* AUTO-GENERATED by generate-command-docs.sh - DO NOT EDIT MANUALLY */}


# cleo reorder

Change task position within sibling group (move to position, before/after, top/bottom)


## Synopsis

```bash
cleo reorder TASK_ID [OPTIONS]
```

## Options

| Option | Description |
|--------|-------------|
| `--position` | New position index |
| `--before` | Insert before task ID |
| `--after` | Insert after task ID |
| `--top` | Move to top of list |
| `--bottom` | Move to bottom of list |
| `--format` | Output format: `text`, `json` |
| `--quiet` | Suppress output, return only essential data |

<Note>This command supports JSON output via `--json` flag for LLM agent integration.</Note>

## Examples

<CodeGroup>

```bash Basic Usage
cleo reorder
```

```bash JSON Output
cleo reorder --json
```

</CodeGroup>

## Exit Codes

| Code | Meaning | Recoverable |
|------|---------|:-----------:|
| `0` | Success | No |
| `2` | Invalid arguments | No |
| `4` | Resource not found | No |

## See Also

- [Commands Index](/commands/commands) - Full command reference
- [add](/commands/add) - Create new tasks
- [update](/commands/update) - Modify tasks
- [complete](/commands/complete) - Mark tasks done
