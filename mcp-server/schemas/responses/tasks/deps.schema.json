{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "tasks.deps response",
  "description": "Dependency graph with upstream/downstream tasks",
  "allOf": [
    {
      "$ref": "../common-success.schema.json"
    },
    {
      "properties": {
        "data": {
          "type": "object",
          "description": "Dependency graph with upstream/downstream tasks"
        }
      }
    }
  ]
}
