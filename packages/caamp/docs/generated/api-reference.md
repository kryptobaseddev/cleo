# API Reference

Generated by [forge-ts](https://github.com/kryptobaseddev/forge-ts) from `.`.

## Table of Contents

- [Functions](#functions)
  - [`getPlatformPaths()`](#getplatformpaths)
  - [`getSystemInfo()`](#getsysteminfo)
  - [`_resetPlatformPathsCache()`](#resetplatformpathscache)
  - [`getPlatformLocations()`](#getplatformlocations)
  - [`getAgentsHome()`](#getagentshome)
  - [`getProjectAgentsDir()`](#getprojectagentsdir)
  - [`resolveProjectPath()`](#resolveprojectpath)
  - [`getCanonicalSkillsDir()`](#getcanonicalskillsdir)
  - [`getLockFilePath()`](#getlockfilepath)
  - [`getAgentsMcpDir()`](#getagentsmcpdir)
  - [`getAgentsMcpServersPath()`](#getagentsmcpserverspath)
  - [`getAgentsInstructFile()`](#getagentsinstructfile)
  - [`getAgentsConfigPath()`](#getagentsconfigpath)
  - [`getAgentsWikiDir()`](#getagentswikidir)
  - [`getAgentsSpecDir()`](#getagentsspecdir)
  - [`getAgentsLinksDir()`](#getagentslinksdir)
  - [`resolveRegistryTemplatePath()`](#resolveregistrytemplatepath)
  - [`resolveProviderConfigPath()`](#resolveproviderconfigpath)
  - [`resolvePreferredConfigScope()`](#resolvepreferredconfigscope)
  - [`resolveProviderSkillsDir()`](#resolveproviderskillsdir)
  - [`resolveProviderSkillsDirs()`](#resolveproviderskillsdirs)
  - [`resolveProviderProjectPath()`](#resolveproviderprojectpath)
  - [`resolveProvidersRegistryPath()`](#resolveprovidersregistrypath)
  - [`normalizeSkillSubPath()`](#normalizeskillsubpath)
  - [`buildSkillSubPathCandidates()`](#buildskillsubpathcandidates)
  - [`getAllProviders()`](#getallproviders)
  - [`getProvider()`](#getprovider)
  - [`resolveAlias()`](#resolvealias)
  - [`getProvidersByPriority()`](#getprovidersbypriority)
  - [`getPrimaryProvider()`](#getprimaryprovider)
  - [`getProvidersByStatus()`](#getprovidersbystatus)
  - [`getProvidersByInstructFile()`](#getprovidersbyinstructfile)
  - [`getInstructionFiles()`](#getinstructionfiles)
  - [`getProviderCount()`](#getprovidercount)
  - [`getRegistryVersion()`](#getregistryversion)
  - [`getProvidersByHookEvent()`](#getprovidersbyhookevent)
  - [`getCommonHookEvents()`](#getcommonhookevents)
  - [`providerSupports()`](#providersupports)
  - [`getSpawnCapableProviders()`](#getspawncapableproviders)
  - [`getProvidersBySpawnCapability()`](#getprovidersbyspawncapability)
  - [`resetRegistry()`](#resetregistry)
  - [`getProvidersBySkillsPrecedence()`](#getprovidersbyskillsprecedence)
  - [`getEffectiveSkillsPaths()`](#geteffectiveskillspaths)
  - [`buildSkillsMap()`](#buildskillsmap)
  - [`getProviderCapabilities()`](#getprovidercapabilities)
  - [`providerSupportsById()`](#providersupportsbyid)
  - [`buildInjectionContent()`](#buildinjectioncontent)
  - [`parseInjectionContent()`](#parseinjectioncontent)
  - [`generateInjectionContent()`](#generateinjectioncontent)
  - [`generateSkillsSection()`](#generateskillssection)
  - [`getInstructFile()`](#getinstructfile)
  - [`groupByInstructFile()`](#groupbyinstructfile)
  - [`checkInjection()`](#checkinjection)
  - [`inject()`](#inject)
  - [`removeInjection()`](#removeinjection)
  - [`checkAllInjections()`](#checkallinjections)
  - [`injectAll()`](#injectall)
  - [`ensureProviderInstructionFile()`](#ensureproviderinstructionfile)
  - [`ensureAllProviderInstructionFiles()`](#ensureallproviderinstructionfiles)
  - [`setVerbose()`](#setverbose)
  - [`setQuiet()`](#setquiet)
  - [`debug()`](#debug)
  - [`info()`](#info)
  - [`warn()`](#warn)
  - [`error()`](#error)
  - [`isVerbose()`](#isverbose)
  - [`isQuiet()`](#isquiet)
  - [`setHuman()`](#sethuman)
  - [`isHuman()`](#ishuman)
  - [`detectProvider()`](#detectprovider)
  - [`detectProjectProvider()`](#detectprojectprovider)
  - [`detectAllProviders()`](#detectallproviders)
  - [`getInstalledProviders()`](#getinstalledproviders)
  - [`detectProjectProviders()`](#detectprojectproviders)
  - [`resetDetectionCache()`](#resetdetectioncache)
  - [`installToCanonical()`](#installtocanonical)
  - [`installSkill()`](#installskill)
  - [`removeSkill()`](#removeskill)
  - [`listCanonicalSkills()`](#listcanonicalskills)
  - [`selectProvidersByMinimumPriority()`](#selectprovidersbyminimumpriority)
  - [`installBatchWithRollback()`](#installbatchwithrollback)
  - [`updateInstructionsSingleOperation()`](#updateinstructionssingleoperation)
  - [`resolveTierDir()`](#resolvetierdir)
  - [`resolveAllTiers()`](#resolvealltiers)
  - [`hasPiAbsentAutoWarned()`](#haspiabsentautowarned)
  - [`hasExplicitNonPiAutoWarned()`](#hasexplicitnonpiautowarned)
  - [`markPiAbsentAutoWarned()`](#markpiabsentautowarned)
  - [`markExplicitNonPiAutoWarned()`](#markexplicitnonpiautowarned)
  - [`resetExclusivityWarningState()`](#resetexclusivitywarningstate)
  - [`isExclusivityMode()`](#isexclusivitymode)
  - [`getExclusivityMode()`](#getexclusivitymode)
  - [`setExclusivityMode()`](#setexclusivitymode)
  - [`resetExclusivityModeOverride()`](#resetexclusivitymodeoverride)
  - [`getHarnessFor()`](#getharnessfor)
  - [`getPrimaryHarness()`](#getprimaryharness)
  - [`getAllHarnesses()`](#getallharnesses)
  - [`resolveDefaultTargetProviders()`](#resolvedefaulttargetproviders)
  - [`dispatchInstallSkillAcrossProviders()`](#dispatchinstallskillacrossproviders)
  - [`dispatchRemoveSkillAcrossProviders()`](#dispatchremoveskillacrossproviders)
  - [`resolveFormat()`](#resolveformat)
  - [`buildEnvelope()`](#buildenvelope)
  - [`emitError()`](#emiterror)
  - [`emitJsonError()`](#emitjsonerror)
  - [`outputSuccess()`](#outputsuccess)
  - [`handleFormatError()`](#handleformaterror)
  - [`emitSuccess()`](#emitsuccess)
  - [`emitError()`](#emiterror)
  - [`runLafsCommand()`](#runlafscommand)
  - [`parsePriority()`](#parsepriority)
  - [`resolveProviders()`](#resolveproviders)
  - [`readJsonFile()`](#readjsonfile)
  - [`readSkillOperations()`](#readskilloperations)
  - [`readTextInput()`](#readtextinput)
  - [`registerAdvancedBatch()`](#registeradvancedbatch)
  - [`registerAdvancedInstructions()`](#registeradvancedinstructions)
  - [`registerAdvancedProviders()`](#registeradvancedproviders)
  - [`registerAdvancedCommands()`](#registeradvancedcommands)
  - [`deepMerge()`](#deepmerge)
  - [`setNestedValue()`](#setnestedvalue)
  - [`getNestedValue()`](#getnestedvalue)
  - [`ensureDir()`](#ensuredir)
  - [`readJsonConfig()`](#readjsonconfig)
  - [`writeJsonConfig()`](#writejsonconfig)
  - [`removeJsonConfig()`](#removejsonconfig)
  - [`readTomlConfig()`](#readtomlconfig)
  - [`writeTomlConfig()`](#writetomlconfig)
  - [`removeTomlConfig()`](#removetomlconfig)
  - [`readYamlConfig()`](#readyamlconfig)
  - [`writeYamlConfig()`](#writeyamlconfig)
  - [`removeYamlConfig()`](#removeyamlconfig)
  - [`readConfig()`](#readconfig)
  - [`writeConfig()`](#writeconfig)
  - [`removeConfig()`](#removeconfig)
  - [`registerConfigCommand()`](#registerconfigcommand)
  - [`readLockFile()`](#readlockfile)
  - [`writeLockFile()`](#writelockfile)
  - [`updateLockFile()`](#updatelockfile)
  - [`getCaampVersion()`](#getcaampversion)
  - [`registerDoctorCommand()`](#registerdoctorcommand)
  - [`registerInstructionsCheck()`](#registerinstructionscheck)
  - [`registerInstructionsInject()`](#registerinstructionsinject)
  - [`registerInstructionsUpdate()`](#registerinstructionsupdate)
  - [`registerInstructionsCommands()`](#registerinstructionscommands)
  - [`resolveMcpConfigPath()`](#resolvemcpconfigpath)
  - [`listMcpServers()`](#listmcpservers)
  - [`listAllMcpServers()`](#listallmcpservers)
  - [`detectMcpInstallations()`](#detectmcpinstallations)
  - [`installMcpServer()`](#installmcpserver)
  - [`removeMcpServer()`](#removemcpserver)
  - [`removeMcpServerFromAll()`](#removemcpserverfromall)
  - [`requireMcpProvider()`](#requiremcpprovider)
  - [`parseScope()`](#parsescope)
  - [`resolveProjectDir()`](#resolveprojectdir)
  - [`parseEnvAssignment()`](#parseenvassignment)
  - [`registerMcpDetectCommand()`](#registermcpdetectcommand)
  - [`registerMcpInstallCommand()`](#registermcpinstallcommand)
  - [`registerMcpListCommand()`](#registermcplistcommand)
  - [`registerMcpRemoveCommand()`](#registermcpremovecommand)
  - [`registerMcpCommands()`](#registermcpcommands)
  - [`fetchWithTimeout()`](#fetchwithtimeout)
  - [`ensureOkResponse()`](#ensureokresponse)
  - [`formatNetworkError()`](#formatnetworkerror)
  - [`cloneRepo()`](#clonerepo)
  - [`fetchRawFile()`](#fetchrawfile)
  - [`repoExists()`](#repoexists)
  - [`cloneGitLabRepo()`](#clonegitlabrepo)
  - [`fetchGitLabRawFile()`](#fetchgitlabrawfile)
  - [`parseSource()`](#parsesource)
  - [`isMarketplaceScoped()`](#ismarketplacescoped)
  - [`requirePiHarness()`](#requirepiharness)
  - [`parseScope()`](#parsescope)
  - [`resolveProjectDir()`](#resolveprojectdir)
  - [`registerPiCantCommands()`](#registerpicantcommands)
  - [`registerPiExtensionsCommands()`](#registerpiextensionscommands)
  - [`registerPiModelsCommands()`](#registerpimodelscommands)
  - [`registerPiPromptsCommands()`](#registerpipromptscommands)
  - [`registerPiSessionsCommands()`](#registerpisessionscommands)
  - [`registerPiThemesCommands()`](#registerpithemescommands)
  - [`registerPiCommands()`](#registerpicommands)
  - [`resetHookMappings()`](#resethookmappings)
  - [`getCanonicalEvent()`](#getcanonicalevent)
  - [`getAllCanonicalEvents()`](#getallcanonicalevents)
  - [`getCanonicalEventsByCategory()`](#getcanonicaleventsbycategory)
  - [`getProviderHookProfile()`](#getproviderhookprofile)
  - [`getMappedProviderIds()`](#getmappedproviderids)
  - [`toNative()`](#tonative)
  - [`toCanonical()`](#tocanonical)
  - [`toNativeBatch()`](#tonativebatch)
  - [`supportsHook()`](#supportshook)
  - [`getHookSupport()`](#gethooksupport)
  - [`getSupportedEvents()`](#getsupportedevents)
  - [`getUnsupportedEvents()`](#getunsupportedevents)
  - [`getProvidersForEvent()`](#getprovidersforevent)
  - [`getCommonEvents()`](#getcommonevents)
  - [`getProviderSummary()`](#getprovidersummary)
  - [`buildHookMatrix()`](#buildhookmatrix)
  - [`getHookSystemType()`](#gethooksystemtype)
  - [`getHookConfigPath()`](#gethookconfigpath)
  - [`getProviderOnlyEvents()`](#getprovideronlyevents)
  - [`translateToAll()`](#translatetoall)
  - [`resolveNativeEvent()`](#resolvenativeevent)
  - [`getHookMappingsVersion()`](#gethookmappingsversion)
  - [`registerProvidersCommand()`](#registerproviderscommand)
  - [`getRulesByCategory()`](#getrulesbycategory)
  - [`getRulesBySeverity()`](#getrulesbyseverity)
  - [`getCategories()`](#getcategories)
  - [`scanFile()`](#scanfile)
  - [`scanDirectory()`](#scandirectory)
  - [`toSarif()`](#tosarif)
  - [`registerSkillsAudit()`](#registerskillsaudit)
  - [`recordSkillInstall()`](#recordskillinstall)
  - [`removeSkillFromLock()`](#removeskillfromlock)
  - [`getTrackedSkills()`](#gettrackedskills)
  - [`checkSkillUpdate()`](#checkskillupdate)
  - [`checkAllSkillUpdates()`](#checkallskillupdates)
  - [`registerSkillsCheck()`](#registerskillscheck)
  - [`tokenizeCriteriaValue()`](#tokenizecriteriavalue)
  - [`validateRecommendationCriteria()`](#validaterecommendationcriteria)
  - [`normalizeRecommendationCriteria()`](#normalizerecommendationcriteria)
  - [`scoreSkillRecommendation()`](#scoreskillrecommendation)
  - [`recommendSkills()`](#recommendskills)
  - [`formatSkillRecommendations()`](#formatskillrecommendations)
  - [`searchSkills()`](#searchskills)
  - [`recommendSkills()`](#recommendskills)
  - [`registerSkillsFind()`](#registerskillsfind)
  - [`registerSkillsInit()`](#registerskillsinit)
  - [`loadLibraryFromModule()`](#loadlibraryfrommodule)
  - [`buildLibraryFromFiles()`](#buildlibraryfromfiles)
  - [`registerSkillLibrary()`](#registerskilllibrary)
  - [`registerSkillLibraryFromPath()`](#registerskilllibraryfrompath)
  - [`clearRegisteredLibrary()`](#clearregisteredlibrary)
  - [`isCatalogAvailable()`](#iscatalogavailable)
  - [`getSkills()`](#getskills)
  - [`getManifest()`](#getmanifest)
  - [`listSkills()`](#listskills)
  - [`getSkill()`](#getskill)
  - [`getSkillPath()`](#getskillpath)
  - [`getSkillDir()`](#getskilldir)
  - [`readSkillContent()`](#readskillcontent)
  - [`getCoreSkills()`](#getcoreskills)
  - [`getSkillsByCategory()`](#getskillsbycategory)
  - [`getSkillDependencies()`](#getskilldependencies)
  - [`resolveDependencyTree()`](#resolvedependencytree)
  - [`listProfiles()`](#listprofiles)
  - [`getProfile()`](#getprofile)
  - [`resolveProfile()`](#resolveprofile)
  - [`listSharedResources()`](#listsharedresources)
  - [`getSharedResourcePath()`](#getsharedresourcepath)
  - [`readSharedResource()`](#readsharedresource)
  - [`listProtocols()`](#listprotocols)
  - [`getProtocolPath()`](#getprotocolpath)
  - [`readProtocol()`](#readprotocol)
  - [`validateSkillFrontmatter()`](#validateskillfrontmatter)
  - [`validateAll()`](#validateall)
  - [`getDispatchMatrix()`](#getdispatchmatrix)
  - [`getVersion()`](#getversion)
  - [`getLibraryRoot()`](#getlibraryroot)
  - [`parseSkillFile()`](#parseskillfile)
  - [`discoverSkill()`](#discoverskill)
  - [`discoverSkills()`](#discoverskills)
  - [`discoverSkillsMulti()`](#discoverskillsmulti)
  - [`registerSkillsInstall()`](#registerskillsinstall)
  - [`registerSkillsList()`](#registerskillslist)
  - [`registerSkillsRemove()`](#registerskillsremove)
  - [`registerSkillsUpdate()`](#registerskillsupdate)
  - [`validateSkill()`](#validateskill)
  - [`registerSkillsValidate()`](#registerskillsvalidate)
  - [`registerSkillsCommands()`](#registerskillscommands)
  - [`isCaampOwnedSkill()`](#iscaampownedskill)
  - [`checkSkillIntegrity()`](#checkskillintegrity)
  - [`checkAllSkillIntegrity()`](#checkallskillintegrity)
  - [`shouldOverrideSkill()`](#shouldoverrideskill)
  - [`validateInstructionIntegrity()`](#validateinstructionintegrity)
  - [`resolveCantImports()`](#resolvecantimports)
  - [`cantToMarkdown()`](#canttomarkdown)
  - [`discoverWellKnown()`](#discoverwellknown)
- [Classes](#classes)
  - [`PiRequiredError`](#pirequirederror)
  - [`PiHarness`](#piharness)
  - [`LAFSCommandError`](#lafscommanderror)
  - [`NetworkError`](#networkerror)
  - [`SkillsMPAdapter`](#skillsmpadapter)
  - [`SkillsShAdapter`](#skillsshadapter)
  - [`MarketplaceUnavailableError`](#marketplaceunavailableerror)
  - [`MarketplaceClient`](#marketplaceclient)
- [Interfaces](#interfaces)
  - [`SkillLibraryEntry`](#skilllibraryentry)
  - [`SkillLibraryValidationResult`](#skilllibraryvalidationresult)
  - [`SkillLibraryValidationIssue`](#skilllibraryvalidationissue)
  - [`SkillLibraryProfile`](#skilllibraryprofile)
  - [`SkillLibraryDispatchMatrix`](#skilllibrarydispatchmatrix)
  - [`SkillLibraryManifestSkill`](#skilllibrarymanifestskill)
  - [`SkillLibraryManifest`](#skilllibrarymanifest)
  - [`SkillLibrary`](#skilllibrary)
  - [`RegistryDetection`](#registrydetection)
  - [`RegistryProvider`](#registryprovider)
  - [`RegistryMcpIntegration`](#registrymcpintegration)
  - [`RegistryHarnessCapability`](#registryharnesscapability)
  - [`RegistrySkillsCapability`](#registryskillscapability)
  - [`RegistryHooksCapability`](#registryhookscapability)
  - [`RegistrySpawnCapability`](#registryspawncapability)
  - [`RegistryCapabilities`](#registrycapabilities)
  - [`ProviderRegistry`](#providerregistry)
  - [`DetectionConfig`](#detectionconfig)
  - [`ProviderMcpCapability`](#providermcpcapability)
  - [`ProviderHarnessCapability`](#providerharnesscapability)
  - [`ProviderSkillsCapability`](#providerskillscapability)
  - [`ProviderHooksCapability`](#providerhookscapability)
  - [`ProviderSpawnCapability`](#providerspawncapability)
  - [`ProviderCapabilities`](#providercapabilities)
  - [`Provider`](#provider)
  - [`McpServerConfig`](#mcpserverconfig)
  - [`ParsedSource`](#parsedsource)
  - [`SkillMetadata`](#skillmetadata)
  - [`SkillEntry`](#skillentry)
  - [`LockEntry`](#lockentry)
  - [`CaampLockFile`](#caamplockfile)
  - [`MarketplaceSkill`](#marketplaceskill)
  - [`MarketplaceSearchResult`](#marketplacesearchresult)
  - [`AuditRule`](#auditrule)
  - [`AuditFinding`](#auditfinding)
  - [`AuditResult`](#auditresult)
  - [`InjectionCheckResult`](#injectioncheckresult)
  - [`McpServerEntry`](#mcpserverentry)
  - [`GlobalOptions`](#globaloptions)
  - [`PlatformPaths`](#platformpaths)
  - [`SystemInfo`](#systeminfo)
  - [`PlatformLocations`](#platformlocations)
  - [`InjectionTemplate`](#injectiontemplate)
  - [`EnsureProviderInstructionFileOptions`](#ensureproviderinstructionfileoptions)
  - [`EnsureProviderInstructionFileResult`](#ensureproviderinstructionfileresult)
  - [`DetectionResult`](#detectionresult)
  - [`DetectionCacheOptions`](#detectioncacheoptions)
  - [`SkillInstallResult`](#skillinstallresult)
  - [`SkillBatchOperation`](#skillbatchoperation)
  - [`BatchInstallOptions`](#batchinstalloptions)
  - [`BatchInstallResult`](#batchinstallresult)
  - [`InstructionUpdateSummary`](#instructionupdatesummary)
  - [`ResolveTierDirOptions`](#resolvetierdiroptions)
  - [`ResolveDefaultTargetProvidersOptions`](#resolvedefaulttargetprovidersoptions)
  - [`ExtensionEntry`](#extensionentry)
  - [`PromptEntry`](#promptentry)
  - [`ThemeEntry`](#themeentry)
  - [`HarnessInstallOptions`](#harnessinstalloptions)
  - [`CantProfileCounts`](#cantprofilecounts)
  - [`CantProfileEntry`](#cantprofileentry)
  - [`CantValidationDiagnostic`](#cantvalidationdiagnostic)
  - [`ValidateCantProfileResult`](#validatecantprofileresult)
  - [`SessionSummary`](#sessionsummary)
  - [`SessionDocument`](#sessiondocument)
  - [`PiModelDefinition`](#pimodeldefinition)
  - [`PiModelProvider`](#pimodelprovider)
  - [`PiModelsConfig`](#pimodelsconfig)
  - [`ModelListEntry`](#modellistentry)
  - [`SubagentTask`](#subagenttask)
  - [`SubagentSpawnOptions`](#subagentspawnoptions)
  - [`SubagentStreamEvent`](#subagentstreamevent)
  - [`SubagentExitResult`](#subagentexitresult)
  - [`SubagentLinkEntry`](#subagentlinkentry)
  - [`SubagentResult`](#subagentresult)
  - [`SubagentHandle`](#subagenthandle)
  - [`Harness`](#harness)
  - [`LAFSEnvelope`](#lafsenvelope)
  - [`FormatOptions`](#formatoptions)
  - [`LAFSCommandOptions`](#lafscommandoptions)
  - [`ProviderTargetOptions`](#providertargetoptions)
  - [`McpDetectionEntry`](#mcpdetectionentry)
  - [`InstallMcpServerOptions`](#installmcpserveroptions)
  - [`InstallMcpServerResult`](#installmcpserverresult)
  - [`RemoveMcpServerOptions`](#removemcpserveroptions)
  - [`RemoveMcpServerResult`](#removemcpserverresult)
  - [`McpCommandBaseOptions`](#mcpcommandbaseoptions)
  - [`McpDetectOptions`](#mcpdetectoptions)
  - [`McpInstallOptions`](#mcpinstalloptions)
  - [`McpListOptions`](#mcplistoptions)
  - [`McpRemoveOptions`](#mcpremoveoptions)
  - [`GitFetchResult`](#gitfetchresult)
  - [`PiCommandBaseOptions`](#picommandbaseoptions)
  - [`PiCantListOptions`](#picantlistoptions)
  - [`PiCantInstallOptions`](#picantinstalloptions)
  - [`PiExtensionsListOptions`](#piextensionslistoptions)
  - [`PiExtensionsInstallOptions`](#piextensionsinstalloptions)
  - [`PiModelsCommandOptions`](#pimodelscommandoptions)
  - [`PiModelsAddOptions`](#pimodelsaddoptions)
  - [`PiPromptsInstallOptions`](#pipromptsinstalloptions)
  - [`PiSessionsListOptions`](#pisessionslistoptions)
  - [`PiSessionsExportOptions`](#pisessionsexportoptions)
  - [`PiThemesInstallOptions`](#pithemesinstalloptions)
  - [`CanonicalEventDefinition`](#canonicaleventdefinition)
  - [`HookMapping`](#hookmapping)
  - [`ProviderHookProfile`](#providerhookprofile)
  - [`NormalizedHookEvent`](#normalizedhookevent)
  - [`HookSupportResult`](#hooksupportresult)
  - [`ProviderHookSummary`](#providerhooksummary)
  - [`CrossProviderMatrix`](#crossprovidermatrix)
  - [`HookMappingsFile`](#hookmappingsfile)
  - [`MarketplaceAdapter`](#marketplaceadapter)
  - [`MarketplaceResult`](#marketplaceresult)
  - [`SearchOptions`](#searchoptions)
  - [`RecommendationValidationIssue`](#recommendationvalidationissue)
  - [`RecommendationValidationResult`](#recommendationvalidationresult)
  - [`RecommendationCriteriaInput`](#recommendationcriteriainput)
  - [`NormalizedRecommendationCriteria`](#normalizedrecommendationcriteria)
  - [`RecommendationReason`](#recommendationreason)
  - [`RecommendationScoreBreakdown`](#recommendationscorebreakdown)
  - [`RankedSkillRecommendation`](#rankedskillrecommendation)
  - [`RecommendationOptions`](#recommendationoptions)
  - [`RecommendationWeights`](#recommendationweights)
  - [`RecommendSkillsResult`](#recommendskillsresult)
  - [`SearchSkillsOptions`](#searchskillsoptions)
  - [`RecommendSkillsQueryOptions`](#recommendskillsqueryoptions)
  - [`ValidationIssue`](#validationissue)
  - [`ValidationResult`](#validationresult)
  - [`SpawnOptions`](#spawnoptions)
  - [`SpawnResult`](#spawnresult)
  - [`SpawnAdapter`](#spawnadapter)
  - [`SkillIntegrityResult`](#skillintegrityresult)
  - [`ResolvedImports`](#resolvedimports)
  - [`WellKnownSkill`](#wellknownskill)
- [Types](#types)
  - [`ProviderPriority`](#providerpriority)
  - [`ProviderStatus`](#providerstatus)
  - [`McpConfigFormat`](#mcpconfigformat)
  - [`McpTransportType`](#mcptransporttype)
  - [`RegistryHarnessKind`](#registryharnesskind)
  - [`SkillsPrecedence`](#skillsprecedence)
  - [`HookEvent`](#hookevent)
  - [`RegistryHookFormat`](#registryhookformat)
  - [`RegistryHookCatalog`](#registryhookcatalog)
  - [`SpawnMechanism`](#spawnmechanism)
  - [`CtSkillEntry`](#ctskillentry)
  - [`CtValidationResult`](#ctvalidationresult)
  - [`CtValidationIssue`](#ctvalidationissue)
  - [`CtProfileDefinition`](#ctprofiledefinition)
  - [`CtDispatchMatrix`](#ctdispatchmatrix)
  - [`CtManifest`](#ctmanifest)
  - [`CtManifestSkill`](#ctmanifestskill)
  - [`ConfigFormat`](#configformat)
  - [`TransportType`](#transporttype)
  - [`DetectionMethod`](#detectionmethod)
  - [`SourceType`](#sourcetype)
  - [`AuditSeverity`](#auditseverity)
  - [`InjectionStatus`](#injectionstatus)
  - [`PathScope`](#pathscope)
  - [`HarnessTier`](#harnesstier)
  - [`HarnessAssetKind`](#harnessassetkind)
  - [`HarnessScope`](#harnessscope)
  - [`ExclusivityMode`](#exclusivitymode)
  - [`MVILevel`](#mvilevel)
  - [`LAFSErrorShape`](#lafserrorshape)
  - [`LAFSWarning`](#lafswarning)
  - [`McpScope`](#mcpscope)
  - [`McpServerEntriesByProvider`](#mcpserverentriesbyprovider)
  - [`PiCantRemoveOptions`](#picantremoveoptions)
  - [`PiExtensionsRemoveOptions`](#piextensionsremoveoptions)
  - [`PiPromptsListOptions`](#pipromptslistoptions)
  - [`PiPromptsRemoveOptions`](#pipromptsremoveoptions)
  - [`PiThemesListOptions`](#pithemeslistoptions)
  - [`PiThemesRemoveOptions`](#pithemesremoveoptions)
  - [`HookCategory`](#hookcategory)
  - [`EventSource`](#eventsource)
  - [`CanonicalHookEvent`](#canonicalhookevent)
  - [`HookSystemType`](#hooksystemtype)
  - [`HookHandlerType`](#hookhandlertype)
  - [`RecommendationErrorCode`](#recommendationerrorcode)
  - [`RecommendationReasonCode`](#recommendationreasoncode)
  - [`SkillIntegrityStatus`](#skillintegritystatus)
- [Variables](#variables)
  - [`AGENTS_HOME`](#agentshome)
  - [`LOCK_FILE_PATH`](#lockfilepath)
  - [`CANONICAL_SKILLS_DIR`](#canonicalskillsdir)
  - [`AGENTS_MCP_DIR`](#agentsmcpdir)
  - [`AGENTS_MCP_SERVERS_PATH`](#agentsmcpserverspath)
  - [`AGENTS_CONFIG_PATH`](#agentsconfigpath)
  - [`TIER_PRECEDENCE`](#tierprecedence)
  - [`DEFAULT_EXCLUSIVITY_MODE`](#defaultexclusivitymode)
  - [`EXCLUSIVITY_MODE_ENV_VAR`](#exclusivitymodeenvvar)
  - [`ErrorCategories`](#errorcategories)
  - [`ErrorCodes`](#errorcodes)
  - [`MCP_ERROR_CODES`](#mcperrorcodes)
  - [`DEFAULT_FETCH_TIMEOUT_MS`](#defaultfetchtimeoutms)
  - [`PI_ERROR_CODES`](#pierrorcodes)
  - [`HOOK_CATEGORIES`](#hookcategories)
  - [`EVENT_SOURCES`](#eventsources)
  - [`CANONICAL_HOOK_EVENTS`](#canonicalhookevents)
  - [`PROVIDER_HOOK_EVENTS`](#providerhookevents)
  - [`DOMAIN_HOOK_EVENTS`](#domainhookevents)
  - [`EVENT_METADATA`](#eventmetadata)
  - [`AUDIT_RULES`](#auditrules)
  - [`RECOMMENDATION_ERROR_CODES`](#recommendationerrorcodes)
  - [`rankSkills`](#rankskills)

## Functions

### `getPlatformPaths()`

*Defined in `src/core/platform-paths.ts:100`*

```typescript
() => PlatformPaths
```

Get OS-appropriate paths for CAAMP's global directories.

**Returns**: Resolved platform paths

**Examples**

```typescript
const paths = getPlatformPaths();
console.log(paths.data); // e.g. "/home/user/.local/share/agents"
```

### `getSystemInfo()`

*Defined in `src/core/platform-paths.ts:142`*

```typescript
() => SystemInfo
```

Get a cached system information snapshot.

**Returns**: Cached system info object

**Examples**

```typescript
const info = getSystemInfo();
console.log(`Running on ${info.platform}/${info.arch}`);
```

### `_resetPlatformPathsCache()`

*Defined in `src/core/platform-paths.ts:164`*

```typescript
() => void
```

Invalidate the path and system info caches. Use in tests after mutating AGENTS\_HOME env var.

### `getPlatformLocations()`

*Defined in `src/core/paths/standard.ts:62`*

```typescript
() => PlatformLocations
```

Resolves platform-specific directory locations for the current OS.

**Returns**: Platform-specific directory locations

**Examples**

```typescript
const locations = getPlatformLocations();
console.log(locations.config); // e.g., "/home/user/.config"
```

### `getAgentsHome()`

*Defined in `src/core/paths/standard.ts:119`*

```typescript
() => string
```

Returns the global agents home directory path.

**Returns**: The absolute path to the global agents home directory

**Examples**

```typescript
const home = getAgentsHome();
// e.g., "/home/user/.local/share/caamp"
```

### `getProjectAgentsDir()`

*Defined in `src/core/paths/standard.ts:141`*

```typescript
(projectRoot?: string) => string
```

Returns the project-local `.agents` directory path.

**Parameters**

- `projectRoot` — The project root directory, defaults to `process.cwd()`

**Returns**: The absolute path to the project's `.agents` directory

**Examples**

```typescript
const dir = getProjectAgentsDir("/home/user/my-project");
// returns "/home/user/my-project/.agents"
```

### `resolveProjectPath()`

*Defined in `src/core/paths/standard.ts:164`*

```typescript
(relativePath: string, projectDir?: string) => string
```

Resolves a relative path against a project directory.

**Parameters**

- `relativePath` — The relative path to resolve
- `projectDir` — The project root directory, defaults to `process.cwd()`

**Returns**: The resolved absolute path

**Examples**

```typescript
const path = resolveProjectPath(".agents/config.toml", "/home/user/project");
// returns "/home/user/project/.agents/config.toml"
```

### `getCanonicalSkillsDir()`

*Defined in `src/core/paths/standard.ts:186`*

```typescript
() => string
```

Returns the canonical skills storage directory path.

**Returns**: The absolute path to the canonical skills directory

**Examples**

```typescript
const dir = getCanonicalSkillsDir();
// e.g., "/home/user/.local/share/caamp/skills"
```

### `getLockFilePath()`

*Defined in `src/core/paths/standard.ts:207`*

```typescript
() => string
```

Returns the path to the CAAMP lock file.

**Returns**: The absolute path to the `.caamp-lock.json` file

**Examples**

```typescript
const lockPath = getLockFilePath();
// e.g., "/home/user/.local/share/caamp/.caamp-lock.json"
```

### `getAgentsMcpDir()`

*Defined in `src/core/paths/standard.ts:233`*

```typescript
(scope?: PathScope, projectDir?: string) => string
```

Gets the MCP directory within the `.agents/` standard structure.

**Parameters**

- `scope` — `"global"` for `~/.agents/mcp/`, `"project"` for `<project>/.agents/mcp/`
- `projectDir` — Project root (defaults to `process.cwd()`)

**Returns**: The absolute path to the MCP directory

**Examples**

```typescript
const globalMcp = getAgentsMcpDir("global");
const projectMcp = getAgentsMcpDir("project", "/home/user/project");
```

### `getAgentsMcpServersPath()`

*Defined in `src/core/paths/standard.ts:257`*

```typescript
(scope?: PathScope, projectDir?: string) => string
```

Gets the MCP servers.json path within the `.agents/` standard structure.

**Parameters**

- `scope` — `"global"` for `~/.agents/mcp/servers.json`, `"project"` for `<project>/.agents/mcp/servers.json`
- `projectDir` — Project root (defaults to `process.cwd()`)

**Returns**: The absolute path to the `servers.json` file

**Examples**

```typescript
const serversPath = getAgentsMcpServersPath("global");
// e.g., "/home/user/.agents/mcp/servers.json"
```

### `getAgentsInstructFile()`

*Defined in `src/core/paths/standard.ts:280`*

```typescript
(scope?: PathScope, projectDir?: string) => string
```

Gets the primary AGENTS.md instruction file path within `.agents/`.

**Parameters**

- `scope` — `"global"` for `~/.agents/AGENTS.md`, `"project"` for `<project>/.agents/AGENTS.md`
- `projectDir` — Project root (defaults to `process.cwd()`)

**Returns**: The absolute path to the AGENTS.md file

**Examples**

```typescript
const agentsFile = getAgentsInstructFile("project", "/home/user/project");
// returns "/home/user/project/.agents/AGENTS.md"
```

### `getAgentsConfigPath()`

*Defined in `src/core/paths/standard.ts:305`*

```typescript
(scope?: PathScope, projectDir?: string) => string
```

Gets the config.toml path within the `.agents/` standard structure.

**Parameters**

- `scope` — `"global"` for `~/.agents/config.toml`, `"project"` for `<project>/.agents/config.toml`
- `projectDir` — Project root (defaults to `process.cwd()`)

**Returns**: The absolute path to the config.toml file

**Examples**

```typescript
const configPath = getAgentsConfigPath("global");
// e.g., "/home/user/.agents/config.toml"
```

### `getAgentsWikiDir()`

*Defined in `src/core/paths/standard.ts:329`*

```typescript
(scope?: PathScope, projectDir?: string) => string
```

Gets the wiki directory within the `.agents/` standard structure.

**Parameters**

- `scope` — `"global"` or `"project"`
- `projectDir` — Project root (defaults to `process.cwd()`)

**Returns**: The absolute path to the wiki directory

**Examples**

```typescript
const wikiDir = getAgentsWikiDir("project", "/home/user/project");
// returns "/home/user/project/.agents/wiki"
```

### `getAgentsSpecDir()`

*Defined in `src/core/paths/standard.ts:353`*

```typescript
(scope?: PathScope, projectDir?: string) => string
```

Gets the spec directory within the `.agents/` standard structure.

**Parameters**

- `scope` — `"global"` or `"project"`
- `projectDir` — Project root (defaults to `process.cwd()`)

**Returns**: The absolute path to the spec directory

**Examples**

```typescript
const specDir = getAgentsSpecDir("global");
// e.g., "/home/user/.agents/spec"
```

### `getAgentsLinksDir()`

*Defined in `src/core/paths/standard.ts:377`*

```typescript
(scope?: PathScope, projectDir?: string) => string
```

Gets the links directory within the `.agents/` standard structure.

**Parameters**

- `scope` — `"global"` or `"project"`
- `projectDir` — Project root (defaults to `process.cwd()`)

**Returns**: The absolute path to the links directory

**Examples**

```typescript
const linksDir = getAgentsLinksDir("project", "/home/user/project");
// returns "/home/user/project/.agents/links"
```

### `resolveRegistryTemplatePath()`

*Defined in `src/core/paths/standard.ts:432`*

```typescript
(template: string) => string
```

Resolves a registry template path by substituting platform variables.

**Parameters**

- `template` — The template string containing `$VARIABLE` placeholders

**Returns**: The resolved absolute path with all variables expanded

**Examples**

```typescript
const path = resolveRegistryTemplatePath("$HOME/.config/claude/settings.json");
// e.g., "/home/user/.config/claude/settings.json"
```

### `resolveProviderConfigPath()`

*Defined in `src/core/paths/standard.ts:468`*

```typescript
(provider: Provider, scope: PathScope, projectDir?: string) => string | null
```

Resolves the configuration file path for a provider at the given scope.

**Parameters**

- `provider` — The provider whose config path to resolve
- `scope` — Whether to resolve global or project config path
- `projectDir` — The project root directory, defaults to `process.cwd()`

**Returns**: The resolved config file path, or null if unavailable for the given scope

**Examples**

```typescript
const configPath = resolveProviderConfigPath(provider, "project", "/home/user/project");
if (configPath) {
  console.log("Config at:", configPath);
}
```

### `resolvePreferredConfigScope()`

*Defined in `src/core/paths/standard.ts:506`*

```typescript
(provider: Provider, useGlobalFlag?: boolean) => PathScope
```

Determines the preferred configuration scope for a provider.

**Parameters**

- `provider` — The provider to determine scope for
- `useGlobalFlag` — Optional flag to force global scope

**Returns**: The preferred path scope for configuration

**Examples**

```typescript
const scope = resolvePreferredConfigScope(provider, false);
// returns "project" if provider has configPathProject, otherwise "global"
```

### `resolveProviderSkillsDir()`

*Defined in `src/core/paths/standard.ts:537`*

```typescript
(provider: Provider, scope: PathScope, projectDir?: string) => string
```

Resolves the skills directory path for a provider at the given scope.

**Parameters**

- `provider` — The provider whose skills directory to resolve
- `scope` — Whether to resolve global or project skills path
- `projectDir` — The project root directory, defaults to `process.cwd()`

**Returns**: The resolved skills directory path

**Examples**

```typescript
const skillsDir = resolveProviderSkillsDir(provider, "global");
// e.g., "/home/user/.claude/skills"
```

### `resolveProviderSkillsDirs()`

*Defined in `src/core/paths/standard.ts:572`*

```typescript
(provider: Provider, scope: PathScope, projectDir?: string) => string[]
```

Gets all target directories for skill installation based on provider precedence.

**Parameters**

- `provider` — Provider to resolve paths for
- `scope` — Whether to resolve global or project paths
- `projectDir` — Project directory for project-scope resolution

**Returns**: Array of target directories for symlink creation

**Examples**

```typescript
const dirs = resolveProviderSkillsDirs(provider, "project", "/home/user/project");
for (const dir of dirs) {
  console.log("Install skill to:", dir);
}
```

### `resolveProviderProjectPath()`

*Defined in `src/core/paths/standard.ts:639`*

```typescript
(provider: Provider, projectDir?: string) => string
```

Resolves a provider's project-level path against a project directory.

**Parameters**

- `provider` — The provider whose project path to resolve
- `projectDir` — The project root directory, defaults to `process.cwd()`

**Returns**: The resolved absolute path for the provider's project directory

**Examples**

```typescript
const projectPath = resolveProviderProjectPath(provider, "/home/user/project");
// e.g., "/home/user/project/.claude"
```

### `resolveProvidersRegistryPath()`

*Defined in `src/core/paths/standard.ts:663`*

```typescript
(startDir: string) => string
```

Locates the providers registry.json file by searching up from a start directory.

**Parameters**

- `startDir` — The directory to start searching from

**Returns**: The absolute path to the found `providers/registry.json` file

**Throws**

- Error if `providers/registry.json` cannot be found within 8 parent levels

**Examples**

```typescript
const registryPath = resolveProvidersRegistryPath(__dirname);
// e.g., "/home/user/caamp/providers/registry.json"
```

### `normalizeSkillSubPath()`

*Defined in `src/core/paths/standard.ts:706`*

```typescript
(path: string | undefined) => string | undefined
```

Normalizes a skill sub-path by cleaning separators and removing SKILL.md suffix.

**Parameters**

- `path` — The raw skill sub-path to normalize

**Returns**: The normalized path, or undefined if the input is empty or falsy

**Examples**

```typescript
const normalized = normalizeSkillSubPath("skills/my-skill/SKILL.md");
// returns "skills/my-skill"
```

### `buildSkillSubPathCandidates()`

*Defined in `src/core/paths/standard.ts:737`*

```typescript
(marketplacePath: string | undefined, parsedPath: string | undefined) => (string | undefined)[]
```

Builds a list of candidate sub-paths for skill file resolution.

**Parameters**

- `marketplacePath` — The sub-path from the marketplace listing
- `parsedPath` — The sub-path parsed from the source URL

**Returns**: A deduplicated array of candidate sub-paths

**Examples**

```typescript
const candidates = buildSkillSubPathCandidates("skills/my-skill", undefined);
// returns ["skills/my-skill", ".agents/skills/my-skill", ".claude/skills/my-skill"]
```

### `getAllProviders()`

*Defined in `src/core/registry/providers.ts:228`*

```typescript
() => Provider[]
```

Retrieve all registered providers with resolved platform paths.  Providers are lazily loaded from `providers/registry.json` on first call and cached for subsequent calls.

**Returns**: Array of all provider definitions

**Examples**

```typescript
const providers = getAllProviders();
console.log(`${providers.length} providers registered`);
```

### `getProvider()`

*Defined in `src/core/registry/providers.ts:253`*

```typescript
(idOrAlias: string) => Provider | undefined
```

Look up a provider by its ID or any of its aliases.

**Parameters**

- `idOrAlias` — Provider ID (e.g. `"claude-code"`) or alias (e.g. `"claude"`)

**Returns**: The matching provider, or `undefined` if not found

**Examples**

```typescript
const provider = getProvider("claude");
// Returns the claude-code provider via alias resolution
```

### `resolveAlias()`

*Defined in `src/core/registry/providers.ts:280`*

```typescript
(idOrAlias: string) => string
```

Resolve an alias to its canonical provider ID.  If the input is already a canonical ID (or unrecognized), it is returned as-is.

**Parameters**

- `idOrAlias` — Provider ID or alias to resolve

**Returns**: The canonical provider ID

**Examples**

```typescript
resolveAlias("claude"); // "claude-code"
resolveAlias("claude-code"); // "claude-code"
resolveAlias("unknown"); // "unknown"
```

### `getProvidersByPriority()`

*Defined in `src/core/registry/providers.ts:305`*

```typescript
(priority: ProviderPriority) => Provider[]
```

Filter providers by their priority tier.

**Parameters**

- `priority` — Priority level to filter by (`"primary"`, `"high"`, `"medium"`, or `"low"`)

**Returns**: Array of providers matching the given priority

**Examples**

```typescript
const highPriority = getProvidersByPriority("high");
console.log(highPriority.map(p => p.toolName));
```

### `getPrimaryProvider()`

*Defined in `src/core/registry/providers.ts:331`*

```typescript
() => Provider | undefined
```

Get the single primary harness provider, if any is registered.

**Returns**: The primary provider, or `undefined` if none is registered

**Examples**

```typescript
const primary = getPrimaryProvider();
if (primary) {
  console.log(`Primary harness: ${primary.toolName}`);
}
```

### `getProvidersByStatus()`

*Defined in `src/core/registry/providers.ts:353`*

```typescript
(status: ProviderStatus) => Provider[]
```

Filter providers by their lifecycle status.

**Parameters**

- `status` — Status to filter by (`"active"`, `"beta"`, `"deprecated"`, or `"planned"`)

**Returns**: Array of providers matching the given status

**Examples**

```typescript
const active = getProvidersByStatus("active");
console.log(`${active.length} active providers`);
```

### `getProvidersByInstructFile()`

*Defined in `src/core/registry/providers.ts:378`*

```typescript
(file: string) => Provider[]
```

Filter providers that use a specific instruction file.  Multiple providers often share the same instruction file (e.g. many use `"AGENTS.md"`).

**Parameters**

- `file` — Instruction file name (e.g. `"CLAUDE.md"`, `"AGENTS.md"`)

**Returns**: Array of providers that use the given instruction file

**Examples**

```typescript
const claudeProviders = getProvidersByInstructFile("CLAUDE.md");
console.log(claudeProviders.map(p => p.id));
```

### `getInstructionFiles()`

*Defined in `src/core/registry/providers.ts:399`*

```typescript
() => string[]
```

Get the set of all unique instruction file names across all providers.

**Returns**: Array of unique instruction file names (e.g. `["CLAUDE.md", "AGENTS.md", "GEMINI.md"]`)

**Examples**

```typescript
const files = getInstructionFiles();
// ["CLAUDE.md", "AGENTS.md", "GEMINI.md"]
```

### `getProviderCount()`

*Defined in `src/core/registry/providers.ts:423`*

```typescript
() => number
```

Get the total number of registered providers.

**Returns**: Count of providers in the registry

**Examples**

```typescript
console.log(`Registry has ${getProviderCount()} providers`);
```

### `getRegistryVersion()`

*Defined in `src/core/registry/providers.ts:444`*

```typescript
() => string
```

Get the semantic version string of the provider registry.

**Returns**: Version string from `providers/registry.json` (e.g. `"2.0.0"`)

**Examples**

```typescript
console.log(`Registry version: ${getRegistryVersion()}`);
```

### `getProvidersByHookEvent()`

*Defined in `src/core/registry/providers.ts:467`*

```typescript
(event: HookEvent) => Provider[]
```

Filter providers that support a specific hook event.

**Parameters**

- `event` — Hook event to filter by (e.g. `"onToolComplete"`)

**Returns**: Array of providers whose hooks capability includes the given event

**Examples**

```typescript
const providers = getProvidersByHookEvent("onToolComplete");
console.log(providers.map(p => p.id));
```

### `getCommonHookEvents()`

*Defined in `src/core/registry/providers.ts:493`*

```typescript
(providerIds?: string[]) => HookEvent[]
```

Get hook events common to all specified providers.  If providerIds is provided, returns the intersection of their supported events. If providerIds is undefined or empty, uses all providers.

**Parameters**

- `providerIds` — Optional array of provider IDs to intersect

**Returns**: Array of hook events supported by ALL specified providers

**Examples**

```typescript
const common = getCommonHookEvents(["claude-code", "gemini-cli"]);
console.log(`${common.length} common hook events`);
```

### `providerSupports()`

*Defined in `src/core/registry/providers.ts:534`*

```typescript
(provider: Provider, dotPath: string) => boolean
```

Check whether a provider supports a specific capability via dot-path query.  The dot-path addresses a value inside `provider.capabilities`. For boolean fields the provider "supports" the capability when the value is `true`. For non-boolean fields the provider "supports" it when the value is neither `null` nor `undefined` (and, for arrays, non-empty).

**Parameters**

- `provider` — Provider to inspect
- `dotPath` — Dot-delimited capability path (e.g. `"spawn.supportsSubagents"`, `"hooks.supported"`)

**Returns**: `true` when the provider has the specified capability

**Examples**

```typescript
const claude = getProvider("claude-code");
providerSupports(claude!, "spawn.supportsSubagents"); // true
providerSupports(claude!, "hooks.supported"); // true (non-empty array)
```

### `getSpawnCapableProviders()`

*Defined in `src/core/registry/providers.ts:564`*

```typescript
() => Provider[]
```

Filter providers that support spawning subagents.

**Returns**: Array of providers where `capabilities.spawn.supportsSubagents === true`

**Examples**

```typescript
const spawnCapable = getSpawnCapableProviders();
console.log(spawnCapable.map(p => p.id));
```

### `getProvidersBySpawnCapability()`

*Defined in `src/core/registry/providers.ts:591`*

```typescript
(flag: keyof Omit<ProviderSpawnCapability, "spawnMechanism" | "spawnCommand">) => Provider[]
```

Filter providers by a specific boolean spawn capability flag.

**Parameters**

- `flag` — One of the four boolean flags on `ProviderSpawnCapability`              (`"supportsSubagents"`, `"supportsProgrammaticSpawn"`,               `"supportsInterAgentComms"`, `"supportsParallelSpawn"`)

**Returns**: Array of providers where the specified flag is `true`

**Examples**

```typescript
const parallel = getProvidersBySpawnCapability("supportsParallelSpawn");
console.log(parallel.map(p => p.id));
```

### `resetRegistry()`

*Defined in `src/core/registry/providers.ts:612`*

```typescript
() => void
```

Reset cached registry data, forcing a reload on next access.

**Examples**

```typescript
resetRegistry();
// Next call to getAllProviders() will re-read registry.json
```

### `getProvidersBySkillsPrecedence()`

*Defined in `src/core/registry/providers.ts:640`*

```typescript
(precedence: SkillsPrecedence) => Provider[]
```

Filter providers by their skills precedence value.

**Parameters**

- `precedence` — Skills precedence to filter by

**Returns**: Array of providers matching the given precedence

**Examples**

```typescript
const vendorOnly = getProvidersBySkillsPrecedence("vendor-only");
console.log(vendorOnly.map(p => p.id));
```

### `getEffectiveSkillsPaths()`

*Defined in `src/core/registry/providers.ts:669`*

```typescript
(provider: Provider, scope: PathScope, projectDir?: string) => Array<{ path: string; source: string; scope: string; }>
```

Get the effective skills paths for a provider, ordered by precedence.

**Parameters**

- `provider` — Provider to resolve paths for
- `scope` — Whether to resolve global or project paths
- `projectDir` — Project directory for project-scope resolution

**Returns**: Ordered array of paths with source and scope metadata

**Examples**

```typescript
const provider = getProvider("claude-code")!;
const paths = getEffectiveSkillsPaths(provider, "global");
for (const p of paths) {
  console.log(`${p.source} (${p.scope}): ${p.path}`);
}
```

### `buildSkillsMap()`

*Defined in `src/core/registry/providers.ts:737`*

```typescript
() => Array<{ providerId: string; toolName: string; precedence: SkillsPrecedence; paths: { global: string | null; project: string | null; }; }>
```

Build a full skills map for all providers.

**Returns**: Array of skills map entries with provider ID, tool name, precedence, and paths

**Examples**

```typescript
const skillsMap = buildSkillsMap();
for (const entry of skillsMap) {
  console.log(`${entry.providerId}: ${entry.precedence}`);
}
```

### `getProviderCapabilities()`

*Defined in `src/core/registry/providers.ts:779`*

```typescript
(idOrAlias: string) => ProviderCapabilities | undefined
```

Get capabilities for a provider by ID or alias.

**Parameters**

- `idOrAlias` — Provider ID or alias

**Returns**: The provider's capabilities, or undefined if not found

**Examples**

```typescript
const caps = getProviderCapabilities("claude-code");
if (caps?.spawn.supportsSubagents) {
  console.log("Supports subagent spawning");
}
```

### `providerSupportsById()`

*Defined in `src/core/registry/providers.ts:809`*

```typescript
(idOrAlias: string, capabilityPath: string) => boolean
```

Check if a provider supports a capability using ID/alias lookup.  Convenience wrapper that resolves the provider first, then delegates to the provider-level `providerSupports`.

**Parameters**

- `idOrAlias` — Provider ID or alias
- `capabilityPath` — Dot-path into capabilities (e.g. "spawn.supportsSubagents")

**Returns**: true if the provider supports the capability, false otherwise

**Examples**

```typescript
if (providerSupportsById("claude-code", "spawn.supportsSubagents")) {
  console.log("Claude Code supports subagent spawning");
}
```

### `buildInjectionContent()`

*Defined in `src/core/instructions/templates.ts:50`*

```typescript
(template: InjectionTemplate) => string
```

Build injection content from a structured template.  Produces a string suitable for injection between CAAMP markers. References are output as `@` lines, content blocks are appended as-is.

**Parameters**

- `template` — Template defining references and content

**Returns**: Formatted injection content string

**Examples**

```typescript
const content = buildInjectionContent({
  references: ["\@AGENTS.md"],
});
```

### `parseInjectionContent()`

*Defined in `src/core/instructions/templates.ts:86`*

```typescript
(content: string) => InjectionTemplate
```

Parse injection content back into template form.  Lines starting with `@` are treated as references. All other non-empty lines are treated as content blocks.

**Parameters**

- `content` — Raw injection content string

**Returns**: Parsed InjectionTemplate

**Examples**

```typescript
const template = parseInjectionContent("\@AGENTS.md\n\@.cleo/config.json");
```

### `generateInjectionContent()`

*Defined in `src/core/instructions/templates.ts:129`*

```typescript
(options?: { mcpServerName?: string; customContent?: string; }) => string
```

Generate a standard CAAMP injection block for instruction files.  Produces markdown content suitable for injection between CAAMP markers. Optionally includes MCP server and custom content sections.

**Parameters**

- `options` — Optional configuration for the generated content

**Returns**: Generated markdown string

**Examples**

```typescript
const content = generateInjectionContent({ mcpServerName: "filesystem" });
```

### `generateSkillsSection()`

*Defined in `src/core/instructions/templates.ts:171`*

```typescript
(skillNames: string[]) => string
```

Generate a skills discovery section for instruction files.

**Parameters**

- `skillNames` — Array of skill names to list

**Returns**: Markdown string listing installed skills

**Examples**

```typescript
const section = generateSkillsSection(["code-review", "testing"]);
```

### `getInstructFile()`

*Defined in `src/core/instructions/templates.ts:203`*

```typescript
(provider: Provider) => string
```

Get the correct instruction file name for a provider.

**Parameters**

- `provider` — Provider registry entry

**Returns**: Instruction file name

**Examples**

```typescript
const fileName = getInstructFile(provider);
// "CLAUDE.md"
```

### `groupByInstructFile()`

*Defined in `src/core/instructions/templates.ts:230`*

```typescript
(providers: Provider[]) => Map<string, Provider[]>
```

Group providers by their instruction file name.  Useful for determining which providers share the same instruction file (e.g. multiple providers using `AGENTS.md`).

**Parameters**

- `providers` — Array of providers to group

**Returns**: Map from instruction file name to array of providers using that file

**Examples**

```typescript
const groups = groupByInstructFile(getAllProviders());
for (const [file, providers] of groups) {
  console.log(`${file}: ${providers.map(p => p.id).join(", ")}`);
}
```

### `checkInjection()`

*Defined in `src/core/instructions/injector.ts:46`*

```typescript
(filePath: string, expectedContent?: string) => Promise<InjectionStatus>
```

Check the status of a CAAMP injection block in an instruction file.  Returns the injection status: - `"missing"` - File does not exist - `"none"` - File exists but has no CAAMP markers - `"current"` - CAAMP block exists and matches expected content (or no expected content given) - `"outdated"` - CAAMP block exists but differs from expected content

**Parameters**

- `filePath` — Absolute path to the instruction file
- `expectedContent` — Optional expected content to compare against

**Returns**: The injection status

**Examples**

```typescript
const status = await checkInjection("/project/CLAUDE.md", expectedContent);
if (status === "outdated") {
  console.log("CAAMP injection needs updating");
}
```

### `inject()`

*Defined in `src/core/instructions/injector.ts:109`*

```typescript
(filePath: string, content: string) => Promise<"created" | "added" | "consolidated" | "updated" | "intact">
```

Inject content into an instruction file between CAAMP markers.  Behavior depends on the file state: - File does not exist: creates the file with the injection block → `"created"` - File exists without markers: prepends the injection block → `"added"` - File exists with multiple markers (duplicates): consolidates into single block → `"consolidated"` - File exists with markers, content differs: replaces the block → `"updated"` - File exists with markers, content matches: no-op → `"intact"`  This function is **idempotent** — calling it multiple times with the same content will not modify the file after the first write.

**Parameters**

- `filePath` — Absolute path to the instruction file
- `content` — Content to inject between CAAMP markers

**Returns**: Action taken: `"created"`, `"added"`, `"consolidated"`, `"updated"`, or `"intact"`

**Examples**

```typescript
const action = await inject("/project/CLAUDE.md", "## My Config\nSome content");
console.log(`File ${action}`); // "created" on first call, "intact" on subsequent
```

### `removeInjection()`

*Defined in `src/core/instructions/injector.ts:181`*

```typescript
(filePath: string) => Promise<boolean>
```

Remove the CAAMP injection block from an instruction file.  If removing the block would leave the file empty, the file is deleted entirely.

**Parameters**

- `filePath` — Absolute path to the instruction file

**Returns**: `true` if a CAAMP block was found and removed, `false` otherwise

**Examples**

```typescript
const removed = await removeInjection("/project/CLAUDE.md");
```

### `checkAllInjections()`

*Defined in `src/core/instructions/injector.ts:227`*

```typescript
(providers: Provider[], projectDir: string, scope: "project" | "global", expectedContent?: string) => Promise<InjectionCheckResult[]>
```

Check injection status across all providers' instruction files.  Deduplicates by file path since multiple providers may share the same instruction file (e.g. many providers use `AGENTS.md`).

**Parameters**

- `providers` — Array of providers to check
- `projectDir` — Absolute path to the project directory
- `scope` — Whether to check project or global instruction files
- `expectedContent` — Optional expected content to compare against

**Returns**: Array of injection check results, one per unique instruction file

**Examples**

```typescript
const results = await checkAllInjections(providers, "/project", "project", expected);
const outdated = results.filter(r => r.status === "outdated");
```

### `injectAll()`

*Defined in `src/core/instructions/injector.ts:284`*

```typescript
(providers: Provider[], projectDir: string, scope: "project" | "global", content: string) => Promise<Map<string, "created" | "added" | "consolidated" | "updated" | "intact">>
```

Inject content into all providers' instruction files.  Deduplicates by file path to avoid injecting the same file multiple times.

**Parameters**

- `providers` — Array of providers to inject into
- `projectDir` — Absolute path to the project directory
- `scope` — Whether to target project or global instruction files
- `content` — Content to inject between CAAMP markers

**Returns**: Map of file path to action taken (`"created"`, `"added"`, `"consolidated"`, `"updated"`, or `"intact"`)

**Examples**

```typescript
const results = await injectAll(providers, "/project", "project", content);
for (const [file, action] of results) {
  console.log(`${file}: ${action}`);
}
```

### `ensureProviderInstructionFile()`

*Defined in `src/core/instructions/injector.ts:372`*

```typescript
(providerId: string, projectDir: string, options: EnsureProviderInstructionFileOptions) => Promise<EnsureProviderInstructionFileResult>
```

Ensure a provider's instruction file exists with the correct CAAMP block.  This is the canonical API for adapters and external packages to manage provider instruction files. Instead of directly creating/modifying CLAUDE.md, GEMINI.md, etc., callers should use this function to delegate instruction file management to CAAMP.  The instruction file name is resolved from CAAMP's provider registry (single source of truth), not hardcoded by the caller.

**Parameters**

- `providerId` — Provider ID from the registry (e.g. `"claude-code"`, `"gemini-cli"`)
- `projectDir` — Absolute path to the project directory
- `options` — References, content, and scope configuration

**Returns**: Result with file path, action taken, and provider metadata

**Throws**

- Error if the provider ID is not found in the registry

**Examples**

```typescript
const result = await ensureProviderInstructionFile("claude-code", "/project", {
  references: ["\@AGENTS.md"],
});
```

### `ensureAllProviderInstructionFiles()`

*Defined in `src/core/instructions/injector.ts:431`*

```typescript
(providerIds: string[], projectDir: string, options: EnsureProviderInstructionFileOptions) => Promise<EnsureProviderInstructionFileResult[]>
```

Ensure instruction files for multiple providers at once.  Deduplicates by file path — providers sharing the same instruction file (e.g. many providers use AGENTS.md) are only written once.

**Parameters**

- `providerIds` — Array of provider IDs from the registry
- `projectDir` — Absolute path to the project directory
- `options` — References, content, and scope configuration

**Returns**: Array of results, one per unique instruction file

**Throws**

- Error if any provider ID is not found in the registry

**Examples**

```typescript
const results = await ensureAllProviderInstructionFiles(
  ["claude-code", "cursor", "gemini-cli"],
  "/project",
  { references: ["\@AGENTS.md"] },
);
```

### `setVerbose()`

*Defined in `src/core/logger.ts:29`*

```typescript
(v: boolean) => void
```

Enable or disable verbose (debug) logging mode.  When enabled, debug messages are written to stderr.

**Parameters**

- `v` — `true` to enable verbose mode, `false` to disable

**Examples**

```typescript
setVerbose(true);
```

### `setQuiet()`

*Defined in `src/core/logger.ts:50`*

```typescript
(q: boolean) => void
```

Enable or disable quiet mode.  When enabled, info and warning messages are suppressed. Errors are always shown.

**Parameters**

- `q` — `true` to enable quiet mode, `false` to disable

**Examples**

```typescript
setQuiet(true);
```

### `debug()`

*Defined in `src/core/logger.ts:70`*

```typescript
(...args: unknown[]) => void
```

Log a debug message to stderr when verbose mode is enabled.

**Parameters**

- `args` — Values to log (forwarded to `console.error`)

**Examples**

```typescript
debug("Loading config from", filePath);
```

### `info()`

*Defined in `src/core/logger.ts:89`*

```typescript
(...args: unknown[]) => void
```

Log an informational message to stdout.

**Parameters**

- `args` — Values to log (forwarded to `console.log`)

**Examples**

```typescript
info("Installed 3 skills");
```

### `warn()`

*Defined in `src/core/logger.ts:108`*

```typescript
(...args: unknown[]) => void
```

Log a warning message to stderr.

**Parameters**

- `args` — Values to log (forwarded to `console.warn`)

**Examples**

```typescript
warn("Deprecated option used");
```

### `error()`

*Defined in `src/core/logger.ts:127`*

```typescript
(...args: unknown[]) => void
```

Log an error message to stderr.

**Parameters**

- `args` — Values to log (forwarded to `console.error`)

**Examples**

```typescript
error("Failed to install skill:", err.message);
```

### `isVerbose()`

*Defined in `src/core/logger.ts:149`*

```typescript
() => boolean
```

Check if verbose (debug) logging is currently enabled.

**Returns**: `true` if verbose mode is active

**Examples**

```typescript
if (isVerbose()) {
  console.error("Extra debug info");
}
```

### `isQuiet()`

*Defined in `src/core/logger.ts:171`*

```typescript
() => boolean
```

Check if quiet mode is currently enabled.

**Returns**: `true` if quiet mode is active

**Examples**

```typescript
if (!isQuiet()) {
  console.log("Status message");
}
```

### `setHuman()`

*Defined in `src/core/logger.ts:192`*

```typescript
(h: boolean) => void
```

Enable or disable human-readable output mode.  When enabled, commands output human-readable format instead of JSON.

**Parameters**

- `h` — `true` to enable human mode, `false` to disable

**Examples**

```typescript
setHuman(true);
```

### `isHuman()`

*Defined in `src/core/logger.ts:216`*

```typescript
() => boolean
```

Check if human-readable output mode is currently enabled.

**Returns**: `true` if human mode is active

**Examples**

```typescript
if (isHuman()) {
  console.log("Human readable output");
} else {
  console.log(JSON.stringify(data));
}
```

### `detectProvider()`

*Defined in `src/core/registry/detection.ts:120`*

```typescript
(provider: Provider) => DetectionResult
```

Detect if a single provider is installed on the system.  Checks each detection method configured for the provider (binary, directory, appBundle, flatpak) and returns which methods matched.

**Parameters**

- `provider` — The provider to detect

**Returns**: Detection result with installation status and matched methods

**Examples**

```typescript
const provider = getProvider("claude-code")!;
const result = detectProvider(provider);
if (result.installed) {
  console.log(`Claude Code found via: ${result.methods.join(", ")}`);
}
```

### `detectProjectProvider()`

*Defined in `src/core/registry/detection.ts:232`*

```typescript
(provider: Provider, projectDir: string) => boolean
```

Detect if a provider has project-level config in the given directory.

**Parameters**

- `provider` — Provider to check for project-level config
- `projectDir` — Absolute path to the project directory

**Returns**: `true` if the provider has a config file in the project directory

**Examples**

```typescript
const provider = getProvider("claude-code")!;
const hasProjectConfig = detectProjectProvider(provider, "/home/user/my-project");
```

### `detectAllProviders()`

*Defined in `src/core/registry/detection.ts:260`*

```typescript
(options?: DetectionCacheOptions) => DetectionResult[]
```

Detect all registered providers and return their installation status.  Runs detection for every provider in the registry.

**Parameters**

- `options` — Cache control options

**Returns**: Array of detection results for all providers

**Examples**

```typescript
const results = detectAllProviders({ forceRefresh: true });
const installed = results.filter(r => r.installed);
console.log(`${installed.length} agents detected`);
```

### `getInstalledProviders()`

*Defined in `src/core/registry/detection.ts:298`*

```typescript
(options?: DetectionCacheOptions) => Provider[]
```

Get only providers that are currently installed on the system.  Convenience wrapper that filters `detectAllProviders` results to only those with `installed === true`.

**Parameters**

- `options` — Cache control options passed through to detection

**Returns**: Array of installed provider definitions

**Examples**

```typescript
const installed = getInstalledProviders({ forceRefresh: true });
console.log(installed.map(p => p.toolName).join(", "));
```

### `detectProjectProviders()`

*Defined in `src/core/registry/detection.ts:334`*

```typescript
(projectDir: string, options?: DetectionCacheOptions) => DetectionResult[]
```

Detect all providers and enrich results with project-level presence.  Extends `detectAllProviders` by also checking whether each provider has a project-level config file in the given directory.

**Parameters**

- `projectDir` — Absolute path to the project directory to check
- `options` — Cache control options passed through to detection

**Returns**: Array of detection results with `projectDetected` populated

**Examples**

```typescript
const results = detectProjectProviders("/home/user/my-project", { forceRefresh: true });
for (const r of results) {
  if (r.projectDetected) {
    console.log(`${r.provider.toolName} has project config`);
  }
}
```

### `resetDetectionCache()`

*Defined in `src/core/registry/detection.ts:363`*

```typescript
() => void
```

Reset the detection result cache, forcing fresh detection on next call.

**Examples**

```typescript
resetDetectionCache();
// Next detectAllProviders() call will bypass cache
const fresh = detectAllProviders();
```

### `installToCanonical()`

*Defined in `src/core/skills/installer.ts:66`*

```typescript
(sourcePath: string, skillName: string) => Promise<string>
```

Copy skill files to the canonical location.

**Parameters**

- `sourcePath` — Absolute path to the source skill directory to copy
- `skillName` — Name for the skill (used as the subdirectory name)

**Returns**: Absolute path to the canonical installation directory

**Examples**

```typescript
const canonicalPath = await installToCanonical("/tmp/my-skill", "my-skill");
console.log(`Installed to: ${canonicalPath}`);
```

### `installSkill()`

*Defined in `src/core/skills/installer.ts:173`*

```typescript
(sourcePath: string, skillName: string, providers: Provider[], isGlobal: boolean, projectDir?: string) => Promise<SkillInstallResult>
```

Install a skill from a local path to the canonical location and link to agents.

**Parameters**

- `sourcePath` — Local path to the skill directory to install
- `skillName` — Name for the installed skill
- `providers` — Target providers to link the skill to
- `isGlobal` — Whether to link to global or project skill directories
- `projectDir` — Project directory (defaults to `process.cwd()`)

**Returns**: Install result with linked agents and any errors

**Examples**

```typescript
const result = await installSkill("/tmp/my-skill", "my-skill", providers, true, "/my/project");
if (result.success) {
  console.log(`Linked to: ${result.linkedAgents.join(", ")}`);
}
```

### `removeSkill()`

*Defined in `src/core/skills/installer.ts:226`*

```typescript
(skillName: string, providers: Provider[], isGlobal: boolean, projectDir?: string) => Promise<{ removed: string[]; errors: string[]; }>
```

Remove a skill from the canonical location and all agent symlinks.

**Parameters**

- `skillName` — Name of the skill to remove
- `providers` — Providers to unlink the skill from
- `isGlobal` — Whether to target global or project skill directories
- `projectDir` — Project directory (defaults to `process.cwd()`)

**Returns**: Object with arrays of successfully removed provider IDs and error messages

**Examples**

```typescript
const { removed, errors } = await removeSkill("my-skill", providers, true, "/my/project");
console.log(`Removed from: ${removed.join(", ")}`);
```

### `listCanonicalSkills()`

*Defined in `src/core/skills/installer.ts:290`*

```typescript
() => Promise<string[]>
```

List all skills installed in the canonical skills directory.

**Returns**: Array of skill names

**Examples**

```typescript
const skills = await listCanonicalSkills();
// ["my-skill", "another-skill"]
```

### `selectProvidersByMinimumPriority()`

*Defined in `src/core/advanced/orchestration.ts:48`*

```typescript
(providers: Provider[], minimumPriority?: ProviderPriority) => Provider[]
```

Filters providers by minimum priority and returns them in deterministic tier order.

**Parameters**

- `providers` — The full list of providers to filter
- `minimumPriority` — The minimum priority threshold, defaults to `"low"` (include all)

**Returns**: A filtered and sorted array of providers meeting the priority threshold

**Examples**

```typescript
const highPriority = selectProvidersByMinimumPriority(allProviders, "high");
// returns only providers with priority "high"
```

### `installBatchWithRollback()`

*Defined in `src/core/advanced/orchestration.ts:278`*

```typescript
(options: BatchInstallOptions) => Promise<BatchInstallResult>
```

Installs multiple skills across filtered providers with rollback.

**Parameters**

- `options` — The batch installation options including providers, operations, and scope

**Returns**: A result object indicating success, applied counts, and any rollback information

**Examples**

```typescript
const result = await installBatchWithRollback({
  minimumPriority: "high",
  skills: [{ sourcePath: "/path/to/skill", skillName: "my-skill" }],
});
if (!result.success) {
  console.error("Failed:", result.error);
}
```

### `updateInstructionsSingleOperation()`

*Defined in `src/core/advanced/orchestration.ts:419`*

```typescript
(providers: Provider[], content: string, scope?: Scope, projectDir?: string) => Promise<InstructionUpdateSummary>
```

Updates instruction files across providers as a single operation.

**Parameters**

- `providers` — The providers whose instruction files to update
- `content` — The instruction content to inject
- `scope` — The scope for instruction updates, defaults to `"project"`
- `projectDir` — The project root directory, defaults to `process.cwd()`

**Returns**: A summary of updated files and actions taken per file

**Examples**

```typescript
const summary = await updateInstructionsSingleOperation(
  providers,
  "## CAAMP Config\nUse these MCP servers...",
  "project",
);
console.log(`Updated ${summary.updatedFiles} files`);
```

### `resolveTierDir()`

*Defined in `src/core/harness/scope.ts:220`*

```typescript
(opts: ResolveTierDirOptions) => string
```

Resolve the on-disk directory for an asset at a given tier.

**Parameters**

- `opts` — Resolution options (see `ResolveTierDirOptions`)

**Returns**: Absolute directory path for the asset at the requested tier

**Throws**

- `Error` when `tier='project'` and no `projectDir` is supplied

**Examples**

```typescript
import { resolveTierDir } from "./scope.js";

const projectExt = resolveTierDir({
  tier: "project",
  kind: "extensions",
  projectDir: "/home/alice/repos/cleo",
});
// → "/home/alice/repos/cleo/.pi/extensions"

const userExt = resolveTierDir({ tier: "user", kind: "extensions" });
// → "/home/alice/.pi/agent/extensions"

const globalExt = resolveTierDir({ tier: "global", kind: "extensions" });
// → "/home/alice/.local/share/cleo/pi-extensions"
```

### `resolveAllTiers()`

*Defined in `src/core/harness/scope.ts:266`*

```typescript
(kind: HarnessAssetKind, projectDir?: string) => Array<{ tier: HarnessTier; dir: string; }>
```

Resolve every tier directory for a given asset kind, in precedence order.

**Parameters**

- `kind` — Asset kind to resolve
- `projectDir` — Project directory for the `project` tier. When   omitted the `project` tier entry is skipped rather than failing.

**Returns**: Array of `{ tier, dir }` pairs in precedence order

**Examples**

```typescript
const tiers = resolveAllTiers("extensions", "/home/alice/repo");
for (const { tier, dir } of tiers) {
  for (const entry of await safeReaddir(dir)) {
    // higher-precedence tier wins on name collision
  }
}
```

### `hasPiAbsentAutoWarned()`

*Defined in `src/core/config/caamp-config.ts:90`*

```typescript
() => boolean
```

Read whether the `auto` + Pi-absent boot warning has already fired for this process.

**Returns**: `true` once the warning has been emitted; `false` until then or   after `resetExclusivityWarningState` runs.

### `hasExplicitNonPiAutoWarned()`

*Defined in `src/core/config/caamp-config.ts:103`*

```typescript
() => boolean
```

Read whether the `auto` + explicit-non-Pi deprecation warning has already fired for this process.

**Returns**: `true` once the warning has been emitted; `false` until then or   after `resetExclusivityWarningState` runs.

### `markPiAbsentAutoWarned()`

*Defined in `src/core/config/caamp-config.ts:117`*

```typescript
() => void
```

Mark the `auto` + Pi-absent warning as already emitted for this process.

### `markExplicitNonPiAutoWarned()`

*Defined in `src/core/config/caamp-config.ts:131`*

```typescript
() => void
```

Mark the `auto` + explicit-non-Pi deprecation warning as already emitted for this process.

### `resetExclusivityWarningState()`

*Defined in `src/core/config/caamp-config.ts:146`*

```typescript
() => void
```

Reset both per-process exclusivity warning latches.

### `isExclusivityMode()`

*Defined in `src/core/config/caamp-config.ts:217`*

```typescript
(value: string) => value is ExclusivityMode
```

Type guard that narrows an arbitrary string to `ExclusivityMode`.

**Parameters**

- `value` — Candidate value to validate.

**Returns**: `true` when `value` is one of `'auto'`, `'force-pi'`, `'legacy'`.

**Examples**

```typescript
if (isExclusivityMode(userInput)) {
  setExclusivityMode(userInput);
}
```

### `getExclusivityMode()`

*Defined in `src/core/config/caamp-config.ts:250`*

```typescript
() => ExclusivityMode
```

Resolve the active CAAMP exclusivity mode using the layered precedence documented in `DEFAULT_EXCLUSIVITY_MODE`.

**Returns**: The currently effective exclusivity mode.

**Examples**

```typescript
const mode = getExclusivityMode();
if (mode === 'force-pi') {
  // ...
}
```

### `setExclusivityMode()`

*Defined in `src/core/config/caamp-config.ts:284`*

```typescript
(mode: ExclusivityMode) => void
```

Install a programmatic override for the exclusivity mode.

**Parameters**

- `mode` — Mode to install.

**Examples**

```typescript
setExclusivityMode('force-pi');
try {
  await runCommand();
} finally {
  resetExclusivityModeOverride();
}
```

### `resetExclusivityModeOverride()`

*Defined in `src/core/config/caamp-config.ts:306`*

```typescript
() => void
```

Clear any programmatic override installed by `setExclusivityMode`.

**Examples**

```typescript
setExclusivityMode("force-pi");
// ...run test...
resetExclusivityModeOverride();
// getExclusivityMode() now reads CAAMP_EXCLUSIVITY_MODE again
```

### `getHarnessFor()`

*Defined in `src/core/harness/index.ts:64`*

```typescript
(provider: Provider) => Harness | null
```

Return the harness implementation for a provider, or `null` if the provider has no first-class harness.

**Parameters**

- `provider` — Resolved provider to look up.

**Returns**: A harness instance, or `null` if the provider is a pure spawn target with no native harness.

**Examples**

```typescript
const pi = getProvider("pi");
if (pi) {
  const harness = getHarnessFor(pi);
  await harness?.installSkill("/path/to/skill", "my-skill", { kind: "global" });
}
```

### `getPrimaryHarness()`

*Defined in `src/core/harness/index.ts:91`*

```typescript
() => Harness | null
```

Return the primary harness declared in the registry, if any.

**Returns**: The primary harness, or `null` if no primary provider exists or the primary provider has no harness implementation.

**Examples**

```typescript
const primary = getPrimaryHarness();
if (primary) {
  console.log(`Primary harness: ${primary.provider.toolName}`);
}
```

### `getAllHarnesses()`

*Defined in `src/core/harness/index.ts:117`*

```typescript
() => Harness[]
```

Return every provider that has a harness implementation.

**Returns**: Array of harness instances, one per provider that implements the `Harness` contract.

**Examples**

```typescript
for (const harness of getAllHarnesses()) {
  console.log(harness.provider.id); // "pi", ...
}
```

### `resolveDefaultTargetProviders()`

*Defined in `src/core/harness/index.ts:174`*

```typescript
(options?: ResolveDefaultTargetProvidersOptions) => Provider[]
```

Resolve the default set of target providers when the user has not passed `--agent`, honouring the active `ExclusivityMode`.

**Parameters**

- `options` — Optional explicit provider selection (e.g. from   `--agent`) used by `auto`-mode deprecation warning detection. Omit to   request the implicit default resolution.

**Returns**: Ordered list of providers to target by default.

**Throws**

- `PiRequiredError` when mode is `'force-pi'` and Pi is not   installed.

**Examples**

```typescript
// Implicit default — used by `caamp skills list` and friends.
const targets = resolveDefaultTargetProviders();

// Explicit user selection — emits a deprecation warning in `auto` mode
// when the selection excludes Pi and Pi is installed.
const explicit = resolveDefaultTargetProviders({
  explicit: [getProvider('claude-code')!],
});
```

### `dispatchInstallSkillAcrossProviders()`

*Defined in `src/core/harness/index.ts:317`*

```typescript
(sourcePath: string, skillName: string, providers: Provider[], isGlobal: boolean, projectDir?: string) => Promise<SkillInstallResult>
```

Install a skill across a mixed set of providers, dispatching each provider to its `Harness` implementation when one exists and falling through to the legacy canonical+symlink installer for generic providers.

**Parameters**

- `sourcePath` — Absolute path to the source skill directory.
- `skillName` — Target skill name.
- `providers` — Ordered list of target providers.
- `isGlobal` — Whether to target global or project scope.
- `projectDir` — Project directory used by the harness project scope   and forwarded to the generic installer when provided. When omitted,   harness project scope falls back to `process.cwd()` and the generic   installer is invoked without a `projectDir` argument so it retains its   legacy default-handling behavior.

**Returns**: Merged install result across the harness and generic paths.

**Examples**

```typescript
const result = await dispatchInstallSkillAcrossProviders(
  "/abs/path/to/skill",
  "my-skill",
  [getProvider("pi")!, getProvider("claude-code")!],
  true,
);
console.log(result.linkedAgents); // e.g. ["pi", "claude-code"]
```

### `dispatchRemoveSkillAcrossProviders()`

*Defined in `src/core/harness/index.ts:410`*

```typescript
(skillName: string, providers: Provider[], isGlobal: boolean, projectDir?: string) => Promise<{ removed: string[]; errors: string[]; }>
```

Remove a skill across a mixed set of providers, dispatching each provider to its `Harness` implementation when one exists and falling through to the legacy canonical+symlink uninstaller for generic providers.

**Parameters**

- `skillName` — Skill name to remove.
- `providers` — Ordered list of target providers.
- `isGlobal` — Whether to target global or project scope.
- `projectDir` — Project directory used by the harness project scope   and forwarded to the generic uninstaller when provided. When omitted,   harness project scope falls back to `process.cwd()` and the generic   uninstaller is invoked without a `projectDir` argument.

**Returns**: Merged `{ removed, errors }` result across both dispatch paths.

**Examples**

```typescript
const result = await dispatchRemoveSkillAcrossProviders(
  "my-skill",
  [getProvider("pi")!, getProvider("claude-code")!],
  true,
);
console.log(result.removed); // providers the skill was removed from
```

### `resolveFormat()`

*Defined in `src/core/lafs.ts:98`*

```typescript
(options: FormatOptions) => "json" | "human"
```

Resolves output format based on flags and defaults.

**Parameters**

- `options` — Format resolution options

**Returns**: `"json"` or `"human"`

**Throws**

- Error if format flags conflict

**Examples**

```typescript
const format = resolveFormat({ jsonFlag: true });
```

### `buildEnvelope()`

*Defined in `src/core/lafs.ts:136`*

```typescript
<T>(operation: string, mvi: MVILevel, result: T | null, error: LAFSErrorShape | null, page?: LAFSPage | null, sessionId?: string, warnings?: LAFSWarning[]) => LAFSEnvelope<T>
```

Builds a standard LAFS envelope.

**Parameters**

- `operation` — Operation identifier (e.g., `"skills.list"`, `"doctor.check"`)
- `mvi` — Machine-Verified Instruction disclosure level
- `result` — Operation result data (`null` if error)
- `error` — Error details (`null` if success)
- `page` — Pagination info (`null` if not applicable)
- `sessionId` — Optional session identifier
- `warnings` — Optional array of warnings to attach

**Returns**: LAFS-compliant envelope

**Examples**

```typescript
const envelope = buildEnvelope(
  "skills.list",
  "full",
  { skills: [], count: 0 },
  null,
);
```

### `emitError()`

*Defined in `src/core/lafs.ts:198`*

```typescript
(operation: string, mvi: MVILevel, code: string, message: string, category: LAFSErrorCategory, details?: Record<string, unknown>, exitCode?: number) => never
```

Emits a JSON error envelope to stderr and exits the process.

**Parameters**

- `operation` — Operation identifier
- `mvi` — Machine-Verified Instruction disclosure level
- `code` — Error code
- `message` — Error message
- `category` — Error category from LAFS protocol
- `details` — Additional error details
- `exitCode` — Process exit code (default: 1)

**Examples**

```typescript
emitError(
  "skills.install",
  "full",
  "E_SKILL_NOT_FOUND",
  "Skill not found",
  "NOT_FOUND",
  { skillName: "my-skill" },
  1,
);
```

### `emitJsonError()`

*Defined in `src/core/lafs.ts:245`*

```typescript
(operation: string, mvi: MVILevel, code: string, message: string, category: LAFSErrorCategory, details?: Record<string, unknown>) => void
```

Emits a JSON error envelope without exiting (for catch blocks).

**Parameters**

- `operation` — Operation identifier
- `mvi` — Machine-Verified Instruction disclosure level
- `code` — Error code
- `message` — Error message
- `category` — Error category from LAFS protocol
- `details` — Additional error details

**Examples**

```typescript
try {
  await riskyOperation();
} catch (err) {
  emitJsonError("operation", "full", "E_FAILED", "Operation failed", "INTERNAL", {});
  process.exit(1);
}
```

### `outputSuccess()`

*Defined in `src/core/lafs.ts:286`*

```typescript
<T>(operation: string, mvi: MVILevel, result: T, page?: LAFSPage, sessionId?: string, warnings?: LAFSWarning[]) => void
```

Outputs a successful LAFS envelope to stdout.

**Parameters**

- `operation` — Operation identifier
- `mvi` — Machine-Verified Instruction disclosure level
- `result` — Operation result data
- `page` — Optional pagination info
- `sessionId` — Optional session identifier
- `warnings` — Optional warnings to attach

**Examples**

```typescript
outputSuccess("skills.list", "full", { skills: [], count: 0 });
```

### `handleFormatError()`

*Defined in `src/core/lafs.ts:343`*

```typescript
(error: unknown, operation: string, mvi: MVILevel, jsonFlag: boolean | undefined) => never
```

Handles format resolution errors consistently.

**Parameters**

- `error` — The error that occurred during format resolution
- `operation` — Operation identifier
- `mvi` — Machine-Verified Instruction disclosure level
- `jsonFlag` — Whether `--json` flag was explicitly set

**Returns**: never (exits process)

**Examples**

```typescript
try {
  format = resolveFormat({ jsonFlag: opts.json, humanFlag: opts.human });
} catch (error) {
  handleFormatError(error, "skills.list", "full", opts.json);
}
```

### `emitSuccess()`

*Defined in `src/commands/advanced/lafs.ts:114`*

```typescript
<T>(operation: string, result: T, mvi?: MVILevel) => void
```

Emits a successful LAFS result envelope to stdout.

**Parameters**

- `operation` — The LAFS operation identifier
- `result` — The result payload to include in the envelope
- `mvi` — The minimum viable information level, defaults to "standard"

**Examples**

```typescript
emitSuccess("advanced.providers", { providers: [...] });
```

### `emitError()`

*Defined in `src/commands/advanced/lafs.ts:147`*

```typescript
(operation: string, error: unknown, mvi?: MVILevel) => void
```

Emits a failed LAFS error envelope to stderr.

**Parameters**

- `operation` — The LAFS operation identifier
- `error` — The error to serialize, either a LAFSCommandError or generic Error/unknown
- `mvi` — The minimum viable information level, defaults to "standard"

**Examples**

```typescript
emitError("advanced.apply", new LAFSCommandError("E_VALIDATION", "bad input", "fix it"));
```

### `runLafsCommand()`

*Defined in `src/commands/advanced/lafs.ts:217`*

```typescript
<T>(command: string, mvi: MVILevel, action: () => Promise<T>) => Promise<void>
```

Runs an async action and emits the result as a LAFS success or error envelope.

**Parameters**

- `command` — The LAFS operation identifier
- `mvi` — The minimum viable information level
- `action` — The async function to execute

**Examples**

```typescript
await runLafsCommand("advanced.batch", "standard", async () => {
  return { installed: 3 };
});
```

### `parsePriority()`

*Defined in `src/commands/advanced/common.ts:45`*

```typescript
(value: string) => ProviderPriority
```

Parses and validates a provider priority tier string.

**Parameters**

- `value` — The priority string to parse

**Returns**: The validated ProviderPriority value

**Examples**

```typescript
const tier = parsePriority("high"); // "high"
```

### `resolveProviders()`

*Defined in `src/commands/advanced/common.ts:73`*

```typescript
(options: ProviderTargetOptions) => Provider[]
```

Resolves the set of target providers from CLI targeting options.

**Parameters**

- `options` — The provider targeting options from the CLI

**Returns**: An array of resolved Provider objects

**Examples**

```typescript
const providers = resolveProviders({ all: true });
```

### `readJsonFile()`

*Defined in `src/commands/advanced/common.ts:116`*

```typescript
(path: string) => Promise<unknown>
```

Reads and parses a JSON file from disk.

**Parameters**

- `path` — Absolute or relative path to the JSON file

**Returns**: The parsed JSON value

**Examples**

```typescript
const data = await readJsonFile("./operations.json");
```

### `readSkillOperations()`

*Defined in `src/commands/advanced/common.ts:148`*

```typescript
(path: string) => Promise<SkillBatchOperation[]>
```

Reads and validates a JSON file containing skill batch operations.

**Parameters**

- `path` — Path to the JSON file containing an array of skill operations

**Returns**: An array of validated SkillBatchOperation objects

**Examples**

```typescript
const ops = await readSkillOperations("./skill-ops.json");
```

### `readTextInput()`

*Defined in `src/commands/advanced/common.ts:225`*

```typescript
(inlineContent: string | undefined, filePath: string | undefined) => Promise<string | undefined>
```

Reads text input from either inline content or a file path, enforcing mutual exclusivity.

**Parameters**

- `inlineContent` — Inline text content from the --content flag, or undefined
- `filePath` — Path to a content file from the --content-file flag, or undefined

**Returns**: The text content string, or undefined if no input was provided

**Examples**

```typescript
const content = await readTextInput(undefined, "./content.txt");
```

### `registerAdvancedBatch()`

*Defined in `src/commands/advanced/batch.ts:30`*

```typescript
(parent: Command) => void
```

Registers the `advanced batch` subcommand for rollback-capable batch install of skills.

**Parameters**

- `parent` — The parent `advanced` Command to attach the batch subcommand to

**Examples**

```bash
caamp advanced batch --skills-file skills.json
caamp advanced batch --skills-file skills.json --min-tier medium
```

### `registerAdvancedInstructions()`

*Defined in `src/commands/advanced/instructions.ts:30`*

```typescript
(parent: Command) => void
```

Registers the `advanced instructions` subcommand for single-operation instruction updates.

**Parameters**

- `parent` — The parent `advanced` Command to attach the instructions subcommand to

**Examples**

```bash
caamp advanced instructions --content "Custom block" --all
caamp advanced instructions --content-file block.md --min-tier high
```

### `registerAdvancedProviders()`

*Defined in `src/commands/advanced/providers.ts:27`*

```typescript
(parent: Command) => void
```

Registers the `advanced providers` subcommand for selecting providers by priority tier.

**Parameters**

- `parent` — The parent `advanced` Command to attach the providers subcommand to

**Examples**

```bash
caamp advanced providers --min-tier high
caamp advanced providers --all --details
```

### `registerAdvancedCommands()`

*Defined in `src/commands/advanced/index.ts:30`*

```typescript
(program: Command) => void
```

Registers the `advanced` command group with providers, batch, and instructions subcommands.

**Parameters**

- `program` — The root Commander program to attach the advanced command group to

**Examples**

```bash
caamp advanced batch --skills-file skills.json
caamp advanced instructions --content "## Setup" --all
```

### `deepMerge()`

*Defined in `src/core/formats/utils.ts:27`*

```typescript
(target: Record<string, unknown>, source: Record<string, unknown>) => Record<string, unknown>
```

Deep merge two objects, with `source` values winning on conflict.  Recursively merges nested plain objects. Arrays and non-object values from `source` overwrite `target` values.

**Parameters**

- `target` — Base object to merge into
- `source` — Object with values that take precedence

**Returns**: A new merged object (does not mutate inputs)

**Examples**

```typescript
const merged = deepMerge({ a: 1, b: { c: 2 } }, { b: { d: 3 } });
// { a: 1, b: { c: 2, d: 3 } }
```

### `setNestedValue()`

*Defined in `src/core/formats/utils.ts:78`*

```typescript
(obj: Record<string, unknown>, keyPath: string, key: string, value: unknown) => Record<string, unknown>
```

Set a nested value using a dot-notation key path.

**Parameters**

- `obj` — Root object to modify
- `keyPath` — Dot-separated path to the parent key (e.g. `"mcpServers"`)
- `key` — Final key name for the value
- `value` — Value to set at the nested location

**Returns**: A new object with the value set at the specified path

**Examples**

```typescript
const result = setNestedValue({}, "a.b", "c", 42);
// { a: { b: { c: 42 } } }
```

### `getNestedValue()`

*Defined in `src/core/formats/utils.ts:127`*

```typescript
(obj: Record<string, unknown>, keyPath: string) => unknown
```

Get a nested value from an object using a dot-notation key path.

**Parameters**

- `obj` — Object to traverse
- `keyPath` — Dot-separated key path (e.g. `"mcpServers"` or `"a.b.c"`)

**Returns**: The value at the key path, or `undefined` if not found

**Examples**

```typescript
getNestedValue({ a: { b: { c: 42 } } }, "a.b.c"); // 42
getNestedValue({ a: 1 }, "a.b"); // undefined
```

### `ensureDir()`

*Defined in `src/core/formats/utils.ts:157`*

```typescript
(filePath: string) => Promise<void>
```

Ensure that the parent directories of a file path exist.  Creates directories recursively if they do not exist.

**Parameters**

- `filePath` — Absolute path to a file (parent directories will be created)

**Examples**

```typescript
await ensureDir("/path/to/new/dir/file.json");
// /path/to/new/dir/ now exists
```

### `readJsonConfig()`

*Defined in `src/core/formats/json.ts:30`*

```typescript
(filePath: string) => Promise<Record<string, unknown>>
```

Read and parse a JSON or JSONC config file.

**Parameters**

- `filePath` — Absolute path to the JSON/JSONC file

**Returns**: Parsed config object

**Examples**

```typescript
const config = await readJsonConfig("/home/user/.config/claude/settings.json");
```

### `writeJsonConfig()`

*Defined in `src/core/formats/json.ts:82`*

```typescript
(filePath: string, configKey: string, serverName: string, serverConfig: unknown) => Promise<void>
```

Write a server config entry to a JSON/JSONC file, preserving comments.

**Parameters**

- `filePath` — Absolute path to the JSON/JSONC file
- `configKey` — Dot-notation key path to the servers section (e.g. `"mcpServers"`)
- `serverName` — Name/key for the server entry
- `serverConfig` — Server configuration object to write

**Examples**

```typescript
await writeJsonConfig("/path/to/config.json", "mcpServers", "my-server", { command: "node" });
```

### `removeJsonConfig()`

*Defined in `src/core/formats/json.ts:147`*

```typescript
(filePath: string, configKey: string, serverName: string) => Promise<boolean>
```

Remove a server entry from a JSON/JSONC config file.

**Parameters**

- `filePath` — Absolute path to the JSON/JSONC file
- `configKey` — Dot-notation key path to the servers section
- `serverName` — Name/key of the server entry to remove

**Returns**: `true` if the entry was removed, `false` if the file or entry was not found

**Examples**

```typescript
const removed = await removeJsonConfig("/path/to/config.json", "mcpServers", "old-server");
```

### `readTomlConfig()`

*Defined in `src/core/formats/toml.ts:27`*

```typescript
(filePath: string) => Promise<Record<string, unknown>>
```

Read and parse a TOML config file.

**Parameters**

- `filePath` — Absolute path to the TOML file

**Returns**: Parsed config object

**Examples**

```typescript
const config = await readTomlConfig("/path/to/config.toml");
```

### `writeTomlConfig()`

*Defined in `src/core/formats/toml.ts:56`*

```typescript
(filePath: string, configKey: string, serverName: string, serverConfig: unknown) => Promise<void>
```

Write a server config entry to a TOML file.

**Parameters**

- `filePath` — Absolute path to the TOML file
- `configKey` — Dot-notation key path to the servers section
- `serverName` — Name/key for the server entry
- `serverConfig` — Server configuration object to write

**Examples**

```typescript
await writeTomlConfig("/path/to/config.toml", "mcpServers", "my-server", { command: "node" });
```

### `removeTomlConfig()`

*Defined in `src/core/formats/toml.ts:100`*

```typescript
(filePath: string, configKey: string, serverName: string) => Promise<boolean>
```

Remove a server entry from a TOML config file.

**Parameters**

- `filePath` — Absolute path to the TOML file
- `configKey` — Dot-notation key path to the servers section
- `serverName` — Name/key of the server entry to remove

**Returns**: `true` if the entry was removed, `false` if the file or entry was not found

**Examples**

```typescript
const removed = await removeTomlConfig("/path/to/config.toml", "mcpServers", "old-server");
```

### `readYamlConfig()`

*Defined in `src/core/formats/yaml.ts:27`*

```typescript
(filePath: string) => Promise<Record<string, unknown>>
```

Read and parse a YAML config file.

**Parameters**

- `filePath` — Absolute path to the YAML file

**Returns**: Parsed config object

**Examples**

```typescript
const config = await readYamlConfig("/path/to/config.yaml");
```

### `writeYamlConfig()`

*Defined in `src/core/formats/yaml.ts:56`*

```typescript
(filePath: string, configKey: string, serverName: string, serverConfig: unknown) => Promise<void>
```

Write a server config entry to a YAML file.

**Parameters**

- `filePath` — Absolute path to the YAML file
- `configKey` — Dot-notation key path to the servers section
- `serverName` — Name/key for the server entry
- `serverConfig` — Server configuration object to write

**Examples**

```typescript
await writeYamlConfig("/path/to/config.yaml", "mcpServers", "my-server", { command: "node" });
```

### `removeYamlConfig()`

*Defined in `src/core/formats/yaml.ts:105`*

```typescript
(filePath: string, configKey: string, serverName: string) => Promise<boolean>
```

Remove a server entry from a YAML config file.

**Parameters**

- `filePath` — Absolute path to the YAML file
- `configKey` — Dot-notation key path to the servers section
- `serverName` — Name/key of the server entry to remove

**Returns**: `true` if the entry was removed, `false` if the file or entry was not found

**Examples**

```typescript
const removed = await removeYamlConfig("/path/to/config.yaml", "mcpServers", "old-server");
```

### `readConfig()`

*Defined in `src/core/formats/index.ts:38`*

```typescript
(filePath: string, format: ConfigFormat) => Promise<Record<string, unknown>>
```

Read and parse a config file in the specified format.  Dispatches to the appropriate format handler (JSON/JSONC, YAML, or TOML).

**Parameters**

- `filePath` — Absolute path to the config file
- `format` — Config file format

**Returns**: Parsed config object

**Throws**

- If the file cannot be read or the format is unsupported

**Examples**

```typescript
const config = await readConfig("/path/to/config.json", "jsonc");
```

### `writeConfig()`

*Defined in `src/core/formats/index.ts:80`*

```typescript
(filePath: string, format: ConfigFormat, key: string, serverName: string, serverConfig: unknown) => Promise<void>
```

Write a server entry to a config file, preserving existing content.  Dispatches to the appropriate format handler. For JSONC files, comments are preserved using `jsonc-parser`.

**Parameters**

- `filePath` — Absolute path to the config file
- `format` — Config file format
- `key` — Dot-notation key path to the servers section (e.g. `"mcpServers"`)
- `serverName` — Name/key for the server entry
- `serverConfig` — Server configuration object to write

**Throws**

- If the format is unsupported

**Examples**

```typescript
await writeConfig("/path/to/config.json", "jsonc", "mcpServers", "my-server", config);
```

### `removeConfig()`

*Defined in `src/core/formats/index.ts:122`*

```typescript
(filePath: string, format: ConfigFormat, key: string, serverName: string) => Promise<boolean>
```

Remove a server entry from a config file in the specified format.

**Parameters**

- `filePath` — Absolute path to the config file
- `format` — Config file format
- `key` — Dot-notation key path to the servers section
- `serverName` — Name/key of the server entry to remove

**Returns**: `true` if the entry was removed, `false` otherwise

**Throws**

- If the format is unsupported

**Examples**

```typescript
const removed = await removeConfig("/path/to/config.json", "jsonc", "mcpServers", "my-server");
```

### `registerConfigCommand()`

*Defined in `src/commands/config.ts:36`*

```typescript
(program: Command) => void
```

Registers the `config` command group with show and path subcommands for viewing provider configurations.

**Parameters**

- `program` — The root Commander program to attach the config command group to

**Examples**

```bash
caamp config show claude-code --global
caamp config path cursor project
```

### `readLockFile()`

*Defined in `src/core/lock-utils.ts:88`*

```typescript
() => Promise<CaampLockFile>
```

Read and parse the CAAMP lock file from disk.

**Returns**: Parsed lock file contents

**Examples**

```typescript
const lock = await readLockFile();
console.log(Object.keys(lock.mcpServers));
```

### `writeLockFile()`

*Defined in `src/core/lock-utils.ts:118`*

```typescript
(lock: CaampLockFile) => Promise<void>
```

Write the lock file atomically under a process lock guard.

**Parameters**

- `lock` — Lock file data to persist

**Examples**

```typescript
const lock = await readLockFile();
lock.mcpServers["my-server"] = entry;
await writeLockFile(lock);
```

### `updateLockFile()`

*Defined in `src/core/lock-utils.ts:147`*

```typescript
(updater: (lock: CaampLockFile) => void | Promise<void>) => Promise<CaampLockFile>
```

Safely read-modify-write the lock file under a process lock guard.

**Parameters**

- `updater` — Callback that modifies the lock object (may be async)

**Returns**: The updated lock file contents after the write

**Examples**

```typescript
const updated = await updateLockFile((lock) => {
  lock.mcpServers["new-server"] = entry;
});
```

### `getCaampVersion()`

*Defined in `src/core/version.ts:25`*

```typescript
() => string
```

Retrieve the current CAAMP package version from the nearest `package.json`.

**Returns**: The semver version string (e.g. `"1.8.1"`)

**Examples**

```typescript
const version = getCaampVersion();
console.log(`CAAMP v${version}`);
```

### `registerDoctorCommand()`

*Defined in `src/commands/doctor.ts:450`*

```typescript
(program: Command) => void
```

Registers the `doctor` command for diagnosing configuration issues and overall system health.

**Parameters**

- `program` — The root Commander program to attach the doctor command to

**Examples**

```bash
caamp doctor --human
caamp doctor --json
```

### `registerInstructionsCheck()`

*Defined in `src/commands/instructions/check.ts:36`*

```typescript
(parent: Command) => void
```

Registers the `instructions check` subcommand for verifying injection status across providers.

**Parameters**

- `parent` — The parent `instructions` Command to attach the check subcommand to

**Examples**

```bash
caamp instructions check --human
caamp instructions check --agent claude-code
```

### `registerInstructionsInject()`

*Defined in `src/commands/instructions/inject.ts:44`*

```typescript
(parent: Command) => void
```

Registers the `instructions inject` subcommand for injecting instruction blocks into provider files.

**Parameters**

- `parent` — The parent `instructions` Command to attach the inject subcommand to

**Examples**

```bash
caamp instructions inject --all --global
caamp instructions inject --agent claude-code --dry-run
```

### `registerInstructionsUpdate()`

*Defined in `src/commands/instructions/update.ts:40`*

```typescript
(parent: Command) => void
```

Registers the `instructions update` subcommand for refreshing all instruction file injections.

**Parameters**

- `parent` — The parent `instructions` Command to attach the update subcommand to

**Examples**

```bash
caamp instructions update --yes
caamp instructions update --global --json
```

### `registerInstructionsCommands()`

*Defined in `src/commands/instructions/index.ts:31`*

```typescript
(program: Command) => void
```

Registers the `instructions` command group with inject, check, and update subcommands.

**Parameters**

- `program` — The root Commander program to attach the instructions command group to

**Examples**

```bash
caamp instructions inject --all
caamp instructions check --human
caamp instructions update --yes
```

### `resolveMcpConfigPath()`

*Defined in `src/core/mcp/reader.ts:103`*

```typescript
(provider: Provider, scope: McpScope, projectDir?: string) => string | null
```

Resolve a provider's MCP config file path for the given scope, or `null` if the provider does not declare an MCP capability or the scope is unsupported.

**Parameters**

- `provider` — Provider to resolve a config path for.
- `scope` — Scope to resolve.
- `projectDir` — Project directory used for the `project` scope.

**Returns**: The absolute config file path, or `null` when unavailable.

**Examples**

```typescript
const claudeCode = getProvider("claude-code")!;
const path = resolveMcpConfigPath(claudeCode, "project", "/tmp/app");
// e.g. "/tmp/app/.mcp.json"
```

### `listMcpServers()`

*Defined in `src/core/mcp/reader.ts:151`*

```typescript
(provider: Provider, scope: McpScope, projectDir?: string) => Promise<McpServerEntry[]>
```

List MCP server entries declared in a single provider's config file.

**Parameters**

- `provider` — Provider whose config file to read.
- `scope` — Scope to resolve (project|global).
- `projectDir` — Project directory used for the `project` scope   (defaults to `process.cwd()`).

**Returns**: Array of MCP server entries, or `[]` when nothing was found.

**Examples**

```typescript
const provider = getProvider("claude-code")!;
const entries = await listMcpServers(provider, "project");
for (const entry of entries) {
  console.log(entry.name, entry.configPath);
}
```

### `listAllMcpServers()`

*Defined in `src/core/mcp/reader.ts:234`*

```typescript
(scope: McpScope, projectDir?: string) => Promise<McpServerEntriesByProvider>
```

List MCP server entries for every MCP-capable provider in the registry at the given scope.

**Parameters**

- `scope` — Scope to resolve for every provider.
- `projectDir` — Project directory used for the `project` scope.

**Returns**: Map of provider id → server entries.

**Examples**

```typescript
const byProvider = await listAllMcpServers("global");
for (const [providerId, entries] of byProvider) {
  console.log(`${providerId}: ${entries.length} server(s)`);
}
```

### `detectMcpInstallations()`

*Defined in `src/core/mcp/reader.ts:271`*

```typescript
(scope: McpScope, projectDir?: string) => Promise<McpDetectionEntry[]>
```

Probe every MCP-capable provider in the registry to determine which ones have a config file on disk and how many servers are configured.

**Parameters**

- `scope` — Scope to probe.
- `projectDir` — Project directory used for the `project` scope.

**Returns**: Array of detection entries, one per MCP-capable provider.

**Examples**

```typescript
const hits = await detectMcpInstallations("project");
const installed = hits.filter((h) => h.exists);
console.log(`MCP found on ${installed.length} providers`);
```

### `installMcpServer()`

*Defined in `src/core/mcp/installer.ts:108`*

```typescript
(provider: Provider, serverName: string, config: McpServerConfig, opts: InstallMcpServerOptions) => Promise<InstallMcpServerResult>
```

Install an MCP server entry into a single provider's config file.

**Parameters**

- `provider` — Target provider.
- `serverName` — Name/key for the new server entry.
- `config` — Canonical `McpServerConfig` payload to write.
- `opts` — Install options (scope, force, projectDir).

**Returns**: Structured install result describing what happened.

**Throws**

- `Error` when the provider has no MCP capability or no   project-scoped config path is available.

**Examples**

```typescript
const provider = getProvider("claude-code")!;
const result = await installMcpServer(
  provider,
  "github",
  { command: "npx", args: ["-y", "@modelcontextprotocol/server-github"] },
  { scope: "project", force: false },
);
console.log(result.installed, result.conflicted);
```

### `removeMcpServer()`

*Defined in `src/core/mcp/remover.ts:93`*

```typescript
(provider: Provider, serverName: string, opts: RemoveMcpServerOptions) => Promise<RemoveMcpServerResult>
```

Remove an MCP server entry from a single provider's config file.

**Parameters**

- `provider` — Provider whose config file to modify.
- `serverName` — Server name/key to remove.
- `opts` — Removal options.

**Returns**: Structured result describing whether the entry was removed.

**Examples**

```typescript
const provider = getProvider("claude-code")!;
const result = await removeMcpServer(provider, "my-server", { scope: "project" });
console.log(result.removed); // true | false
```

### `removeMcpServerFromAll()`

*Defined in `src/core/mcp/remover.ts:163`*

```typescript
(serverName: string, opts: RemoveMcpServerOptions) => Promise<RemoveMcpServerResult[]>
```

Remove an MCP server entry from every MCP-capable provider in the registry that currently has it configured.

**Parameters**

- `serverName` — Server name/key to remove from every provider.
- `opts` — Removal options applied uniformly to every provider.

**Returns**: Array of per-provider removal results.

**Examples**

```typescript
const results = await removeMcpServerFromAll("my-server", { scope: "global" });
const removed = results.filter((r) => r.removed);
console.log(`Removed from ${removed.length} providers`);
```

### `requireMcpProvider()`

*Defined in `src/commands/mcp/common.ts:95`*

```typescript
(providerId: string) => Provider
```

Look up an MCP-capable provider in the registry by id, throwing a typed `LAFSCommandError` when the id is unknown or the provider does not declare an MCP capability.

**Parameters**

- `providerId` — Raw provider id supplied via `--provider <id>`.

**Returns**: The resolved `Provider` entry.

**Throws**

- `LAFSCommandError` when the provider is unknown or has no   MCP capability.

**Examples**

```typescript
const provider = requireMcpProvider("claude-code");
console.log(provider.toolName); // "Claude Code"
```

### `parseScope()`

*Defined in `src/commands/mcp/common.ts:140`*

```typescript
(raw: string | undefined, defaultScope: McpScope) => McpScope
```

Parse and validate a `--scope` option value into a typed `McpScope`.

**Parameters**

- `raw` — The raw option value from Commander (may be undefined).
- `defaultScope` — Scope to use when `raw` is undefined.

**Returns**: A resolved `McpScope`.

**Throws**

- `LAFSCommandError` when `raw` is set to an invalid value.

**Examples**

```typescript
parseScope(undefined, "project"); // "project"
parseScope("global", "project");  // "global"
parseScope("weird", "project");   // throws LAFSCommandError(E_VALIDATION_SCHEMA)
```

### `resolveProjectDir()`

*Defined in `src/commands/mcp/common.ts:174`*

```typescript
(scope: McpScope, explicit: string | undefined) => string | undefined
```

Resolve the project directory used for the `project` scope.

**Parameters**

- `scope` — Resolved scope.
- `explicit` — The raw `--project-dir` option value.

**Returns**: Absolute project dir for `project`, else `undefined`.

**Examples**

```typescript
resolveProjectDir("project", undefined);  // process.cwd()
resolveProjectDir("project", "/tmp/app"); // "/tmp/app"
resolveProjectDir("global", "/tmp/app");  // undefined
```

### `parseEnvAssignment()`

*Defined in `src/commands/mcp/common.ts:205`*

```typescript
(raw: string) => [string, string]
```

Parse a single `--env KEY=VALUE` option value into a `[key, value]` pair, throwing a typed validation error when the shape is wrong.

**Parameters**

- `raw` — Single `KEY=VALUE` token from Commander.

**Returns**: Tuple of `[key, value]`.

**Throws**

- `LAFSCommandError` when the token is malformed.

**Examples**

```typescript
parseEnvAssignment("GITHUB_TOKEN=ghp_abc"); // ["GITHUB_TOKEN", "ghp_abc"]
parseEnvAssignment("NO_EQUALS");            // throws LAFSCommandError
```

### `registerMcpDetectCommand()`

*Defined in `src/commands/mcp/detect.ts:55`*

```typescript
(parent: Command) => void
```

Registers the `caamp mcp detect` subcommand.

**Parameters**

- `parent` — Parent `mcp` Command to attach the subcommand to.

**Examples**

```bash
caamp mcp detect
caamp mcp detect --scope global
caamp mcp detect --only-existing
```

### `registerMcpInstallCommand()`

*Defined in `src/commands/mcp/install.ts:216`*

```typescript
(parent: Command) => void
```

Registers the `caamp mcp install` subcommand.

**Parameters**

- `parent` — Parent `mcp` Command to attach the subcommand to.

**Examples**

```bash
# Inline form
caamp mcp install github --provider claude-desktop -- \
  npx -y @modelcontextprotocol/server-github

# From file
caamp mcp install github --provider cursor --from ./github.json

# With env vars
caamp mcp install github --provider claude-code \
  --env GITHUB_TOKEN=ghp_xxx -- \
  npx -y @modelcontextprotocol/server-github
```

### `registerMcpListCommand()`

*Defined in `src/commands/mcp/list.ts:66`*

```typescript
(parent: Command) => void
```

Registers the `caamp mcp list` subcommand.

**Parameters**

- `parent` — Parent `mcp` Command to attach the subcommand to.

**Examples**

```bash
caamp mcp list
caamp mcp list --provider claude-code
caamp mcp list --provider cursor --scope global
```

### `registerMcpRemoveCommand()`

*Defined in `src/commands/mcp/remove.ts:63`*

```typescript
(parent: Command) => void
```

Registers the `caamp mcp remove` subcommand.

**Parameters**

- `parent` — Parent `mcp` Command to attach the subcommand to.

**Examples**

```bash
caamp mcp remove github --provider claude-desktop
caamp mcp remove github --provider cursor --scope global
caamp mcp remove github --all-providers
```

### `registerMcpCommands()`

*Defined in `src/commands/mcp/index.ts:53`*

```typescript
(program: Command) => void
```

Register the `mcp` command group and all sub-verbs on the root program.

**Parameters**

- `program` — The root Commander program to attach the `mcp`   group to.

**Examples**

```bash
caamp mcp detect
caamp mcp list --provider claude-code
caamp mcp install github --provider claude-desktop -- npx -y @modelcontextprotocol/server-github
caamp mcp remove github --provider claude-desktop
```

### `fetchWithTimeout()`

*Defined in `src/core/network/fetch.ts:62`*

```typescript
(url: string, init?: RequestInit, timeoutMs?: number) => Promise<Response>
```

Fetch a URL with an automatic timeout via `AbortSignal.timeout`.

**Parameters**

- `url` — URL to fetch
- `init` — Optional `RequestInit` options forwarded to `fetch`
- `timeoutMs` — Timeout in milliseconds (defaults to `DEFAULT_FETCH_TIMEOUT_MS`)

**Returns**: The `Response` object from the fetch call

**Throws**

- `NetworkError` on timeout or network failure

**Examples**

```typescript
const response = await fetchWithTimeout("https://api.example.com/data", undefined, 5000);
```

### `ensureOkResponse()`

*Defined in `src/core/network/fetch.ts:100`*

```typescript
(response: Response, url: string) => Response
```

Assert that a `Response` has an OK status, throwing on failure.

**Parameters**

- `response` — Fetch `Response` to validate
- `url` — Original request URL (included in the error)

**Returns**: The same `Response` if status is OK

**Throws**

- `NetworkError` when `response.ok` is `false`

**Examples**

```typescript
const res = await fetchWithTimeout(url);
ensureOkResponse(res, url);
```

### `formatNetworkError()`

*Defined in `src/core/network/fetch.ts:134`*

```typescript
(error: unknown) => string
```

Format a network error into a user-friendly message string.

**Parameters**

- `error` — The caught error value

**Returns**: Human-readable error description

**Examples**

```typescript
try {
  await fetchWithTimeout(url);
} catch (err) {
  console.error(formatNetworkError(err));
}
```

### `cloneRepo()`

*Defined in `src/core/sources/github.ts:51`*

```typescript
(owner: string, repo: string, ref?: string, subPath?: string) => Promise<GitFetchResult>
```

Clone a GitHub repo to a temp directory.

**Parameters**

- `owner` — GitHub repository owner (user or organization)
- `repo` — GitHub repository name
- `ref` — Branch or tag to clone (defaults to the repo's default branch)
- `subPath` — Subdirectory within the repo to target

**Returns**: Object with local path and cleanup function

**Examples**

```typescript
const { localPath, cleanup } = await cloneRepo("anthropics", "courses", "main", "skills");
try {
  console.log(`Cloned to: ${localPath}`);
} finally {
  await cleanup();
}
```

### `fetchRawFile()`

*Defined in `src/core/sources/github.ts:106`*

```typescript
(owner: string, repo: string, path: string, ref?: string) => Promise<string | null>
```

Fetch a specific file from GitHub using the raw API.

**Parameters**

- `owner` — GitHub repository owner
- `repo` — GitHub repository name
- `path` — File path within the repository
- `ref` — Branch or tag to fetch from (defaults to `"main"`)

**Returns**: File content as a string, or `null` if the file cannot be fetched

**Examples**

```typescript
const content = await fetchRawFile("owner", "repo", "skills/my-skill/SKILL.md");
if (content) {
  console.log(content);
}
```

### `repoExists()`

*Defined in `src/core/sources/github.ts:142`*

```typescript
(owner: string, repo: string) => Promise<boolean>
```

Check if a GitHub repo exists.

**Parameters**

- `owner` — GitHub repository owner
- `repo` — GitHub repository name

**Returns**: `true` if the repository exists and is accessible

**Examples**

```typescript
const exists = await repoExists("anthropics", "courses");
console.log(exists ? "Repo found" : "Repo not found");
```

### `cloneGitLabRepo()`

*Defined in `src/core/sources/gitlab.ts:38`*

```typescript
(owner: string, repo: string, ref?: string, subPath?: string) => Promise<GitFetchResult>
```

Clone a GitLab repo to a temp directory.

**Parameters**

- `owner` — GitLab repository owner (user or group)
- `repo` — GitLab repository name
- `ref` — Branch or tag to clone (defaults to the repo's default branch)
- `subPath` — Subdirectory within the repo to target

**Returns**: Object with local path and cleanup function

**Examples**

```typescript
const { localPath, cleanup } = await cloneGitLabRepo("mygroup", "skills-repo");
try {
  console.log(`Cloned to: ${localPath}`);
} finally {
  await cleanup();
}
```

### `fetchGitLabRawFile()`

*Defined in `src/core/sources/gitlab.ts:94`*

```typescript
(owner: string, repo: string, path: string, ref?: string) => Promise<string | null>
```

Fetch a specific file from GitLab using the raw API.

**Parameters**

- `owner` — GitLab repository owner (user or group)
- `repo` — GitLab repository name
- `path` — File path within the repository
- `ref` — Branch or tag to fetch from (defaults to `"main"`)

**Returns**: File content as a string, or `null` if the file cannot be fetched

**Examples**

```typescript
const content = await fetchGitLabRawFile("mygroup", "skills", "my-skill/SKILL.md");
if (content) {
  console.log(content);
}
```

### `parseSource()`

*Defined in `src/core/sources/parser.ts:108`*

```typescript
(input: string) => ParsedSource
```

Parse and classify a source string into a typed `ParsedSource`.

**Parameters**

- `input` — Raw source string to classify

**Returns**: Parsed source with type, value, and inferred name

**Examples**

```typescript
parseSource("owner/repo");
// { type: "github", value: "https://github.com/owner/repo", inferredName: "repo", ... }

parseSource("https://mcp.example.com/sse");
// { type: "remote", value: "https://mcp.example.com/sse", inferredName: "example" }

parseSource("@modelcontextprotocol/server-filesystem");
// { type: "package", value: "@modelcontextprotocol/server-filesystem", inferredName: "filesystem" }
```

### `isMarketplaceScoped()`

*Defined in `src/core/sources/parser.ts:254`*

```typescript
(input: string) => boolean
```

Check if a source string looks like a marketplace scoped name (`@author/name`).

**Parameters**

- `input` — Source string to check

**Returns**: `true` if the input matches the `@scope/name` pattern

**Examples**

```typescript
isMarketplaceScoped("@anthropic/my-skill"); // true
isMarketplaceScoped("my-skill");             // false
isMarketplaceScoped("owner/repo");           // false
```

### `requirePiHarness()`

*Defined in `src/commands/pi/common.ts:100`*

```typescript
() => PiHarness
```

Resolve and validate Pi's installation, returning a ready-to-use `PiHarness`.

**Returns**: A PiHarness bound to the resolved Pi provider entry.

**Throws**

- `LAFSCommandError` when Pi is not installed.

**Examples**

```typescript
// Inside a `caamp pi <verb>` command action:
const harness = requirePiHarness();
const skills = await harness.listSkills({ kind: "global" });
```

### `parseScope()`

*Defined in `src/commands/pi/common.ts:157`*

```typescript
(raw: string | undefined, defaultTier: HarnessTier) => HarnessTier
```

Parse and validate a `--scope` option value into a typed tier.

**Parameters**

- `raw` — The raw option value from Commander (may be undefined).
- `defaultTier` — Tier to use when `raw` is undefined.

**Returns**: A resolved `HarnessTier`.

**Throws**

- `LAFSCommandError` when `raw` is set to an invalid value.

**Examples**

```typescript
parseScope(undefined, "project"); // "project"
parseScope("user", "project");    // "user"
parseScope("weird", "project");   // throws LAFSCommandError(E_VALIDATION_SCHEMA)
```

### `resolveProjectDir()`

*Defined in `src/commands/pi/common.ts:190`*

```typescript
(tier: HarnessTier, explicit: string | undefined) => string | undefined
```

Resolve the project directory to use for the `project` tier, honouring an explicit `--project-dir` flag and falling back to cwd.

**Parameters**

- `tier` — Resolved tier the verb is targeting.
- `explicit` — The raw `--project-dir` option value.

**Returns**: Absolute project dir when `tier === 'project'`, else `undefined`.

**Examples**

```typescript
resolveProjectDir("project", undefined);  // process.cwd()
resolveProjectDir("project", "/tmp/app"); // "/tmp/app"
resolveProjectDir("user", "/tmp/app");    // undefined
```

### `registerPiCantCommands()`

*Defined in `src/commands/pi/cant.ts:365`*

```typescript
(parent: Command) => void
```

Registers the `caamp pi cant` command group.

**Parameters**

- `parent` — The parent `pi` Command to attach the cant group to.

**Examples**

```bash
caamp pi cant list
caamp pi cant install ./my-profile.cant --scope user
caamp pi cant install owner/repo/path/profile.cant --scope global
caamp pi cant remove my-profile --scope user
caamp pi cant validate ./my-profile.cant
```

### `registerPiExtensionsCommands()`

*Defined in `src/commands/pi/extensions.ts:269`*

```typescript
(parent: Command) => void
```

Registers the `caamp pi extensions` command group.

**Parameters**

- `parent` — The parent `pi` Command to attach the extensions group to.

**Examples**

```bash
caamp pi extensions list
caamp pi extensions install ./my-ext.ts --scope user
caamp pi extensions install owner/repo/path/ext.ts --scope global
caamp pi extensions remove my-ext --scope user
```

### `registerPiModelsCommands()`

*Defined in `src/commands/pi/models.ts:147`*

```typescript
(parent: Command) => void
```

Registers the `caamp pi models` command group.

**Parameters**

- `parent` — The parent `pi` Command to attach the models group to.

**Examples**

```bash
caamp pi models list
caamp pi models add custom-provider:my-model --display-name "My Model"
caamp pi models enable anthropic:claude-opus-4-20250514
caamp pi models default anthropic:claude-sonnet-4-20250514
```

### `registerPiPromptsCommands()`

*Defined in `src/commands/pi/prompts.ts:96`*

```typescript
(parent: Command) => void
```

Registers the `caamp pi prompts` command group.

**Parameters**

- `parent` — The parent `pi` Command to attach the prompts group to.

**Examples**

```bash
caamp pi prompts install ./prompts/my-prompt --scope user
caamp pi prompts list
caamp pi prompts remove my-prompt --scope user
```

### `registerPiSessionsCommands()`

*Defined in `src/commands/pi/sessions.ts:194`*

```typescript
(parent: Command) => void
```

Registers the `caamp pi sessions` command group.

**Parameters**

- `parent` — The parent `pi` Command to attach the sessions group to.

**Examples**

```bash
caamp pi sessions list
caamp pi sessions show sess-abc123
caamp pi sessions export sess-abc123 --md --output session.md
caamp pi sessions resume sess-abc123
```

### `registerPiThemesCommands()`

*Defined in `src/commands/pi/themes.ts:97`*

```typescript
(parent: Command) => void
```

Registers the `caamp pi themes` command group.

**Parameters**

- `parent` — The parent `pi` Command to attach the themes group to.

**Examples**

```bash
caamp pi themes install ./themes/my-theme.json --scope user
caamp pi themes list
caamp pi themes remove my-theme --scope user
```

### `registerPiCommands()`

*Defined in `src/commands/pi/index.ts:51`*

```typescript
(program: Command) => void
```

Register the `pi` command group and all Wave-1 sub-verbs on the root program.

**Parameters**

- `program` — The root Commander program to attach the `pi` group to.

**Examples**

```bash
caamp pi extensions list
caamp pi sessions list
caamp pi models list
caamp pi prompts list
caamp pi themes list
caamp pi cant list
```

### `resetHookMappings()`

*Defined in `src/core/hooks/normalizer.ts:68`*

```typescript
() => void
```

Reset the cached hook mappings data.

**Examples**

```typescript
import { resetHookMappings, getHookMappingsVersion } from "./normalizer.js";

// Force a fresh load from disk
resetHookMappings();
const version = getHookMappingsVersion();
```

### `getCanonicalEvent()`

*Defined in `src/core/hooks/normalizer.ts:96`*

```typescript
(event: CanonicalHookEvent) => CanonicalEventDefinition
```

Get the canonical event definition (category, description, canBlock).

**Parameters**

- `event` — The canonical event name to look up.

**Returns**: The event definition containing category, description, and canBlock flag.

**Examples**

```typescript
import { getCanonicalEvent } from "./normalizer.js";

const def = getCanonicalEvent("PreToolUse");
console.log(def.category);  // "tool"
console.log(def.canBlock);  // true
```

### `getAllCanonicalEvents()`

*Defined in `src/core/hooks/normalizer.ts:123`*

```typescript
() => Record<CanonicalHookEvent, CanonicalEventDefinition>
```

Get all canonical event definitions.

**Returns**: A record mapping every canonical event name to its definition.

**Examples**

```typescript
import { getAllCanonicalEvents } from "./normalizer.js";

const events = getAllCanonicalEvents();
for (const [name, def] of Object.entries(events)) {
  console.log(`${name}: ${def.description}`);
}
```

### `getCanonicalEventsByCategory()`

*Defined in `src/core/hooks/normalizer.ts:148`*

```typescript
(category: HookCategory) => CanonicalHookEvent[]
```

Get canonical events filtered by category.

**Parameters**

- `category` — The hook category to filter by (e.g. `"session"`, `"tool"`).

**Returns**: Array of canonical event names that belong to the specified category.

**Examples**

```typescript
import { getCanonicalEventsByCategory } from "./normalizer.js";

const toolEvents = getCanonicalEventsByCategory("tool");
// ["PreToolUse", "PostToolUse", "PostToolUseFailure"]
```

### `getProviderHookProfile()`

*Defined in `src/core/hooks/normalizer.ts:177`*

```typescript
(providerId: string) => ProviderHookProfile | undefined
```

Get the full hook profile for a provider.

**Parameters**

- `providerId` — The provider identifier (e.g. `"claude-code"`, `"gemini-cli"`).

**Returns**: The provider's hook profile, or `undefined` if not found.

**Examples**

```typescript
import { getProviderHookProfile } from "./normalizer.js";

const profile = getProviderHookProfile("claude-code");
if (profile) {
  console.log(profile.hookSystem); // "config"
  console.log(profile.experimental); // false
}
```

### `getMappedProviderIds()`

*Defined in `src/core/hooks/normalizer.ts:202`*

```typescript
() => string[]
```

Get all provider IDs that have hook mappings.

**Returns**: Array of provider ID strings.

**Examples**

```typescript
import { getMappedProviderIds } from "./normalizer.js";

const ids = getMappedProviderIds();
// ["claude-code", "gemini-cli", "cursor", "kimi", ...]
```

### `toNative()`

*Defined in `src/core/hooks/normalizer.ts:233`*

```typescript
(canonical: CanonicalHookEvent, providerId: string) => string | null
```

Translate a CAAMP canonical event name to the provider's native name.

**Parameters**

- `canonical` — The CAAMP canonical event name to translate.
- `providerId` — The target provider identifier.

**Returns**: The native event name, or `null` if unsupported.

**Examples**

```typescript
import { toNative } from "./normalizer.js";

toNative("PreToolUse", "claude-code");   // "PreToolUse"
toNative("PreToolUse", "gemini-cli");    // "BeforeTool"
toNative("PreToolUse", "cursor");        // "preToolUse"
toNative("PreToolUse", "kimi");          // null
```

### `toCanonical()`

*Defined in `src/core/hooks/normalizer.ts:263`*

```typescript
(nativeName: string, providerId: string) => CanonicalHookEvent | null
```

Translate a provider-native event name to the CAAMP canonical name.

**Parameters**

- `nativeName` — The provider-native event name to look up.
- `providerId` — The provider identifier to search within.

**Returns**: The canonical event name, or `null` if no mapping exists.

**Examples**

```typescript
import { toCanonical } from "./normalizer.js";

toCanonical("BeforeTool", "gemini-cli");     // "PreToolUse"
toCanonical("stop", "cursor");               // "ResponseComplete"
toCanonical("UserPromptSubmit", "claude-code"); // "PromptSubmit"
```

### `toNativeBatch()`

*Defined in `src/core/hooks/normalizer.ts:301`*

```typescript
(canonicals: CanonicalHookEvent[], providerId: string) => NormalizedHookEvent[]
```

Batch-translate multiple canonical events to native names for a provider.

**Parameters**

- `canonicals` — Array of canonical event names to translate.
- `providerId` — The target provider identifier.

**Returns**: Array of normalized events (only supported ones included).

**Examples**

```typescript
import { toNativeBatch } from "./normalizer.js";

const events = toNativeBatch(
  ["PreToolUse", "PostToolUse", "ConfigChange"],
  "claude-code",
);
// Returns NormalizedHookEvent[] for supported events only
```

### `supportsHook()`

*Defined in `src/core/hooks/normalizer.ts:348`*

```typescript
(canonical: CanonicalHookEvent, providerId: string) => boolean
```

Check if a provider supports a specific canonical hook event.

**Parameters**

- `canonical` — The canonical event name to check.
- `providerId` — The provider identifier to check against.

**Returns**: `true` if the provider supports this canonical event, `false` otherwise.

**Examples**

```typescript
import { supportsHook } from "./normalizer.js";

supportsHook("PreToolUse", "claude-code"); // true
supportsHook("PreToolUse", "kimi");        // false
```

### `getHookSupport()`

*Defined in `src/core/hooks/normalizer.ts:378`*

```typescript
(canonical: CanonicalHookEvent, providerId: string) => HookSupportResult
```

Get full hook support details for a canonical event on a provider.

**Parameters**

- `canonical` — The canonical event name to query.
- `providerId` — The provider identifier to query against.

**Returns**: Support result including native name and optional notes.

**Examples**

```typescript
import { getHookSupport } from "./normalizer.js";

const result = getHookSupport("PreToolUse", "claude-code");
console.log(result.supported); // true
console.log(result.native);    // "PreToolUse"
```

### `getSupportedEvents()`

*Defined in `src/core/hooks/normalizer.ts:415`*

```typescript
(providerId: string) => CanonicalHookEvent[]
```

Get all supported canonical events for a provider.

**Parameters**

- `providerId` — The provider identifier to query.

**Returns**: Array of canonical event names the provider supports.

**Examples**

```typescript
import { getSupportedEvents } from "./normalizer.js";

const events = getSupportedEvents("claude-code");
// ["SessionStart", "SessionEnd", "PreToolUse", ...]
```

### `getUnsupportedEvents()`

*Defined in `src/core/hooks/normalizer.ts:442`*

```typescript
(providerId: string) => CanonicalHookEvent[]
```

Get all unsupported canonical events for a provider.

**Parameters**

- `providerId` — The provider identifier to query.

**Returns**: Array of canonical event names the provider does not support.

**Examples**

```typescript
import { getUnsupportedEvents } from "./normalizer.js";

const missing = getUnsupportedEvents("kimi");
// Returns all canonical events (kimi has no hook support)
```

### `getProvidersForEvent()`

*Defined in `src/core/hooks/normalizer.ts:470`*

```typescript
(canonical: CanonicalHookEvent) => string[]
```

Get providers that support a specific canonical event.

**Parameters**

- `canonical` — The canonical event name to search for.

**Returns**: Array of provider IDs that support this event.

**Examples**

```typescript
import { getProvidersForEvent } from "./normalizer.js";

const providers = getProvidersForEvent("PreToolUse");
// ["claude-code", "gemini-cli", "cursor"]
```

### `getCommonEvents()`

*Defined in `src/core/hooks/normalizer.ts:499`*

```typescript
(providerIds: string[]) => CanonicalHookEvent[]
```

Get canonical events common to all specified providers.

**Parameters**

- `providerIds` — Array of provider IDs to intersect.

**Returns**: Array of canonical events supported by all specified providers.

**Examples**

```typescript
import { getCommonEvents } from "./normalizer.js";

const common = getCommonEvents(["claude-code", "gemini-cli"]);
// Returns only events both providers support
```

### `getProviderSummary()`

*Defined in `src/core/hooks/normalizer.ts:532`*

```typescript
(providerId: string) => ProviderHookSummary | undefined
```

Get a summary of hook support for a provider.

**Parameters**

- `providerId` — The provider identifier to summarize.

**Returns**: The hook support summary, or `undefined` if the provider is not found.

**Examples**

```typescript
import { getProviderSummary } from "./normalizer.js";

const summary = getProviderSummary("claude-code");
if (summary) {
  console.log(`${summary.coverage}% coverage`);
  console.log(`${summary.supportedCount}/${summary.totalCanonical} events`);
}
```

### `buildHookMatrix()`

*Defined in `src/core/hooks/normalizer.ts:578`*

```typescript
(providerIds?: string[]) => CrossProviderMatrix
```

Build a cross-provider hook support matrix.

**Parameters**

- `providerIds` — Optional array of provider IDs to include. Defaults to all mapped providers.

**Returns**: The cross-provider matrix with events, providers, and mapping data.

**Examples**

```typescript
import { buildHookMatrix } from "./normalizer.js";

const matrix = buildHookMatrix(["claude-code", "gemini-cli"]);
for (const event of matrix.events) {
  for (const provider of matrix.providers) {
    console.log(`${event} @ ${provider}: ${matrix.matrix[event][provider].supported}`);
  }
}
```

### `getHookSystemType()`

*Defined in `src/core/hooks/normalizer.ts:623`*

```typescript
(providerId: string) => HookSystemType
```

Get the hook system type for a provider.

**Parameters**

- `providerId` — The provider identifier to query.

**Returns**: The hook system type (`"config"`, `"plugin"`, or `"none"`).

**Examples**

```typescript
import { getHookSystemType } from "./normalizer.js";

getHookSystemType("claude-code"); // "config"
getHookSystemType("unknown");     // "none"
```

### `getHookConfigPath()`

*Defined in `src/core/hooks/normalizer.ts:649`*

```typescript
(providerId: string) => string | null
```

Get the resolved hook config path for a provider.

**Parameters**

- `providerId` — The provider identifier to query.

**Returns**: The resolved filesystem path, or `null` if not available.

**Examples**

```typescript
import { getHookConfigPath } from "./normalizer.js";

const path = getHookConfigPath("claude-code");
// "/home/user/.claude/settings.json" (resolved from template)
```

### `getProviderOnlyEvents()`

*Defined in `src/core/hooks/normalizer.ts:676`*

```typescript
(providerId: string) => string[]
```

Get provider-only events (native events with no canonical mapping).

**Parameters**

- `providerId` — The provider identifier to query.

**Returns**: Array of native event names unique to this provider.

**Examples**

```typescript
import { getProviderOnlyEvents } from "./normalizer.js";

const extras = getProviderOnlyEvents("claude-code");
// Returns any events specific to Claude Code with no canonical equivalent
```

### `translateToAll()`

*Defined in `src/core/hooks/normalizer.ts:707`*

```typescript
(canonical: CanonicalHookEvent, providerIds: string[]) => Record<string, string>
```

Translate a canonical event to native names across multiple providers.

**Parameters**

- `canonical` — The canonical event name to translate.
- `providerIds` — Array of provider IDs to translate for.

**Returns**: Record mapping provider IDs to their native event names (supported only).

**Examples**

```typescript
import { translateToAll } from "./normalizer.js";

const result = translateToAll("PreToolUse", ["claude-code", "gemini-cli", "kimi"]);
// { "claude-code": "PreToolUse", "gemini-cli": "BeforeTool" }
// (kimi excluded — unsupported)
```

### `resolveNativeEvent()`

*Defined in `src/core/hooks/normalizer.ts:743`*

```typescript
(nativeName: string) => Array<{ providerId: string; canonical: CanonicalHookEvent; }>
```

Find the best canonical match for a native event name across all providers.

**Parameters**

- `nativeName` — The provider-native event name to resolve.

**Returns**: Array of matches, each containing the provider ID and canonical event name.

**Examples**

```typescript
import { resolveNativeEvent } from "./normalizer.js";

const matches = resolveNativeEvent("BeforeTool");
// [{ providerId: "gemini-cli", canonical: "PreToolUse" }]
```

### `getHookMappingsVersion()`

*Defined in `src/core/hooks/normalizer.ts:781`*

```typescript
() => string
```

Get the version of the hook mappings data.

**Returns**: The semver version string of the loaded hook mappings data.

**Examples**

```typescript
import { getHookMappingsVersion } from "./normalizer.js";

const version = getHookMappingsVersion();
console.log(`Hook mappings v${version}`);
```

### `registerProvidersCommand()`

*Defined in `src/commands/providers.ts:59`*

```typescript
(program: Command) => void
```

Registers the `providers` command group with list, detect, show, skills-map, hooks, and capabilities subcommands.

**Parameters**

- `program` — The root Commander program to attach the providers command group to

**Examples**

```bash
caamp providers list --tier high
caamp providers detect --project
caamp providers show claude-code
```

### `getRulesByCategory()`

*Defined in `src/core/skills/audit/rules.ts:414`*

```typescript
(category: string) => AuditRule[]
```

Get audit rules filtered by category.

**Parameters**

- `category` — Category name to filter by

**Returns**: Array of rules matching the given category

**Examples**

```typescript
const piRules = getRulesByCategory("prompt-injection");
console.log(`${piRules.length} prompt injection rules`);
```

### `getRulesBySeverity()`

*Defined in `src/core/skills/audit/rules.ts:436`*

```typescript
(severity: AuditSeverity) => AuditRule[]
```

Get audit rules filtered by severity level.

**Parameters**

- `severity` — Severity level to filter by

**Returns**: Array of rules matching the given severity

**Examples**

```typescript
const criticalRules = getRulesBySeverity("critical");
console.log(`${criticalRules.length} critical rules`);
```

### `getCategories()`

*Defined in `src/core/skills/audit/rules.ts:456`*

```typescript
() => string[]
```

Get all unique rule categories.

**Returns**: Array of unique category name strings

**Examples**

```typescript
const categories = getCategories();
// ["prompt-injection", "command-injection", "data-exfiltration", ...]
```

### `scanFile()`

*Defined in `src/core/skills/audit/scanner.ts:41`*

```typescript
(filePath: string, rules?: AuditRule[]) => Promise<AuditResult>
```

Scan a single file against security audit rules.

**Parameters**

- `filePath` — Absolute path to the file to scan
- `rules` — Custom rules to scan against (defaults to the built-in 46+ rules)

**Returns**: Audit result with findings, score, and pass/fail status

**Examples**

```typescript
const result = await scanFile("/path/to/SKILL.md");
console.log(`Score: ${result.score}/100, Passed: ${result.passed}`);
```

### `scanDirectory()`

*Defined in `src/core/skills/audit/scanner.ts:99`*

```typescript
(dirPath: string) => Promise<AuditResult[]>
```

Scan a directory of skills for security issues.

**Parameters**

- `dirPath` — Absolute path to the skills directory to scan

**Returns**: Array of audit results, one per scanned SKILL.md

**Examples**

```typescript
import { getCanonicalSkillsDir } from "../../paths/standard.js";

const results = await scanDirectory(getCanonicalSkillsDir());
const failing = results.filter(r => !r.passed);
```

### `toSarif()`

*Defined in `src/core/skills/audit/scanner.ts:139`*

```typescript
(results: AuditResult[]) => object
```

Convert audit results to SARIF 2.1.0 format (Static Analysis Results Interchange Format).

**Parameters**

- `results` — Array of audit results to convert

**Returns**: SARIF 2.1.0 JSON object

**Examples**

```typescript
const results = await scanDirectory("/path/to/skills");
const sarif = toSarif(results);
writeFileSync("audit.sarif", JSON.stringify(sarif, null, 2));
```

### `registerSkillsAudit()`

*Defined in `src/commands/skills/audit.ts:59`*

```typescript
(parent: Command) => void
```

Registers the `skills audit` subcommand for security scanning skill files.

**Parameters**

- `parent` — The parent `skills` Command to attach the audit subcommand to

**Examples**

```bash
caamp skills audit ./my-skill/SKILL.md
caamp skills audit ./skills-dir --sarif
```

### `recordSkillInstall()`

*Defined in `src/core/skills/lock.ts:46`*

```typescript
(skillName: string, scopedName: string, source: string, sourceType: SourceType, agents: string[], canonicalPath: string, isGlobal: boolean, projectDir?: string, version?: string) => Promise<void>
```

Record a skill installation in the lock file.

**Parameters**

- `skillName` — Skill name
- `scopedName` — Scoped name (may include marketplace scope)
- `source` — Original source string
- `sourceType` — Classified source type
- `agents` — Provider IDs the skill was linked to
- `canonicalPath` — Absolute path to the canonical installation
- `isGlobal` — Whether this is a global installation
- `projectDir` — Project directory (for project-scoped installs)
- `version` — Version string or commit SHA

**Examples**

```typescript
import { getCanonicalSkillsDir } from "../paths/standard.js";
import { join } from "node:path";

await recordSkillInstall(
  "my-skill", "my-skill", "owner/repo", "github",
  ["claude-code"], join(getCanonicalSkillsDir(), "my-skill"), true,
);
```

### `removeSkillFromLock()`

*Defined in `src/core/skills/lock.ts:95`*

```typescript
(skillName: string) => Promise<boolean>
```

Remove a skill entry from the lock file.

**Parameters**

- `skillName` — Name of the skill to remove

**Returns**: `true` if the entry was found and removed, `false` if not found

**Examples**

```typescript
const removed = await removeSkillFromLock("my-skill");
console.log(removed ? "Removed" : "Not found");
```

### `getTrackedSkills()`

*Defined in `src/core/skills/lock.ts:123`*

```typescript
() => Promise<Record<string, LockEntry>>
```

Get all skills tracked in the lock file.

**Returns**: Record of skill name to lock entry

**Examples**

```typescript
const skills = await getTrackedSkills();
for (const [name, entry] of Object.entries(skills)) {
  console.log(`${name}: ${entry.source}`);
}
```

### `checkSkillUpdate()`

*Defined in `src/core/skills/lock.ts:176`*

```typescript
(skillName: string) => Promise<{ hasUpdate: boolean; currentVersion?: string; latestVersion?: string; status: "up-to-date" | "update-available" | "unknown"; }>
```

Check if a skill has updates available by comparing the installed version against the latest remote commit SHA.

**Parameters**

- `skillName` — Name of the installed skill to check

**Returns**: Object with update status, current version, and latest version

**Examples**

```typescript
const update = await checkSkillUpdate("my-skill");
if (update.hasUpdate) {
  console.log(`Update available: ${update.currentVersion} -> ${update.latestVersion}`);
}
```

### `checkAllSkillUpdates()`

*Defined in `src/core/skills/lock.ts:282`*

```typescript
() => Promise<Record<string, { hasUpdate: boolean; currentVersion?: string; latestVersion?: string; status: "up-to-date" | "update-available" | "unknown"; }>>
```

Check for updates across all tracked skills.

**Returns**: Object mapping skill names to their update status

**Examples**

```typescript
const updates = await checkAllSkillUpdates();
for (const [name, status] of Object.entries(updates)) {
  if (status.hasUpdate) {
    console.log(`${name}: ${status.currentVersion} -> ${status.latestVersion}`);
  }
}
```

### `registerSkillsCheck()`

*Defined in `src/commands/skills/check.ts:34`*

```typescript
(parent: Command) => void
```

Registers the `skills check` subcommand for checking available skill updates.

**Parameters**

- `parent` — The parent `skills` Command to attach the check subcommand to

**Examples**

```bash
caamp skills check --human
caamp skills check --json
```

### `tokenizeCriteriaValue()`

*Defined in `src/core/skills/recommendation.ts:315`*

```typescript
(value: string) => string[]
```

Splits a comma-separated criteria string into normalized tokens.

**Parameters**

- `value` — The comma-separated string to tokenize

**Returns**: An array of trimmed, lowercased, non-empty tokens

**Examples**

```typescript
const tokens = tokenizeCriteriaValue("React, TypeScript, svelte");
// returns ["react", "typescript", "svelte"]
```

### `validateRecommendationCriteria()`

*Defined in `src/core/skills/recommendation.ts:367`*

```typescript
(input: RecommendationCriteriaInput) => RecommendationValidationResult
```

Validates recommendation criteria input for correctness and consistency.

**Parameters**

- `input` — The raw recommendation criteria to validate

**Returns**: A validation result indicating success or listing all issues

**Examples**

```typescript
const result = validateRecommendationCriteria({
  query: "gitbook",
  mustHave: "api",
  exclude: "legacy",
});
if (!result.valid) {
  console.error(result.issues);
}
```

### `normalizeRecommendationCriteria()`

*Defined in `src/core/skills/recommendation.ts:465`*

```typescript
(input: RecommendationCriteriaInput) => NormalizedRecommendationCriteria
```

Normalizes raw recommendation criteria into a consistent tokenized form.

**Parameters**

- `input` — The raw recommendation criteria to normalize

**Returns**: Normalized criteria with tokenized, sorted, deduplicated terms

**Examples**

```typescript
const criteria = normalizeRecommendationCriteria({
  query: "GitBook API",
  mustHave: "sync, api",
  prefer: ["modern"],
});
// criteria.queryTokens => ["api", "gitbook"]
// criteria.mustHave => ["api", "sync"]
```

### `scoreSkillRecommendation()`

*Defined in `src/core/skills/recommendation.ts:526`*

```typescript
(skill: MarketplaceResult, criteria: NormalizedRecommendationCriteria, options?: RecommendationOptions) => RankedSkillRecommendation
```

Computes a recommendation score for a single skill against normalized criteria.

**Parameters**

- `skill` — The marketplace skill result to score
- `criteria` — The normalized recommendation criteria to score against
- `options` — Optional scoring configuration including weights and markers

**Returns**: A ranked recommendation with score, reasons, and tradeoffs

**Examples**

```typescript
const criteria = normalizeRecommendationCriteria({ query: "gitbook" });
const ranked = scoreSkillRecommendation(marketplaceSkill, criteria, {
  includeDetails: true,
});
console.log(ranked.score, ranked.reasons);
```

### `recommendSkills()`

*Defined in `src/core/skills/recommendation.ts:662`*

```typescript
(skills: MarketplaceResult[], criteriaInput: RecommendationCriteriaInput, options?: RecommendationOptions) => RecommendSkillsResult
```

Validates, normalizes, scores, and ranks a list of skills against criteria.

**Parameters**

- `skills` — The array of marketplace skill results to rank
- `criteriaInput` — The raw recommendation criteria from the user
- `options` — Optional configuration for scoring and result limiting

**Returns**: The normalized criteria and ranked skill recommendations

**Throws**

- Error with `code` and `issues` properties when criteria are invalid

**Examples**

```typescript
const result = recommendSkills(
  marketplaceResults,
  { query: "gitbook", mustHave: "api", exclude: "legacy" },
  { top: 5, includeDetails: true },
);
for (const rec of result.ranking) {
  console.log(rec.skill.name, rec.score);
}
```

### `formatSkillRecommendations()`

*Defined in `src/core/skills/recommendation-api.ts:50`*

```typescript
(result: RecommendSkillsResult, opts: { mode: "human" | "json"; details?: boolean; }) => string | Record<string, unknown>
```

Format skill recommendation results for display or serialization.

**Parameters**

- `result` — The recommendation result to format
- `opts` — Formatting options including output mode and detail level

**Returns**: Formatted string for human mode, or a structured object for JSON mode

**Examples**

```typescript
const result = await recommendSkills("testing", { taskType: "test-writing" });
const output = formatSkillRecommendations(result, { mode: "human" });
console.log(output);
```

### `searchSkills()`

*Defined in `src/core/skills/recommendation-api.ts:113`*

```typescript
(query: string, options?: SearchSkillsOptions) => Promise<import("/mnt/projects/cleocode/.claude/worktrees/agent-aea50da0/packages/caamp/src/index").MarketplaceResult[]>
```

Search for skills via marketplace APIs.

**Parameters**

- `query` — Search query string (must be non-empty)
- `options` — Search options including result limit

**Returns**: Array of marketplace skill entries matching the query

**Examples**

```typescript
const results = await searchSkills("test runner", { limit: 10 });
console.log(`Found ${results.length} skills`);
```

### `recommendSkills()`

*Defined in `src/core/skills/recommendation-api.ts:154`*

```typescript
(query: string, criteria: Omit<RecommendationCriteriaInput, "query">, options?: RecommendSkillsQueryOptions) => Promise<RecommendSkillsResult>
```

Search and rank skills based on query and recommendation criteria.

**Parameters**

- `query` — Search query string
- `criteria` — Recommendation criteria (task type, context, preferences)
- `options` — Options for limiting and tuning results

**Returns**: Ranked recommendation results with scores and reasons

**Examples**

```typescript
const result = await recommendSkills("testing", { taskType: "test-writing" });
const best = result.ranking[0];
console.log(`Top pick: ${best.skill.scopedName} (score: ${best.score})`);
```

### `registerSkillsFind()`

*Defined in `src/commands/skills/find.ts:79`*

```typescript
(parent: Command) => void
```

Registers the `skills find` subcommand for searching marketplaces and recommending skills.

**Parameters**

- `parent` — The parent `skills` Command to attach the find subcommand to

**Examples**

```bash
caamp skills find "testing framework"
caamp skills find --recommend --must-have typescript --prefer vitest
```

### `registerSkillsInit()`

*Defined in `src/commands/skills/init.ts:36`*

```typescript
(parent: Command) => void
```

Registers the `skills init` subcommand for scaffolding new SKILL.md templates.

**Parameters**

- `parent` — The parent `skills` Command to attach the init subcommand to

**Examples**

```bash
caamp skills init my-skill
caamp skills init --dir ./skills/new-skill
```

### `loadLibraryFromModule()`

*Defined in `src/core/skills/library-loader.ts:44`*

```typescript
(root: string) => SkillLibrary
```

Load a SkillLibrary from a module (index.js) at the given root directory.

**Parameters**

- `root` — Absolute path to the library root (must contain index.js or package.json with main)

**Returns**: A validated SkillLibrary instance

**Throws**

- If the module cannot be loaded or does not implement SkillLibrary

**Examples**

```typescript
const library = loadLibraryFromModule("/home/user/.agents/libraries/cleocode-skills");
console.log(`Loaded v${library.version} with ${library.listSkills().length} skills`);
```

### `buildLibraryFromFiles()`

*Defined in `src/core/skills/library-loader.ts:121`*

```typescript
(root: string) => SkillLibrary
```

Build a SkillLibrary from raw files in a directory.

**Parameters**

- `root` — Absolute path to the library root directory

**Returns**: A SkillLibrary instance backed by filesystem reads

**Throws**

- If skills.json is not found at the root

**Examples**

```typescript
const library = buildLibraryFromFiles("/home/user/.agents/libraries/cleocode-skills");
const coreSkills = library.getCoreSkills();
console.log(`Core skills: ${coreSkills.map(s => s.name).join(", ")}`);
```

### `registerSkillLibrary()`

*Defined in `src/core/skills/catalog.ts:45`*

```typescript
(library: SkillLibrary) => void
```

Registers a SkillLibrary instance directly as the active catalog.

**Parameters**

- `library` — A SkillLibrary implementation to use as the catalog

**Examples**

```typescript
const library = buildLibraryFromFiles("/path/to/skills");
registerSkillLibrary(library);
```

### `registerSkillLibraryFromPath()`

*Defined in `src/core/skills/catalog.ts:69`*

```typescript
(root: string) => void
```

Registers a skill library by loading it from a directory path.

**Parameters**

- `root` — Absolute path to the skill library root directory

**Throws**

- Error if the library cannot be loaded from the given path

**Examples**

```typescript
registerSkillLibraryFromPath("/home/user/.agents/skill-library");
const skills = listSkills();
```

### `clearRegisteredLibrary()`

*Defined in `src/core/skills/catalog.ts:96`*

```typescript
() => void
```

Clears the registered skill library instance.

**Examples**

```typescript
clearRegisteredLibrary();
// isCatalogAvailable() will now return false unless auto-discovery succeeds
```

### `isCatalogAvailable()`

*Defined in `src/core/skills/catalog.ts:170`*

```typescript
() => boolean
```

Checks whether a skill library is available for use.

**Returns**: True if a skill library is registered or discoverable, false otherwise

**Examples**

```typescript
if (isCatalogAvailable()) {
  const skills = listSkills();
}
```

### `getSkills()`

*Defined in `src/core/skills/catalog.ts:196`*

```typescript
() => SkillLibraryEntry[]
```

Returns all skill entries from the catalog.

**Returns**: An array of all skill library entries

**Examples**

```typescript
const allSkills = getSkills();
console.log(`Found ${allSkills.length} skills`);
```

### `getManifest()`

*Defined in `src/core/skills/catalog.ts:217`*

```typescript
() => SkillLibraryManifest
```

Returns the parsed skill library manifest.

**Returns**: The skill library manifest object

**Examples**

```typescript
const manifest = getManifest();
console.log(manifest.version);
```

### `listSkills()`

*Defined in `src/core/skills/catalog.ts:238`*

```typescript
() => string[]
```

Lists all available skill names in the catalog.

**Returns**: An array of skill name strings

**Examples**

```typescript
const names = listSkills();
// e.g., ["ct-orchestrator", "ct-dev-workflow", "ct-validator"]
```

### `getSkill()`

*Defined in `src/core/skills/catalog.ts:262`*

```typescript
(name: string) => SkillLibraryEntry | undefined
```

Gets skill metadata by name from the catalog.

**Parameters**

- `name` — The unique skill name to look up

**Returns**: The skill entry if found, or undefined

**Examples**

```typescript
const skill = getSkill("ct-orchestrator");
if (skill) {
  console.log(skill.category);
}
```

### `getSkillPath()`

*Defined in `src/core/skills/catalog.ts:284`*

```typescript
(name: string) => string
```

Resolves the absolute path to a skill's SKILL.md file.

**Parameters**

- `name` — The unique skill name to resolve

**Returns**: The absolute path to the skill's SKILL.md file

**Examples**

```typescript
const path = getSkillPath("ct-orchestrator");
// e.g., "/home/user/.agents/skill-library/skills/ct-orchestrator/SKILL.md"
```

### `getSkillDir()`

*Defined in `src/core/skills/catalog.ts:306`*

```typescript
(name: string) => string
```

Resolves the absolute path to a skill's directory.

**Parameters**

- `name` — The unique skill name to resolve

**Returns**: The absolute path to the skill's directory

**Examples**

```typescript
const dir = getSkillDir("ct-orchestrator");
// e.g., "/home/user/.agents/skill-library/skills/ct-orchestrator"
```

### `readSkillContent()`

*Defined in `src/core/skills/catalog.ts:328`*

```typescript
(name: string) => string
```

Reads a skill's SKILL.md content as a string.

**Parameters**

- `name` — The unique skill name to read

**Returns**: The full text content of the skill's SKILL.md file

**Examples**

```typescript
const content = readSkillContent("ct-orchestrator");
console.log(content.substring(0, 100));
```

### `getCoreSkills()`

*Defined in `src/core/skills/catalog.ts:349`*

```typescript
() => SkillLibraryEntry[]
```

Returns all skills marked as core in the catalog.

**Returns**: An array of core skill entries

**Examples**

```typescript
const coreSkills = getCoreSkills();
console.log(`${coreSkills.length} core skills available`);
```

### `getSkillsByCategory()`

*Defined in `src/core/skills/catalog.ts:370`*

```typescript
(category: SkillLibraryEntry["category"]) => SkillLibraryEntry[]
```

Returns skills filtered by category.

**Parameters**

- `category` — The category to filter by

**Returns**: An array of skill entries in the specified category

**Examples**

```typescript
const planningSkills = getSkillsByCategory("planning");
```

### `getSkillDependencies()`

*Defined in `src/core/skills/catalog.ts:392`*

```typescript
(name: string) => string[]
```

Gets the direct dependency names for a skill.

**Parameters**

- `name` — The unique skill name to query dependencies for

**Returns**: An array of direct dependency skill names

**Examples**

```typescript
const deps = getSkillDependencies("ct-task-executor");
// e.g., ["ct-orchestrator"]
```

### `resolveDependencyTree()`

*Defined in `src/core/skills/catalog.ts:415`*

```typescript
(names: string[]) => string[]
```

Resolves the full dependency tree for a set of skill names.

**Parameters**

- `names` — The skill names to resolve dependencies for

**Returns**: A deduplicated array of all required skill names including transitive dependencies

**Examples**

```typescript
const allDeps = resolveDependencyTree(["ct-task-executor", "ct-validator"]);
// includes all transitive dependencies
```

### `listProfiles()`

*Defined in `src/core/skills/catalog.ts:436`*

```typescript
() => string[]
```

Lists all available profile names in the catalog.

**Returns**: An array of profile name strings

**Examples**

```typescript
const profiles = listProfiles();
// e.g., ["default", "minimal", "full"]
```

### `getProfile()`

*Defined in `src/core/skills/catalog.ts:461`*

```typescript
(name: string) => SkillLibraryProfile | undefined
```

Gets a profile definition by name from the catalog.

**Parameters**

- `name` — The unique profile name to look up

**Returns**: The profile definition if found, or undefined

**Examples**

```typescript
const profile = getProfile("default");
if (profile) {
  console.log(profile.skills);
}
```

### `resolveProfile()`

*Defined in `src/core/skills/catalog.ts:483`*

```typescript
(name: string) => string[]
```

Resolves a profile to its full skill list including inherited skills.

**Parameters**

- `name` — The profile name to resolve

**Returns**: A deduplicated array of all skill names required by the profile

**Examples**

```typescript
const skills = resolveProfile("default");
// includes all skills from extended profiles and their dependencies
```

### `listSharedResources()`

*Defined in `src/core/skills/catalog.ts:504`*

```typescript
() => string[]
```

Lists all available shared resource names in the catalog.

**Returns**: An array of shared resource name strings

**Examples**

```typescript
const resources = listSharedResources();
// e.g., ["testing-framework-config.md", "error-handling.md"]
```

### `getSharedResourcePath()`

*Defined in `src/core/skills/catalog.ts:525`*

```typescript
(name: string) => string | undefined
```

Gets the absolute path to a shared resource file.

**Parameters**

- `name` — The shared resource name to resolve

**Returns**: The absolute path to the resource file, or undefined if not found

**Examples**

```typescript
const path = getSharedResourcePath("testing-framework-config.md");
```

### `readSharedResource()`

*Defined in `src/core/skills/catalog.ts:549`*

```typescript
(name: string) => string | undefined
```

Reads a shared resource file's content as a string.

**Parameters**

- `name` — The shared resource name to read

**Returns**: The text content of the resource, or undefined if not found

**Examples**

```typescript
const content = readSharedResource("testing-framework-config.md");
if (content) {
  console.log(content);
}
```

### `listProtocols()`

*Defined in `src/core/skills/catalog.ts:570`*

```typescript
() => string[]
```

Lists all available protocol names in the catalog.

**Returns**: An array of protocol name strings

**Examples**

```typescript
const protocols = listProtocols();
// e.g., ["research", "implementation", "contribution"]
```

### `getProtocolPath()`

*Defined in `src/core/skills/catalog.ts:591`*

```typescript
(name: string) => string | undefined
```

Gets the absolute path to a protocol file.

**Parameters**

- `name` — The protocol name to resolve

**Returns**: The absolute path to the protocol file, or undefined if not found

**Examples**

```typescript
const path = getProtocolPath("research");
```

### `readProtocol()`

*Defined in `src/core/skills/catalog.ts:615`*

```typescript
(name: string) => string | undefined
```

Reads a protocol file's content as a string.

**Parameters**

- `name` — The protocol name to read

**Returns**: The text content of the protocol, or undefined if not found

**Examples**

```typescript
const content = readProtocol("research");
if (content) {
  console.log(content);
}
```

### `validateSkillFrontmatter()`

*Defined in `src/core/skills/catalog.ts:639`*

```typescript
(name: string) => SkillLibraryValidationResult
```

Validates a single skill's frontmatter against the schema.

**Parameters**

- `name` — The skill name to validate

**Returns**: A validation result indicating success or listing errors

**Examples**

```typescript
const result = validateSkillFrontmatter("ct-orchestrator");
if (!result.valid) {
  console.error(result.errors);
}
```

### `validateAll()`

*Defined in `src/core/skills/catalog.ts:662`*

```typescript
() => Map<string, SkillLibraryValidationResult>
```

Validates all skills in the catalog and returns results per skill.

**Returns**: A map of skill names to their validation results

**Examples**

```typescript
const results = validateAll();
for (const [name, result] of results) {
  if (!result.valid) console.error(`${name}: invalid`);
}
```

### `getDispatchMatrix()`

*Defined in `src/core/skills/catalog.ts:683`*

```typescript
() => SkillLibraryDispatchMatrix
```

Gets the dispatch matrix from the skill library manifest.

**Returns**: The dispatch matrix object from the manifest

**Examples**

```typescript
const matrix = getDispatchMatrix();
console.log(matrix);
```

### `getVersion()`

*Defined in `src/core/skills/catalog.ts:704`*

```typescript
() => string
```

Returns the skill library version string.

**Returns**: The library version string

**Examples**

```typescript
const version = getVersion();
// e.g., "1.0.0"
```

### `getLibraryRoot()`

*Defined in `src/core/skills/catalog.ts:725`*

```typescript
() => string
```

Returns the absolute path to the skill library root directory.

**Returns**: The absolute path to the library root

**Examples**

```typescript
const root = getLibraryRoot();
// e.g., "/home/user/.agents/skill-library"
```

### `parseSkillFile()`

*Defined in `src/core/skills/discovery.ts:34`*

```typescript
(filePath: string) => Promise<SkillMetadata | null>
```

Parse a SKILL.md file and extract its frontmatter metadata.

**Parameters**

- `filePath` — Absolute path to the SKILL.md file

**Returns**: Parsed metadata, or `null` if invalid

**Examples**

```typescript
const meta = await parseSkillFile("/path/to/SKILL.md");
if (meta) {
  console.log(`${meta.name}: ${meta.description}`);
}
```

### `discoverSkill()`

*Defined in `src/core/skills/discovery.ts:86`*

```typescript
(skillDir: string) => Promise<SkillEntry | null>
```

Discover a single skill at a given directory path.

**Parameters**

- `skillDir` — Absolute path to a skill directory (containing SKILL.md)

**Returns**: Skill entry with metadata, or `null` if no valid SKILL.md exists

**Examples**

```typescript
import { getCanonicalSkillsDir } from "../paths/standard.js";
import { join } from "node:path";

const skill = await discoverSkill(join(getCanonicalSkillsDir(), "my-skill"));
if (skill) {
  console.log(`Found: ${skill.name}`);
}
```

### `discoverSkills()`

*Defined in `src/core/skills/discovery.ts:121`*

```typescript
(rootDir: string) => Promise<SkillEntry[]>
```

Scan a directory for skill subdirectories, each containing a SKILL.md file.

**Parameters**

- `rootDir` — Absolute path to a skills root directory to scan

**Returns**: Array of discovered skill entries

**Examples**

```typescript
import { getCanonicalSkillsDir } from "../paths/standard.js";

const skills = await discoverSkills(getCanonicalSkillsDir());
console.log(`Found ${skills.length} skills`);
```

### `discoverSkillsMulti()`

*Defined in `src/core/skills/discovery.ts:158`*

```typescript
(dirs: string[]) => Promise<SkillEntry[]>
```

Discover skills across multiple directories.

**Parameters**

- `dirs` — Array of absolute paths to skills directories to scan

**Returns**: Deduplicated array of discovered skill entries

**Examples**

```typescript
const skills = await discoverSkillsMulti(["/home/user/.agents/skills", "./project-skills"]);
console.log(`Found ${skills.length} unique skills`);
```

### `registerSkillsInstall()`

*Defined in `src/commands/skills/install.ts:75`*

```typescript
(parent: Command) => void
```

Registers the `skills install` subcommand for installing skills from various sources.

**Parameters**

- `parent` — The parent `skills` Command to attach the install subcommand to

**Examples**

```bash
caamp skills install owner/repo
caamp skills install @author/skill-name --agent claude-code
caamp skills install --profile recommended --all
```

### `registerSkillsList()`

*Defined in `src/commands/skills/list.ts:49`*

```typescript
(parent: Command) => void
```

Registers the `skills list` subcommand for listing installed skills.

**Parameters**

- `parent` — The parent `skills` Command to attach the list subcommand to

**Examples**

```bash
caamp skills list --human
caamp skills list --agent claude-code --global
```

### `registerSkillsRemove()`

*Defined in `src/commands/skills/remove.ts:39`*

```typescript
(parent: Command) => void
```

Registers the `skills remove` subcommand for removing installed skills.

**Parameters**

- `parent` — The parent `skills` Command to attach the remove subcommand to

**Examples**

```bash
caamp skills remove my-skill
caamp skills remove --yes
```

### `registerSkillsUpdate()`

*Defined in `src/commands/skills/update.ts:40`*

```typescript
(parent: Command) => void
```

Registers the `skills update` subcommand for updating all outdated skills.

**Parameters**

- `parent` — The parent `skills` Command to attach the update subcommand to

**Examples**

```bash
caamp skills update --yes
caamp skills update --json
```

### `validateSkill()`

*Defined in `src/core/skills/validator.ts:96`*

```typescript
(filePath: string) => Promise<ValidationResult>
```

Validate a SKILL.md file against the Agent Skills standard.

**Parameters**

- `filePath` — Absolute path to the SKILL.md file to validate

**Returns**: Validation result with issues and parsed metadata

**Examples**

```typescript
const result = await validateSkill("/path/to/SKILL.md");
console.log(result.valid ? "Valid" : `${result.issues.length} issues found`);
```

### `registerSkillsValidate()`

*Defined in `src/commands/skills/validate.ts:29`*

```typescript
(parent: Command) => void
```

Registers the `skills validate` subcommand for validating SKILL.md file format.

**Parameters**

- `parent` — The parent `skills` Command to attach the validate subcommand to

**Examples**

```bash
caamp skills validate ./my-skill/SKILL.md
caamp skills validate --json
```

### `registerSkillsCommands()`

*Defined in `src/commands/skills/index.ts:37`*

```typescript
(program: Command) => void
```

Registers the `skills` command group with all skill management subcommands.

**Parameters**

- `program` — The root Commander program to attach the skills command group to

**Examples**

```bash
caamp skills install owner/repo
caamp skills list --human
caamp skills find "testing"
```

### `isCaampOwnedSkill()`

*Defined in `src/core/skills/integrity.ts:76`*

```typescript
(skillName: string) => boolean
```

Check whether a skill name is reserved by CAAMP (ct-\* prefix).

**Parameters**

- `skillName` — Skill name to check

**Returns**: `true` if the skill name starts with `ct-`

**Examples**

```typescript
isCaampOwnedSkill("ct-research-agent"); // true
isCaampOwnedSkill("my-custom-skill");   // false
```

### `checkSkillIntegrity()`

*Defined in `src/core/skills/integrity.ts:104`*

```typescript
(skillName: string, providers: Provider[], scope?: "global" | "project", projectDir?: string) => Promise<SkillIntegrityResult>
```

Check the integrity of a single installed skill.

**Parameters**

- `skillName` — Name of the skill to check
- `providers` — Providers to check symlinks for
- `scope` — Whether to check global or project links
- `projectDir` — Project directory (for project scope)

**Returns**: Integrity check result

**Examples**

```typescript
const result = await checkSkillIntegrity("ct-research-agent", providers, "global");
if (result.status !== "intact") {
  console.log(`Issue: ${result.issue}`);
}
```

### `checkAllSkillIntegrity()`

*Defined in `src/core/skills/integrity.ts:239`*

```typescript
(providers: Provider[], scope?: "global" | "project", projectDir?: string) => Promise<Map<string, SkillIntegrityResult>>
```

Check integrity of all tracked skills.

**Parameters**

- `providers` — Providers to check symlinks for
- `scope` — Whether to check global or project links
- `projectDir` — Project directory (for project scope)

**Returns**: Map of skill name to integrity result

**Examples**

```typescript
const results = await checkAllSkillIntegrity(providers);
for (const [name, result] of results) {
  console.log(`${name}: ${result.status}`);
}
```

### `shouldOverrideSkill()`

*Defined in `src/core/skills/integrity.ts:278`*

```typescript
(skillName: string, incomingSource: string, existingEntry: LockEntry | undefined) => boolean
```

Resolve a skill name conflict where a user-installed skill collides with a CAAMP-owned (ct-\*) skill.

**Parameters**

- `skillName` — Skill name to check
- `incomingSource` — Source of the incoming skill installation
- `existingEntry` — Existing lock entry, if any

**Returns**: `true` if the incoming installation should proceed

**Examples**

```typescript
const proceed = shouldOverrideSkill("ct-research-agent", "library", existingEntry);
if (proceed) {
  // Safe to install/override
}
```

### `validateInstructionIntegrity()`

*Defined in `src/core/skills/integrity.ts:321`*

```typescript
(providers: Provider[], projectDir: string, scope: "project" | "global", expectedContent?: string) => Promise<Array<{ file: string; providerId: string; issue: string; }>>
```

Validate instruction file injection status across all providers.

**Parameters**

- `providers` — Providers to check
- `projectDir` — Project directory
- `scope` — Whether to check global or project files
- `expectedContent` — Expected CAAMP block content

**Returns**: Array of file paths with issues

**Examples**

```typescript
const issues = await validateInstructionIntegrity(providers, process.cwd(), "project");
for (const issue of issues) {
  console.log(`${issue.providerId}: ${issue.issue} (${issue.file})`);
}
```

### `resolveCantImports()`

*Defined in `src/core/instructions/cant-resolver.ts:67`*

```typescript
(content: string, projectRoot: string) => ResolvedImports
```

Resolve `@import *.cant` references in instruction file content.

**Parameters**

- `content` — Raw instruction file content
- `projectRoot` — Absolute path to the project root directory

**Returns**: Resolved content, imported file list, and any errors

**Examples**

```typescript
const result = resolveCantImports(
  '@import .cleo/agents/core-agent.cant',
  '/home/user/project',
);
console.log(result.resolvedContent);
// ## Agent: core-agent
// - **Model**: opus
// ...
```

### `cantToMarkdown()`

*Defined in `src/core/instructions/cant-resolver.ts:143`*

```typescript
(cantContent: string) => string
```

Convert a `.cant` file's content to markdown equivalent.

**Parameters**

- `cantContent` — Raw `.cant` file content

**Returns**: Markdown representation of the `.cant` definitions

**Examples**

```typescript
const md = cantToMarkdown(`---
kind: agent
version: 1
---

agent ops-lead:
  model: opus
  prompt: "Coordinate operations"
`);
// Returns markdown with ## Agent: ops-lead heading
```

### `discoverWellKnown()`

*Defined in `src/core/sources/wellknown.ts:44`*

```typescript
(domain: string) => Promise<WellKnownSkill[]>
```

Discover skills from a well-known URL.

**Parameters**

- `domain` — Domain name to query (e.g. `"example.com"`)

**Returns**: Array of discovered skill entries

**Examples**

```typescript
const skills = await discoverWellKnown("example.com");
for (const skill of skills) {
  console.log(`${skill.name}: ${skill.url}`);
}
```

## Classes

### `PiRequiredError`

*Defined in `src/core/config/caamp-config.ts:179`*

```typescript
typeof PiRequiredError
```

Error raised when `getExclusivityMode` resolves to `'force-pi'` but Pi is not installed at the moment a runtime dispatch is requested.

**Examples**

```typescript
try {
  const targets = resolveDefaultTargetProviders();
} catch (err) {
  if (err instanceof PiRequiredError) {
    process.exit(4);
  }
  throw err;
}
```

#### `code`

*Defined in `src/core/config/caamp-config.ts:181`*

```typescript
"E_NOT_FOUND_RESOURCE"
```

LAFS-stable error code identifying this failure mode.

### `PiHarness`

*Defined in `src/core/harness/pi.ts:258`*

```typescript
typeof PiHarness
```

Pi coding agent harness — CAAMP's first-class primary harness.

#### `id`

*Defined in `src/core/harness/pi.ts:260`*

```typescript
"pi"
```

Provider id, always `"pi"`.

#### `skillsDir()`

*Defined in `src/core/harness/pi.ts:274`*

```typescript
(scope: HarnessScope) => string
```

Resolve the skills directory for a given scope.

#### `settingsPath()`

*Defined in `src/core/harness/pi.ts:283`*

```typescript
(scope: HarnessScope) => string
```

Resolve the settings.json path for a given scope.

#### `agentsMdPath()`

*Defined in `src/core/harness/pi.ts:297`*

```typescript
(scope: HarnessScope) => string
```

Resolve the AGENTS.md instruction file path for a given scope.

#### `installSkill()`

*Defined in `src/core/harness/pi.ts:308`*

```typescript
(sourcePath: string, skillName: string, scope: HarnessScope) => Promise<void>
```

Install a skill directory into the resolved Pi skills location.

#### `removeSkill()`

*Defined in `src/core/harness/pi.ts:318`*

```typescript
(skillName: string, scope: HarnessScope) => Promise<void>
```

Remove a skill directory from the resolved Pi skills location.

#### `listSkills()`

*Defined in `src/core/harness/pi.ts:326`*

```typescript
(scope: HarnessScope) => Promise<string[]>
```

List the installed skill directories at the given scope.

#### `injectInstructions()`

*Defined in `src/core/harness/pi.ts:339`*

```typescript
(content: string, scope: HarnessScope) => Promise<void>
```

Inject or replace a CAAMP-managed instruction block inside the Pi `AGENTS.md` file for the resolved scope.

#### `removeInstructions()`

*Defined in `src/core/harness/pi.ts:366`*

```typescript
(scope: HarnessScope) => Promise<void>
```

Remove the CAAMP-managed instruction block from the Pi `AGENTS.md` file at the resolved scope.

#### `spawnSubagent()`

*Defined in `src/core/harness/pi.ts:451`*

```typescript
(task: SubagentTask, opts?: SubagentSpawnOptions) => Promise<SubagentHandle>
```

Spawn a subagent through Pi's configured `spawnCommand` and return a live handle bound to the canonical streaming, attribution, and cleanup contract.

**Parameters**

- `task` — Subagent task specification.
- `opts` — Per-call streaming and cleanup overrides.

**Returns**: A live subagent handle.

#### `raceSubagents()`

*Defined in `src/core/harness/pi.ts:776`*

```typescript
(handles: SubagentHandle[]) => Promise<SubagentExitResult>
```

Race a set of subagent handles, returning the first one that exits.

**Parameters**

- `handles` — Subagent handles to race.

**Returns**: The `SubagentExitResult` of the first child to exit.

**Throws**

- When `handles` is empty (caller bug — a race over zero   children has no winner).

#### `settleAllSubagents()`

*Defined in `src/core/harness/pi.ts:813`*

```typescript
(handles: SubagentHandle[]) => Promise<PromiseSettledResult<SubagentExitResult>[]>
```

Settle a set of subagent handles, returning a parallel array of results.

**Parameters**

- `handles` — Subagent handles to settle.

**Returns**: Parallel array of settled exit results, one per input.

#### `handleStdoutLine()`

*Defined in `src/core/harness/pi.ts:828`*

```typescript
(rawLine: string, ctx: { subagentId: string; increment: () => number; incrementNonJson: () => number; writeChildSession: (entry: Record<string, unknown>) => void; safeOnStream: (event: SubagentStreamEvent) => void; }) => void
```

Per-line stdout dispatcher used by the streaming buffer flusher.

#### `readSettings()`

*Defined in `src/core/harness/pi.ts:865`*

```typescript
(scope: HarnessScope) => Promise<unknown>
```

Read the Pi `settings.json` file for the resolved scope.

#### `writeSettings()`

*Defined in `src/core/harness/pi.ts:880`*

```typescript
(patch: Record<string, unknown>, scope: HarnessScope) => Promise<void>
```

Merge a partial patch into the Pi `settings.json` file for the resolved scope using an atomic write.

#### `configureModels()`

*Defined in `src/core/harness/pi.ts:892`*

```typescript
(modelPatterns: string[], scope: HarnessScope) => Promise<void>
```

Persist the supplied model-name patterns into `settings.enabledModels` at the resolved scope.

#### `modelsConfigPath()`

*Defined in `src/core/harness/pi.ts:906`*

```typescript
(scope: HarnessScope) => string
```

Resolve the `models.json` path for a given legacy two-tier scope.

#### `sessionsDir()`

*Defined in `src/core/harness/pi.ts:917`*

```typescript
() => string
```

Resolve the sessions directory — always user-tier because Pi owns session storage and the three-tier model folds session listings to the single authoritative location per ADR-035 §D2.

#### `installExtension()`

*Defined in `src/core/harness/pi.ts:927`*

```typescript
(sourcePath: string, name: string, tier: HarnessTier, projectDir?: string, opts?: HarnessInstallOptions) => Promise<{ targetPath: string; tier: HarnessTier; }>
```

Install a Pi extension `.ts` source file into the resolved tier's extensions directory, validating that it has a default export.

#### `removeExtension()`

*Defined in `src/core/harness/pi.ts:973`*

```typescript
(name: string, tier: HarnessTier, projectDir?: string) => Promise<boolean>
```

Remove a Pi extension `.ts` source file from the resolved tier.

#### `listExtensions()`

*Defined in `src/core/harness/pi.ts:985`*

```typescript
(projectDir?: string) => Promise<ExtensionEntry[]>
```

List Pi extension files across every tier in precedence order, flagging shadowed entries from lower tiers.

#### `listSessions()`

*Defined in `src/core/harness/pi.ts:1023`*

```typescript
(opts?: { includeSubagents?: boolean; }) => Promise<SessionSummary[]>
```

List Pi session JSONL files (including subagent children when requested), summarising only the header line per file.

#### `showSession()`

*Defined in `src/core/harness/pi.ts:1074`*

```typescript
(id: string) => Promise<SessionDocument>
```

Show the full entries of a single Pi session by id.

#### `readModelsConfig()`

*Defined in `src/core/harness/pi.ts:1098`*

```typescript
(scope: HarnessScope) => Promise<PiModelsConfig>
```

Read the Pi `models.json` file for the resolved scope, tolerating missing or malformed files by returning an empty provider map.

#### `writeModelsConfig()`

*Defined in `src/core/harness/pi.ts:1128`*

```typescript
(config: PiModelsConfig, scope: HarnessScope) => Promise<void>
```

Write the Pi `models.json` file for the resolved scope via an atomic tmp-then-rename sequence.

#### `listModels()`

*Defined in `src/core/harness/pi.ts:1137`*

```typescript
(scope: HarnessScope) => Promise<ModelListEntry[]>
```

Compose a flat `ModelListEntry` list from `models.json` plus the `enabledModels` and default-model hints in `settings.json`.

#### `installPrompt()`

*Defined in `src/core/harness/pi.ts:1224`*

```typescript
(sourceDir: string, name: string, tier: HarnessTier, projectDir?: string, opts?: HarnessInstallOptions) => Promise<{ targetPath: string; tier: HarnessTier; }>
```

Install a Pi prompt directory (containing `prompt.md`) into the resolved tier's prompts directory.

#### `listPrompts()`

*Defined in `src/core/harness/pi.ts:1263`*

```typescript
(projectDir?: string) => Promise<PromptEntry[]>
```

List Pi prompt directories across every tier in precedence order, flagging shadowed entries from lower tiers.

#### `removePrompt()`

*Defined in `src/core/harness/pi.ts:1298`*

```typescript
(name: string, tier: HarnessTier, projectDir?: string) => Promise<boolean>
```

Remove a Pi prompt directory from the resolved tier.

#### `installTheme()`

*Defined in `src/core/harness/pi.ts:1313`*

```typescript
(sourceFile: string, name: string, tier: HarnessTier, projectDir?: string, opts?: HarnessInstallOptions) => Promise<{ targetPath: string; tier: HarnessTier; }>
```

Install a Pi theme file (`.ts`/`.tsx`/`.mts`/`.json`) into the resolved tier's themes directory, blocking same-stem conflicts unless `--force` is supplied.

#### `listThemes()`

*Defined in `src/core/harness/pi.ts:1368`*

```typescript
(projectDir?: string) => Promise<ThemeEntry[]>
```

List Pi theme files across every tier in precedence order, flagging shadowed entries from lower tiers.

#### `removeTheme()`

*Defined in `src/core/harness/pi.ts:1406`*

```typescript
(name: string, tier: HarnessTier, projectDir?: string) => Promise<boolean>
```

Remove a Pi theme from the resolved tier, matching any of the supported theme extensions for the given name stem.

#### `installCantProfile()`

*Defined in `src/core/harness/pi.ts:1431`*

```typescript
(sourcePath: string, name: string, tier: HarnessTier, projectDir?: string, opts?: HarnessInstallOptions) => Promise<{ targetPath: string; tier: HarnessTier; counts: CantProfileCounts; }>
```

Install a `.cant` profile into the resolved tier after passing it through the cant-core validator.  Validates the source via `PiHarness.validateCantProfile` before copying so we never persist a `.cant` file the runtime bridge cannot load. The target layout is `<tier-root>/cant/<name>.cant`, resolved through `resolveTierDir` so the project/user/global hierarchy stays consistent with the other Wave-1 verbs.

#### `removeCantProfile()`

*Defined in `src/core/harness/pi.ts:1483`*

```typescript
(name: string, tier: HarnessTier, projectDir?: string) => Promise<boolean>
```

Remove a `.cant` profile from the resolved tier.

#### `listCantProfiles()`

*Defined in `src/core/harness/pi.ts:1503`*

```typescript
(projectDir?: string) => Promise<CantProfileEntry[]>
```

List installed `.cant` profiles across every tier in precedence order, parsing each file to extract section counts.  Walks every tier in `TIER_PRECEDENCE` order, parsing each discovered `.cant` file via cant-core to extract a `CantProfileCounts` bag. Higher-precedence tiers shadow lower-precedence entries with the same name; shadowed entries still appear in the result but carry the `shadowedByHigherTier` flag so callers can render the precedence story without losing visibility of the duplicate.

#### `validateCantProfile()`

*Defined in `src/core/harness/pi.ts:1551`*

```typescript
(sourcePath: string) => Promise<ValidateCantProfileResult>
```

Validate a `.cant` source file against cant-core's parser and 42-rule linter, returning section counts and per-diagnostic detail.  Pure validator. Reads the file, runs `parseDocument` to derive counts (when parsing succeeds) and `validateDocument` to collect the 42-rule diagnostic feed. The two calls are kept independent so we can still report counts for files that pass parsing but fail a lint rule.

### `LAFSCommandError`

*Defined in `src/commands/advanced/lafs.ts:37`*

```typescript
typeof LAFSCommandError
```

Structured error class for LAFS-compliant command failures with error codes and recovery hints.

#### `code`

*Defined in `src/commands/advanced/lafs.ts:39`*

```typescript
string
```

LAFS error code identifying the failure type.

#### `category`

*Defined in `src/commands/advanced/lafs.ts:41`*

```typescript
LAFSErrorCategory
```

LAFS error category inferred from the error code.

#### `recoverable`

*Defined in `src/commands/advanced/lafs.ts:43`*

```typescript
boolean
```

Whether the operation can be retried after fixing the root cause.

#### `suggestion`

*Defined in `src/commands/advanced/lafs.ts:45`*

```typescript
string
```

Human-readable suggestion for resolving the error.

#### `retryAfterMs`

*Defined in `src/commands/advanced/lafs.ts:47`*

```typescript
number | null
```

Optional delay in milliseconds before retrying, or null.

#### `details`

*Defined in `src/commands/advanced/lafs.ts:49`*

```typescript
unknown
```

Optional additional error details payload.

### `NetworkError`

*Defined in `src/core/network/fetch.ts:20`*

```typescript
typeof NetworkError
```

Structured error for network failures with categorized kind.

#### `kind`

*Defined in `src/core/network/fetch.ts:22`*

```typescript
NetworkErrorKind
```

Classification of the failure.

#### `url`

*Defined in `src/core/network/fetch.ts:24`*

```typescript
string
```

URL that was being fetched.

#### `status`

*Defined in `src/core/network/fetch.ts:26`*

```typescript
number | undefined
```

HTTP status code (only present for `"http"` kind).

### `SkillsMPAdapter`

*Defined in `src/core/marketplace/skillsmp.ts:73`*

```typescript
typeof SkillsMPAdapter
```

Marketplace adapter for the agentskills.in API.

#### `name`

*Defined in `src/core/marketplace/skillsmp.ts:75`*

```typescript
string
```

The marketplace identifier used in search results.

#### `search()`

*Defined in `src/core/marketplace/skillsmp.ts:84`*

```typescript
(query: string, limit?: number) => Promise<MarketplaceResult[]>
```

Search for skills by query string.

**Parameters**

- `query` — Search query to match against skill names and descriptions.
- `limit` — Maximum number of results to return.

**Returns**: Array of marketplace results sorted by stars.

#### `getSkill()`

*Defined in `src/core/marketplace/skillsmp.ts:103`*

```typescript
(scopedName: string) => Promise<MarketplaceResult | null>
```

Look up a specific skill by its scoped name.

**Parameters**

- `scopedName` — The scoped skill name (e.g. `"@author/skill-name"`).

**Returns**: The matching marketplace result, or `null` if not found.

### `SkillsShAdapter`

*Defined in `src/core/marketplace/skillssh.ts:51`*

```typescript
typeof SkillsShAdapter
```

Marketplace adapter for the skills.sh API.

#### `name`

*Defined in `src/core/marketplace/skillssh.ts:53`*

```typescript
string
```

The marketplace identifier used in search results.

#### `search()`

*Defined in `src/core/marketplace/skillssh.ts:62`*

```typescript
(query: string, limit?: number) => Promise<MarketplaceResult[]>
```

Search for skills by query string.

**Parameters**

- `query` — Search query to match against skill names.
- `limit` — Maximum number of results to return.

**Returns**: Array of marketplace results.

#### `getSkill()`

*Defined in `src/core/marketplace/skillssh.ts:80`*

```typescript
(scopedName: string) => Promise<MarketplaceResult | null>
```

Look up a specific skill by its scoped name.

**Parameters**

- `scopedName` — The scoped skill name (e.g. `"@author/skill-name"`).

**Returns**: The matching marketplace result, or `null` if not found.

### `MarketplaceUnavailableError`

*Defined in `src/core/marketplace/client.ts:21`*

```typescript
typeof MarketplaceUnavailableError
```

Error thrown when all marketplace sources fail to respond.

#### `details`

*Defined in `src/core/marketplace/client.ts:23`*

```typescript
string[]
```

Per-adapter failure messages.

### `MarketplaceClient`

*Defined in `src/core/marketplace/client.ts:53`*

```typescript
typeof MarketplaceClient
```

Unified marketplace client that aggregates results from multiple marketplace adapters.  Queries all configured marketplaces in parallel, deduplicates results by scoped name, and sorts by star count.

**Examples**

```typescript
const client = new MarketplaceClient();
const results = await client.search("filesystem");
for (const r of results) {
  console.log(`${r.scopedName} (${r.stars} stars)`);
}
```

#### `adapters`

*Defined in `src/core/marketplace/client.ts:55`*

```typescript
MarketplaceAdapter[]
```

Configured marketplace adapters.

#### `search()`

*Defined in `src/core/marketplace/client.ts:91`*

```typescript
(query: string, limit?: number) => Promise<MarketplaceResult[]>
```

Search all marketplaces and return deduplicated, sorted results.  Queries all adapters in parallel and deduplicates by `scopedName`, keeping the entry with the highest star count. Results are sorted by stars descending.

**Parameters**

- `query` — Search query string
- `limit` — Maximum number of results to return (default: 20)

**Returns**: Deduplicated and sorted marketplace results

**Examples**

```typescript
const results = await client.search("code review", 10);
```

#### `getSkill()`

*Defined in `src/core/marketplace/client.ts:144`*

```typescript
(scopedName: string) => Promise<MarketplaceResult | null>
```

Get a specific skill by its scoped name from any marketplace.  Tries each adapter in order and returns the first match.

**Parameters**

- `scopedName` — Scoped skill name (e.g. `"@author/my-skill"`)

**Returns**: The marketplace result, or `null` if not found in any marketplace

**Examples**

```typescript
const skill = await client.getSkill("@anthropic/memory");
```

## Interfaces

### `SkillLibraryEntry`

*Defined in `src/core/skills/skill-library.ts:16`*

```typescript
SkillLibraryEntry
```

A single skill entry in a library catalog.

#### `name`

*Defined in `src/core/skills/skill-library.ts:18`*

```typescript
string
```

Skill name (e.g. `"ct-research-agent"`).

#### `description`

*Defined in `src/core/skills/skill-library.ts:20`*

```typescript
string
```

Human-readable description.

#### `version`

*Defined in `src/core/skills/skill-library.ts:22`*

```typescript
string
```

Semantic version string.

#### `path`

*Defined in `src/core/skills/skill-library.ts:24`*

```typescript
string
```

Relative path within the skills library.

#### `references`

*Defined in `src/core/skills/skill-library.ts:26`*

```typescript
string[]
```

File references used by the skill.

#### `core`

*Defined in `src/core/skills/skill-library.ts:28`*

```typescript
boolean
```

Whether this is a core skill.

#### `category`

*Defined in `src/core/skills/skill-library.ts:30`*

```typescript
"core" | "recommended" | "specialist" | "composition" | "meta"
```

Skill category tier.

#### `tier`

*Defined in `src/core/skills/skill-library.ts:32`*

```typescript
number
```

Numeric tier (0-3).

#### `protocol`

*Defined in `src/core/skills/skill-library.ts:34`*

```typescript
string | null
```

Associated protocol name, or `null`.

#### `dependencies`

*Defined in `src/core/skills/skill-library.ts:36`*

```typescript
string[]
```

Direct dependency skill names.

#### `sharedResources`

*Defined in `src/core/skills/skill-library.ts:38`*

```typescript
string[]
```

Shared resource names this skill uses.

#### `compatibility`

*Defined in `src/core/skills/skill-library.ts:40`*

```typescript
string[]
```

Compatible agent/context types.

#### `license`

*Defined in `src/core/skills/skill-library.ts:42`*

```typescript
string
```

SPDX license identifier.

#### `metadata`

*Defined in `src/core/skills/skill-library.ts:44`*

```typescript
Record<string, unknown>
```

Arbitrary metadata.

### `SkillLibraryValidationResult`

*Defined in `src/core/skills/skill-library.ts:52`*

```typescript
SkillLibraryValidationResult
```

Validation result from skill frontmatter validation.

#### `valid`

*Defined in `src/core/skills/skill-library.ts:54`*

```typescript
boolean
```

Whether the skill passed validation (no error-level issues).

#### `issues`

*Defined in `src/core/skills/skill-library.ts:56`*

```typescript
SkillLibraryValidationIssue[]
```

Individual validation issues.

### `SkillLibraryValidationIssue`

*Defined in `src/core/skills/skill-library.ts:64`*

```typescript
SkillLibraryValidationIssue
```

A single validation issue.

#### `level`

*Defined in `src/core/skills/skill-library.ts:66`*

```typescript
"error" | "warn"
```

Severity level.

#### `field`

*Defined in `src/core/skills/skill-library.ts:68`*

```typescript
string
```

Field that triggered the issue.

#### `message`

*Defined in `src/core/skills/skill-library.ts:70`*

```typescript
string
```

Human-readable message.

### `SkillLibraryProfile`

*Defined in `src/core/skills/skill-library.ts:78`*

```typescript
SkillLibraryProfile
```

Profile definition for grouped skill installation.

#### `name`

*Defined in `src/core/skills/skill-library.ts:80`*

```typescript
string
```

Profile name (e.g. `"minimal"`, `"core"`, `"recommended"`, `"full"`).

#### `description`

*Defined in `src/core/skills/skill-library.ts:82`*

```typescript
string
```

Human-readable description.

#### `extends`

*Defined in `src/core/skills/skill-library.ts:84`*

```typescript
string | undefined
```

Name of parent profile to extend.

#### `skills`

*Defined in `src/core/skills/skill-library.ts:86`*

```typescript
string[]
```

Skill names included in this profile.

#### `includeShared`

*Defined in `src/core/skills/skill-library.ts:88`*

```typescript
boolean | undefined
```

Whether to include \_shared resources.

#### `includeProtocols`

*Defined in `src/core/skills/skill-library.ts:90`*

```typescript
string[]
```

Protocol names to include.

### `SkillLibraryDispatchMatrix`

*Defined in `src/core/skills/skill-library.ts:98`*

```typescript
SkillLibraryDispatchMatrix
```

Dispatch matrix for task routing to skills.

#### `by_task_type`

*Defined in `src/core/skills/skill-library.ts:100`*

```typescript
Record<string, string>
```

Task type to skill mapping.

#### `by_keyword`

*Defined in `src/core/skills/skill-library.ts:102`*

```typescript
Record<string, string>
```

Keyword to skill mapping.

#### `by_protocol`

*Defined in `src/core/skills/skill-library.ts:104`*

```typescript
Record<string, string>
```

Protocol to skill mapping.

### `SkillLibraryManifestSkill`

*Defined in `src/core/skills/skill-library.ts:112`*

```typescript
SkillLibraryManifestSkill
```

Skill entry within the library manifest.

#### `name`

*Defined in `src/core/skills/skill-library.ts:114`*

```typescript
string
```

Skill name.

#### `version`

*Defined in `src/core/skills/skill-library.ts:116`*

```typescript
string
```

Version.

#### `description`

*Defined in `src/core/skills/skill-library.ts:118`*

```typescript
string
```

Description.

#### `path`

*Defined in `src/core/skills/skill-library.ts:120`*

```typescript
string
```

Path within library.

#### `tags`

*Defined in `src/core/skills/skill-library.ts:122`*

```typescript
string[]
```

Tags.

#### `status`

*Defined in `src/core/skills/skill-library.ts:124`*

```typescript
string
```

Status.

#### `tier`

*Defined in `src/core/skills/skill-library.ts:126`*

```typescript
number
```

Tier.

#### `token_budget`

*Defined in `src/core/skills/skill-library.ts:128`*

```typescript
number
```

Token budget.

#### `references`

*Defined in `src/core/skills/skill-library.ts:130`*

```typescript
string[]
```

References.

#### `capabilities`

*Defined in `src/core/skills/skill-library.ts:132`*

```typescript
{ inputs: string[]; outputs: string[]; dependencies: string[]; dispatch_triggers: string[]; compatible_subagent_types: string[]; chains_to: string[]; dispatch_keywords: { primary: string[]; secondary: string[]; }; }
```

Capabilities.

#### `constraints`

*Defined in `src/core/skills/skill-library.ts:145`*

```typescript
{ max_context_tokens: number; requires_session: boolean; requires_epic: boolean; }
```

Constraints.

### `SkillLibraryManifest`

*Defined in `src/core/skills/skill-library.ts:157`*

```typescript
SkillLibraryManifest
```

Full manifest structure for a skill library.

#### `$schema`

*Defined in `src/core/skills/skill-library.ts:159`*

```typescript
string
```

JSON schema reference.

#### `_meta`

*Defined in `src/core/skills/skill-library.ts:161`*

```typescript
Record<string, unknown>
```

Metadata.

#### `dispatch_matrix`

*Defined in `src/core/skills/skill-library.ts:163`*

```typescript
SkillLibraryDispatchMatrix
```

Dispatch matrix for skill routing.

#### `skills`

*Defined in `src/core/skills/skill-library.ts:165`*

```typescript
SkillLibraryManifestSkill[]
```

Manifest skill entries.

### `SkillLibrary`

*Defined in `src/core/skills/skill-library.ts:178`*

```typescript
SkillLibrary
```

Standard interface for a skill library.  Any directory or module providing skills must implement this contract. CAAMP uses it to discover, resolve, and install skills from any source.

#### `version`

*Defined in `src/core/skills/skill-library.ts:180`*

```typescript
string
```

Library version string.

#### `libraryRoot`

*Defined in `src/core/skills/skill-library.ts:182`*

```typescript
string
```

Absolute path to the library root directory.

#### `skills`

*Defined in `src/core/skills/skill-library.ts:184`*

```typescript
SkillLibraryEntry[]
```

All skill entries in the catalog.

#### `manifest`

*Defined in `src/core/skills/skill-library.ts:186`*

```typescript
SkillLibraryManifest
```

The parsed manifest.

#### `listSkills()`

*Defined in `src/core/skills/skill-library.ts:191`*

```typescript
() => string[]
```

List all skill names.

#### `getSkill()`

*Defined in `src/core/skills/skill-library.ts:193`*

```typescript
(name: string) => SkillLibraryEntry | undefined
```

Get skill metadata by name.

#### `getSkillPath()`

*Defined in `src/core/skills/skill-library.ts:195`*

```typescript
(name: string) => string
```

Resolve absolute path to a skill's SKILL.md file.

#### `getSkillDir()`

*Defined in `src/core/skills/skill-library.ts:197`*

```typescript
(name: string) => string
```

Resolve absolute path to a skill's directory.

#### `readSkillContent()`

*Defined in `src/core/skills/skill-library.ts:199`*

```typescript
(name: string) => string
```

Read a skill's SKILL.md content as a string.

#### `getCoreSkills()`

*Defined in `src/core/skills/skill-library.ts:204`*

```typescript
() => SkillLibraryEntry[]
```

Get all skills where `core === true`.

#### `getSkillsByCategory()`

*Defined in `src/core/skills/skill-library.ts:206`*

```typescript
(category: SkillLibraryEntry["category"]) => SkillLibraryEntry[]
```

Get skills filtered by category.

#### `getSkillDependencies()`

*Defined in `src/core/skills/skill-library.ts:208`*

```typescript
(name: string) => string[]
```

Get direct dependency names for a skill.

#### `resolveDependencyTree()`

*Defined in `src/core/skills/skill-library.ts:210`*

```typescript
(names: string[]) => string[]
```

Resolve full dependency tree for a set of skill names (includes transitive deps).

#### `listProfiles()`

*Defined in `src/core/skills/skill-library.ts:215`*

```typescript
() => string[]
```

List available profile names.

#### `getProfile()`

*Defined in `src/core/skills/skill-library.ts:217`*

```typescript
(name: string) => SkillLibraryProfile | undefined
```

Get a profile definition by name.

#### `resolveProfile()`

*Defined in `src/core/skills/skill-library.ts:219`*

```typescript
(name: string) => string[]
```

Resolve a profile to its full skill list (follows extends, resolves deps).

#### `listSharedResources()`

*Defined in `src/core/skills/skill-library.ts:224`*

```typescript
() => string[]
```

List available shared resource names.

#### `getSharedResourcePath()`

*Defined in `src/core/skills/skill-library.ts:226`*

```typescript
(name: string) => string | undefined
```

Get absolute path to a shared resource file.

#### `readSharedResource()`

*Defined in `src/core/skills/skill-library.ts:228`*

```typescript
(name: string) => string | undefined
```

Read a shared resource file content.

#### `listProtocols()`

*Defined in `src/core/skills/skill-library.ts:233`*

```typescript
() => string[]
```

List available protocol names.

#### `getProtocolPath()`

*Defined in `src/core/skills/skill-library.ts:235`*

```typescript
(name: string) => string | undefined
```

Get absolute path to a protocol file.

#### `readProtocol()`

*Defined in `src/core/skills/skill-library.ts:237`*

```typescript
(name: string) => string | undefined
```

Read a protocol file content.

#### `validateSkillFrontmatter()`

*Defined in `src/core/skills/skill-library.ts:242`*

```typescript
(name: string) => SkillLibraryValidationResult
```

Validate a single skill's frontmatter.

#### `validateAll()`

*Defined in `src/core/skills/skill-library.ts:244`*

```typescript
() => Map<string, SkillLibraryValidationResult>
```

Validate all skills.

#### `getDispatchMatrix()`

*Defined in `src/core/skills/skill-library.ts:249`*

```typescript
() => SkillLibraryDispatchMatrix
```

Get the dispatch matrix from the manifest.

### `RegistryDetection`

*Defined in `src/core/registry/types.ts:19`*

```typescript
RegistryDetection
```

Raw detection configuration as stored in registry.json.

#### `methods`

*Defined in `src/core/registry/types.ts:21`*

```typescript
string[]
```

Detection methods to try, in order (e.g. `["binary", "directory"]`).

#### `binary`

*Defined in `src/core/registry/types.ts:26`*

```typescript
string | undefined
```

Binary name to look up on PATH (for the `"binary"` method).

#### `directories`

*Defined in `src/core/registry/types.ts:31`*

```typescript
string[] | undefined
```

Directories to check for existence (for the `"directory"` method).

#### `appBundle`

*Defined in `src/core/registry/types.ts:36`*

```typescript
string | undefined
```

macOS .app bundle name (for the `"appBundle"` method).

#### `flatpakId`

*Defined in `src/core/registry/types.ts:41`*

```typescript
string | undefined
```

Flatpak application ID (for the `"flatpak"` method).

### `RegistryProvider`

*Defined in `src/core/registry/types.ts:89`*

```typescript
RegistryProvider
```

Raw provider definition as stored in registry.json before path resolution.

#### `id`

*Defined in `src/core/registry/types.ts:91`*

```typescript
string
```

Unique provider identifier (e.g. `"claude-code"`).

#### `toolName`

*Defined in `src/core/registry/types.ts:93`*

```typescript
string
```

Human-readable tool name (e.g. `"Claude Code"`).

#### `vendor`

*Defined in `src/core/registry/types.ts:95`*

```typescript
string
```

Vendor/company name (e.g. `"Anthropic"`).

#### `agentFlag`

*Defined in `src/core/registry/types.ts:97`*

```typescript
string
```

CLI flag name for `--agent` selection.

#### `aliases`

*Defined in `src/core/registry/types.ts:99`*

```typescript
string[]
```

Alternative names that resolve to this provider.

#### `pathGlobal`

*Defined in `src/core/registry/types.ts:102`*

```typescript
string
```

Global instruction file directory path (may contain platform variables).

#### `pathProject`

*Defined in `src/core/registry/types.ts:104`*

```typescript
string
```

Project-relative instruction file directory path.

#### `instructFile`

*Defined in `src/core/registry/types.ts:107`*

```typescript
string
```

Instruction file name (e.g. `"CLAUDE.md"`, `"AGENTS.md"`).

#### `pathSkills`

*Defined in `src/core/registry/types.ts:110`*

```typescript
string
```

Global skills directory path (may contain platform variables).

#### `pathProjectSkills`

*Defined in `src/core/registry/types.ts:112`*

```typescript
string
```

Project-relative skills directory path.

#### `detection`

*Defined in `src/core/registry/types.ts:115`*

```typescript
RegistryDetection
```

Detection configuration for auto-discovering this provider.

#### `priority`

*Defined in `src/core/registry/types.ts:118`*

```typescript
ProviderPriority
```

Priority tier identifier. Exactly zero or one provider should be `"primary"`.

#### `status`

*Defined in `src/core/registry/types.ts:120`*

```typescript
ProviderStatus
```

Lifecycle status identifier.

#### `agentSkillsCompatible`

*Defined in `src/core/registry/types.ts:122`*

```typescript
boolean
```

Whether the provider is compatible with the Agent Skills standard.

#### `capabilities`

*Defined in `src/core/registry/types.ts:128`*

```typescript
RegistryCapabilities | undefined
```

Optional provider capabilities for MCP, harness role, skills, hooks, and spawn.

### `RegistryMcpIntegration`

*Defined in `src/core/registry/types.ts:164`*

```typescript
RegistryMcpIntegration
```

MCP server integration metadata for providers that consume MCP servers via a per-agent config file.

#### `configKey`

*Defined in `src/core/registry/types.ts:166`*

```typescript
string
```

Dot-notation key path for MCP server config (e.g. `"mcpServers"`).

#### `configFormat`

*Defined in `src/core/registry/types.ts:168`*

```typescript
McpConfigFormat
```

Config file format identifier.

#### `configPathGlobal`

*Defined in `src/core/registry/types.ts:170`*

```typescript
string
```

Global config file path (may contain platform variables).

#### `configPathProject`

*Defined in `src/core/registry/types.ts:172`*

```typescript
string | null
```

Project-relative config file path, or `null` if unsupported.

#### `supportedTransports`

*Defined in `src/core/registry/types.ts:174`*

```typescript
McpTransportType[]
```

MCP transport protocol identifiers this provider supports.

#### `supportsHeaders`

*Defined in `src/core/registry/types.ts:176`*

```typescript
boolean
```

Whether the provider supports custom HTTP headers for remote MCP servers.

### `RegistryHarnessCapability`

*Defined in `src/core/registry/types.ts:202`*

```typescript
RegistryHarnessCapability
```

First-class harness role declaration.

#### `kind`

*Defined in `src/core/registry/types.ts:204`*

```typescript
RegistryHarnessKind
```

The harness kind (`"orchestrator"` or `"standalone"`).

#### `spawnTargets`

*Defined in `src/core/registry/types.ts:206`*

```typescript
string[]
```

Provider ids this harness can spawn as subagents. Empty for standalone.

#### `supportsConductorLoop`

*Defined in `src/core/registry/types.ts:208`*

```typescript
boolean
```

Whether the harness drives a CleoOS conductor loop.

#### `supportsStageGuidance`

*Defined in `src/core/registry/types.ts:210`*

```typescript
boolean
```

Whether the harness accepts stage guidance injection.

#### `supportsCantBridge`

*Defined in `src/core/registry/types.ts:212`*

```typescript
boolean
```

Whether the harness bridges CANT events.

#### `extensionsPath`

*Defined in `src/core/registry/types.ts:214`*

```typescript
string
```

Path to the harness's runtime extensions directory (file paths, not a config file).

#### `globalExtensionsHub`

*Defined in `src/core/registry/types.ts:219`*

```typescript
string | undefined
```

Optional CLEO-managed shared extensions hub.

### `RegistrySkillsCapability`

*Defined in `src/core/registry/types.ts:252`*

```typescript
RegistrySkillsCapability
```

Raw skills capability definition as stored in registry.json.

#### `agentsGlobalPath`

*Defined in `src/core/registry/types.ts:254`*

```typescript
string | null
```

Resolved global `.agents/skills` path, or `null` if unsupported.

#### `agentsProjectPath`

*Defined in `src/core/registry/types.ts:256`*

```typescript
string | null
```

Project-relative `.agents/skills` path, or `null` if unsupported.

#### `precedence`

*Defined in `src/core/registry/types.ts:258`*

```typescript
SkillsPrecedence
```

How this provider resolves skill file precedence.

### `RegistryHooksCapability`

*Defined in `src/core/registry/types.ts:316`*

```typescript
RegistryHooksCapability
```

Raw hooks capability definition as stored in registry.json.

#### `supported`

*Defined in `src/core/registry/types.ts:318`*

```typescript
string[]
```

Hook lifecycle event identifiers this provider supports.

#### `hookConfigPath`

*Defined in `src/core/registry/types.ts:320`*

```typescript
string | null
```

Path to the hook configuration file or directory, or `null` if not applicable.

#### `hookConfigPathProject`

*Defined in `src/core/registry/types.ts:325`*

```typescript
string | undefined
```

Project-relative path to the hook configuration file or directory.

#### `hookFormat`

*Defined in `src/core/registry/types.ts:327`*

```typescript
RegistryHookFormat | null
```

Format of the hook config, or `null` when the provider has no hook system.

#### `nativeEventCatalog`

*Defined in `src/core/registry/types.ts:333`*

```typescript
RegistryHookCatalog | undefined
```

Which native event catalog this provider's hooks are drawn from. Defaults to `"canonical"` when omitted.

#### `canInjectSystemPrompt`

*Defined in `src/core/registry/types.ts:338`*

```typescript
boolean | undefined
```

Whether hooks may inject or modify the system prompt.

#### `canBlockTools`

*Defined in `src/core/registry/types.ts:343`*

```typescript
boolean | undefined
```

Whether hooks may block tool calls.

### `RegistrySpawnCapability`

*Defined in `src/core/registry/types.ts:371`*

```typescript
RegistrySpawnCapability
```

Raw spawn capability definition as stored in registry.json.

#### `supportsSubagents`

*Defined in `src/core/registry/types.ts:373`*

```typescript
boolean
```

Whether the provider supports spawning subagents.

#### `supportsProgrammaticSpawn`

*Defined in `src/core/registry/types.ts:375`*

```typescript
boolean
```

Whether subagents can be spawned programmatically.

#### `supportsInterAgentComms`

*Defined in `src/core/registry/types.ts:377`*

```typescript
boolean
```

Whether spawned agents can communicate with each other.

#### `supportsParallelSpawn`

*Defined in `src/core/registry/types.ts:379`*

```typescript
boolean
```

Whether multiple agents can be spawned in parallel.

#### `spawnMechanism`

*Defined in `src/core/registry/types.ts:381`*

```typescript
SpawnMechanism | null
```

Mechanism used for spawning, or `null` if spawning is unsupported.

#### `spawnCommand`

*Defined in `src/core/registry/types.ts:388`*

```typescript
string[] | undefined
```

Literal command-line invocation used by the harness to spawn a child worker (e.g. Pi's `["pi", "--mode", "json", "-p", "--no-session"]`). Only meaningful when `spawnMechanism === "native-child-process"`.

### `RegistryCapabilities`

*Defined in `src/core/registry/types.ts:403`*

```typescript
RegistryCapabilities
```

Aggregate capability block for a provider in registry.json.

#### `mcp`

*Defined in `src/core/registry/types.ts:409`*

```typescript
RegistryMcpIntegration | undefined
```

MCP server integration metadata. Omitted for providers (like Pi) that do not consume MCP servers via a config file.

#### `harness`

*Defined in `src/core/registry/types.ts:415`*

```typescript
RegistryHarnessCapability | undefined
```

First-class harness role. Present only for orchestrators or standalone harnesses, not for pure spawn targets.

#### `skills`

*Defined in `src/core/registry/types.ts:420`*

```typescript
RegistrySkillsCapability | undefined
```

Skills path resolution and precedence capabilities.

#### `hooks`

*Defined in `src/core/registry/types.ts:425`*

```typescript
RegistryHooksCapability | undefined
```

Hook/lifecycle event capabilities.

#### `spawn`

*Defined in `src/core/registry/types.ts:430`*

```typescript
RegistrySpawnCapability | undefined
```

Subagent spawn capabilities.

### `ProviderRegistry`

*Defined in `src/core/registry/types.ts:443`*

```typescript
ProviderRegistry
```

Top-level structure of the provider registry JSON file.

#### `version`

*Defined in `src/core/registry/types.ts:445`*

```typescript
string
```

Schema version of the registry file.

#### `lastUpdated`

*Defined in `src/core/registry/types.ts:447`*

```typescript
string
```

ISO 8601 timestamp of the last registry update.

#### `providers`

*Defined in `src/core/registry/types.ts:449`*

```typescript
Record<string, RegistryProvider>
```

Provider definitions keyed by provider ID.

### `DetectionConfig`

*Defined in `src/types.ts:154`*

```typescript
DetectionConfig
```

Configuration for detecting whether a provider is installed.

**Examples**

```typescript
const config: DetectionConfig = {
  methods: ["binary", "directory"],
  binary: "claude",
  directories: ["~/.config/claude"],
};
```

#### `methods`

*Defined in `src/types.ts:156`*

```typescript
DetectionMethod[]
```

Detection methods to try, in order.

#### `binary`

*Defined in `src/types.ts:161`*

```typescript
string | undefined
```

Binary name to look up on PATH (for `"binary"` method).

#### `directories`

*Defined in `src/types.ts:166`*

```typescript
string[] | undefined
```

Directories to check for existence (for `"directory"` method).

#### `appBundle`

*Defined in `src/types.ts:171`*

```typescript
string | undefined
```

macOS .app bundle name (for `"appBundle"` method).

#### `flatpakId`

*Defined in `src/types.ts:176`*

```typescript
string | undefined
```

Flatpak application ID (for `"flatpak"` method).

### `ProviderMcpCapability`

*Defined in `src/types.ts:204`*

```typescript
ProviderMcpCapability
```

Resolved MCP server integration metadata for a provider.

#### `configKey`

*Defined in `src/types.ts:206`*

```typescript
string
```

Dot-notation key path for MCP server config (e.g. `"mcpServers"`).

#### `configFormat`

*Defined in `src/types.ts:208`*

```typescript
ConfigFormat
```

Resolved config file format.

#### `configPathGlobal`

*Defined in `src/types.ts:210`*

```typescript
string
```

Resolved global config file path.

#### `configPathProject`

*Defined in `src/types.ts:212`*

```typescript
string | null
```

Project-relative config file path, or `null` if unsupported.

#### `supportedTransports`

*Defined in `src/types.ts:214`*

```typescript
TransportType[]
```

MCP transport protocols this provider supports.

#### `supportsHeaders`

*Defined in `src/types.ts:216`*

```typescript
boolean
```

Whether the provider supports custom HTTP headers for remote MCP servers.

### `ProviderHarnessCapability`

*Defined in `src/types.ts:229`*

```typescript
ProviderHarnessCapability
```

Resolved first-class harness capability for a provider.

#### `kind`

*Defined in `src/types.ts:231`*

```typescript
import("/mnt/projects/cleocode/.claude/worktrees/agent-aea50da0/packages/caamp/src/core/registry/types").RegistryHarnessKind
```

Harness kind (`"orchestrator"` or `"standalone"`).

#### `spawnTargets`

*Defined in `src/types.ts:233`*

```typescript
string[]
```

Provider ids this harness can spawn as subagents. Empty for standalone.

#### `supportsConductorLoop`

*Defined in `src/types.ts:235`*

```typescript
boolean
```

Whether the harness drives a CleoOS conductor loop.

#### `supportsStageGuidance`

*Defined in `src/types.ts:237`*

```typescript
boolean
```

Whether the harness accepts stage guidance injection.

#### `supportsCantBridge`

*Defined in `src/types.ts:239`*

```typescript
boolean
```

Whether the harness bridges CANT events.

#### `extensionsPath`

*Defined in `src/types.ts:241`*

```typescript
string
```

Resolved path to the harness's runtime extensions directory.

#### `globalExtensionsHub`

*Defined in `src/types.ts:243`*

```typescript
string | null
```

Resolved CLEO-managed shared extensions hub path, if configured.

### `ProviderSkillsCapability`

*Defined in `src/types.ts:256`*

```typescript
ProviderSkillsCapability
```

Resolved skills capability for a provider at runtime.

#### `agentsGlobalPath`

*Defined in `src/types.ts:258`*

```typescript
string | null
```

Resolved global `.agents/skills` path, or `null` if unsupported.

#### `agentsProjectPath`

*Defined in `src/types.ts:260`*

```typescript
string | null
```

Project-relative `.agents/skills` path, or `null` if unsupported.

#### `precedence`

*Defined in `src/types.ts:262`*

```typescript
import("/mnt/projects/cleocode/.claude/worktrees/agent-aea50da0/packages/caamp/src/core/registry/types").SkillsPrecedence
```

How this provider resolves skill file precedence.

### `ProviderHooksCapability`

*Defined in `src/types.ts:278`*

```typescript
ProviderHooksCapability
```

Resolved hooks capability for a provider at runtime.

#### `supported`

*Defined in `src/types.ts:280`*

```typescript
string[]
```

Hook lifecycle events this provider supports.

#### `hookConfigPath`

*Defined in `src/types.ts:282`*

```typescript
string | null
```

Resolved path to the hook configuration file or directory, or `null`.

#### `hookConfigPathProject`

*Defined in `src/types.ts:284`*

```typescript
string | null
```

Resolved project-relative hook configuration path, or `null`.

#### `hookFormat`

*Defined in `src/types.ts:286`*

```typescript
import("/mnt/projects/cleocode/.claude/worktrees/agent-aea50da0/packages/caamp/src/core/registry/types").RegistryHookFormat | null
```

Format of the hook config.

#### `nativeEventCatalog`

*Defined in `src/types.ts:288`*

```typescript
import("/mnt/projects/cleocode/.claude/worktrees/agent-aea50da0/packages/caamp/src/core/registry/types").RegistryHookCatalog
```

Which native event catalog this provider's hooks are drawn from.

#### `canInjectSystemPrompt`

*Defined in `src/types.ts:290`*

```typescript
boolean
```

Whether hooks may inject or modify the system prompt.

#### `canBlockTools`

*Defined in `src/types.ts:292`*

```typescript
boolean
```

Whether hooks may block tool calls.

### `ProviderSpawnCapability`

*Defined in `src/types.ts:305`*

```typescript
ProviderSpawnCapability
```

Resolved spawn capability for a provider at runtime.

#### `supportsSubagents`

*Defined in `src/types.ts:307`*

```typescript
boolean
```

Whether the provider supports spawning subagents.

#### `supportsProgrammaticSpawn`

*Defined in `src/types.ts:309`*

```typescript
boolean
```

Whether subagents can be spawned programmatically.

#### `supportsInterAgentComms`

*Defined in `src/types.ts:311`*

```typescript
boolean
```

Whether spawned agents can communicate with each other.

#### `supportsParallelSpawn`

*Defined in `src/types.ts:313`*

```typescript
boolean
```

Whether multiple agents can be spawned in parallel.

#### `spawnMechanism`

*Defined in `src/types.ts:315`*

```typescript
import("/mnt/projects/cleocode/.claude/worktrees/agent-aea50da0/packages/caamp/src/core/registry/types").SpawnMechanism | null
```

Mechanism used for spawning.

#### `spawnCommand`

*Defined in `src/types.ts:320`*

```typescript
string[] | null
```

Literal command-line invocation used by the harness to spawn a child worker. Only meaningful when `spawnMechanism === "native-child-process"`.

### `ProviderCapabilities`

*Defined in `src/types.ts:336`*

```typescript
ProviderCapabilities
```

Aggregate provider capabilities for MCP, harness role, skills, hooks, and spawn.

#### `mcp`

*Defined in `src/types.ts:338`*

```typescript
ProviderMcpCapability | null
```

MCP server integration, when the provider consumes MCP via a config file.

#### `harness`

*Defined in `src/types.ts:340`*

```typescript
ProviderHarnessCapability | null
```

Harness role, present only for orchestrators and standalone harnesses.

#### `skills`

*Defined in `src/types.ts:342`*

```typescript
ProviderSkillsCapability
```

Skills path resolution and precedence.

#### `hooks`

*Defined in `src/types.ts:344`*

```typescript
ProviderHooksCapability
```

Hook/lifecycle event support.

#### `spawn`

*Defined in `src/types.ts:346`*

```typescript
ProviderSpawnCapability
```

Subagent spawn capabilities.

### `Provider`

*Defined in `src/types.ts:381`*

```typescript
Provider
```

A resolved AI agent provider definition with platform-specific paths.

**Examples**

```typescript
const provider = getProvider("claude-code");
if (provider?.capabilities.mcp) {
  console.log(provider.capabilities.mcp.configPathGlobal);
}
```

#### `id`

*Defined in `src/types.ts:383`*

```typescript
string
```

Unique provider identifier (e.g. `"claude-code"`).

#### `toolName`

*Defined in `src/types.ts:385`*

```typescript
string
```

Human-readable tool name (e.g. `"Claude Code"`).

#### `vendor`

*Defined in `src/types.ts:387`*

```typescript
string
```

Vendor/company name (e.g. `"Anthropic"`).

#### `agentFlag`

*Defined in `src/types.ts:389`*

```typescript
string
```

CLI flag name for `--agent` selection.

#### `aliases`

*Defined in `src/types.ts:391`*

```typescript
string[]
```

Alternative names that resolve to this provider.

#### `pathGlobal`

*Defined in `src/types.ts:394`*

```typescript
string
```

Resolved global instruction file directory path.

#### `pathProject`

*Defined in `src/types.ts:396`*

```typescript
string
```

Project-relative instruction file directory path.

#### `instructFile`

*Defined in `src/types.ts:399`*

```typescript
string
```

Instruction file name (e.g. `"CLAUDE.md"`, `"AGENTS.md"`).

#### `pathSkills`

*Defined in `src/types.ts:402`*

```typescript
string
```

Resolved global skills directory path.

#### `pathProjectSkills`

*Defined in `src/types.ts:404`*

```typescript
string
```

Project-relative skills directory path.

#### `detection`

*Defined in `src/types.ts:407`*

```typescript
DetectionConfig
```

Detection configuration for auto-discovering this provider.

#### `priority`

*Defined in `src/types.ts:410`*

```typescript
ProviderPriority
```

Priority tier for sorting and default selection.

#### `status`

*Defined in `src/types.ts:412`*

```typescript
ProviderStatus
```

Lifecycle status in the registry.

#### `agentSkillsCompatible`

*Defined in `src/types.ts:414`*

```typescript
boolean
```

Whether the provider is compatible with the Agent Skills standard.

#### `capabilities`

*Defined in `src/types.ts:417`*

```typescript
ProviderCapabilities
```

Provider capabilities (MCP, harness, skills, hooks, spawn). Always populated at runtime.

### `McpServerConfig`

*Defined in `src/types.ts:449`*

```typescript
McpServerConfig
```

Canonical MCP server configuration.

**Examples**

```typescript
// Remote server
const remote: McpServerConfig = {
  type: "http",
  url: "https://mcp.example.com/sse",
};

// Local stdio server
const local: McpServerConfig = {
  command: "npx",
  args: ["-y", "@modelcontextprotocol/server-filesystem"],
};
```

#### `type`

*Defined in `src/types.ts:454`*

```typescript
TransportType | undefined
```

Transport type (`"stdio"`, `"sse"`, or `"http"`).

#### `url`

*Defined in `src/types.ts:459`*

```typescript
string | undefined
```

URL for remote MCP servers.

#### `headers`

*Defined in `src/types.ts:464`*

```typescript
Record<string, string> | undefined
```

HTTP headers for remote MCP servers.

#### `command`

*Defined in `src/types.ts:469`*

```typescript
string | undefined
```

Command to run for stdio MCP servers.

#### `args`

*Defined in `src/types.ts:474`*

```typescript
string[] | undefined
```

Arguments for the stdio command.

#### `env`

*Defined in `src/types.ts:479`*

```typescript
Record<string, string> | undefined
```

Environment variables for the stdio process.

### `ParsedSource`

*Defined in `src/types.ts:528`*

```typescript
ParsedSource
```

Result of parsing a source string into its typed components.

**Examples**

```typescript
const parsed: ParsedSource = {
  type: "github",
  value: "https://github.com/owner/repo",
  inferredName: "repo",
  owner: "owner",
  repo: "repo",
};
```

#### `type`

*Defined in `src/types.ts:530`*

```typescript
SourceType
```

Classified source type.

#### `value`

*Defined in `src/types.ts:532`*

```typescript
string
```

Original or normalized source value.

#### `inferredName`

*Defined in `src/types.ts:534`*

```typescript
string
```

Display name inferred from the source.

#### `owner`

*Defined in `src/types.ts:539`*

```typescript
string | undefined
```

Repository owner (for GitHub/GitLab sources).

#### `repo`

*Defined in `src/types.ts:544`*

```typescript
string | undefined
```

Repository name (for GitHub/GitLab sources).

#### `path`

*Defined in `src/types.ts:549`*

```typescript
string | undefined
```

Path within the repository (for GitHub/GitLab sources).

#### `ref`

*Defined in `src/types.ts:554`*

```typescript
string | undefined
```

Git ref / branch / tag (for GitHub/GitLab sources).

### `SkillMetadata`

*Defined in `src/types.ts:578`*

```typescript
SkillMetadata
```

Metadata extracted from a SKILL.md frontmatter.

**Examples**

```typescript
const meta: SkillMetadata = {
  name: "my-skill",
  description: "A useful skill for code generation",
  version: "1.0.0",
};
```

#### `name`

*Defined in `src/types.ts:580`*

```typescript
string
```

Skill name (lowercase, hyphens only).

#### `description`

*Defined in `src/types.ts:582`*

```typescript
string
```

Human-readable description.

#### `license`

*Defined in `src/types.ts:587`*

```typescript
string | undefined
```

SPDX license identifier.

#### `compatibility`

*Defined in `src/types.ts:592`*

```typescript
string | undefined
```

Compatibility notes (e.g. agent versions).

#### `metadata`

*Defined in `src/types.ts:597`*

```typescript
Record<string, string> | undefined
```

Arbitrary key-value metadata.

#### `allowedTools`

*Defined in `src/types.ts:602`*

```typescript
string[] | undefined
```

List of tools the skill is allowed to use.

#### `version`

*Defined in `src/types.ts:607`*

```typescript
string | undefined
```

Semantic version string.

### `SkillEntry`

*Defined in `src/types.ts:634`*

```typescript
SkillEntry
```

A discovered skill entry with its location and metadata.

**Examples**

```typescript
import { getCanonicalSkillsDir } from "./core/paths/standard.js";
import { join } from "node:path";

const entry: SkillEntry = {
  name: "my-skill",
  scopedName: "my-skill",
  path: join(getCanonicalSkillsDir(), "my-skill"),
  metadata: { name: "my-skill", description: "A skill" },
};
```

#### `name`

*Defined in `src/types.ts:636`*

```typescript
string
```

Skill name.

#### `scopedName`

*Defined in `src/types.ts:638`*

```typescript
string
```

Scoped name (may include marketplace scope).

#### `path`

*Defined in `src/types.ts:640`*

```typescript
string
```

Absolute path to the skill directory.

#### `metadata`

*Defined in `src/types.ts:642`*

```typescript
SkillMetadata
```

Parsed SKILL.md frontmatter metadata.

#### `source`

*Defined in `src/types.ts:647`*

```typescript
string | undefined
```

Original source from which the skill was installed.

### `LockEntry`

*Defined in `src/types.ts:680`*

```typescript
LockEntry
```

A single entry in the CAAMP lock file tracking an installed skill or MCP server.

**Examples**

```typescript
import { getCanonicalSkillsDir } from "./core/paths/standard.js";
import { join } from "node:path";

const entry: LockEntry = {
  name: "my-skill",
  scopedName: "my-skill",
  source: "https://github.com/owner/repo",
  sourceType: "github",
  installedAt: "2025-01-15T10:30:00.000Z",
  agents: ["claude-code", "cursor"],
  canonicalPath: join(getCanonicalSkillsDir(), "my-skill"),
  isGlobal: true,
};
```

#### `name`

*Defined in `src/types.ts:682`*

```typescript
string
```

Skill or server name.

#### `scopedName`

*Defined in `src/types.ts:684`*

```typescript
string
```

Scoped name (may include marketplace scope).

#### `source`

*Defined in `src/types.ts:686`*

```typescript
string
```

Original source string.

#### `sourceType`

*Defined in `src/types.ts:688`*

```typescript
SourceType
```

Classified source type.

#### `version`

*Defined in `src/types.ts:693`*

```typescript
string | undefined
```

Version string or commit SHA.

#### `installedAt`

*Defined in `src/types.ts:695`*

```typescript
string
```

ISO 8601 timestamp of first installation.

#### `updatedAt`

*Defined in `src/types.ts:700`*

```typescript
string | undefined
```

ISO 8601 timestamp of last update.

#### `agents`

*Defined in `src/types.ts:702`*

```typescript
string[]
```

Provider IDs this entry is linked to.

#### `canonicalPath`

*Defined in `src/types.ts:704`*

```typescript
string
```

Absolute path to canonical installation.

#### `isGlobal`

*Defined in `src/types.ts:706`*

```typescript
boolean
```

Whether this was installed globally.

#### `projectDir`

*Defined in `src/types.ts:711`*

```typescript
string | undefined
```

Project directory (for project-scoped installs).

### `CaampLockFile`

*Defined in `src/types.ts:735`*

```typescript
CaampLockFile
```

The CAAMP lock file structure, stored at the resolved canonical lock path.

**Examples**

```typescript
const lock: CaampLockFile = {
  version: 1,
  skills: {},
  mcpServers: {},
  lastSelectedAgents: ["claude-code"],
};
```

#### `version`

*Defined in `src/types.ts:737`*

```typescript
1
```

Lock file schema version.

#### `skills`

*Defined in `src/types.ts:739`*

```typescript
Record<string, LockEntry>
```

Installed skills keyed by name.

#### `mcpServers`

*Defined in `src/types.ts:741`*

```typescript
Record<string, LockEntry>
```

Installed MCP servers keyed by name.

#### `lastSelectedAgents`

*Defined in `src/types.ts:746`*

```typescript
string[] | undefined
```

Last selected agent IDs for UX persistence.

### `MarketplaceSkill`

*Defined in `src/types.ts:778`*

```typescript
MarketplaceSkill
```

A skill listing from a marketplace search result.

**Examples**

```typescript
const skill: MarketplaceSkill = {
  id: "abc123",
  name: "my-skill",
  scopedName: "@author/my-skill",
  description: "A useful skill",
  author: "author",
  stars: 42,
  forks: 5,
  githubUrl: "https://github.com/author/my-skill",
  repoFullName: "author/my-skill",
  path: "/",
  hasContent: true,
};
```

#### `id`

*Defined in `src/types.ts:780`*

```typescript
string
```

Unique marketplace identifier.

#### `name`

*Defined in `src/types.ts:782`*

```typescript
string
```

Skill name.

#### `scopedName`

*Defined in `src/types.ts:784`*

```typescript
string
```

Scoped name (e.g. `"@author/my-skill"`).

#### `description`

*Defined in `src/types.ts:786`*

```typescript
string
```

Short description.

#### `author`

*Defined in `src/types.ts:788`*

```typescript
string
```

Author / publisher name.

#### `stars`

*Defined in `src/types.ts:790`*

```typescript
number
```

GitHub star count.

#### `forks`

*Defined in `src/types.ts:792`*

```typescript
number
```

GitHub fork count.

#### `githubUrl`

*Defined in `src/types.ts:794`*

```typescript
string
```

GitHub repository URL.

#### `repoFullName`

*Defined in `src/types.ts:796`*

```typescript
string
```

Full `owner/repo` name.

#### `path`

*Defined in `src/types.ts:798`*

```typescript
string
```

Path within the repository.

#### `category`

*Defined in `src/types.ts:803`*

```typescript
string | undefined
```

Optional category tag.

#### `hasContent`

*Defined in `src/types.ts:805`*

```typescript
boolean
```

Whether SKILL.md content was fetched.

### `MarketplaceSearchResult`

*Defined in `src/types.ts:828`*

```typescript
MarketplaceSearchResult
```

Paginated search results from a marketplace API.

**Examples**

```typescript
const result: MarketplaceSearchResult = {
  skills: [],
  total: 0,
  limit: 20,
  offset: 0,
};
```

#### `skills`

*Defined in `src/types.ts:830`*

```typescript
MarketplaceSkill[]
```

Array of matching skills.

#### `total`

*Defined in `src/types.ts:832`*

```typescript
number
```

Total number of matching results.

#### `limit`

*Defined in `src/types.ts:834`*

```typescript
number
```

Maximum results per page.

#### `offset`

*Defined in `src/types.ts:836`*

```typescript
number
```

Offset into the result set.

### `AuditRule`

*Defined in `src/types.ts:873`*

```typescript
AuditRule
```

A security audit rule definition with a regex pattern to match against skill content.

**Examples**

```typescript
const rule: AuditRule = {
  id: "SEC001",
  name: "shell-injection",
  description: "Potential shell injection vector",
  severity: "critical",
  category: "injection",
  pattern: /rm\s+-rf\s+\//,
};
```

#### `id`

*Defined in `src/types.ts:875`*

```typescript
string
```

Unique rule identifier (e.g. `"SEC001"`).

#### `name`

*Defined in `src/types.ts:877`*

```typescript
string
```

Rule name.

#### `description`

*Defined in `src/types.ts:879`*

```typescript
string
```

Human-readable description of what the rule detects.

#### `severity`

*Defined in `src/types.ts:881`*

```typescript
AuditSeverity
```

Severity level of findings from this rule.

#### `category`

*Defined in `src/types.ts:883`*

```typescript
string
```

Category grouping (e.g. `"injection"`, `"exfiltration"`).

#### `pattern`

*Defined in `src/types.ts:885`*

```typescript
RegExp
```

Regex pattern to match against each line of content.

### `AuditFinding`

*Defined in `src/types.ts:909`*

```typescript
AuditFinding
```

A single finding from a security audit scan, with line-level location.

**Examples**

```typescript
const finding: AuditFinding = {
  rule: myRule,
  line: 42,
  column: 10,
  match: "rm -rf /",
  context: "Execute: rm -rf / to clean up",
};
```

#### `rule`

*Defined in `src/types.ts:911`*

```typescript
AuditRule
```

The rule that triggered this finding.

#### `line`

*Defined in `src/types.ts:913`*

```typescript
number
```

Line number (1-based).

#### `column`

*Defined in `src/types.ts:915`*

```typescript
number
```

Column number (1-based).

#### `match`

*Defined in `src/types.ts:917`*

```typescript
string
```

The matched text.

#### `context`

*Defined in `src/types.ts:919`*

```typescript
string
```

The full line of text for context.

### `AuditResult`

*Defined in `src/types.ts:942`*

```typescript
AuditResult
```

Aggregate audit result for a single file.

**Examples**

```typescript
const result: AuditResult = {
  file: "/path/to/SKILL.md",
  findings: [],
  score: 100,
  passed: true,
};
```

#### `file`

*Defined in `src/types.ts:944`*

```typescript
string
```

Path to the scanned file.

#### `findings`

*Defined in `src/types.ts:946`*

```typescript
AuditFinding[]
```

All findings for this file.

#### `score`

*Defined in `src/types.ts:948`*

```typescript
number
```

Security score from 0 (dangerous) to 100 (clean).

#### `passed`

*Defined in `src/types.ts:950`*

```typescript
boolean
```

Whether the file passed the audit (no critical/high findings).

### `InjectionCheckResult`

*Defined in `src/types.ts:987`*

```typescript
InjectionCheckResult
```

Result of checking a single instruction file for CAAMP injection status.

**Examples**

```typescript
const check: InjectionCheckResult = {
  file: "/project/CLAUDE.md",
  provider: "claude-code",
  status: "current",
  fileExists: true,
};
```

#### `file`

*Defined in `src/types.ts:989`*

```typescript
string
```

Absolute path to the instruction file.

#### `provider`

*Defined in `src/types.ts:991`*

```typescript
string
```

Provider ID that owns this instruction file.

#### `status`

*Defined in `src/types.ts:993`*

```typescript
InjectionStatus
```

Current injection status.

#### `fileExists`

*Defined in `src/types.ts:995`*

```typescript
boolean
```

Whether the instruction file exists on disk.

### `McpServerEntry`

*Defined in `src/types.ts:1024`*

```typescript
McpServerEntry
```

An MCP server entry read from a provider's config file.

**Examples**

```typescript
const entry: McpServerEntry = {
  name: "filesystem",
  providerId: "claude-code",
  providerName: "Claude Code",
  scope: "project",
  configPath: "/project/.claude.json",
  config: { command: "npx", args: ["-y", "@mcp/server-filesystem"] },
};
```

#### `name`

*Defined in `src/types.ts:1026`*

```typescript
string
```

Server name (the key in the config file).

#### `providerId`

*Defined in `src/types.ts:1028`*

```typescript
string
```

Provider ID that owns this config file.

#### `providerName`

*Defined in `src/types.ts:1030`*

```typescript
string
```

Human-readable provider name.

#### `scope`

*Defined in `src/types.ts:1032`*

```typescript
"project" | "global"
```

Whether from project or global config.

#### `configPath`

*Defined in `src/types.ts:1034`*

```typescript
string
```

Absolute path to the config file.

#### `config`

*Defined in `src/types.ts:1036`*

```typescript
Record<string, unknown>
```

Raw server configuration object.

### `GlobalOptions`

*Defined in `src/types.ts:1060`*

```typescript
GlobalOptions
```

Global CLI options shared across all CAAMP commands.

**Examples**

```typescript
const opts: GlobalOptions = {
  agent: ["claude-code", "cursor"],
  global: true,
  json: true,
};
```

#### `agent`

*Defined in `src/types.ts:1065`*

```typescript
string[] | undefined
```

Target agent IDs (repeatable).

#### `global`

*Defined in `src/types.ts:1070`*

```typescript
boolean | undefined
```

Operate on global config instead of project.

#### `yes`

*Defined in `src/types.ts:1075`*

```typescript
boolean | undefined
```

Skip confirmation prompts.

#### `all`

*Defined in `src/types.ts:1080`*

```typescript
boolean | undefined
```

Target all detected agents.

#### `json`

*Defined in `src/types.ts:1085`*

```typescript
boolean | undefined
```

Output as JSON.

#### `dryRun`

*Defined in `src/types.ts:1090`*

```typescript
boolean | undefined
```

Preview changes without writing.

#### `verbose`

*Defined in `src/types.ts:1095`*

```typescript
boolean | undefined
```

Enable debug logging.

#### `quiet`

*Defined in `src/types.ts:1100`*

```typescript
boolean | undefined
```

Suppress non-error output.

### `PlatformPaths`

*Defined in `src/core/platform-paths.ts:45`*

```typescript
PlatformPaths
```

OS-appropriate directory paths for CAAMP's global storage.

#### `data`

*Defined in `src/core/platform-paths.ts:47`*

```typescript
string
```

User data dir. Override with AGENTS\_HOME env var.

#### `config`

*Defined in `src/core/platform-paths.ts:49`*

```typescript
string
```

OS config dir (XDG\_CONFIG\_HOME / Library/Preferences / %APPDATA%).

#### `cache`

*Defined in `src/core/platform-paths.ts:51`*

```typescript
string
```

OS cache dir.

#### `log`

*Defined in `src/core/platform-paths.ts:53`*

```typescript
string
```

OS log dir.

#### `temp`

*Defined in `src/core/platform-paths.ts:55`*

```typescript
string
```

OS temp dir.

### `SystemInfo`

*Defined in `src/core/platform-paths.ts:63`*

```typescript
SystemInfo
```

Snapshot of the current system environment and resolved platform paths.

#### `platform`

*Defined in `src/core/platform-paths.ts:65`*

```typescript
NodeJS.Platform
```

Operating system platform identifier.

#### `arch`

*Defined in `src/core/platform-paths.ts:67`*

```typescript
string
```

CPU architecture (e.g. `"x64"`, `"arm64"`).

#### `release`

*Defined in `src/core/platform-paths.ts:69`*

```typescript
string
```

OS kernel release version string.

#### `hostname`

*Defined in `src/core/platform-paths.ts:71`*

```typescript
string
```

Machine hostname.

#### `nodeVersion`

*Defined in `src/core/platform-paths.ts:73`*

```typescript
string
```

Node.js version string (e.g. `"v20.11.0"`).

#### `paths`

*Defined in `src/core/platform-paths.ts:75`*

```typescript
PlatformPaths
```

Resolved platform directory paths.

### `PlatformLocations`

*Defined in `src/core/paths/standard.ts:28`*

```typescript
PlatformLocations
```

Platform-specific directory locations for agent configuration.

#### `home`

*Defined in `src/core/paths/standard.ts:30`*

```typescript
string
```

The user's home directory path.

#### `config`

*Defined in `src/core/paths/standard.ts:32`*

```typescript
string
```

The platform-specific configuration directory.

#### `vscodeConfig`

*Defined in `src/core/paths/standard.ts:34`*

```typescript
string
```

The VS Code user settings directory.

#### `zedConfig`

*Defined in `src/core/paths/standard.ts:36`*

```typescript
string
```

The Zed editor configuration directory.

#### `claudeDesktopConfig`

*Defined in `src/core/paths/standard.ts:38`*

```typescript
string
```

The Claude Desktop application configuration directory.

#### `applications`

*Defined in `src/core/paths/standard.ts:40`*

```typescript
string[]
```

List of application directories (macOS only).

### `InjectionTemplate`

*Defined in `src/core/instructions/templates.ts:21`*

```typescript
InjectionTemplate
```

Structured template for injection content.

#### `references`

*Defined in `src/core/instructions/templates.ts:23`*

```typescript
string[]
```

References to include (e.g. `"\@AGENTS.md"`, `"\@.cleo/project-context.json"`).

#### `content`

*Defined in `src/core/instructions/templates.ts:25`*

```typescript
string[] | undefined
```

Inline content blocks (raw markdown/text).

### `EnsureProviderInstructionFileOptions`

*Defined in `src/core/instructions/injector.ts:317`*

```typescript
EnsureProviderInstructionFileOptions
```

Options for ensuring a provider instruction file.

#### `references`

*Defined in `src/core/instructions/injector.ts:319`*

```typescript
string[]
```

`\@` references to inject (e.g. `["\@AGENTS.md"]`).

#### `content`

*Defined in `src/core/instructions/injector.ts:321`*

```typescript
string[] | undefined
```

Optional inline content blocks.

#### `scope`

*Defined in `src/core/instructions/injector.ts:323`*

```typescript
"project" | "global" | undefined
```

Whether this is a global or project-level file.

### `EnsureProviderInstructionFileResult`

*Defined in `src/core/instructions/injector.ts:331`*

```typescript
EnsureProviderInstructionFileResult
```

Result of ensuring a provider instruction file.

#### `filePath`

*Defined in `src/core/instructions/injector.ts:333`*

```typescript
string
```

Absolute path to the instruction file.

#### `instructFile`

*Defined in `src/core/instructions/injector.ts:335`*

```typescript
string
```

Instruction file name from the provider registry.

#### `action`

*Defined in `src/core/instructions/injector.ts:337`*

```typescript
"created" | "added" | "consolidated" | "updated" | "intact"
```

Action taken.

#### `providerId`

*Defined in `src/core/instructions/injector.ts:339`*

```typescript
string
```

Provider ID.

### `DetectionResult`

*Defined in `src/core/registry/detection.ts:30`*

```typescript
DetectionResult
```

Result of detecting whether a provider is installed on the system.

**Examples**

```typescript
const provider = getProvider("claude-code")!;
const result = detectProvider(provider);
if (result.installed) {
  console.log(`Found via: ${result.methods.join(", ")}`);
}
```

#### `provider`

*Defined in `src/core/registry/detection.ts:32`*

```typescript
Provider
```

The provider that was checked.

#### `installed`

*Defined in `src/core/registry/detection.ts:34`*

```typescript
boolean
```

Whether the provider was detected as installed.

#### `methods`

*Defined in `src/core/registry/detection.ts:36`*

```typescript
string[]
```

Detection methods that matched (e.g. `["binary", "directory"]`).

#### `projectDetected`

*Defined in `src/core/registry/detection.ts:38`*

```typescript
boolean
```

Whether the provider has project-level config in the current directory.

### `DetectionCacheOptions`

*Defined in `src/core/registry/detection.ts:46`*

```typescript
DetectionCacheOptions
```

Options for controlling the detection result cache.

#### `forceRefresh`

*Defined in `src/core/registry/detection.ts:48`*

```typescript
boolean | undefined
```

Whether to bypass the cache and force a fresh detection scan.

#### `ttlMs`

*Defined in `src/core/registry/detection.ts:50`*

```typescript
number | undefined
```

Time-to-live for cached results in milliseconds.

### `SkillInstallResult`

*Defined in `src/core/skills/installer.ts:28`*

```typescript
SkillInstallResult
```

Result of installing a skill to the canonical location and linking to agents.

**Examples**

```typescript
const result = await installSkill(sourcePath, "my-skill", providers, true);
if (result.success) {
  console.log(`Installed to ${result.canonicalPath}`);
  console.log(`Linked to: ${result.linkedAgents.join(", ")}`);
}
```

#### `name`

*Defined in `src/core/skills/installer.ts:30`*

```typescript
string
```

Skill name.

#### `canonicalPath`

*Defined in `src/core/skills/installer.ts:32`*

```typescript
string
```

Absolute path to the canonical installation directory.

#### `linkedAgents`

*Defined in `src/core/skills/installer.ts:34`*

```typescript
string[]
```

Provider IDs that were successfully linked.

#### `errors`

*Defined in `src/core/skills/installer.ts:36`*

```typescript
string[]
```

Error messages from failed link operations.

#### `success`

*Defined in `src/core/skills/installer.ts:38`*

```typescript
boolean
```

Whether at least one agent was successfully linked.

### `SkillBatchOperation`

*Defined in `src/core/advanced/orchestration.ts:68`*

```typescript
SkillBatchOperation
```

Single skill operation entry used by batch orchestration.

#### `sourcePath`

*Defined in `src/core/advanced/orchestration.ts:70`*

```typescript
string
```

The filesystem path to the skill source files.

#### `skillName`

*Defined in `src/core/advanced/orchestration.ts:72`*

```typescript
string
```

The unique name for the skill being installed.

#### `isGlobal`

*Defined in `src/core/advanced/orchestration.ts:74`*

```typescript
boolean | undefined
```

Whether to install globally or project-scoped, defaults to true.

### `BatchInstallOptions`

*Defined in `src/core/advanced/orchestration.ts:87`*

```typescript
BatchInstallOptions
```

Options for rollback-capable batch installation.

#### `providers`

*Defined in `src/core/advanced/orchestration.ts:89`*

```typescript
Provider[] | undefined
```

Explicit list of providers to target, auto-detected if omitted.

#### `minimumPriority`

*Defined in `src/core/advanced/orchestration.ts:91`*

```typescript
ProviderPriority | undefined
```

Minimum provider priority threshold for filtering.

#### `skills`

*Defined in `src/core/advanced/orchestration.ts:93`*

```typescript
SkillBatchOperation[] | undefined
```

Skill operations to apply in the batch.

#### `projectDir`

*Defined in `src/core/advanced/orchestration.ts:95`*

```typescript
string | undefined
```

Project root directory, defaults to `process.cwd()`.

### `BatchInstallResult`

*Defined in `src/core/advanced/orchestration.ts:107`*

```typescript
BatchInstallResult
```

Result of rollback-capable batch installation.

#### `success`

*Defined in `src/core/advanced/orchestration.ts:109`*

```typescript
boolean
```

Whether all operations completed successfully.

#### `providerIds`

*Defined in `src/core/advanced/orchestration.ts:111`*

```typescript
string[]
```

IDs of providers that were targeted.

#### `skillsApplied`

*Defined in `src/core/advanced/orchestration.ts:113`*

```typescript
number
```

Number of skill installations that were applied.

#### `rollbackPerformed`

*Defined in `src/core/advanced/orchestration.ts:115`*

```typescript
boolean
```

Whether rollback was performed due to a failure.

#### `rollbackErrors`

*Defined in `src/core/advanced/orchestration.ts:117`*

```typescript
string[]
```

Error messages from any failures during rollback.

#### `error`

*Defined in `src/core/advanced/orchestration.ts:119`*

```typescript
string | undefined
```

Error message from the operation that triggered rollback.

### `InstructionUpdateSummary`

*Defined in `src/core/advanced/orchestration.ts:378`*

```typescript
InstructionUpdateSummary
```

Result of a single-operation instruction update across providers.

#### `scope`

*Defined in `src/core/advanced/orchestration.ts:380`*

```typescript
Scope
```

The scope at which instructions were updated.

#### `updatedFiles`

*Defined in `src/core/advanced/orchestration.ts:382`*

```typescript
number
```

The total number of instruction files that were modified.

#### `actions`

*Defined in `src/core/advanced/orchestration.ts:384`*

```typescript
{ file: string; action: "created" | "added" | "consolidated" | "updated" | "intact"; providers: string[]; configFormats: ConfigFormat[]; }[]
```

Detailed action log per instruction file.

### `ResolveTierDirOptions`

*Defined in `src/core/harness/scope.ts:171`*

```typescript
ResolveTierDirOptions
```

Options accepted by `resolveTierDir`.

#### `tier`

*Defined in `src/core/harness/scope.ts:173`*

```typescript
HarnessTier
```

Tier to resolve.

#### `kind`

*Defined in `src/core/harness/scope.ts:175`*

```typescript
HarnessAssetKind
```

Asset kind (extensions, prompts, themes, sessions, cant).

#### `projectDir`

*Defined in `src/core/harness/scope.ts:184`*

```typescript
string | undefined
```

Project directory used when `tier` is `project`. Ignored for other tiers. When omitted with `tier='project'` the caller MUST substitute `process.cwd()` before invoking the resolver, so the harness never silently resolves to an unexpected working directory.

### `ResolveDefaultTargetProvidersOptions`

*Defined in `src/core/harness/types.ts:55`*

```typescript
ResolveDefaultTargetProvidersOptions
```

Options accepted by `resolveDefaultTargetProviders` (defined in `./index.ts`).

#### `explicit`

*Defined in `src/core/harness/types.ts:73`*

```typescript
Provider[] | undefined
```

Explicit list of providers requested by the user (e.g. via `--agent`).

### `ExtensionEntry`

*Defined in `src/core/harness/types.ts:118`*

```typescript
ExtensionEntry
```

Metadata describing a Pi extension discovered on disk.

#### `name`

*Defined in `src/core/harness/types.ts:120`*

```typescript
string
```

Extension name (file basename without the `.ts` extension).

#### `tier`

*Defined in `src/core/harness/types.ts:122`*

```typescript
HarnessTier
```

Tier at which this entry lives.

#### `path`

*Defined in `src/core/harness/types.ts:124`*

```typescript
string
```

Absolute on-disk path to the extension file.

#### `shadowed`

*Defined in `src/core/harness/types.ts:131`*

```typescript
boolean | undefined
```

When `true`, this entry is shadowed by a higher-precedence entry with the same name. Exposed so list output can warn about cross-tier name collisions per ADR-035 §D1.

### `PromptEntry`

*Defined in `src/core/harness/types.ts:145`*

```typescript
PromptEntry
```

Metadata describing a Pi prompt directory discovered on disk.

#### `name`

*Defined in `src/core/harness/types.ts:147`*

```typescript
string
```

Prompt name (directory basename).

#### `tier`

*Defined in `src/core/harness/types.ts:149`*

```typescript
HarnessTier
```

Tier at which this entry lives.

#### `path`

*Defined in `src/core/harness/types.ts:151`*

```typescript
string
```

Absolute on-disk path to the prompt directory.

#### `shadowed`

*Defined in `src/core/harness/types.ts:153`*

```typescript
boolean | undefined
```

See `ExtensionEntry.shadowed`.

### `ThemeEntry`

*Defined in `src/core/harness/types.ts:165`*

```typescript
ThemeEntry
```

Metadata describing a Pi theme discovered on disk.

#### `name`

*Defined in `src/core/harness/types.ts:167`*

```typescript
string
```

Theme name (file basename without the extension).

#### `tier`

*Defined in `src/core/harness/types.ts:169`*

```typescript
HarnessTier
```

Tier at which this entry lives.

#### `path`

*Defined in `src/core/harness/types.ts:171`*

```typescript
string
```

Absolute on-disk path to the theme file.

#### `fileExt`

*Defined in `src/core/harness/types.ts:173`*

```typescript
string
```

File extension of the theme file (e.g. `".ts"`, `".json"`).

#### `shadowed`

*Defined in `src/core/harness/types.ts:175`*

```typescript
boolean | undefined
```

See `ExtensionEntry.shadowed`.

### `HarnessInstallOptions`

*Defined in `src/core/harness/types.ts:183`*

```typescript
HarnessInstallOptions
```

Options accepted by the Pi install verbs (extensions, prompts, themes).

#### `force`

*Defined in `src/core/harness/types.ts:189`*

```typescript
boolean | undefined
```

When `true`, overwrite an existing file at the target tier. When `false` (the default) the install verb throws if the target exists.

### `CantProfileCounts`

*Defined in `src/core/harness/types.ts:212`*

```typescript
CantProfileCounts
```

Counts of top-level CANT sections discovered in a `.cant` file.

#### `agentCount`

*Defined in `src/core/harness/types.ts:214`*

```typescript
number
```

Number of `agent { ... }` sections in the document.

#### `workflowCount`

*Defined in `src/core/harness/types.ts:216`*

```typescript
number
```

Number of `workflow { ... }` sections in the document.

#### `pipelineCount`

*Defined in `src/core/harness/types.ts:218`*

```typescript
number
```

Number of `pipeline { ... }` sections in the document.

#### `hookCount`

*Defined in `src/core/harness/types.ts:223`*

```typescript
number
```

Number of hook bodies discovered. Top-level `Hook` sections plus the `hooks` array nested inside every Agent section are summed.

#### `skillCount`

*Defined in `src/core/harness/types.ts:229`*

```typescript
number
```

Number of distinct skill names referenced via an Agent section's `skills:` property (e.g. `skills: ["ct-cleo", "ct-task-executor"]`). The count is de-duplicated across agents within a single document.

### `CantProfileEntry`

*Defined in `src/core/harness/types.ts:245`*

```typescript
CantProfileEntry
```

Metadata describing a `.cant` profile installed at one of the three Pi harness tiers.

#### `name`

*Defined in `src/core/harness/types.ts:247`*

```typescript
string
```

Profile name (basename of the `.cant` file without the extension).

#### `tier`

*Defined in `src/core/harness/types.ts:249`*

```typescript
HarnessTier
```

Tier at which this entry lives.

#### `sourcePath`

*Defined in `src/core/harness/types.ts:251`*

```typescript
string
```

Absolute on-disk path to the `.cant` file.

#### `counts`

*Defined in `src/core/harness/types.ts:253`*

```typescript
CantProfileCounts
```

Parsed section counts for the profile.

#### `shadowedByHigherTier`

*Defined in `src/core/harness/types.ts:260`*

```typescript
boolean | undefined
```

When `true`, this entry is shadowed by a higher-precedence entry with the same name. Mirrors the shadow flag emitted by `Harness.listExtensions` so the same UI logic applies.

### `CantValidationDiagnostic`

*Defined in `src/core/harness/types.ts:275`*

```typescript
CantValidationDiagnostic
```

Diagnostic emitted by the cant-core 42-rule validator, normalised to the harness layer's vocabulary.

#### `ruleId`

*Defined in `src/core/harness/types.ts:277`*

```typescript
string
```

Cant-core rule id (`PARSE`, `S01`, `P06`, ...).

#### `message`

*Defined in `src/core/harness/types.ts:279`*

```typescript
string
```

Human-readable diagnostic message.

#### `line`

*Defined in `src/core/harness/types.ts:281`*

```typescript
number
```

1-based line number where the diagnostic was emitted.

#### `col`

*Defined in `src/core/harness/types.ts:283`*

```typescript
number
```

1-based column number where the diagnostic was emitted.

#### `severity`

*Defined in `src/core/harness/types.ts:285`*

```typescript
"error" | "info" | "warning" | "hint"
```

Severity bucket from cant-core (`error`, `warning`, `info`, `hint`).

### `ValidateCantProfileResult`

*Defined in `src/core/harness/types.ts:302`*

```typescript
ValidateCantProfileResult
```

Result of validating a `.cant` profile via the cant-core 42-rule engine.

#### `valid`

*Defined in `src/core/harness/types.ts:304`*

```typescript
boolean
```

Whether validation passed (no error-severity diagnostics).

#### `errors`

*Defined in `src/core/harness/types.ts:306`*

```typescript
CantValidationDiagnostic[]
```

All diagnostics emitted by the 42-rule engine, in source order.

#### `counts`

*Defined in `src/core/harness/types.ts:308`*

```typescript
CantProfileCounts
```

Section counts for the profile (zero when parsing failed).

### `SessionSummary`

*Defined in `src/core/harness/types.ts:323`*

```typescript
SessionSummary
```

Summary header extracted from the first line of a Pi session JSONL file.

#### `id`

*Defined in `src/core/harness/types.ts:325`*

```typescript
string
```

Session identifier as recorded in the line-1 header.

#### `version`

*Defined in `src/core/harness/types.ts:327`*

```typescript
number
```

Session version as recorded in the line-1 header (e.g. `3`).

#### `timestamp`

*Defined in `src/core/harness/types.ts:329`*

```typescript
string | null
```

ISO-8601 timestamp from the line-1 header, or `null` when absent.

#### `cwd`

*Defined in `src/core/harness/types.ts:331`*

```typescript
string | null
```

Working directory recorded when the session was created.

#### `parentSession`

*Defined in `src/core/harness/types.ts:333`*

```typescript
string | null
```

Parent session id, if this session was forked from another.

#### `filePath`

*Defined in `src/core/harness/types.ts:335`*

```typescript
string
```

Absolute path to the session JSONL file on disk.

#### `mtimeMs`

*Defined in `src/core/harness/types.ts:337`*

```typescript
number
```

File modification time in milliseconds since the epoch.

### `SessionDocument`

*Defined in `src/core/harness/types.ts:354`*

```typescript
SessionDocument
```

Raw content of a Pi session JSONL file, preserved line-by-line.

#### `summary`

*Defined in `src/core/harness/types.ts:356`*

```typescript
SessionSummary
```

Header summary (same shape as `SessionSummary`).

#### `entries`

*Defined in `src/core/harness/types.ts:358`*

```typescript
string[]
```

Raw JSONL lines in file order, excluding the line-1 header.

### `PiModelDefinition`

*Defined in `src/core/harness/types.ts:372`*

```typescript
PiModelDefinition
```

Pi model definition as recorded under `models.json:providers[].models`.

#### `id`

*Defined in `src/core/harness/types.ts:374`*

```typescript
string
```

Model id within the provider (e.g. `"claude-opus-4-20250514"`).

#### `name`

*Defined in `src/core/harness/types.ts:376`*

```typescript
string
```

Human-readable model name.

#### `reasoning`

*Defined in `src/core/harness/types.ts:381`*

```typescript
boolean | undefined
```

Whether the model supports reasoning/thinking tokens.

#### `input`

*Defined in `src/core/harness/types.ts:386`*

```typescript
("text" | "image")[] | undefined
```

Allowed input modalities (e.g. `["text"]`, `["text", "image"]`).

#### `contextWindow`

*Defined in `src/core/harness/types.ts:391`*

```typescript
number | undefined
```

Context window size in tokens.

#### `maxTokens`

*Defined in `src/core/harness/types.ts:396`*

```typescript
number | undefined
```

Maximum output tokens.

### `PiModelProvider`

*Defined in `src/core/harness/types.ts:407`*

```typescript
PiModelProvider
```

Pi provider block as recorded under `models.json:providers[id]`.

#### `baseUrl`

*Defined in `src/core/harness/types.ts:412`*

```typescript
string | undefined
```

Custom base URL for the provider (overrides default).

#### `apiKey`

*Defined in `src/core/harness/types.ts:417`*

```typescript
string | undefined
```

API key or `$ENV_VAR` reference.

#### `models`

*Defined in `src/core/harness/types.ts:422`*

```typescript
PiModelDefinition[] | undefined
```

Custom model definitions declared by the user.

### `PiModelsConfig`

*Defined in `src/core/harness/types.ts:435`*

```typescript
PiModelsConfig
```

Entire `models.json` document shape used by Pi.

#### `providers`

*Defined in `src/core/harness/types.ts:437`*

```typescript
Record<string, PiModelProvider>
```

Map of provider id → provider block.

### `ModelListEntry`

*Defined in `src/core/harness/types.ts:451`*

```typescript
ModelListEntry
```

A model entry as surfaced by `Harness.listModels`.

#### `provider`

*Defined in `src/core/harness/types.ts:453`*

```typescript
string
```

Provider id (e.g. `"anthropic"`).

#### `id`

*Defined in `src/core/harness/types.ts:455`*

```typescript
string
```

Model id within the provider.

#### `name`

*Defined in `src/core/harness/types.ts:457`*

```typescript
string | null
```

Human-readable name, from `models.json` when available.

#### `enabled`

*Defined in `src/core/harness/types.ts:459`*

```typescript
boolean
```

`true` when the model is present in `settings.json:enabledModels`.

#### `isDefault`

*Defined in `src/core/harness/types.ts:461`*

```typescript
boolean
```

`true` when the model is the configured default.

#### `custom`

*Defined in `src/core/harness/types.ts:467`*

```typescript
boolean
```

`true` when this model is defined in `models.json` (custom). When `false`, the entry originates from `settings.json:enabledModels` only and is assumed to resolve against Pi's built-in registry.

### `SubagentTask`

*Defined in `src/core/harness/types.ts:487`*

```typescript
SubagentTask
```

Description of a subagent task to be spawned under a harness.

#### `targetProviderId`

*Defined in `src/core/harness/types.ts:489`*

```typescript
string
```

Provider id of the agent to spawn (e.g. `"claude-code"`).

#### `prompt`

*Defined in `src/core/harness/types.ts:491`*

```typescript
string
```

The prompt / instruction to give the spawned agent.

#### `taskId`

*Defined in `src/core/harness/types.ts:503`*

```typescript
string | undefined
```

Stable task identifier used to derive the child session filename and to correlate streamed events with their originating task.

#### `parentSessionId`

*Defined in `src/core/harness/types.ts:515`*

```typescript
string | undefined
```

Identifier of the parent session that owns this subagent.

#### `parentSessionPath`

*Defined in `src/core/harness/types.ts:527`*

```typescript
string | undefined
```

Absolute path to the parent session JSONL file.

#### `cwd`

*Defined in `src/core/harness/types.ts:532`*

```typescript
string | undefined
```

Working directory for the spawned agent.

#### `env`

*Defined in `src/core/harness/types.ts:537`*

```typescript
Record<string, string> | undefined
```

Environment variable overrides layered atop the parent environment.

#### `signal`

*Defined in `src/core/harness/types.ts:543`*

```typescript
AbortSignal | undefined
```

Abort signal. When it aborts, the harness will terminate the subagent via the configured SIGTERM-then-SIGKILL cleanup sequence.

### `SubagentSpawnOptions`

*Defined in `src/core/harness/types.ts:556`*

```typescript
SubagentSpawnOptions
```

Per-call options that override harness-wide spawn defaults.

#### `onStream`

*Defined in `src/core/harness/types.ts:569`*

```typescript
((event: SubagentStreamEvent) => void) | undefined
```

Streaming callback invoked once per parsed event from the child.

#### `terminateGraceMs`

*Defined in `src/core/harness/types.ts:581`*

```typescript
number | undefined
```

Override the SIGTERM grace window before SIGKILL fires.

#### `env`

*Defined in `src/core/harness/types.ts:592`*

```typescript
Record<string, string> | undefined
```

Environment variable overrides layered atop the task-level env.

#### `cwd`

*Defined in `src/core/harness/types.ts:602`*

```typescript
string | undefined
```

Working directory override that wins over `SubagentTask.cwd`.

### `SubagentStreamEvent`

*Defined in `src/core/harness/types.ts:624`*

```typescript
SubagentStreamEvent
```

One streaming event surfaced through `SubagentSpawnOptions.onStream`.

#### `kind`

*Defined in `src/core/harness/types.ts:626`*

```typescript
"message" | "stderr" | "exit" | "link"
```

Event kind discriminator.

#### `subagentId`

*Defined in `src/core/harness/types.ts:628`*

```typescript
string
```

Subagent identifier (matches `SubagentHandle.subagentId`).

#### `lineNumber`

*Defined in `src/core/harness/types.ts:634`*

```typescript
number | undefined
```

1-based line number within the child's stdout stream. Only set for `"message"` events that originated from a parsed stdout line.

#### `payload`

*Defined in `src/core/harness/types.ts:636`*

```typescript
unknown
```

Event payload, shaped according to `kind`.

### `SubagentExitResult`

*Defined in `src/core/harness/types.ts:650`*

```typescript
SubagentExitResult
```

Resolution value of `SubagentHandle.exitPromise`.

#### `code`

*Defined in `src/core/harness/types.ts:655`*

```typescript
number | null
```

Process exit code, or `null` when the child was terminated by a signal before exiting normally.

#### `signal`

*Defined in `src/core/harness/types.ts:657`*

```typescript
NodeJS.Signals | null
```

Terminating signal, or `null` when the child exited normally.

#### `childSessionPath`

*Defined in `src/core/harness/types.ts:659`*

```typescript
string
```

Absolute path to the child session JSONL file on disk.

#### `durationMs`

*Defined in `src/core/harness/types.ts:661`*

```typescript
number
```

Wall-clock duration from spawn to exit, in milliseconds.

### `SubagentLinkEntry`

*Defined in `src/core/harness/types.ts:675`*

```typescript
SubagentLinkEntry
```

`subagent_link` custom entry written into the parent session JSONL.

#### `type`

*Defined in `src/core/harness/types.ts:677`*

```typescript
"subagent_link"
```

Entry type discriminator (always `"subagent_link"`).

#### `subagentId`

*Defined in `src/core/harness/types.ts:679`*

```typescript
string
```

Subagent identifier matching `SubagentHandle.subagentId`.

#### `taskId`

*Defined in `src/core/harness/types.ts:681`*

```typescript
string
```

Task identifier from `SubagentTask.taskId`.

#### `childSessionPath`

*Defined in `src/core/harness/types.ts:683`*

```typescript
string
```

Absolute path to the child session JSONL file.

#### `startedAt`

*Defined in `src/core/harness/types.ts:685`*

```typescript
string
```

ISO-8601 timestamp captured when the child was spawned.

### `SubagentResult`

*Defined in `src/core/harness/types.ts:700`*

```typescript
SubagentResult
```

Final result of a subagent's execution (legacy v1 shape).

#### `exitCode`

*Defined in `src/core/harness/types.ts:702`*

```typescript
number | null
```

Process exit code, or `null` if the process was killed by a signal.

#### `stdout`

*Defined in `src/core/harness/types.ts:704`*

```typescript
string
```

Full stdout captured from the subagent.

#### `stderr`

*Defined in `src/core/harness/types.ts:706`*

```typescript
string
```

Full stderr captured from the subagent.

#### `parsed`

*Defined in `src/core/harness/types.ts:712`*

```typescript
unknown
```

Parsed JSON output, when the target supports a JSON output mode and emitted well-formed JSON on stdout.

### `SubagentHandle`

*Defined in `src/core/harness/types.ts:735`*

```typescript
SubagentHandle
```

Live handle to a running subagent.

#### `subagentId`

*Defined in `src/core/harness/types.ts:745`*

```typescript
string
```

Stable subagent identifier generated at spawn time.

#### `taskId`

*Defined in `src/core/harness/types.ts:747`*

```typescript
string
```

Task identifier from `SubagentTask.taskId` (or generated default).

#### `childSessionPath`

*Defined in `src/core/harness/types.ts:749`*

```typescript
string
```

Absolute path to the child session JSONL file on disk.

#### `pid`

*Defined in `src/core/harness/types.ts:751`*

```typescript
number | null
```

PID of the spawned process, or `null` if spawning did not yield one.

#### `startedAt`

*Defined in `src/core/harness/types.ts:753`*

```typescript
Date
```

Wall-clock timestamp captured immediately after spawn.

#### `exitPromise`

*Defined in `src/core/harness/types.ts:760`*

```typescript
Promise<SubagentExitResult>
```

Promise resolving to the rich exit result once the child process has fully terminated. NEVER rejects — failures are encoded in the resolved value (non-zero code, non-null signal, partial output in the session file).

#### `result`

*Defined in `src/core/harness/types.ts:770`*

```typescript
Promise<SubagentResult>
```

Promise resolving to the legacy `SubagentResult` shape.

#### `terminate()`

*Defined in `src/core/harness/types.ts:780`*

```typescript
() => Promise<void>
```

Terminate the subagent gracefully.

#### `abort`

*Defined in `src/core/harness/types.ts:790`*

```typescript
() => void
```

Synchronously trigger the cleanup sequence (legacy v1 alias for `terminate`).

#### `recentStderr()`

*Defined in `src/core/harness/types.ts:800`*

```typescript
() => string[]
```

Snapshot of the most recent stderr lines captured for this child.

### `Harness`

*Defined in `src/core/harness/types.ts:819`*

```typescript
Harness
```

Contract every first-class harness must implement.

#### `id`

*Defined in `src/core/harness/types.ts:821`*

```typescript
string
```

Short id matching the provider id (e.g. `"pi"`).

#### `provider`

*Defined in `src/core/harness/types.ts:824`*

```typescript
Provider
```

The underlying resolved provider entry.

#### `installSkill()`

*Defined in `src/core/harness/types.ts:839`*

```typescript
(sourcePath: string, skillName: string, scope: HarnessScope) => Promise<void>
```

Install a skill using the harness's native mechanism.

**Parameters**

- `sourcePath` — Absolute path to the skill directory to install.
- `skillName` — Target skill name (becomes the directory name on disk).
- `scope` — Install target scope.

#### `removeSkill()`

*Defined in `src/core/harness/types.ts:851`*

```typescript
(skillName: string, scope: HarnessScope) => Promise<void>
```

Remove a skill previously installed via this harness.

**Parameters**

- `skillName` — Skill name to remove.
- `scope` — Install target scope.

#### `listSkills()`

*Defined in `src/core/harness/types.ts:863`*

```typescript
(scope: HarnessScope) => Promise<string[]>
```

List skills installed in this harness's skill directory for a scope.

**Parameters**

- `scope` — Scope to inspect.

**Returns**: Array of skill directory names.

#### `injectInstructions()`

*Defined in `src/core/harness/types.ts:878`*

```typescript
(content: string, scope: HarnessScope) => Promise<void>
```

Inject content into the harness's instruction file using a marker-based idempotent block.

**Parameters**

- `content` — The content to inject inside the marker block.
- `scope` — Instruction file scope.

#### `removeInstructions()`

*Defined in `src/core/harness/types.ts:888`*

```typescript
(scope: HarnessScope) => Promise<void>
```

Remove the CAAMP injection block from the harness's instruction file.

**Parameters**

- `scope` — Instruction file scope.

#### `spawnSubagent()`

*Defined in `src/core/harness/types.ts:914`*

```typescript
((task: SubagentTask, opts?: SubagentSpawnOptions) => Promise<SubagentHandle>) | undefined
```

Spawn a subagent under this harness's control.

**Parameters**

- `task` — Subagent task specification.
- `opts` — Per-call streaming and cleanup overrides.

**Returns**: A live subagent handle.

#### `configureModels()`

*Defined in `src/core/harness/types.ts:927`*

```typescript
((modelPatterns: string[], scope: HarnessScope) => Promise<void>) | undefined
```

Configure which models are available in the harness's model picker.

**Parameters**

- `modelPatterns` — Glob patterns enumerating enabled models.
- `scope` — Settings scope.

#### `readSettings()`

*Defined in `src/core/harness/types.ts:939`*

```typescript
(scope: HarnessScope) => Promise<unknown>
```

Read the harness's current settings as an opaque object.

**Parameters**

- `scope` — Settings scope.

**Returns**: Opaque settings blob.

#### `writeSettings()`

*Defined in `src/core/harness/types.ts:951`*

```typescript
(patch: Record<string, unknown>, scope: HarnessScope) => Promise<void>
```

Deep-merge a patch into the harness's settings and persist the result.

**Parameters**

- `patch` — Partial settings object to merge.
- `scope` — Settings scope.

#### `installExtension()`

*Defined in `src/core/harness/types.ts:975`*

```typescript
((sourcePath: string, name: string, tier: HarnessTier, projectDir?: string, opts?: HarnessInstallOptions) => Promise<{ targetPath: string; tier: HarnessTier; }>) | undefined
```

Install a Pi extension TypeScript file from a local source path into the given tier.

**Parameters**

- `sourcePath` — Absolute path to the source `.ts` file on disk.
- `name` — Extension name (used as the target file basename).
- `tier` — Target tier (`project`/`user`/`global`).
- `projectDir` — Project directory (required when `tier='project'`).
- `opts` — Install options (see `HarnessInstallOptions`).

#### `removeExtension()`

*Defined in `src/core/harness/types.ts:995`*

```typescript
((name: string, tier: HarnessTier, projectDir?: string) => Promise<boolean>) | undefined
```

Remove a Pi extension by name from the given tier.

**Parameters**

- `name` — Extension name (basename without `.ts`).
- `tier` — Target tier to remove from.
- `projectDir` — Project directory (required when `tier='project'`).

**Returns**: `true` when a file was removed, `false` when none existed.

#### `listExtensions()`

*Defined in `src/core/harness/types.ts:1010`*

```typescript
((projectDir?: string) => Promise<ExtensionEntry[]>) | undefined
```

List Pi extensions across all tiers, precedence-ordered.

**Parameters**

- `projectDir` — Project directory for the `project` tier. When   omitted the `project` tier is skipped rather than failing.

#### `listSessions()`

*Defined in `src/core/harness/types.ts:1022`*

```typescript
((opts?: { includeSubagents?: boolean; }) => Promise<SessionSummary[]>) | undefined
```

List Pi sessions from the user-tier sessions directory.

**Parameters**

- `opts` — Options controlling which directories to scan.

#### `showSession()`

*Defined in `src/core/harness/types.ts:1034`*

```typescript
((id: string) => Promise<SessionDocument>) | undefined
```

Load a Pi session's full body by id.

**Parameters**

- `id` — Session id as recorded in the line-1 header.

#### `listModels()`

*Defined in `src/core/harness/types.ts:1048`*

```typescript
((scope: HarnessScope) => Promise<ModelListEntry[]>) | undefined
```

List every model known to Pi — both custom (`models.json`) and enabled selections (`settings.json:enabledModels`).

**Parameters**

- `scope` — Legacy two-tier scope (global/project) that   determines which `models.json` and `settings.json` files to read.

#### `readModelsConfig()`

*Defined in `src/core/harness/types.ts:1058`*

```typescript
((scope: HarnessScope) => Promise<PiModelsConfig>) | undefined
```

Read `models.json` for the given scope.

#### `writeModelsConfig()`

*Defined in `src/core/harness/types.ts:1068`*

```typescript
((config: PiModelsConfig, scope: HarnessScope) => Promise<void>) | undefined
```

Write `models.json` for the given scope atomically.

#### `installPrompt()`

*Defined in `src/core/harness/types.ts:1079`*

```typescript
((sourceDir: string, name: string, tier: HarnessTier, projectDir?: string, opts?: HarnessInstallOptions) => Promise<{ targetPath: string; tier: HarnessTier; }>) | undefined
```

Install a Pi prompt from a source directory into the given tier.

#### `listPrompts()`

*Defined in `src/core/harness/types.ts:1088`*

```typescript
((projectDir?: string) => Promise<PromptEntry[]>) | undefined
```

List Pi prompts across all tiers.

#### `removePrompt()`

*Defined in `src/core/harness/types.ts:1091`*

```typescript
((name: string, tier: HarnessTier, projectDir?: string) => Promise<boolean>) | undefined
```

Remove a Pi prompt by name from the given tier.

#### `installTheme()`

*Defined in `src/core/harness/types.ts:1101`*

```typescript
((sourceFile: string, name: string, tier: HarnessTier, projectDir?: string, opts?: HarnessInstallOptions) => Promise<{ targetPath: string; tier: HarnessTier; }>) | undefined
```

Install a Pi theme from a source file into the given tier.

#### `listThemes()`

*Defined in `src/core/harness/types.ts:1110`*

```typescript
((projectDir?: string) => Promise<ThemeEntry[]>) | undefined
```

List Pi themes across all tiers.

#### `removeTheme()`

*Defined in `src/core/harness/types.ts:1113`*

```typescript
((name: string, tier: HarnessTier, projectDir?: string) => Promise<boolean>) | undefined
```

Remove a Pi theme by name from the given tier.

#### `installCantProfile()`

*Defined in `src/core/harness/types.ts:1138`*

```typescript
((sourcePath: string, name: string, tier: HarnessTier, projectDir?: string, opts?: HarnessInstallOptions) => Promise<{ targetPath: string; tier: HarnessTier; counts: CantProfileCounts; }>) | undefined
```

Install a CANT profile (`.cant` file) into the given tier.

**Parameters**

- `sourcePath` — Absolute path to the source `.cant` file on disk.
- `name` — Profile name (used as the target file basename).
- `tier` — Target tier (`project`/`user`/`global`).
- `projectDir` — Project directory (required when `tier='project'`).
- `opts` — Install options (see `HarnessInstallOptions`).

#### `removeCantProfile()`

*Defined in `src/core/harness/types.ts:1158`*

```typescript
((name: string, tier: HarnessTier, projectDir?: string) => Promise<boolean>) | undefined
```

Remove a CANT profile by name from the given tier.

**Parameters**

- `name` — Profile name (basename without `.cant`).
- `tier` — Target tier to remove from.
- `projectDir` — Project directory (required when `tier='project'`).

**Returns**: `true` when a file was removed, `false` when none existed.

#### `listCantProfiles()`

*Defined in `src/core/harness/types.ts:1174`*

```typescript
((projectDir?: string) => Promise<CantProfileEntry[]>) | undefined
```

List CANT profiles across all tiers, precedence-ordered.

**Parameters**

- `projectDir` — Project directory for the `project` tier. When   omitted the `project` tier is skipped rather than failing.

#### `validateCantProfile()`

*Defined in `src/core/harness/types.ts:1187`*

```typescript
((sourcePath: string) => Promise<ValidateCantProfileResult>) | undefined
```

Validate a `.cant` file via cant-core's 42-rule engine without installing anything.

**Parameters**

- `sourcePath` — Absolute path to the `.cant` file on disk.

### `LAFSEnvelope`

*Defined in `src/core/lafs.ts:50`*

```typescript
LAFSEnvelope<T>
```

Generic LAFS Envelope structure for type-safe command results.

#### `$schema`

*Defined in `src/core/lafs.ts:52`*

```typescript
"https://lafs.dev/schemas/v1/envelope.schema.json"
```

JSON Schema URI for envelope validation.

#### `_meta`

*Defined in `src/core/lafs.ts:54`*

```typescript
LAFSMeta
```

Envelope metadata (timestamps, request IDs, MVI level).

#### `success`

*Defined in `src/core/lafs.ts:56`*

```typescript
boolean
```

Whether the operation succeeded.

#### `result`

*Defined in `src/core/lafs.ts:58`*

```typescript
T | null
```

Operation result payload, or `null` on error.

#### `error`

*Defined in `src/core/lafs.ts:60`*

```typescript
LAFSError | null
```

Error details, or `null` on success.

#### `page`

*Defined in `src/core/lafs.ts:62`*

```typescript
LAFSPage | null
```

Pagination metadata, or `null` when not applicable.

### `FormatOptions`

*Defined in `src/core/lafs.ts:70`*

```typescript
FormatOptions
```

Format resolution options.

#### `jsonFlag`

*Defined in `src/core/lafs.ts:72`*

```typescript
boolean | undefined
```

Whether `--json` was explicitly passed.

#### `humanFlag`

*Defined in `src/core/lafs.ts:74`*

```typescript
boolean | undefined
```

Whether `--human` was explicitly passed.

#### `projectDefault`

*Defined in `src/core/lafs.ts:76`*

```typescript
"json" | "human" | undefined
```

Project-level default format when no flag is given.

### `LAFSCommandOptions`

*Defined in `src/core/lafs.ts:310`*

```typescript
LAFSCommandOptions
```

Standard command options interface for LAFS-compliant commands.

#### `json`

*Defined in `src/core/lafs.ts:312`*

```typescript
boolean | undefined
```

Whether to force JSON output.

#### `human`

*Defined in `src/core/lafs.ts:314`*

```typescript
boolean | undefined
```

Whether to force human-readable output.

### `ProviderTargetOptions`

*Defined in `src/commands/advanced/common.ts:22`*

```typescript
ProviderTargetOptions
```

Options for resolving which providers to target in advanced commands.

#### `all`

*Defined in `src/commands/advanced/common.ts:24`*

```typescript
boolean | undefined
```

When true, target all registry providers including undetected ones.

#### `agent`

*Defined in `src/commands/advanced/common.ts:26`*

```typescript
string[] | undefined
```

Specific provider IDs or aliases to target.

### `McpDetectionEntry`

*Defined in `src/core/mcp/reader.ts:62`*

```typescript
McpDetectionEntry
```

Result of a single provider's MCP installation probe.

#### `providerId`

*Defined in `src/core/mcp/reader.ts:64`*

```typescript
string
```

Provider id (e.g. `"claude-code"`).

#### `providerName`

*Defined in `src/core/mcp/reader.ts:66`*

```typescript
string
```

Human-readable provider name.

#### `scope`

*Defined in `src/core/mcp/reader.ts:68`*

```typescript
McpScope
```

Resolved scope of the probed config file.

#### `configPath`

*Defined in `src/core/mcp/reader.ts:70`*

```typescript
string
```

Absolute path to the provider's MCP config file.

#### `exists`

*Defined in `src/core/mcp/reader.ts:72`*

```typescript
boolean
```

Whether the config file exists on disk.

#### `serverCount`

*Defined in `src/core/mcp/reader.ts:74`*

```typescript
number | null
```

Number of server entries found, or `null` when the file is missing/unparseable.

#### `lastModified`

*Defined in `src/core/mcp/reader.ts:76`*

```typescript
string | null
```

ISO 8601 timestamp of the file's last modification, or `null` when the file is missing.

### `InstallMcpServerOptions`

*Defined in `src/core/mcp/installer.ts:39`*

```typescript
InstallMcpServerOptions
```

Options accepted by `installMcpServer`.

#### `scope`

*Defined in `src/core/mcp/installer.ts:41`*

```typescript
McpScope
```

Scope to write to (project|global).

#### `force`

*Defined in `src/core/mcp/installer.ts:43`*

```typescript
boolean | undefined
```

When `true`, overwrite an existing server entry instead of failing.

#### `projectDir`

*Defined in `src/core/mcp/installer.ts:45`*

```typescript
string | undefined
```

Project directory used for the `project` scope.

### `InstallMcpServerResult`

*Defined in `src/core/mcp/installer.ts:59`*

```typescript
InstallMcpServerResult
```

Result of an `installMcpServer` call.

#### `installed`

*Defined in `src/core/mcp/installer.ts:61`*

```typescript
boolean
```

Whether the entry was written to the config file.

#### `conflicted`

*Defined in `src/core/mcp/installer.ts:63`*

```typescript
boolean
```

Whether the target server name already existed before the call.

#### `sourcePath`

*Defined in `src/core/mcp/installer.ts:65`*

```typescript
string
```

Absolute path to the config file that was (or would have been) written.

#### `providerId`

*Defined in `src/core/mcp/installer.ts:67`*

```typescript
string
```

Provider id the entry was written for.

#### `serverName`

*Defined in `src/core/mcp/installer.ts:69`*

```typescript
string
```

Server name that was written.

### `RemoveMcpServerOptions`

*Defined in `src/core/mcp/remover.ts:31`*

```typescript
RemoveMcpServerOptions
```

Options accepted by `removeMcpServer` and `removeMcpServerFromAll`.

#### `scope`

*Defined in `src/core/mcp/remover.ts:33`*

```typescript
McpScope
```

Scope to target (project|global).

#### `projectDir`

*Defined in `src/core/mcp/remover.ts:35`*

```typescript
string | undefined
```

Project directory used for the `project` scope.

### `RemoveMcpServerResult`

*Defined in `src/core/mcp/remover.ts:49`*

```typescript
RemoveMcpServerResult
```

Result of a single-provider `removeMcpServer` call.

#### `providerId`

*Defined in `src/core/mcp/remover.ts:51`*

```typescript
string
```

Provider id the call targeted.

#### `serverName`

*Defined in `src/core/mcp/remover.ts:53`*

```typescript
string
```

Server name the call targeted.

#### `sourcePath`

*Defined in `src/core/mcp/remover.ts:55`*

```typescript
string | null
```

Resolved config file path, or `null` when the provider had no MCP capability.

#### `removed`

*Defined in `src/core/mcp/remover.ts:57`*

```typescript
boolean
```

Whether an entry was actually deleted.

#### `reason`

*Defined in `src/core/mcp/remover.ts:68`*

```typescript
"no-mcp-capability" | "no-config-path" | "file-missing" | "entry-missing" | null
```

Diagnostic discriminator when `removed` is `false`.  - `"no-mcp-capability"` — provider does not consume MCP servers - `"no-config-path"` — provider has no config path for the scope - `"file-missing"` — config file does not exist on disk - `"entry-missing"` — config file exists but had no matching entry  Set to `null` when `removed` is `true`.

### `McpCommandBaseOptions`

*Defined in `src/commands/mcp/common.ts:62`*

```typescript
McpCommandBaseOptions
```

Standard option shape accepted by every `caamp mcp <verb>` command.

#### `scope`

*Defined in `src/commands/mcp/common.ts:64`*

```typescript
string | undefined
```

`--scope project|global` (default: project).

#### `projectDir`

*Defined in `src/commands/mcp/common.ts:66`*

```typescript
string | undefined
```

`--project-dir <path>` — override cwd for the `project` scope.

### `McpDetectOptions`

*Defined in `src/commands/mcp/detect.ts:30`*

```typescript
McpDetectOptions
```

Options accepted by `caamp mcp detect`.

#### `onlyExisting`

*Defined in `src/commands/mcp/detect.ts:32`*

```typescript
boolean | undefined
```

Show only providers that actually have a config file on disk.

### `McpInstallOptions`

*Defined in `src/commands/mcp/install.ts:54`*

```typescript
McpInstallOptions
```

Options accepted by `caamp mcp install`.

#### `provider`

*Defined in `src/commands/mcp/install.ts:56`*

```typescript
string | undefined
```

Provider id to install into (required).

#### `from`

*Defined in `src/commands/mcp/install.ts:58`*

```typescript
string | undefined
```

Optional path to a JSON file containing an `McpServerConfig`.

#### `env`

*Defined in `src/commands/mcp/install.ts:60`*

```typescript
string[] | undefined
```

Repeatable `KEY=VALUE` env assignments.

#### `force`

*Defined in `src/commands/mcp/install.ts:62`*

```typescript
boolean | undefined
```

Overwrite an existing entry instead of failing.

### `McpListOptions`

*Defined in `src/commands/mcp/list.ts:41`*

```typescript
McpListOptions
```

Options accepted by `caamp mcp list`.

#### `provider`

*Defined in `src/commands/mcp/list.ts:43`*

```typescript
string | undefined
```

Restrict the listing to a single provider id.

### `McpRemoveOptions`

*Defined in `src/commands/mcp/remove.ts:36`*

```typescript
McpRemoveOptions
```

Options accepted by `caamp mcp remove`.

#### `provider`

*Defined in `src/commands/mcp/remove.ts:38`*

```typescript
string | undefined
```

Provider id to remove from (mutually exclusive with --all-providers).

#### `allProviders`

*Defined in `src/commands/mcp/remove.ts:40`*

```typescript
boolean | undefined
```

Remove from every MCP-capable provider in the registry.

### `GitFetchResult`

*Defined in `src/core/sources/github.ts:18`*

```typescript
GitFetchResult
```

Result of fetching a Git repository to a local temporary directory.

#### `localPath`

*Defined in `src/core/sources/github.ts:20`*

```typescript
string
```

Absolute path to the fetched content on disk.

#### `cleanup`

*Defined in `src/core/sources/github.ts:22`*

```typescript
() => Promise<void>
```

Cleanup function that removes the temporary directory.

### `PiCommandBaseOptions`

*Defined in `src/commands/pi/common.ts:67`*

```typescript
PiCommandBaseOptions
```

Standard option shape accepted by every `caamp pi <verb>` command.

#### `scope`

*Defined in `src/commands/pi/common.ts:69`*

```typescript
string | undefined
```

`--scope project|user|global`.

#### `force`

*Defined in `src/commands/pi/common.ts:71`*

```typescript
boolean | undefined
```

`--force` — overwrite existing targets on install verbs.

#### `projectDir`

*Defined in `src/commands/pi/common.ts:73`*

```typescript
string | undefined
```

`--project-dir <path>` — override cwd for the `project` tier.

### `PiCantListOptions`

*Defined in `src/commands/pi/cant.ts:56`*

```typescript
PiCantListOptions
```

Options accepted by `caamp pi cant list`.

#### `projectDir`

*Defined in `src/commands/pi/cant.ts:58`*

```typescript
string | undefined
```

Project directory used for the `project` tier.

### `PiCantInstallOptions`

*Defined in `src/commands/pi/cant.ts:66`*

```typescript
PiCantInstallOptions
```

Options accepted by `caamp pi cant install`.

#### `name`

*Defined in `src/commands/pi/cant.ts:68`*

```typescript
string | undefined
```

Profile name override. Defaults to the inferred source name.

### `PiExtensionsListOptions`

*Defined in `src/commands/pi/extensions.ts:45`*

```typescript
PiExtensionsListOptions
```

Options accepted by `caamp pi extensions list`.

#### `projectDir`

*Defined in `src/commands/pi/extensions.ts:47`*

```typescript
string | undefined
```

Project directory used for the `project` tier.

### `PiExtensionsInstallOptions`

*Defined in `src/commands/pi/extensions.ts:55`*

```typescript
PiExtensionsInstallOptions
```

Options accepted by `caamp pi extensions install`.

#### `name`

*Defined in `src/commands/pi/extensions.ts:57`*

```typescript
string | undefined
```

Extension name override. Defaults to the inferred source name.

### `PiModelsCommandOptions`

*Defined in `src/commands/pi/models.ts:38`*

```typescript
PiModelsCommandOptions
```

Options accepted by every `caamp pi models` verb.

#### `global`

*Defined in `src/commands/pi/models.ts:40`*

```typescript
boolean | undefined
```

`--global` targets the Pi global state root instead of the project.

### `PiModelsAddOptions`

*Defined in `src/commands/pi/models.ts:48`*

```typescript
PiModelsAddOptions
```

Options accepted by `caamp pi models add`.

#### `displayName`

*Defined in `src/commands/pi/models.ts:50`*

```typescript
string | undefined
```

Human-readable model name.

#### `baseUrl`

*Defined in `src/commands/pi/models.ts:52`*

```typescript
string | undefined
```

Override the provider base URL.

#### `reasoning`

*Defined in `src/commands/pi/models.ts:54`*

```typescript
boolean | undefined
```

Reasoning-capable flag.

#### `contextWindow`

*Defined in `src/commands/pi/models.ts:56`*

```typescript
string | undefined
```

Context window size in tokens.

#### `maxTokens`

*Defined in `src/commands/pi/models.ts:58`*

```typescript
string | undefined
```

Maximum output tokens.

### `PiPromptsInstallOptions`

*Defined in `src/commands/pi/prompts.ts:36`*

```typescript
PiPromptsInstallOptions
```

Options accepted by `caamp pi prompts install`.

#### `name`

*Defined in `src/commands/pi/prompts.ts:38`*

```typescript
string | undefined
```

Override the inferred prompt directory name.

### `PiSessionsListOptions`

*Defined in `src/commands/pi/sessions.ts:33`*

```typescript
PiSessionsListOptions
```

Options accepted by `caamp pi sessions list`.

#### `includeSubagents`

*Defined in `src/commands/pi/sessions.ts:35`*

```typescript
boolean | undefined
```

Include sessions under the `subagents/` subdirectory (default: true).

### `PiSessionsExportOptions`

*Defined in `src/commands/pi/sessions.ts:43`*

```typescript
PiSessionsExportOptions
```

Options accepted by `caamp pi sessions export`.

#### `jsonl`

*Defined in `src/commands/pi/sessions.ts:45`*

```typescript
boolean | undefined
```

Emit raw JSONL. Mutually exclusive with `md`.

#### `md`

*Defined in `src/commands/pi/sessions.ts:47`*

```typescript
boolean | undefined
```

Emit Markdown (filtered to message/custom\_message entries).

#### `output`

*Defined in `src/commands/pi/sessions.ts:49`*

```typescript
string | undefined
```

Write to this file path instead of stdout.

### `PiThemesInstallOptions`

*Defined in `src/commands/pi/themes.ts:37`*

```typescript
PiThemesInstallOptions
```

Options accepted by `caamp pi themes install`.

#### `name`

*Defined in `src/commands/pi/themes.ts:39`*

```typescript
string | undefined
```

Override the inferred theme name.

### `CanonicalEventDefinition`

*Defined in `src/core/hooks/types.ts:40`*

```typescript
CanonicalEventDefinition
```

Definition of a canonical hook event including its category and behavior.

#### `category`

*Defined in `src/core/hooks/types.ts:42`*

```typescript
"agent" | "context" | "memory" | "pipeline" | "prompt" | "session" | "task" | "tool"
```

The lifecycle category this event belongs to (e.g. `"session"`, `"tool"`).

#### `description`

*Defined in `src/core/hooks/types.ts:44`*

```typescript
string
```

Human-readable description of when this event fires.

#### `canBlock`

*Defined in `src/core/hooks/types.ts:46`*

```typescript
boolean
```

Whether a hook handler can block or cancel the associated action.

### `HookMapping`

*Defined in `src/core/hooks/types.ts:87`*

```typescript
HookMapping
```

Mapping of a single canonical event to a provider's native representation.

#### `nativeName`

*Defined in `src/core/hooks/types.ts:89`*

```typescript
string | null
```

The provider-native event name, or `null` if the event has no native equivalent.

#### `supported`

*Defined in `src/core/hooks/types.ts:91`*

```typescript
boolean
```

Whether this canonical event is supported by the provider.

#### `notes`

*Defined in `src/core/hooks/types.ts:97`*

```typescript
string | undefined
```

Optional notes about support limitations or behavioral differences.

### `ProviderHookProfile`

*Defined in `src/core/hooks/types.ts:110`*

```typescript
ProviderHookProfile
```

Complete hook profile for a single provider.

#### `hookSystem`

*Defined in `src/core/hooks/types.ts:112`*

```typescript
HookSystemType
```

The type of hook system the provider uses (`"config"`, `"plugin"`, or `"none"`).

#### `hookConfigPath`

*Defined in `src/core/hooks/types.ts:114`*

```typescript
string | null
```

Filesystem path template to the provider's hook configuration file, or `null`.

#### `hookFormat`

*Defined in `src/core/hooks/types.ts:116`*

```typescript
string | null
```

The configuration format used for hooks (e.g. `"json"`, `"yaml"`), or `null`.

#### `handlerTypes`

*Defined in `src/core/hooks/types.ts:118`*

```typescript
HookHandlerType[]
```

The handler execution mechanisms this provider supports.

#### `experimental`

*Defined in `src/core/hooks/types.ts:120`*

```typescript
boolean
```

Whether the provider's hook system is considered experimental or unstable.

#### `mappings`

*Defined in `src/core/hooks/types.ts:122`*

```typescript
Record<"ConfigChange" | "Notification" | "PermissionRequest" | "PostCompact" | "PostModel" | "PostToolUse" | "PostToolUseFailure" | "PreCompact" | "PreModel" | "PreToolUse" | "PromptSubmit" | "ResponseComplete" | "SessionEnd" | "SessionStart" | "SubagentStart" | "SubagentStop" | "ApprovalExpired" | "ApprovalGranted" | "ApprovalRequested" | "MemoryDecisionStored" | "MemoryLearningStored" | "MemoryObserved" | "MemoryPatternStored" | "PipelineManifestAppended" | "PipelineStageCompleted" | "SessionEnded" | "SessionStarted" | "TaskBlocked" | "TaskCompleted" | "TaskCreated" | "TaskStarted", HookMapping>
```

Mapping of every canonical event to this provider's native representation.

#### `providerOnlyEvents`

*Defined in `src/core/hooks/types.ts:124`*

```typescript
string[]
```

Native event names that exist only in this provider with no canonical equivalent.

### `NormalizedHookEvent`

*Defined in `src/core/hooks/types.ts:140`*

```typescript
NormalizedHookEvent
```

A fully resolved hook event with both canonical and native names.

#### `canonical`

*Defined in `src/core/hooks/types.ts:142`*

```typescript
"ConfigChange" | "Notification" | "PermissionRequest" | "PostCompact" | "PostModel" | "PostToolUse" | "PostToolUseFailure" | "PreCompact" | "PreModel" | "PreToolUse" | "PromptSubmit" | "ResponseComplete" | "SessionEnd" | "SessionStart" | "SubagentStart" | "SubagentStop" | "ApprovalExpired" | "ApprovalGranted" | "ApprovalRequested" | "MemoryDecisionStored" | "MemoryLearningStored" | "MemoryObserved" | "MemoryPatternStored" | "PipelineManifestAppended" | "PipelineStageCompleted" | "SessionEnded" | "SessionStarted" | "TaskBlocked" | "TaskCompleted" | "TaskCreated" | "TaskStarted"
```

The CAAMP canonical event name.

#### `native`

*Defined in `src/core/hooks/types.ts:144`*

```typescript
string
```

The provider-native event name.

#### `providerId`

*Defined in `src/core/hooks/types.ts:146`*

```typescript
string
```

The provider this event was resolved for.

#### `category`

*Defined in `src/core/hooks/types.ts:148`*

```typescript
"agent" | "context" | "memory" | "pipeline" | "prompt" | "session" | "task" | "tool"
```

The lifecycle category of this event.

#### `canBlock`

*Defined in `src/core/hooks/types.ts:150`*

```typescript
boolean
```

Whether a handler for this event can block the associated action.

### `HookSupportResult`

*Defined in `src/core/hooks/types.ts:163`*

```typescript
HookSupportResult
```

Result of querying whether a provider supports a specific canonical event.

#### `canonical`

*Defined in `src/core/hooks/types.ts:165`*

```typescript
"ConfigChange" | "Notification" | "PermissionRequest" | "PostCompact" | "PostModel" | "PostToolUse" | "PostToolUseFailure" | "PreCompact" | "PreModel" | "PreToolUse" | "PromptSubmit" | "ResponseComplete" | "SessionEnd" | "SessionStart" | "SubagentStart" | "SubagentStop" | "ApprovalExpired" | "ApprovalGranted" | "ApprovalRequested" | "MemoryDecisionStored" | "MemoryLearningStored" | "MemoryObserved" | "MemoryPatternStored" | "PipelineManifestAppended" | "PipelineStageCompleted" | "SessionEnded" | "SessionStarted" | "TaskBlocked" | "TaskCompleted" | "TaskCreated" | "TaskStarted"
```

The canonical event that was queried.

#### `supported`

*Defined in `src/core/hooks/types.ts:167`*

```typescript
boolean
```

Whether the provider supports this event.

#### `native`

*Defined in `src/core/hooks/types.ts:169`*

```typescript
string | null
```

The provider-native event name, or `null` if unsupported.

#### `notes`

*Defined in `src/core/hooks/types.ts:175`*

```typescript
string | undefined
```

Optional notes about support caveats.

### `ProviderHookSummary`

*Defined in `src/core/hooks/types.ts:188`*

```typescript
ProviderHookSummary
```

Aggregated hook support summary for a single provider.

#### `providerId`

*Defined in `src/core/hooks/types.ts:190`*

```typescript
string
```

The provider identifier.

#### `hookSystem`

*Defined in `src/core/hooks/types.ts:192`*

```typescript
HookSystemType
```

The type of hook system the provider uses.

#### `experimental`

*Defined in `src/core/hooks/types.ts:194`*

```typescript
boolean
```

Whether the provider's hook system is experimental.

#### `supportedCount`

*Defined in `src/core/hooks/types.ts:196`*

```typescript
number
```

Number of canonical events this provider supports.

#### `totalCanonical`

*Defined in `src/core/hooks/types.ts:198`*

```typescript
number
```

Total number of canonical events in the taxonomy.

#### `supported`

*Defined in `src/core/hooks/types.ts:200`*

```typescript
("ConfigChange" | "Notification" | "PermissionRequest" | "PostCompact" | "PostModel" | "PostToolUse" | "PostToolUseFailure" | "PreCompact" | "PreModel" | "PreToolUse" | "PromptSubmit" | "ResponseComplete" | "SessionEnd" | "SessionStart" | "SubagentStart" | "SubagentStop" | "ApprovalExpired" | "ApprovalGranted" | "ApprovalRequested" | "MemoryDecisionStored" | "MemoryLearningStored" | "MemoryObserved" | "MemoryPatternStored" | "PipelineManifestAppended" | "PipelineStageCompleted" | "SessionEnded" | "SessionStarted" | "TaskBlocked" | "TaskCompleted" | "TaskCreated" | "TaskStarted")[]
```

List of canonical events this provider supports.

#### `unsupported`

*Defined in `src/core/hooks/types.ts:202`*

```typescript
("ConfigChange" | "Notification" | "PermissionRequest" | "PostCompact" | "PostModel" | "PostToolUse" | "PostToolUseFailure" | "PreCompact" | "PreModel" | "PreToolUse" | "PromptSubmit" | "ResponseComplete" | "SessionEnd" | "SessionStart" | "SubagentStart" | "SubagentStop" | "ApprovalExpired" | "ApprovalGranted" | "ApprovalRequested" | "MemoryDecisionStored" | "MemoryLearningStored" | "MemoryObserved" | "MemoryPatternStored" | "PipelineManifestAppended" | "PipelineStageCompleted" | "SessionEnded" | "SessionStarted" | "TaskBlocked" | "TaskCompleted" | "TaskCreated" | "TaskStarted")[]
```

List of canonical events this provider does not support.

#### `providerOnly`

*Defined in `src/core/hooks/types.ts:204`*

```typescript
string[]
```

Native events unique to this provider with no canonical mapping.

#### `coverage`

*Defined in `src/core/hooks/types.ts:206`*

```typescript
number
```

Percentage of canonical events supported (0-100).

### `CrossProviderMatrix`

*Defined in `src/core/hooks/types.ts:219`*

```typescript
CrossProviderMatrix
```

Cross-provider hook support matrix comparing multiple providers.

#### `events`

*Defined in `src/core/hooks/types.ts:221`*

```typescript
("ConfigChange" | "Notification" | "PermissionRequest" | "PostCompact" | "PostModel" | "PostToolUse" | "PostToolUseFailure" | "PreCompact" | "PreModel" | "PreToolUse" | "PromptSubmit" | "ResponseComplete" | "SessionEnd" | "SessionStart" | "SubagentStart" | "SubagentStop" | "ApprovalExpired" | "ApprovalGranted" | "ApprovalRequested" | "MemoryDecisionStored" | "MemoryLearningStored" | "MemoryObserved" | "MemoryPatternStored" | "PipelineManifestAppended" | "PipelineStageCompleted" | "SessionEnded" | "SessionStarted" | "TaskBlocked" | "TaskCompleted" | "TaskCreated" | "TaskStarted")[]
```

The canonical events included in this matrix (rows).

#### `providers`

*Defined in `src/core/hooks/types.ts:223`*

```typescript
string[]
```

The provider IDs included in this matrix (columns).

#### `matrix`

*Defined in `src/core/hooks/types.ts:225`*

```typescript
Record<"ConfigChange" | "Notification" | "PermissionRequest" | "PostCompact" | "PostModel" | "PostToolUse" | "PostToolUseFailure" | "PreCompact" | "PreModel" | "PreToolUse" | "PromptSubmit" | "ResponseComplete" | "SessionEnd" | "SessionStart" | "SubagentStart" | "SubagentStop" | "ApprovalExpired" | "ApprovalGranted" | "ApprovalRequested" | "MemoryDecisionStored" | "MemoryLearningStored" | "MemoryObserved" | "MemoryPatternStored" | "PipelineManifestAppended" | "PipelineStageCompleted" | "SessionEnded" | "SessionStarted" | "TaskBlocked" | "TaskCompleted" | "TaskCreated" | "TaskStarted", Record<string, HookMapping>>
```

Nested record mapping each canonical event to each provider's hook mapping.

### `HookMappingsFile`

*Defined in `src/core/hooks/types.ts:240`*

```typescript
HookMappingsFile
```

Schema for the `providers/hook-mappings.json` data file.

#### `version`

*Defined in `src/core/hooks/types.ts:242`*

```typescript
string
```

Semver version string of the hook mappings schema.

#### `lastUpdated`

*Defined in `src/core/hooks/types.ts:244`*

```typescript
string
```

ISO 8601 date string of the last update to mappings data.

#### `description`

*Defined in `src/core/hooks/types.ts:246`*

```typescript
string
```

Human-readable description of the mappings file purpose.

#### `canonicalEvents`

*Defined in `src/core/hooks/types.ts:248`*

```typescript
Record<"ConfigChange" | "Notification" | "PermissionRequest" | "PostCompact" | "PostModel" | "PostToolUse" | "PostToolUseFailure" | "PreCompact" | "PreModel" | "PreToolUse" | "PromptSubmit" | "ResponseComplete" | "SessionEnd" | "SessionStart" | "SubagentStart" | "SubagentStop" | "ApprovalExpired" | "ApprovalGranted" | "ApprovalRequested" | "MemoryDecisionStored" | "MemoryLearningStored" | "MemoryObserved" | "MemoryPatternStored" | "PipelineManifestAppended" | "PipelineStageCompleted" | "SessionEnded" | "SessionStarted" | "TaskBlocked" | "TaskCompleted" | "TaskCreated" | "TaskStarted", CanonicalEventDefinition>
```

Definitions for every canonical event in the taxonomy.

#### `providerMappings`

*Defined in `src/core/hooks/types.ts:250`*

```typescript
Record<string, ProviderHookProfile>
```

Hook profiles keyed by provider ID.

### `MarketplaceAdapter`

*Defined in `src/core/marketplace/types.ts:10`*

```typescript
MarketplaceAdapter
```

Contract that each marketplace backend adapter must implement.

#### `name`

*Defined in `src/core/marketplace/types.ts:12`*

```typescript
string
```

Human-readable adapter name (e.g. `"agentskills.in"`).

#### `search()`

*Defined in `src/core/marketplace/types.ts:14`*

```typescript
(query: string, limit?: number) => Promise<MarketplaceResult[]>
```

Search the marketplace for skills matching a query.

#### `getSkill()`

*Defined in `src/core/marketplace/types.ts:16`*

```typescript
(scopedName: string) => Promise<MarketplaceResult | null>
```

Retrieve a single skill by its scoped name.

### `MarketplaceResult`

*Defined in `src/core/marketplace/types.ts:34`*

```typescript
MarketplaceResult
```

Normalized marketplace record returned by all adapters.

#### `name`

*Defined in `src/core/marketplace/types.ts:36`*

```typescript
string
```

Short skill name (e.g. `"memory"`).

#### `scopedName`

*Defined in `src/core/marketplace/types.ts:38`*

```typescript
string
```

Scoped name including author prefix (e.g. `"\@anthropic/memory"`).

#### `description`

*Defined in `src/core/marketplace/types.ts:40`*

```typescript
string
```

Short description of what the skill does.

#### `author`

*Defined in `src/core/marketplace/types.ts:42`*

```typescript
string
```

Author or organization name.

#### `stars`

*Defined in `src/core/marketplace/types.ts:44`*

```typescript
number
```

GitHub star count.

#### `githubUrl`

*Defined in `src/core/marketplace/types.ts:46`*

```typescript
string
```

Full GitHub repository URL.

#### `repoFullName`

*Defined in `src/core/marketplace/types.ts:48`*

```typescript
string
```

GitHub `owner/repo` path.

#### `path`

*Defined in `src/core/marketplace/types.ts:50`*

```typescript
string
```

Path within the repository to the skill file.

#### `source`

*Defined in `src/core/marketplace/types.ts:52`*

```typescript
string
```

Name of the marketplace source this result came from.

### `SearchOptions`

*Defined in `src/core/marketplace/types.ts:60`*

```typescript
SearchOptions
```

Options for marketplace search requests.

#### `query`

*Defined in `src/core/marketplace/types.ts:62`*

```typescript
string
```

Free-text search query.

#### `limit`

*Defined in `src/core/marketplace/types.ts:64`*

```typescript
number | undefined
```

Maximum number of results.

#### `offset`

*Defined in `src/core/marketplace/types.ts:66`*

```typescript
number | undefined
```

Pagination offset.

#### `sortBy`

*Defined in `src/core/marketplace/types.ts:68`*

```typescript
"stars" | "recent" | "name" | undefined
```

Sort order for results.

#### `category`

*Defined in `src/core/marketplace/types.ts:70`*

```typescript
string | undefined
```

Filter by skill category.

#### `author`

*Defined in `src/core/marketplace/types.ts:72`*

```typescript
string | undefined
```

Filter by author name.

### `RecommendationValidationIssue`

*Defined in `src/core/skills/recommendation.ts:40`*

```typescript
RecommendationValidationIssue
```

Describes a single validation issue found in recommendation criteria.

#### `code`

*Defined in `src/core/skills/recommendation.ts:42`*

```typescript
RecommendationErrorCode
```

The error code identifying the type of validation failure.

#### `field`

*Defined in `src/core/skills/recommendation.ts:44`*

```typescript
"query" | "mustHave" | "prefer" | "exclude"
```

The criteria field that caused the validation issue.

#### `message`

*Defined in `src/core/skills/recommendation.ts:46`*

```typescript
string
```

A human-readable description of the validation issue.

### `RecommendationValidationResult`

*Defined in `src/core/skills/recommendation.ts:59`*

```typescript
RecommendationValidationResult
```

Result of validating recommendation criteria input.

#### `valid`

*Defined in `src/core/skills/recommendation.ts:61`*

```typescript
boolean
```

Whether the criteria passed all validation checks.

#### `issues`

*Defined in `src/core/skills/recommendation.ts:63`*

```typescript
RecommendationValidationIssue[]
```

List of validation issues found, empty when valid.

### `RecommendationCriteriaInput`

*Defined in `src/core/skills/recommendation.ts:76`*

```typescript
RecommendationCriteriaInput
```

Raw user-provided criteria for skill recommendations.

#### `query`

*Defined in `src/core/skills/recommendation.ts:78`*

```typescript
string | undefined
```

Free-text search query to match against skill metadata.

#### `mustHave`

*Defined in `src/core/skills/recommendation.ts:80`*

```typescript
string | string[] | undefined
```

Terms that a skill must match to be considered relevant.

#### `prefer`

*Defined in `src/core/skills/recommendation.ts:82`*

```typescript
string | string[] | undefined
```

Terms that boost a skill's score when matched.

#### `exclude`

*Defined in `src/core/skills/recommendation.ts:84`*

```typescript
string | string[] | undefined
```

Terms that penalize a skill's score when matched.

### `NormalizedRecommendationCriteria`

*Defined in `src/core/skills/recommendation.ts:97`*

```typescript
NormalizedRecommendationCriteria
```

Normalized and tokenized form of recommendation criteria.

#### `query`

*Defined in `src/core/skills/recommendation.ts:99`*

```typescript
string
```

The lowercased, trimmed query string.

#### `queryTokens`

*Defined in `src/core/skills/recommendation.ts:101`*

```typescript
string[]
```

Individual tokens extracted from the query string.

#### `mustHave`

*Defined in `src/core/skills/recommendation.ts:103`*

```typescript
string[]
```

Sorted, deduplicated list of required match terms.

#### `prefer`

*Defined in `src/core/skills/recommendation.ts:105`*

```typescript
string[]
```

Sorted, deduplicated list of preferred match terms.

#### `exclude`

*Defined in `src/core/skills/recommendation.ts:107`*

```typescript
string[]
```

Sorted, deduplicated list of exclusion terms.

### `RecommendationReason`

*Defined in `src/core/skills/recommendation.ts:144`*

```typescript
RecommendationReason
```

A single reason contributing to a skill's recommendation score.

#### `code`

*Defined in `src/core/skills/recommendation.ts:146`*

```typescript
RecommendationReasonCode
```

The reason code identifying the scoring signal.

#### `detail`

*Defined in `src/core/skills/recommendation.ts:148`*

```typescript
string | undefined
```

Optional detail providing additional context, such as match count.

### `RecommendationScoreBreakdown`

*Defined in `src/core/skills/recommendation.ts:161`*

```typescript
RecommendationScoreBreakdown
```

Detailed breakdown of a skill's recommendation score by category.

#### `mustHave`

*Defined in `src/core/skills/recommendation.ts:163`*

```typescript
number
```

Score contribution from must-have term matches.

#### `prefer`

*Defined in `src/core/skills/recommendation.ts:165`*

```typescript
number
```

Score contribution from preferred term matches.

#### `query`

*Defined in `src/core/skills/recommendation.ts:167`*

```typescript
number
```

Score contribution from query token matches.

#### `stars`

*Defined in `src/core/skills/recommendation.ts:169`*

```typescript
number
```

Score contribution from repository star count signal.

#### `metadata`

*Defined in `src/core/skills/recommendation.ts:171`*

```typescript
number
```

Score contribution from metadata quality and source confidence.

#### `modernity`

*Defined in `src/core/skills/recommendation.ts:173`*

```typescript
number
```

Score contribution from modern vs legacy marker detection.

#### `exclusionPenalty`

*Defined in `src/core/skills/recommendation.ts:175`*

```typescript
number
```

Penalty applied for matching excluded terms.

#### `total`

*Defined in `src/core/skills/recommendation.ts:177`*

```typescript
number
```

The final aggregated recommendation score.

### `RankedSkillRecommendation`

*Defined in `src/core/skills/recommendation.ts:190`*

```typescript
RankedSkillRecommendation
```

A single skill recommendation with its computed score and explanations.

#### `skill`

*Defined in `src/core/skills/recommendation.ts:192`*

```typescript
MarketplaceResult
```

The marketplace skill result being scored.

#### `score`

*Defined in `src/core/skills/recommendation.ts:194`*

```typescript
number
```

The computed recommendation score, higher is better.

#### `reasons`

*Defined in `src/core/skills/recommendation.ts:196`*

```typescript
RecommendationReason[]
```

List of reasons explaining the score contributions.

#### `tradeoffs`

*Defined in `src/core/skills/recommendation.ts:198`*

```typescript
string[]
```

Human-readable tradeoff warnings for the skill.

#### `excluded`

*Defined in `src/core/skills/recommendation.ts:200`*

```typescript
boolean
```

Whether the skill matched one or more exclusion terms.

#### `breakdown`

*Defined in `src/core/skills/recommendation.ts:202`*

```typescript
RecommendationScoreBreakdown | undefined
```

Optional detailed score breakdown by category.

### `RecommendationOptions`

*Defined in `src/core/skills/recommendation.ts:214`*

```typescript
RecommendationOptions
```

Configuration options for the skill recommendation engine.

#### `top`

*Defined in `src/core/skills/recommendation.ts:216`*

```typescript
number | undefined
```

Maximum number of results to return from the ranked list.

#### `includeDetails`

*Defined in `src/core/skills/recommendation.ts:218`*

```typescript
boolean | undefined
```

Whether to include detailed score breakdown in each result.

#### `weights`

*Defined in `src/core/skills/recommendation.ts:220`*

```typescript
Partial<RecommendationWeights> | undefined
```

Partial weight overrides for individual scoring factors.

#### `modernMarkers`

*Defined in `src/core/skills/recommendation.ts:222`*

```typescript
string[] | undefined
```

Custom modern technology marker strings for modernity scoring.

#### `legacyMarkers`

*Defined in `src/core/skills/recommendation.ts:224`*

```typescript
string[] | undefined
```

Custom legacy technology marker strings for modernity scoring.

### `RecommendationWeights`

*Defined in `src/core/skills/recommendation.ts:237`*

```typescript
RecommendationWeights
```

Numeric weights controlling the recommendation scoring algorithm.

#### `mustHaveMatch`

*Defined in `src/core/skills/recommendation.ts:239`*

```typescript
number
```

Weight applied per must-have term match.

#### `preferMatch`

*Defined in `src/core/skills/recommendation.ts:241`*

```typescript
number
```

Weight applied per preferred term match.

#### `queryTokenMatch`

*Defined in `src/core/skills/recommendation.ts:243`*

```typescript
number
```

Weight applied per query token match.

#### `starsFactor`

*Defined in `src/core/skills/recommendation.ts:245`*

```typescript
number
```

Multiplier for the logarithmic star count signal.

#### `metadataBoost`

*Defined in `src/core/skills/recommendation.ts:247`*

```typescript
number
```

Boost for metadata quality and source confidence.

#### `modernMarkerBoost`

*Defined in `src/core/skills/recommendation.ts:249`*

```typescript
number
```

Boost applied per modern technology marker match.

#### `legacyMarkerPenalty`

*Defined in `src/core/skills/recommendation.ts:251`*

```typescript
number
```

Penalty applied per legacy technology marker match.

#### `excludePenalty`

*Defined in `src/core/skills/recommendation.ts:253`*

```typescript
number
```

Penalty applied per excluded term match.

#### `missingMustHavePenalty`

*Defined in `src/core/skills/recommendation.ts:255`*

```typescript
number
```

Penalty applied per missing must-have term.

### `RecommendSkillsResult`

*Defined in `src/core/skills/recommendation.ts:267`*

```typescript
RecommendSkillsResult
```

The complete result of a skill recommendation operation.

#### `criteria`

*Defined in `src/core/skills/recommendation.ts:269`*

```typescript
NormalizedRecommendationCriteria
```

The normalized criteria used for scoring.

#### `ranking`

*Defined in `src/core/skills/recommendation.ts:271`*

```typescript
RankedSkillRecommendation[]
```

Skills ranked by recommendation score, highest first.

### `SearchSkillsOptions`

*Defined in `src/core/skills/recommendation-api.ts:15`*

```typescript
SearchSkillsOptions
```

Options for searching skills via marketplace APIs.

#### `limit`

*Defined in `src/core/skills/recommendation-api.ts:17`*

```typescript
number | undefined
```

Maximum number of results to return.

### `RecommendSkillsQueryOptions`

*Defined in `src/core/skills/recommendation-api.ts:25`*

```typescript
RecommendSkillsQueryOptions
```

Options for the recommendation query combining ranking options with a result limit.

#### `limit`

*Defined in `src/core/skills/recommendation-api.ts:27`*

```typescript
number | undefined
```

Maximum number of results to return.

### `ValidationIssue`

*Defined in `src/core/skills/validator.ts:25`*

```typescript
ValidationIssue
```

A single validation issue found during SKILL.md validation.

**Examples**

```typescript
const issue: ValidationIssue = {
  level: "error",
  field: "name",
  message: "Missing required field: name",
};
```

#### `level`

*Defined in `src/core/skills/validator.ts:27`*

```typescript
"error" | "warning"
```

Severity: `"error"` causes validation failure, `"warning"` does not.

#### `field`

*Defined in `src/core/skills/validator.ts:29`*

```typescript
string
```

The field or section that triggered the issue.

#### `message`

*Defined in `src/core/skills/validator.ts:31`*

```typescript
string
```

Human-readable description of the issue.

### `ValidationResult`

*Defined in `src/core/skills/validator.ts:49`*

```typescript
ValidationResult
```

Result of validating a SKILL.md file against the Agent Skills standard.

**Examples**

```typescript
const result = await validateSkill("/path/to/SKILL.md");
if (!result.valid) {
  for (const issue of result.issues) {
    console.log(`[${issue.level}] ${issue.field}: ${issue.message}`);
  }
}
```

#### `valid`

*Defined in `src/core/skills/validator.ts:51`*

```typescript
boolean
```

Whether the skill passed validation (no error-level issues).

#### `issues`

*Defined in `src/core/skills/validator.ts:53`*

```typescript
ValidationIssue[]
```

All issues found during validation.

#### `metadata`

*Defined in `src/core/skills/validator.ts:55`*

```typescript
Record<string, unknown> | null
```

Parsed frontmatter metadata, or `null` if parsing failed.

### `SpawnOptions`

*Defined in `src/core/registry/spawn-adapter.ts:15`*

```typescript
SpawnOptions
```

Options for spawning a subagent.

#### `prompt`

*Defined in `src/core/registry/spawn-adapter.ts:17`*

```typescript
string
```

The prompt or instruction to give the spawned agent.

#### `model`

*Defined in `src/core/registry/spawn-adapter.ts:19`*

```typescript
string | undefined
```

Model to use for the spawned agent.

#### `tools`

*Defined in `src/core/registry/spawn-adapter.ts:21`*

```typescript
string[] | undefined
```

Tools to make available to the spawned agent.

#### `timeout`

*Defined in `src/core/registry/spawn-adapter.ts:23`*

```typescript
number | undefined
```

Timeout in milliseconds for the spawned agent.

#### `isolate`

*Defined in `src/core/registry/spawn-adapter.ts:25`*

```typescript
boolean | undefined
```

Whether to isolate the spawned agent (e.g. in a worktree).

### `SpawnResult`

*Defined in `src/core/registry/spawn-adapter.ts:33`*

```typescript
SpawnResult
```

Result from a spawn operation.

#### `instanceId`

*Defined in `src/core/registry/spawn-adapter.ts:35`*

```typescript
string
```

Unique identifier for the spawned agent instance.

#### `status`

*Defined in `src/core/registry/spawn-adapter.ts:37`*

```typescript
"running" | "completed" | "failed"
```

Current status of the spawned agent.

#### `output`

*Defined in `src/core/registry/spawn-adapter.ts:39`*

```typescript
string | undefined
```

Output produced by the spawned agent.

### `SpawnAdapter`

*Defined in `src/core/registry/spawn-adapter.ts:50`*

```typescript
SpawnAdapter
```

Provider-neutral interface for spawning and managing subagents.  Concrete implementations will be provider-specific (e.g. ClaudeCodeSpawnAdapter, CodexSpawnAdapter) and registered by CLEO's orchestration layer.

#### `canSpawn()`

*Defined in `src/core/registry/spawn-adapter.ts:52`*

```typescript
(provider: Provider) => boolean
```

Check if a provider supports spawning via this adapter.

#### `spawn()`

*Defined in `src/core/registry/spawn-adapter.ts:54`*

```typescript
(provider: Provider, options: SpawnOptions) => Promise<SpawnResult>
```

Spawn a new subagent for the given provider.

#### `listRunning()`

*Defined in `src/core/registry/spawn-adapter.ts:56`*

```typescript
(provider: Provider) => Promise<SpawnResult[]>
```

List currently running subagent instances.

#### `terminate()`

*Defined in `src/core/registry/spawn-adapter.ts:58`*

```typescript
(provider: Provider, instanceId: string) => Promise<void>
```

Terminate a running subagent instance.

### `SkillIntegrityResult`

*Defined in `src/core/skills/integrity.ts:35`*

```typescript
SkillIntegrityResult
```

Result of checking a single skill's integrity.

#### `name`

*Defined in `src/core/skills/integrity.ts:37`*

```typescript
string
```

Skill name.

#### `status`

*Defined in `src/core/skills/integrity.ts:39`*

```typescript
SkillIntegrityStatus
```

Overall integrity status.

#### `canonicalExists`

*Defined in `src/core/skills/integrity.ts:41`*

```typescript
boolean
```

Whether the canonical directory exists.

#### `canonicalPath`

*Defined in `src/core/skills/integrity.ts:43`*

```typescript
string | null
```

Expected canonical path from lock file.

#### `linkStatuses`

*Defined in `src/core/skills/integrity.ts:45`*

```typescript
{ providerId: string; linkPath: string; exists: boolean; isSymlink: boolean; pointsToCanonical: boolean; }[]
```

Provider link statuses — which agents have valid symlinks.

#### `isCaampOwned`

*Defined in `src/core/skills/integrity.ts:53`*

```typescript
boolean
```

Whether this is a CAAMP-reserved (ct-\*) skill.

#### `issue`

*Defined in `src/core/skills/integrity.ts:55`*

```typescript
string | undefined
```

Human-readable issue description, if any.

### `ResolvedImports`

*Defined in `src/core/instructions/cant-resolver.ts:19`*

```typescript
ResolvedImports
```

Result of resolving `@import` lines in content.

#### `resolvedContent`

*Defined in `src/core/instructions/cant-resolver.ts:21`*

```typescript
string
```

Content with `@import` lines replaced by their resolved markdown.

#### `importedFiles`

*Defined in `src/core/instructions/cant-resolver.ts:23`*

```typescript
string[]
```

Absolute paths of successfully resolved .cant files.

#### `errors`

*Defined in `src/core/instructions/cant-resolver.ts:25`*

```typescript
string[]
```

Error messages for failed resolutions.

### `WellKnownSkill`

*Defined in `src/core/sources/wellknown.ts:14`*

```typescript
WellKnownSkill
```

A skill entry discovered via the RFC 8615 well-known endpoint.

#### `name`

*Defined in `src/core/sources/wellknown.ts:16`*

```typescript
string
```

Skill name.

#### `description`

*Defined in `src/core/sources/wellknown.ts:18`*

```typescript
string
```

Human-readable description of the skill.

#### `url`

*Defined in `src/core/sources/wellknown.ts:20`*

```typescript
string
```

URL where the skill content can be fetched.

## Types

### `ProviderPriority`

*Defined in `src/core/registry/types.ts:57`*

```typescript
ProviderPriority
```

Priority tier identifier stored in registry.json.

### `ProviderStatus`

*Defined in `src/core/registry/types.ts:69`*

```typescript
ProviderStatus
```

Lifecycle status identifier stored in registry.json.

### `McpConfigFormat`

*Defined in `src/core/registry/types.ts:143`*

```typescript
McpConfigFormat
```

Supported MCP config file formats.

### `McpTransportType`

*Defined in `src/core/registry/types.ts:150`*

```typescript
McpTransportType
```

MCP transport protocols a provider may advertise.

### `RegistryHarnessKind`

*Defined in `src/core/registry/types.ts:188`*

```typescript
RegistryHarnessKind
```

Harness role category for a primary or standalone harness.

### `SkillsPrecedence`

*Defined in `src/core/registry/types.ts:235`*

```typescript
SkillsPrecedence
```

How a provider resolves skill file precedence between vendor and agents directories.

### `HookEvent`

> **Deprecated**: Use `CanonicalHookEvent` from `../hooks/types.js` for the normalized CAAMP taxonomy. This type remains for backward compatibility with registry.json's `capabilities.hooks.supported` string arrays.

*Defined in `src/core/registry/types.ts:275`*

```typescript
string
```

Hook lifecycle event identifier from registry.json.

### `RegistryHookFormat`

*Defined in `src/core/registry/types.ts:287`*

```typescript
RegistryHookFormat
```

The on-disk layout of a provider's hook configuration.

### `RegistryHookCatalog`

*Defined in `src/core/registry/types.ts:301`*

```typescript
RegistryHookCatalog
```

Which native event catalog a provider's hook system uses.

### `SpawnMechanism`

*Defined in `src/core/registry/types.ts:359`*

```typescript
SpawnMechanism
```

Mechanism a provider uses to spawn subagents.

### `CtSkillEntry`

> **Deprecated**: Use `SkillLibraryEntry` instead.

*Defined in `src/types.ts:38`*

```typescript
SkillLibraryEntry
```

Backward-compatible alias for `SkillLibraryEntry`.

### `CtValidationResult`

> **Deprecated**: Use `SkillLibraryValidationResult` instead.

*Defined in `src/types.ts:45`*

```typescript
SkillLibraryValidationResult
```

Backward-compatible alias for `SkillLibraryValidationResult`.

### `CtValidationIssue`

> **Deprecated**: Use `SkillLibraryValidationIssue` instead.

*Defined in `src/types.ts:52`*

```typescript
SkillLibraryValidationIssue
```

Backward-compatible alias for `SkillLibraryValidationIssue`.

### `CtProfileDefinition`

> **Deprecated**: Use `SkillLibraryProfile` instead.

*Defined in `src/types.ts:59`*

```typescript
SkillLibraryProfile
```

Backward-compatible alias for `SkillLibraryProfile`.

### `CtDispatchMatrix`

> **Deprecated**: Use `SkillLibraryDispatchMatrix` instead.

*Defined in `src/types.ts:66`*

```typescript
SkillLibraryDispatchMatrix
```

Backward-compatible alias for `SkillLibraryDispatchMatrix`.

### `CtManifest`

> **Deprecated**: Use `SkillLibraryManifest` instead.

*Defined in `src/types.ts:73`*

```typescript
SkillLibraryManifest
```

Backward-compatible alias for `SkillLibraryManifest`.

### `CtManifestSkill`

> **Deprecated**: Use `SkillLibraryManifestSkill` instead.

*Defined in `src/types.ts:80`*

```typescript
SkillLibraryManifestSkill
```

Backward-compatible alias for `SkillLibraryManifestSkill`.

### `ConfigFormat`

*Defined in `src/types.ts:99`*

```typescript
ConfigFormat
```

Supported configuration file formats.  - `"json"` - Standard JSON - `"jsonc"` - JSON with comments (comment-preserving via jsonc-parser) - `"yaml"` - YAML (via js-yaml) - `"toml"` - TOML (via iarna/toml)

**Examples**

```typescript
const format: ConfigFormat = "jsonc";
```

### `TransportType`

*Defined in `src/types.ts:118`*

```typescript
TransportType
```

MCP server transport protocol type.  - `"stdio"` - Standard input/output (local process) - `"sse"` - Server-Sent Events (remote) - `"http"` - HTTP/Streamable HTTP (remote) - `"websocket"` - WebSocket full-duplex (remote)

**Examples**

```typescript
const transport: TransportType = "stdio";
```

### `DetectionMethod`

*Defined in `src/types.ts:132`*

```typescript
DetectionMethod
```

Method used to detect whether an AI agent is installed on the system.  - `"binary"` - Check if a CLI binary exists on PATH - `"directory"` - Check if known config/data directories exist - `"appBundle"` - Check for macOS .app bundle in standard app directories - `"flatpak"` - Check for Flatpak installation on Linux

### `SourceType`

*Defined in `src/types.ts:497`*

```typescript
SourceType
```

Classified type of an MCP server or skill source.  - `"remote"` - HTTP/HTTPS URL to a remote MCP server - `"package"` - npm package name - `"command"` - Shell command string - `"github"` - GitHub repository (URL or shorthand) - `"gitlab"` - GitLab repository URL - `"local"` - Local filesystem path - `"library"` - Built-in skill library reference

### `AuditSeverity`

*Defined in `src/types.ts:848`*

```typescript
AuditSeverity
```

Severity level for a security audit finding.  Ordered from most to least severe: `"critical"`  `"high"`  `"medium"`  `"low"`  `"info"`.

### `InjectionStatus`

*Defined in `src/types.ts:965`*

```typescript
InjectionStatus
```

Status of a CAAMP injection block in an instruction file.  - `"current"` - Injection block exists and matches expected content - `"outdated"` - Injection block exists but content differs - `"missing"` - Instruction file does not exist - `"none"` - File exists but has no CAAMP injection block

### `PathScope`

*Defined in `src/core/paths/standard.ts:16`*

```typescript
PathScope
```

Scope for path resolution, either global (user home) or project-local.

### `HarnessTier`

*Defined in `src/core/harness/scope.ts:46`*

```typescript
HarnessTier
```

Three-tier scope identifier for Pi harness operations.

### `HarnessAssetKind`

*Defined in `src/core/harness/scope.ts:64`*

```typescript
HarnessAssetKind
```

Asset kinds managed by the three-tier scope model.

### `HarnessScope`

*Defined in `src/core/harness/types.ts:38`*

```typescript
HarnessScope
```

Scope at which a harness operation should be performed.

### `ExclusivityMode`

*Defined in `src/core/harness/types.ts:106`*

```typescript
ExclusivityMode
```

Controls how `resolveDefaultTargetProviders` (defined in `./index.ts`) selects target providers at runtime invocation time.

### `MVILevel`

*Defined in `src/core/lafs.ts:22`*

```typescript
MVILevel
```

LAFS MVI disclosure level - defined locally to avoid CI module resolution issues with re-exported types.

### `LAFSErrorShape`

*Defined in `src/core/lafs.ts:32`*

```typescript
LAFSError
```

LAFS Error structure - re-exported from protocol as LAFSErrorShape for CAAMP compatibility.

### `LAFSWarning`

*Defined in `src/core/lafs.ts:39`*

```typescript
Warning
```

LAFS Warning structure - re-exported from protocol.

### `McpScope`

*Defined in `src/core/mcp/reader.ts:47`*

```typescript
McpScope
```

Scope identifier for MCP config file resolution.

### `McpServerEntriesByProvider`

*Defined in `src/core/mcp/reader.ts:203`*

```typescript
McpServerEntriesByProvider
```

Map of provider id → MCP server entries for that provider.

### `PiCantRemoveOptions`

*Defined in `src/commands/pi/cant.ts:76`*

```typescript
PiCommandBaseOptions
```

Options accepted by `caamp pi cant remove`.

### `PiExtensionsRemoveOptions`

*Defined in `src/commands/pi/extensions.ts:65`*

```typescript
PiCommandBaseOptions
```

Options accepted by `caamp pi extensions remove`.

### `PiPromptsListOptions`

*Defined in `src/commands/pi/prompts.ts:46`*

```typescript
PiCommandBaseOptions
```

Options accepted by `caamp pi prompts list`.

### `PiPromptsRemoveOptions`

*Defined in `src/commands/pi/prompts.ts:53`*

```typescript
PiCommandBaseOptions
```

Options accepted by `caamp pi prompts remove`.

### `PiThemesListOptions`

*Defined in `src/commands/pi/themes.ts:47`*

```typescript
PiCommandBaseOptions
```

Options accepted by `caamp pi themes list`.

### `PiThemesRemoveOptions`

*Defined in `src/commands/pi/themes.ts:54`*

```typescript
PiCommandBaseOptions
```

Options accepted by `caamp pi themes remove`.

### `HookCategory`

*Defined in `src/core/hooks/generated.ts:20`*

```typescript
"agent" | "context" | "memory" | "pipeline" | "prompt" | "session" | "task" | "tool"
```

Union type of valid hook category strings.

### `EventSource`

*Defined in `src/core/hooks/generated.ts:29`*

```typescript
"domain" | "provider"
```

Union type of valid event source types.

### `CanonicalHookEvent`

*Defined in `src/core/hooks/generated.ts:75`*

```typescript
"ConfigChange" | "Notification" | "PermissionRequest" | "PostCompact" | "PostModel" | "PostToolUse" | "PostToolUseFailure" | "PreCompact" | "PreModel" | "PreToolUse" | "PromptSubmit" | "ResponseComplete" | "SessionEnd" | "SessionStart" | "SubagentStart" | "SubagentStop" | "ApprovalExpired" | "ApprovalGranted" | "ApprovalRequested" | "MemoryDecisionStored" | "MemoryLearningStored" | "MemoryObserved" | "MemoryPatternStored" | "PipelineManifestAppended" | "PipelineStageCompleted" | "SessionEnded" | "SessionStarted" | "TaskBlocked" | "TaskCompleted" | "TaskCreated" | "TaskStarted"
```

Union type of all canonical hook event names.

### `HookSystemType`

*Defined in `src/core/hooks/types.ts:61`*

```typescript
HookSystemType
```

The type of hook system a provider uses.

### `HookHandlerType`

*Defined in `src/core/hooks/types.ts:75`*

```typescript
HookHandlerType
```

The mechanism a provider uses to execute hook handlers.

### `RecommendationErrorCode`

*Defined in `src/core/skills/recommendation.ts:28`*

```typescript
RecommendationErrorCode
```

Union type of all recommendation error code string literals.

### `RecommendationReasonCode`

*Defined in `src/core/skills/recommendation.ts:120`*

```typescript
RecommendationReasonCode
```

String literal union of all reason codes emitted during skill scoring.

### `SkillIntegrityStatus`

*Defined in `src/core/skills/integrity.ts:22`*

```typescript
SkillIntegrityStatus
```

Status of a single skill's integrity check.

## Variables

### `AGENTS_HOME`

*Defined in `src/core/paths/agents.ts:14`*

```typescript
string
```

Global `.agents/` home directory (`~/.agents/` or `$AGENTS_HOME`).

### `LOCK_FILE_PATH`

*Defined in `src/core/paths/agents.ts:20`*

```typescript
string
```

CAAMP lock file path (`~/.agents/.caamp-lock.json`).

### `CANONICAL_SKILLS_DIR`

*Defined in `src/core/paths/agents.ts:26`*

```typescript
string
```

Canonical skills directory (`~/.agents/skills/`).

### `AGENTS_MCP_DIR`

*Defined in `src/core/paths/agents.ts:32`*

```typescript
string
```

Global MCP directory (`~/.agents/mcp/`).

### `AGENTS_MCP_SERVERS_PATH`

*Defined in `src/core/paths/agents.ts:38`*

```typescript
string
```

Global MCP servers.json path (`~/.agents/mcp/servers.json`).

### `AGENTS_CONFIG_PATH`

*Defined in `src/core/paths/agents.ts:44`*

```typescript
string
```

Global agents config.toml path (`~/.agents/config.toml`).

### `TIER_PRECEDENCE`

*Defined in `src/core/harness/scope.ts:76`*

```typescript
readonly HarnessTier[]
```

Precedence-ordered iteration of tiers for read operations.

### `DEFAULT_EXCLUSIVITY_MODE`

*Defined in `src/core/config/caamp-config.ts:40`*

```typescript
ExclusivityMode
```

Default exclusivity mode used when no override is configured.

### `EXCLUSIVITY_MODE_ENV_VAR`

*Defined in `src/core/config/caamp-config.ts:53`*

```typescript
"CAAMP_EXCLUSIVITY_MODE"
```

Environment variable name read by `getExclusivityMode` when no programmatic override is active.

### `ErrorCategories`

*Defined in `src/core/lafs.ts:365`*

```typescript
{ readonly VALIDATION: LAFSErrorCategory; readonly AUTH: LAFSErrorCategory; readonly PERMISSION: LAFSErrorCategory; readonly NOT_FOUND: LAFSErrorCategory; readonly CONFLICT: LAFSErrorCategory; readonly RATE_LIMIT: LAFSErrorCategory; readonly TRANSIENT: LAFSErrorCategory; readonly INTERNAL: LAFSErrorCategory; readonly CONTRACT: LAFSErrorCategory; readonly MIGRATION: LAFSErrorCategory; }
```

Common error categories mapping for convenience.

### `ErrorCodes`

*Defined in `src/core/lafs.ts:383`*

```typescript
{ readonly FORMAT_CONFLICT: "E_FORMAT_CONFLICT"; readonly INVALID_JSON: "E_INVALID_JSON"; readonly SKILL_NOT_FOUND: "E_SKILL_NOT_FOUND"; readonly PROVIDER_NOT_FOUND: "E_PROVIDER_NOT_FOUND"; readonly MCP_SERVER_NOT_FOUND: "E_MCP_SERVER_NOT_FOUND"; readonly FILE_NOT_FOUND: "E_FILE_NOT_FOUND"; readonly INVALID_INPUT: "E_INVALID_INPUT"; readonly INVALID_CONSTRAINT: "E_INVALID_CONSTRAINT"; readonly INVALID_FORMAT: "E_INVALID_FORMAT"; readonly INSTALL_FAILED: "E_INSTALL_FAILED"; readonly REMOVE_FAILED: "E_REMOVE_FAILED"; readonly UPDATE_FAILED: "E_UPDATE_FAILED"; readonly VALIDATION_FAILED: "E_VALIDATION_FAILED"; readonly AUDIT_FAILED: "E_AUDIT_FAILED"; readonly NETWORK_ERROR: "E_NETWORK_ERROR"; readonly FILE_SYSTEM_ERROR: "E_FILE_SYSTEM_ERROR"; readonly PERMISSION_DENIED: "E_PERMISSION_DENIED"; readonly INTERNAL_ERROR: "E_INTERNAL_ERROR"; }
```

Common error codes for consistency.

### `MCP_ERROR_CODES`

*Defined in `src/commands/mcp/common.ts:46`*

```typescript
{ readonly VALIDATION: "E_VALIDATION_SCHEMA"; readonly NOT_FOUND: "E_NOT_FOUND_RESOURCE"; readonly CONFLICT: "E_CONFLICT_VERSION"; readonly TRANSIENT: "E_TRANSIENT_UPSTREAM"; }
```

Canonical LAFS error codes used by the `caamp mcp` command group.

### `DEFAULT_FETCH_TIMEOUT_MS`

*Defined in `src/core/network/fetch.ts:6`*

```typescript
10000
```

Default timeout in milliseconds for outbound HTTP requests.

### `PI_ERROR_CODES`

*Defined in `src/commands/pi/common.ts:51`*

```typescript
{ readonly VALIDATION: "E_VALIDATION_SCHEMA"; readonly NOT_FOUND: "E_NOT_FOUND_RESOURCE"; readonly CONFLICT: "E_CONFLICT_VERSION"; readonly TRANSIENT: "E_TRANSIENT_UPSTREAM"; }
```

Canonical LAFS error codes used by the `caamp pi` command group.

### `HOOK_CATEGORIES`

*Defined in `src/core/hooks/generated.ts:8`*

```typescript
readonly ["agent", "context", "memory", "pipeline", "prompt", "session", "task", "tool"]
```

All hook event categories (8 total).

### `EVENT_SOURCES`

*Defined in `src/core/hooks/generated.ts:26`*

```typescript
readonly ["domain", "provider"]
```

Event source types: domain, provider.

### `CANONICAL_HOOK_EVENTS`

*Defined in `src/core/hooks/generated.ts:38`*

```typescript
readonly ["ConfigChange", "Notification", "PermissionRequest", "PostCompact", "PostModel", "PostToolUse", "PostToolUseFailure", "PreCompact", "PreModel", "PreToolUse", "PromptSubmit", "ResponseComplete", "SessionEnd", "SessionStart", "SubagentStart", "SubagentStop", "ApprovalExpired", "ApprovalGranted", "ApprovalRequested", "MemoryDecisionStored", "MemoryLearningStored", "MemoryObserved", "MemoryPatternStored", "PipelineManifestAppended", "PipelineStageCompleted", "SessionEnded", "SessionStarted", "TaskBlocked", "TaskCompleted", "TaskCreated", "TaskStarted"]
```

All canonical hook events (31 total: 16 provider, 15 domain).  This replaces the previously hardcoded tuple in types.ts. Single source of truth: providers/hook-mappings.json

### `PROVIDER_HOOK_EVENTS`

*Defined in `src/core/hooks/generated.ts:78`*

```typescript
readonly ["ConfigChange", "Notification", "PermissionRequest", "PostCompact", "PostModel", "PostToolUse", "PostToolUseFailure", "PreCompact", "PreModel", "PreToolUse", "PromptSubmit", "ResponseComplete", "SessionEnd", "SessionStart", "SubagentStart", "SubagentStop"]
```

Provider-sourced events only (original 16 CAAMP events).

### `DOMAIN_HOOK_EVENTS`

*Defined in `src/core/hooks/generated.ts:98`*

```typescript
readonly ["ApprovalExpired", "ApprovalGranted", "ApprovalRequested", "MemoryDecisionStored", "MemoryLearningStored", "MemoryObserved", "MemoryPatternStored", "PipelineManifestAppended", "PipelineStageCompleted", "SessionEnded", "SessionStarted", "TaskBlocked", "TaskCompleted", "TaskCreated", "TaskStarted"]
```

Domain-sourced events only (CLEO business events).

### `EVENT_METADATA`

*Defined in `src/core/hooks/generated.ts:117`*

```typescript
Record<"ConfigChange" | "Notification" | "PermissionRequest" | "PostCompact" | "PostModel" | "PostToolUse" | "PostToolUseFailure" | "PreCompact" | "PreModel" | "PreToolUse" | "PromptSubmit" | "ResponseComplete" | "SessionEnd" | "SessionStart" | "SubagentStart" | "SubagentStop" | "ApprovalExpired" | "ApprovalGranted" | "ApprovalRequested" | "MemoryDecisionStored" | "MemoryLearningStored" | "MemoryObserved" | "MemoryPatternStored" | "PipelineManifestAppended" | "PipelineStageCompleted" | "SessionEnded" | "SessionStarted" | "TaskBlocked" | "TaskCompleted" | "TaskCreated" | "TaskStarted", { category: HookCategory; source: EventSource; canBlock: boolean; description: string; }>
```

Metadata for each canonical event, derived from hook-mappings.json.

### `AUDIT_RULES`

*Defined in `src/core/skills/audit/rules.ts:31`*

```typescript
AuditRule[]
```

Complete set of security audit rules for SKILL.md scanning.

### `RECOMMENDATION_ERROR_CODES`

*Defined in `src/core/skills/recommendation.ts:12`*

```typescript
{ readonly QUERY_INVALID: "E_SKILLS_QUERY_INVALID"; readonly NO_MATCHES: "E_SKILLS_NO_MATCHES"; readonly SOURCE_UNAVAILABLE: "E_SKILLS_SOURCE_UNAVAILABLE"; readonly CRITERIA_CONFLICT: "E_SKILLS_CRITERIA_CONFLICT"; }
```

Error codes used in skill recommendation validation.

### `rankSkills`

*Defined in `src/core/skills/recommendation.ts:703`*

```typescript
(skills: MarketplaceResult[], criteriaInput: RecommendationCriteriaInput, options?: RecommendationOptions) => RecommendSkillsResult
```

Alias for `recommendSkills` providing a shorter function name.
