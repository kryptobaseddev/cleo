---
id: t12245-truthful-knowledge
tasks: [T12245]
kind: fix
summary: Report knowledge coverage separately from impact and resolve ambiguous symbols explicitly
---

Absent, stale, partial, or failed graph coverage now yields UNKNOWN impact. NONE is reserved for an assessed graph with no detected impact; static analysis cannot prove every runtime caller. Context, impact, task footprints, and why use shared project-scoped coverage and qualified symbol resolution. Ambiguous names return candidates rather than choosing a checkout.

Evidence readers preserve file-level precision, current canonical memory authority, and sourced decision relations. Historical records remain intact. These helper prerequisites land with the truthful readers so each intermediate commit builds independently; retrieval policy and index publication follow in subsequent changesets.

The exact paths export and its build entry preserve runtime deep-import resolution. Existing CLI imports use that supported export. Architecture scanners exclude generated Studio output without changing their baselines.

Code placed in packages/contracts/ and packages/core/ per Package-Boundary Check — verified against AGENTS.md. CLI changes remain dispatch and rendering; Nexus retains structural impact analysis.
