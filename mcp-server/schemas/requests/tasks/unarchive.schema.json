{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "tasks.unarchive request",
  "description": "Restore from archive",
  "type": "object",
  "properties": {
    "taskId": {
      "type": "string",
      "pattern": "^T[0-9]+$",
      "description": "Task ID to restore"
    }
  },
  "required": ["taskId"],
  "additionalProperties": false
}
