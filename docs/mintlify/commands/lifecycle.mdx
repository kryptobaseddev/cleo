---
title: "cleo lifecycle"
description: "Manage RCSD-IVTR lifecycle pipeline stages and gates"
icon: "terminal"
---

{/* AUTO-GENERATED by generate-command-docs.sh - DO NOT EDIT MANUALLY */}

# cleo lifecycle

Manage RCSD-IVTR lifecycle pipeline stages and gates

<Tip>Manages RCSD-IVTR lifecycle gate enforcement for epic progression</Tip>

<Info>**Agent Relevance**: High - Frequently used by LLM agents</Info>

## Synopsis

```bash
cleo lifecycle <stages|status|validate|report|export|record|enforce|skip|unskip|import> [OPTIONS]
```

## Subcommands

| Subcommand | Description |
|------------|-------------|
| `stages` | Run `cleo lifecycle stages --help` for details |
| `status` | Run `cleo lifecycle status --help` for details |
| `validate` | Run `cleo lifecycle validate --help` for details |
| `report` | Run `cleo lifecycle report --help` for details |
| `export` | Run `cleo lifecycle export --help` for details |
| `record` | Run `cleo lifecycle record --help` for details |
| `enforce` | Run `cleo lifecycle enforce --help` for details |
| `skip` | Run `cleo lifecycle skip --help` for details |
| `unskip` | Run `cleo lifecycle unskip --help` for details |
| `import` | Run `cleo lifecycle import --help` for details |

## Options

| Option | Description |
|--------|-------------|
| `--format` | Output format: `text`, `json` |
| `--json` | Force JSON output |
| `--human` | Force human-readable output |
| `--pipeline` | See `cleo lifecycle --help` for details |
| `--reason` | Reason for operation (often required) |
| `--notes` | Add notes to task |
| `--strict` | See `cleo lifecycle --help` for details |
| `--overwrite` | See `cleo lifecycle --help` for details |
| `--history` | Include history |

<Note>This command supports JSON output via `--json` flag for LLM agent integration.</Note>

## Examples

<CodeGroup>

```bash Basic Usage
cleo lifecycle
```

```bash JSON Output
cleo lifecycle --json
```

</CodeGroup>

## Exit Codes

| Code | Meaning | Recoverable |
|------|---------|:-----------:|
| `0` | Success | No |
| `4` | Resource not found | No |
| `80` | Unknown | No |
| `83` | Unknown | No |

## See Also

- [Commands Index](/commands/commands) - Full command reference
