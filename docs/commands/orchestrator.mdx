---
title: "cleo orchestrator"
description: "Multi-agent coordination for parallel task execution with dependency analysis"
icon: "eye"
---

{/* AUTO-GENERATED by generate-command-docs.sh - DO NOT EDIT MANUALLY */}

# cleo orchestrator

Multi-agent coordination for parallel task execution with dependency analysis

<Tip>Part of Orchestrator Protocol - enables LLM agents to coordinate parallel work via subagents</Tip>

<Info>**Agent Relevance**: Critical - Essential for LLM agent workflows</Info>

## Synopsis

```bash
cleo orchestrator <start|status|next|ready|context|spawn|analyze|parallel|check|validate|skill> [OPTIONS]
```

## Subcommands

| Subcommand | Description |
|------------|-------------|
| `start` | Run `cleo orchestrator start --help` for details |
| `status` | Run `cleo orchestrator status --help` for details |
| `next` | Run `cleo orchestrator next --help` for details |
| `ready` | Run `cleo orchestrator ready --help` for details |
| `context` | Run `cleo orchestrator context --help` for details |
| `spawn` | Run `cleo orchestrator spawn --help` for details |
| `analyze` | Run `cleo orchestrator analyze --help` for details |
| `parallel` | Run `cleo orchestrator parallel --help` for details |
| `check` | Run `cleo orchestrator check --help` for details |
| `validate` | Run `cleo orchestrator validate --help` for details |
| `skill` | Run `cleo orchestrator skill --help` for details |

## Options

| Option | Description |
|--------|-------------|
| `--epic` | See `cleo orchestrator --help` for details |
| `--tokens` | See `cleo orchestrator --help` for details |
| `--template` | See `cleo orchestrator --help` for details |
| `--subagent` | See `cleo orchestrator --help` for details |
| `--manifest` | See `cleo orchestrator --help` for details |
| `--orchestrator` | See `cleo orchestrator --help` for details |
| `--install` | See `cleo orchestrator --help` for details |
| `--verify` | See `cleo orchestrator --help` for details |

<Note>This command supports JSON output via `--json` flag for LLM agent integration.</Note>

## Examples

<CodeGroup>

```bash Basic Usage
cleo orchestrator
```

```bash JSON Output
cleo orchestrator --json
```

</CodeGroup>

## Exit Codes

| Code | Meaning | Recoverable |
|------|---------|:-----------:|
| `0` | Success | No |
| `2` | Invalid arguments | No |
| `3` | File operation failure | No |
| `4` | Resource not found | No |
| `52` | Context critical (90-94%) | No |

## See Also

- [Commands Index](/commands/commands) - Full command reference
- [list](/commands/list) - View tasks
- [show](/commands/show) - Task details
- [find](/commands/find) - Search tasks
