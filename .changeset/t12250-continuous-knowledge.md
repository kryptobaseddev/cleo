---
id: t12250-continuous-knowledge
tasks: [T12250]
kind: fix
summary: Keep current authority visible during bounded orientation and verify task evidence across explicit source roots
---

Default briefing and focus responses retain current authority and coverage without allowing hundreds of stale-file reasons or long evidence excerpts to monopolize the output budget. Compact summaries preserve status, project/revision provenance, total counts, repair states, and a supported detailed follow-up. Current guidance and sourced corrections are separate from immutable historical handoffs.

Task evidence resolves paths and commits through explicitly included nested repositories. Unique matches become qualified evidence; ambiguous or unsupported references remain findings. Task-symbol output preserves coverage instead of presenting an empty result as assessed knowledge.

Diagnostics count the full population of dangling graph relationships across both endpoints, separately from bounded display samples. Structural repair proposals backfill reproducible derived nodes from canonical records without deleting substantive historical edges.

The same contract accompanies task start, evidence recording, completion, handoff, and the existing foreground CleoOS agent. Source-guided repair does not require a background model. Sanitized audit scenarios and real-store behavior regressions preserve the original external audit unchanged.

Code placed in packages/core/ for shared knowledge and workflow services, packages/contracts/ for shared types, packages/cleo/ for thin dispatch, and packages/cleo-os/ for harness behavior per Package-Boundary Check — verified against AGENTS.md.
