# API Reference

Generated by [forge-ts](https://github.com/kryptobaseddev/forge-ts) from `/mnt/projects/cleocode/packages/lafs`.

## Table of Contents

- [Functions](#functions)
  - [`estimateTokens()`](#estimatetokens)
  - [`estimateTokensJSON()`](#estimatetokensjson)
  - [`isMVILevel()`](#ismvilevel)
  - [`isAgentAction()`](#isagentaction)
  - [`applyBudgetEnforcement()`](#applybudgetenforcement)
  - [`withBudget()`](#withbudget)
  - [`checkBudget()`](#checkbudget)
  - [`withBudgetSync()`](#withbudgetsync)
  - [`wrapWithBudget()`](#wrapwithbudget)
  - [`composeMiddleware()`](#composemiddleware)
  - [`getConformanceProfiles()`](#getconformanceprofiles)
  - [`getChecksForTier()`](#getchecksfortier)
  - [`validateConformanceProfiles()`](#validateconformanceprofiles)
  - [`getErrorRegistry()`](#geterrorregistry)
  - [`isRegisteredErrorCode()`](#isregisterederrorcode)
  - [`getRegistryCode()`](#getregistrycode)
  - [`getAgentAction()`](#getagentaction)
  - [`getTypeUri()`](#gettypeuri)
  - [`getDocUrl()`](#getdocurl)
  - [`getTransportMapping()`](#gettransportmapping)
  - [`resolveOutputFormat()`](#resolveoutputformat)
  - [`validateEnvelope()`](#validateenvelope)
  - [`assertEnvelope()`](#assertenvelope)
  - [`runEnvelopeConformance()`](#runenvelopeconformance)
  - [`runFlagConformance()`](#runflagconformance)
  - [`enforceCompliance()`](#enforcecompliance)
  - [`assertCompliance()`](#assertcompliance)
  - [`withCompliance()`](#withcompliance)
  - [`createComplianceMiddleware()`](#createcompliancemiddleware)
  - [`getDeprecationRegistry()`](#getdeprecationregistry)
  - [`detectDeprecatedEnvelopeFields()`](#detectdeprecatedenvelopefields)
  - [`emitDeprecationWarnings()`](#emitdeprecationwarnings)
  - [`parseExtensionsHeader()`](#parseextensionsheader)
  - [`negotiateExtensions()`](#negotiateextensions)
  - [`formatExtensionsHeader()`](#formatextensionsheader)
  - [`buildLafsExtension()`](#buildlafsextension)
  - [`buildExtension()`](#buildextension)
  - [`isValidExtensionKind()`](#isvalidextensionkind)
  - [`validateExtensionDeclaration()`](#validateextensiondeclaration)
  - [`extensionNegotiationMiddleware()`](#extensionnegotiationmiddleware)
  - [`discoveryMiddleware()`](#discoverymiddleware)
  - [`discoveryFastifyPlugin()`](#discoveryfastifyplugin)
  - [`createEnvelope()`](#createenvelope)
  - [`parseLafsResponse()`](#parselafsresponse)
  - [`resolveFieldExtraction()`](#resolvefieldextraction)
  - [`extractFieldFromResult()`](#extractfieldfromresult)
  - [`extractFieldFromEnvelope()`](#extractfieldfromenvelope)
  - [`applyFieldFilter()`](#applyfieldfilter)
  - [`resolveFlags()`](#resolveflags)
  - [`createLafsArtifact()`](#createlafsartifact)
  - [`createTextArtifact()`](#createtextartifact)
  - [`createFileArtifact()`](#createfileartifact)
  - [`isExtensionRequired()`](#isextensionrequired)
  - [`getExtensionParams()`](#getextensionparams)
  - [`isValidTransition()`](#isvalidtransition)
  - [`isTerminalState()`](#isterminalstate)
  - [`isInterruptedState()`](#isinterruptedstate)
  - [`attachLafsEnvelope()`](#attachlafsenvelope)
  - [`streamTaskEvents()`](#streamtaskevents)
  - [`createJsonRpcRequest()`](#createjsonrpcrequest)
  - [`createJsonRpcResponse()`](#createjsonrpcresponse)
  - [`createJsonRpcErrorResponse()`](#createjsonrpcerrorresponse)
  - [`createA2AErrorResponse()`](#createa2aerrorresponse)
  - [`validateJsonRpcRequest()`](#validatejsonrpcrequest)
  - [`isA2AError()`](#isa2aerror)
  - [`createGrpcStatus()`](#creategrpcstatus)
  - [`createProblemDetails()`](#createproblemdetails)
  - [`createLafsProblemDetails()`](#createlafsproblemdetails)
  - [`buildUrl()`](#buildurl)
  - [`parseListTasksQuery()`](#parselisttasksquery)
  - [`getErrorCodeMapping()`](#geterrorcodemapping)
  - [`parseA2AVersionHeader()`](#parsea2aversionheader)
  - [`negotiateA2AVersion()`](#negotiatea2aversion)
  - [`circuitBreakerMiddleware()`](#circuitbreakermiddleware)
  - [`healthCheck()`](#healthcheck)
  - [`createDatabaseHealthCheck()`](#createdatabasehealthcheck)
  - [`createExternalServiceHealthCheck()`](#createexternalservicehealthcheck)
  - [`livenessProbe()`](#livenessprobe)
  - [`readinessProbe()`](#readinessprobe)
  - [`wrapMCPResult()`](#wrapmcpresult)
  - [`createAdapterErrorEnvelope()`](#createadaptererrorenvelope)
  - [`isTextContent()`](#istextcontent)
  - [`parseMCPTextContent()`](#parsemcptextcontent)
  - [`projectEnvelope()`](#projectenvelope)
  - [`estimateProjectedTokens()`](#estimateprojectedtokens)
  - [`lafsErrorToProblemDetails()`](#lafserrortoproblemdetails)
  - [`gracefulShutdown()`](#gracefulshutdown)
  - [`isShuttingDown()`](#isshuttingdown)
  - [`getShutdownState()`](#getshutdownstate)
  - [`forceShutdown()`](#forceshutdown)
  - [`shutdownMiddleware()`](#shutdownmiddleware)
  - [`waitForShutdown()`](#waitforshutdown)
- [Classes](#classes)
  - [`TokenEstimator`](#tokenestimator)
  - [`LAFSFlagError`](#lafsflagerror)
  - [`ComplianceError`](#complianceerror)
  - [`ExtensionSupportRequiredError`](#extensionsupportrequirederror)
  - [`LafsError`](#lafserror)
  - [`LafsA2AResult`](#lafsa2aresult)
  - [`InvalidStateTransitionError`](#invalidstatetransitionerror)
  - [`TaskImmutabilityError`](#taskimmutabilityerror)
  - [`TaskNotFoundError`](#tasknotfounderror)
  - [`TaskRefinementError`](#taskrefinementerror)
  - [`TaskManager`](#taskmanager)
  - [`TaskEventBus`](#taskeventbus)
  - [`PushNotificationConfigStore`](#pushnotificationconfigstore)
  - [`PushNotificationDispatcher`](#pushnotificationdispatcher)
  - [`TaskArtifactAssembler`](#taskartifactassembler)
  - [`CircuitBreakerError`](#circuitbreakererror)
  - [`CircuitBreaker`](#circuitbreaker)
  - [`CircuitBreakerRegistry`](#circuitbreakerregistry)
- [Interfaces](#interfaces)
  - [`TokenEstimatorOptions`](#tokenestimatoroptions)
  - [`Warning`](#warning)
  - [`LAFSMeta`](#lafsmeta)
  - [`LAFSError`](#lafserror)
  - [`LAFSPageCursor`](#lafspagecursor)
  - [`LAFSPageOffset`](#lafspageoffset)
  - [`LAFSPageNone`](#lafspagenone)
  - [`ContextLedgerEntry`](#contextledgerentry)
  - [`ContextLedger`](#contextledger)
  - [`LAFSEnvelope`](#lafsenvelope)
  - [`FlagInput`](#flaginput)
  - [`ConformanceReport`](#conformancereport)
  - [`TokenEstimate`](#tokenestimate)
  - [`LAFSMetaWithBudget`](#lafsmetawithbudget)
  - [`LAFSEnvelopeWithBudget`](#lafsenvelopewithbudget)
  - [`BudgetEnforcementResult`](#budgetenforcementresult)
  - [`ConformanceProfiles`](#conformanceprofiles)
  - [`RegistryCode`](#registrycode)
  - [`ErrorRegistry`](#errorregistry)
  - [`FlagResolution`](#flagresolution)
  - [`StructuredValidationError`](#structuredvalidationerror)
  - [`EnvelopeValidationResult`](#envelopevalidationresult)
  - [`EnvelopeConformanceOptions`](#envelopeconformanceoptions)
  - [`ComplianceIssue`](#complianceissue)
  - [`EnforceComplianceOptions`](#enforcecomplianceoptions)
  - [`ComplianceResult`](#complianceresult)
  - [`DeprecationEntry`](#deprecationentry)
  - [`LafsExtensionParams`](#lafsextensionparams)
  - [`ExtensionNegotiationResult`](#extensionnegotiationresult)
  - [`BuildLafsExtensionOptions`](#buildlafsextensionoptions)
  - [`BuildExtensionOptions`](#buildextensionoptions)
  - [`ExtensionNegotiationMiddlewareOptions`](#extensionnegotiationmiddlewareoptions)
  - [`AgentProvider`](#agentprovider)
  - [`AgentCapabilities`](#agentcapabilities)
  - [`AgentExtension`](#agentextension)
  - [`AgentSkill`](#agentskill)
  - [`SecurityScheme`](#securityscheme)
  - [`AgentCard`](#agentcard)
  - [`Capability`](#capability)
  - [`ServiceConfig`](#serviceconfig)
  - [`EndpointConfig`](#endpointconfig)
  - [`DiscoveryDocument`](#discoverydocument)
  - [`DiscoveryConfig`](#discoveryconfig)
  - [`DiscoveryMiddlewareOptions`](#discoverymiddlewareoptions)
  - [`CreateEnvelopeMetaInput`](#createenvelopemetainput)
  - [`CreateEnvelopeSuccessInput`](#createenvelopesuccessinput)
  - [`CreateEnvelopeErrorInput`](#createenvelopeerrorinput)
  - [`ParseLafsResponseOptions`](#parselafsresponseoptions)
  - [`FieldExtractionInput`](#fieldextractioninput)
  - [`FieldExtractionResolution`](#fieldextractionresolution)
  - [`UnifiedFlagInput`](#unifiedflaginput)
  - [`UnifiedFlagResolution`](#unifiedflagresolution)
  - [`LafsA2AConfig`](#lafsa2aconfig)
  - [`LafsSendMessageParams`](#lafssendmessageparams)
  - [`CreateTaskOptions`](#createtaskoptions)
  - [`ListTasksOptions`](#listtasksoptions)
  - [`ListTasksResult`](#listtasksresult)
  - [`StreamIteratorOptions`](#streamiteratoroptions)
  - [`PushNotificationDeliveryResult`](#pushnotificationdeliveryresult)
  - [`JsonRpcRequest`](#jsonrpcrequest)
  - [`JsonRpcResponse`](#jsonrpcresponse)
  - [`JsonRpcErrorResponse`](#jsonrpcerrorresponse)
  - [`GrpcServiceMethod`](#grpcservicemethod)
  - [`GrpcStatus`](#grpcstatus)
  - [`GrpcErrorInfo`](#grpcerrorinfo)
  - [`ProblemDetails`](#problemdetails)
  - [`ListTasksQueryParams`](#listtasksqueryparams)
  - [`ErrorCodeMapping`](#errorcodemapping)
  - [`CircuitBreakerConfig`](#circuitbreakerconfig)
  - [`CircuitBreakerMetrics`](#circuitbreakermetrics)
  - [`HealthCheckConfig`](#healthcheckconfig)
  - [`HealthCheckResult`](#healthcheckresult)
  - [`HealthStatus`](#healthstatus)
  - [`LafsProblemDetails`](#lafsproblemdetails)
  - [`GracefulShutdownConfig`](#gracefulshutdownconfig)
  - [`ShutdownState`](#shutdownstate)
- [Types](#types)
  - [`LAFSTransport`](#lafstransport)
  - [`LAFSErrorCategory`](#lafserrorcategory)
  - [`MVILevel`](#mvilevel)
  - [`LAFSAgentAction`](#lafsagentaction)
  - [`LAFSPage`](#lafspage)
  - [`BudgetEnforcementOptions`](#budgetenforcementoptions)
  - [`MiddlewareFunction`](#middlewarefunction)
  - [`NextFunction`](#nextfunction)
  - [`BudgetMiddleware`](#budgetmiddleware)
  - [`ConformanceTier`](#conformancetier)
  - [`TransportMapping`](#transportmapping)
  - [`ComplianceStage`](#compliancestage)
  - [`ComplianceMiddleware`](#compliancemiddleware)
  - [`ExtensionKind`](#extensionkind)
  - [`CreateEnvelopeInput`](#createenvelopeinput)
  - [`TaskStreamEvent`](#taskstreamevent)
  - [`PushTransport`](#pushtransport)
  - [`JsonRpcMethod`](#jsonrpcmethod)
  - [`A2AErrorType`](#a2aerrortype)
  - [`GrpcStatusCode`](#grpcstatuscode)
  - [`GrpcStatusName`](#grpcstatusname)
  - [`HttpEndpoint`](#httpendpoint)
  - [`CircuitState`](#circuitstate)
  - [`HealthCheckFunction`](#healthcheckfunction)
- [Variables](#variables)
  - [`defaultEstimator`](#defaultestimator)
  - [`MVI_LEVELS`](#mvilevels)
  - [`AGENT_ACTIONS`](#agentactions)
  - [`LAFS_EXTENSION_URI`](#lafsextensionuri)
  - [`A2A_EXTENSIONS_HEADER`](#a2aextensionsheader)
  - [`LAFS_SCHEMA_URL`](#lafsschemaurl)
  - [`CATEGORY_ACTION_MAP`](#categoryactionmap)
  - [`TERMINAL_STATES`](#terminalstates)
  - [`INTERRUPTED_STATES`](#interruptedstates)
  - [`VALID_TRANSITIONS`](#validtransitions)
  - [`JSONRPC_METHODS`](#jsonrpcmethods)
  - [`JSONRPC_STANDARD_ERROR_CODES`](#jsonrpcstandarderrorcodes)
  - [`JSONRPC_A2A_ERROR_CODES`](#jsonrpca2aerrorcodes)
  - [`GRPC_STATUS_CODE`](#grpcstatuscode)
  - [`A2A_GRPC_STATUS_CODES`](#a2agrpcstatuscodes)
  - [`A2A_GRPC_ERROR_REASONS`](#a2agrpcerrorreasons)
  - [`A2A_GRPC_ERROR_DOMAIN`](#a2agrpcerrordomain)
  - [`GRPC_SERVICE_METHODS`](#grpcservicemethods)
  - [`GRPC_METADATA_VERSION_KEY`](#grpcmetadataversionkey)
  - [`GRPC_METADATA_EXTENSIONS_KEY`](#grpcmetadataextensionskey)
  - [`HTTP_ENDPOINTS`](#httpendpoints)
  - [`A2A_HTTP_STATUS_CODES`](#a2ahttpstatuscodes)
  - [`A2A_ERROR_TYPE_URIS`](#a2aerrortypeuris)
  - [`A2A_ERROR_MAPPINGS`](#a2aerrormappings)
  - [`SUPPORTED_A2A_VERSIONS`](#supporteda2aversions)
  - [`DEFAULT_A2A_VERSION`](#defaulta2aversion)
  - [`PROBLEM_DETAILS_CONTENT_TYPE`](#problemdetailscontenttype)

## Functions

### `estimateTokens()`

*Defined in `src/tokenEstimator.ts:373`*

```typescript
(value: unknown, options?: TokenEstimatorOptions) => number
```

Convenience function to estimate tokens for a value.

**Parameters**

- `value` — Any JavaScript value to estimate
- `options` — Optional estimator configuration overrides

**Returns**: Estimated token count

**Examples**

```typescript
const tokens = estimateTokens({ data: [1, 2, 3] });
```

### `estimateTokensJSON()`

*Defined in `src/tokenEstimator.ts:394`*

```typescript
(json: string, options?: TokenEstimatorOptions) => number
```

Convenience function to estimate tokens from a JSON string.

**Parameters**

- `json` — Pre-serialized JSON string
- `options` — Optional estimator configuration overrides

**Returns**: Estimated token count

**Examples**

```typescript
const tokens = estimateTokensJSON('{"key": "value"}');
```

### `isMVILevel()`

*Defined in `src/types.ts:114`*

```typescript
(value: unknown) => value is MVILevel
```

Type guard that checks whether an unknown value is a valid `MVILevel`.

**Parameters**

- `value` — The value to test.

**Returns**: `true` if `value` is one of the recognised MVI level strings.

**Examples**

```ts
const level: unknown = 'minimal';
if (isMVILevel(level)) {
  // level is narrowed to MVILevel
}
```

### `isAgentAction()`

*Defined in `src/types.ts:227`*

```typescript
(value: unknown) => value is LAFSAgentAction
```

Type guard that checks whether an unknown value is a valid `LAFSAgentAction`.

**Parameters**

- `value` — The value to test.

**Returns**: `true` if `value` is one of the recognised agent action strings.

**Examples**

```ts
const action: unknown = 'retry';
if (isAgentAction(action)) {
  // action is narrowed to LAFSAgentAction
}
```

### `applyBudgetEnforcement()`

*Defined in `src/budgetEnforcement.ts:275`*

```typescript
(envelope: LAFSEnvelope, budget: number, options?: BudgetEnforcementOptions) => BudgetEnforcementResult
```

Apply budget enforcement to an envelope.

**Parameters**

- `envelope` — The LAFS envelope to check
- `budget` — Maximum allowed token count
- `options` — Budget enforcement options (truncation, callbacks)

**Returns**: Enforcement result with the (possibly modified) envelope, budget status, and token estimates

**Examples**

```typescript
const result = applyBudgetEnforcement(envelope, 1000, { truncateOnExceed: true });
if (!result.withinBudget) {
  console.warn("Budget exceeded:", result.estimatedTokens);
}
```

### `withBudget()`

*Defined in `src/budgetEnforcement.ts:392`*

```typescript
(budget: number, options?: BudgetEnforcementOptions) => EnvelopeMiddleware
```

Create a budget enforcement middleware function.

**Parameters**

- `budget` — Maximum allowed token count for the response
- `options` — Budget enforcement options (truncation, callbacks)

**Returns**: Async middleware function that enforces the token budget

**Examples**

```typescript
const middleware = withBudget(1000, { truncateOnExceed: true });
const result = await middleware(envelope, async () => nextEnvelope);
```

### `checkBudget()`

*Defined in `src/budgetEnforcement.ts:429`*

```typescript
(envelope: LAFSEnvelope, budget: number) => { exceeded: boolean; estimated: number; remaining: number; }
```

Check if an envelope has exceeded its budget without modifying it.

**Parameters**

- `envelope` — The LAFS envelope to check
- `budget` — Maximum allowed token count

**Returns**: Object with `exceeded` flag, `estimated` token count, and `remaining` budget

**Examples**

```typescript
const { exceeded, estimated, remaining } = checkBudget(envelope, 500);
if (exceeded) {
  console.warn(`Over budget by ${estimated - 500} tokens`);
}
```

### `withBudgetSync()`

*Defined in `src/budgetEnforcement.ts:460`*

```typescript
(budget: number, options?: BudgetEnforcementOptions) => (envelope: LAFSEnvelope, next: () => LAFSEnvelope) => LAFSEnvelope
```

Synchronous version of withBudget for non-async contexts.

**Parameters**

- `budget` — Maximum allowed token count for the response
- `options` — Budget enforcement options (truncation, callbacks)

**Returns**: Synchronous middleware function that enforces the token budget

**Examples**

```typescript
const middleware = withBudgetSync(500);
const result = middleware(envelope, () => nextEnvelope);
```

### `wrapWithBudget()`

*Defined in `src/budgetEnforcement.ts:495`*

```typescript
<TArgs extends unknown[], TResult extends LAFSEnvelope>(handler: (...args: TArgs) => TResult | Promise<TResult>, budget: number, options?: BudgetEnforcementOptions) => (...args: TArgs) => Promise<LAFSEnvelope>
```

Higher-order function that wraps a handler with budget enforcement.

**Parameters**

- `handler` — The handler function to wrap
- `handler` — The handler function to wrap with budget enforcement
- `budget` — Maximum allowed tokens
- `options` — Budget enforcement options

**Returns**: Wrapped handler with budget enforcement

**Examples**

```typescript
const myHandler = async (request: Request) => ({ success: true, result: { data } });
const budgetedHandler = wrapWithBudget(myHandler, 1000, { truncateOnExceed: true });
const result = await budgetedHandler(request);
```

### `composeMiddleware()`

*Defined in `src/budgetEnforcement.ts:527`*

```typescript
(...middlewares: EnvelopeMiddleware[]) => EnvelopeMiddleware
```

Compose multiple middleware functions into a single middleware.

**Parameters**

- `middlewares` — Middleware functions to compose (executed left to right)

**Returns**: A single middleware function that chains all provided middlewares

**Examples**

```typescript
const pipeline = composeMiddleware(
  withBudget(1000),
  loggingMiddleware,
);
const result = await pipeline(envelope, () => finalEnvelope);
```

### `getConformanceProfiles()`

*Defined in `src/conformanceProfiles.ts:40`*

```typescript
() => ConformanceProfiles
```

Loads the conformance profiles from the bundled JSON schema.

**Returns**: The full `ConformanceProfiles` object.

**Examples**

```ts
const profiles = getConformanceProfiles();
console.log(profiles.tiers.core);
```

### `getChecksForTier()`

*Defined in `src/conformanceProfiles.ts:59`*

```typescript
(tier: ConformanceTier) => string[]
```

Returns the list of check names that belong to the given conformance tier.

**Parameters**

- `tier` — The conformance tier to retrieve checks for.

**Returns**: An array of check name strings for the specified tier.

**Examples**

```ts
const coreChecks = getChecksForTier('core');
```

### `validateConformanceProfiles()`

*Defined in `src/conformanceProfiles.ts:85`*

```typescript
(availableChecks: string[]) => { valid: boolean; errors: string[]; }
```

Validates that the conformance profiles are internally consistent and reference only known checks.

**Parameters**

- `availableChecks` — The full list of check names implemented by the conformance runner.

**Returns**: An object with `valid` (true when no errors) and an `errors` array of diagnostic strings.

**Examples**

```ts
const result = validateConformanceProfiles(['envelope_schema_valid', 'envelope_invariants']);
if (!result.valid) {
  console.error(result.errors);
}
```

### `getErrorRegistry()`

*Defined in `src/errorRegistry.ts:85`*

```typescript
() => ErrorRegistry
```

Loads the full LAFS error registry from the bundled JSON.

**Returns**: The complete error registry with version and all registered codes.

**Examples**

```ts
const registry = getErrorRegistry();
console.log(registry.version, registry.codes.length);
```

### `isRegisteredErrorCode()`

*Defined in `src/errorRegistry.ts:105`*

```typescript
(code: string) => boolean
```

Checks whether a given error code exists in the LAFS error registry.

**Parameters**

- `code` — The error code string to look up (e.g., `"E_FORMAT_CONFLICT"`).

**Returns**: `true` if the code is registered, `false` otherwise.

**Examples**

```ts
isRegisteredErrorCode('E_FORMAT_CONFLICT'); // true
isRegisteredErrorCode('E_UNKNOWN');         // false
```

### `getRegistryCode()`

*Defined in `src/errorRegistry.ts:128`*

```typescript
(code: string) => RegistryCode | undefined
```

Retrieves the full registry entry for a given error code.

**Parameters**

- `code` — The error code string to look up.

**Returns**: The matching `RegistryCode` or `undefined` if not found.

**Examples**

```ts
const entry = getRegistryCode('E_FORMAT_CONFLICT');
if (entry) {
  console.log(entry.httpStatus); // 409
}
```

### `getAgentAction()`

*Defined in `src/errorRegistry.ts:149`*

```typescript
(code: string) => LAFSAgentAction | undefined
```

Returns the default agent action for a given error code.

**Parameters**

- `code` — The error code string to look up.

**Returns**: The `LAFSAgentAction` or `undefined` if unavailable.

**Examples**

```ts
const action = getAgentAction('E_RATE_LIMIT');
console.log(action); // "retry"
```

### `getTypeUri()`

*Defined in `src/errorRegistry.ts:170`*

```typescript
(code: string) => string | undefined
```

Returns the RFC 9457 type URI for a given error code.

**Parameters**

- `code` — The error code string to look up.

**Returns**: The type URI string or `undefined` if unavailable.

**Examples**

```ts
const uri = getTypeUri('E_VALIDATION');
// "https://lafs.dev/errors/E_VALIDATION"
```

### `getDocUrl()`

*Defined in `src/errorRegistry.ts:191`*

```typescript
(code: string) => string | undefined
```

Returns the documentation URL for a given error code.

**Parameters**

- `code` — The error code string to look up.

**Returns**: The documentation URL string or `undefined` if unavailable.

**Examples**

```ts
const url = getDocUrl('E_VALIDATION');
// "https://lafs.dev/docs/errors/E_VALIDATION"
```

### `getTransportMapping()`

*Defined in `src/errorRegistry.ts:214`*

```typescript
(code: string, transport: "http" | "grpc" | "cli") => TransportMapping | null
```

Resolves the transport-specific status value for a given error code and transport.

**Parameters**

- `code` — The error code string to look up.
- `transport` — The transport protocol to resolve a mapping for.

**Returns**: A `TransportMapping` or `null` if the code is unregistered.

**Examples**

```ts
const mapping = getTransportMapping('E_NOT_FOUND', 'http');
console.log(mapping); // { transport: 'http', value: 404 }
```

### `resolveOutputFormat()`

*Defined in `src/flagSemantics.ts:106`*

```typescript
(input: FlagInput) => FlagResolution
```

Resolve the output format from flag inputs using the LAFS precedence chain.

**Parameters**

- `input` — The flag inputs including explicit flags, project/user defaults, and TTY state

**Returns**: The resolved format, its source layer, and quiet mode status

**Throws**

- `LAFSFlagError` When `humanFlag` and `jsonFlag` are both truthy.

**Examples**

```ts
const resolution = resolveOutputFormat({ humanFlag: true });
// => { format: 'human', source: 'flag', quiet: false }
```

### `validateEnvelope()`

*Defined in `src/validateEnvelope.ts:91`*

```typescript
(input: unknown) => EnvelopeValidationResult
```

Validates an unknown input against the LAFS envelope JSON Schema (Draft-07).

**Parameters**

- `input` — The raw value to validate.

**Returns**: An `EnvelopeValidationResult` with validity status and any errors.

**Examples**

```ts
const result = validateEnvelope(JSON.parse(rawJson));
if (!result.valid) {
  console.error(result.errors);
}
```

### `assertEnvelope()`

*Defined in `src/validateEnvelope.ts:134`*

```typescript
(input: unknown) => LAFSEnvelope
```

Validates input and throws on schema failure, returning a typed envelope on success.

**Parameters**

- `input` — The raw value to validate as a LAFS envelope.

**Returns**: The input cast to `LAFSEnvelope` when schema validation passes.

**Throws**

- Error When the input does not conform to the envelope schema.

**Examples**

```ts
const envelope = assertEnvelope(parsed);
console.log(envelope.success);
```

### `runEnvelopeConformance()`

*Defined in `src/conformance.ts:55`*

```typescript
(envelope: unknown, options?: EnvelopeConformanceOptions) => ConformanceReport
```

Runs the full suite of LAFS envelope conformance checks.

**Parameters**

- `envelope` — The raw value to validate as a LAFS envelope.
- `options` — Optional tier filter for the conformance checks.

**Returns**: A `ConformanceReport` with individual check results and an overall pass/fail.

**Examples**

```ts
const report = runEnvelopeConformance(parsedJson, { tier: 'core' });
if (!report.ok) {
  console.error(report.checks.filter(c => !c.pass));
}
```

### `runFlagConformance()`

*Defined in `src/conformance.ts:412`*

```typescript
(flags: FlagInput) => ConformanceReport
```

Runs LAFS flag-semantics conformance checks against a set of flag inputs.

**Parameters**

- `flags` — The flag input to validate.

**Returns**: A `ConformanceReport` with individual check results and an overall pass/fail.

**Examples**

```ts
const report = runFlagConformance({ humanFlag: true, jsonFlag: false });
console.log(report.ok); // true
```

### `enforceCompliance()`

*Defined in `src/compliance.ts:164`*

```typescript
(input: unknown, options?: EnforceComplianceOptions) => ComplianceResult
```

Runs the full LAFS compliance pipeline against an unknown input value.

**Parameters**

- `input` — The raw value to validate as a LAFS envelope.
- `options` — Controls which optional stages execute.

**Returns**: A `ComplianceResult` with the aggregate pass/fail status and per-stage reports.

**Examples**

```ts
const result = enforceCompliance(rawJson, { checkFlags: true, flags: { jsonFlag: true } });
if (!result.ok) {
  console.error(result.issues);
}
```

### `assertCompliance()`

*Defined in `src/compliance.ts:246`*

```typescript
(input: unknown, options?: EnforceComplianceOptions) => LAFSEnvelope
```

Validates input and throws `ComplianceError` on any failure.

**Parameters**

- `input` — The raw value to validate as a LAFS envelope.
- `options` — Controls which optional stages execute.

**Returns**: The validated `LAFSEnvelope` when all stages pass.

**Throws**

- `ComplianceError` When any compliance stage fails.

**Examples**

```ts
const envelope = assertCompliance(rawJson);
```

### `withCompliance()`

*Defined in `src/compliance.ts:277`*

```typescript
<TArgs extends unknown[], TResult extends LAFSEnvelope>(producer: (...args: TArgs) => TResult | Promise<TResult>, options?: EnforceComplianceOptions) => (...args: TArgs) => Promise<LAFSEnvelope>
```

Wraps an envelope-producing function with automatic compliance enforcement.

**Parameters**

- `producer` — A sync or async function that produces a LAFS envelope.
- `options` — Compliance options forwarded to `assertCompliance`.

**Returns**: An async function with the same signature that enforces compliance on every call.

**Examples**

```ts
const safeFetch = withCompliance(fetchEnvelope, { checkConformance: true });
const envelope = await safeFetch('/api/data');
```

### `createComplianceMiddleware()`

*Defined in `src/compliance.ts:320`*

```typescript
(options?: EnforceComplianceOptions) => ComplianceMiddleware
```

Creates a `ComplianceMiddleware` that enforces LAFS compliance on the next handler's output.

**Parameters**

- `options` — Compliance options forwarded to `assertCompliance`.

**Returns**: A middleware function that validates the downstream envelope.

**Examples**

```ts
const mw = createComplianceMiddleware({ checkConformance: true });
const result = await mw(currentEnvelope, () => produceEnvelope());
```

### `getDeprecationRegistry()`

*Defined in `src/deprecationRegistry.ts:71`*

```typescript
() => DeprecationEntry[]
```

Retrieve all registered deprecation entries.

**Returns**: Array of all `DeprecationEntry` rules in the registry

**Examples**

```typescript
const entries = getDeprecationRegistry();
console.log(entries.length); // number of registered deprecations
```

### `detectDeprecatedEnvelopeFields()`

*Defined in `src/deprecationRegistry.ts:93`*

```typescript
(envelope: LAFSEnvelope) => Warning[]
```

Detect deprecated field usage in a LAFS envelope.

**Parameters**

- `envelope` — The LAFS envelope to inspect

**Returns**: Array of `Warning` objects for each detected deprecation

**Examples**

```typescript
const warnings = detectDeprecatedEnvelopeFields(envelope);
for (const w of warnings) {
  console.warn(`${w.code}: ${w.message}`);
}
```

### `emitDeprecationWarnings()`

*Defined in `src/deprecationRegistry.ts:122`*

```typescript
(envelope: LAFSEnvelope) => LAFSEnvelope
```

Emit deprecation warnings by attaching them to the envelope metadata.

**Parameters**

- `envelope` — The LAFS envelope to augment

**Returns**: A new envelope with deprecation warnings appended to `_meta.warnings`

**Examples**

```typescript
const enriched = emitDeprecationWarnings(envelope);
console.log(enriched._meta.warnings); // includes any deprecation warnings
```

### `parseExtensionsHeader()`

*Defined in `src/a2a/extensions.ts:98`*

```typescript
(headerValue: string | undefined) => string[]
```

Parse A2A-Extensions header value into URI array.

**Parameters**

- `headerValue` — Raw header value string, or undefined if absent

**Returns**: Array of trimmed extension URI strings

**Examples**

```typescript
const uris = parseExtensionsHeader('https://lafs.dev/ext/v1, https://example.com/ext');
// => ['https://lafs.dev/ext/v1', 'https://example.com/ext']
```

### `negotiateExtensions()`

*Defined in `src/a2a/extensions.ts:129`*

```typescript
(requestedUris: string[], agentExtensions: AgentExtension[]) => ExtensionNegotiationResult
```

Negotiate extensions between client-requested and agent-declared sets.

**Parameters**

- `requestedUris` — Extension URIs requested by the client
- `agentExtensions` — Extensions declared in the agent's Agent Card

**Returns**: Negotiation result with activated, unsupported, and missing required sets

**Examples**

```typescript
const result = negotiateExtensions(
  ['https://lafs.dev/extensions/envelope/v1'],
  agentCard.capabilities.extensions,
);
if (result.missingRequired.length > 0) {
  throw new ExtensionSupportRequiredError(result.missingRequired);
}
```

### `formatExtensionsHeader()`

*Defined in `src/a2a/extensions.ts:186`*

```typescript
(activatedUris: string[]) => string
```

Format activated extension URIs into header value.

**Parameters**

- `activatedUris` — Extension URIs that were successfully negotiated

**Returns**: Comma-separated header value string

**Examples**

```typescript
res.setHeader('A2A-Extensions', formatExtensionsHeader(result.activated));
```

### `buildLafsExtension()`

*Defined in `src/a2a/extensions.ts:231`*

```typescript
(options?: BuildLafsExtensionOptions) => AgentExtension
```

Build an A2A AgentExtension object declaring LAFS support.

**Parameters**

- `options` — Configuration options for the LAFS extension declaration

**Returns**: A2A AgentExtension object ready for inclusion in an Agent Card

**Examples**

```typescript
const ext = buildLafsExtension({ required: true, supportsTokenBudgets: true });
agentCard.capabilities.extensions.push(ext);
```

### `buildExtension()`

*Defined in `src/a2a/extensions.ts:284`*

```typescript
(options: BuildExtensionOptions) => AgentExtension
```

Build a generic A2A AgentExtension object.

**Parameters**

- `options` — Configuration for the extension declaration

**Returns**: A2A AgentExtension object

**Examples**

```typescript
const ext = buildExtension({
  uri: 'https://example.com/ext/v1',
  description: 'Custom extension',
  kind: 'data-only',
});
```

### `isValidExtensionKind()`

*Defined in `src/a2a/extensions.ts:313`*

```typescript
(kind: string) => kind is ExtensionKind
```

Check whether a string is a valid extension kind.

**Parameters**

- `kind` — String value to validate

**Returns**: True if the value is a recognized ExtensionKind

**Examples**

```typescript
if (isValidExtensionKind(userInput)) {
  // userInput is now typed as ExtensionKind
}
```

### `validateExtensionDeclaration()`

*Defined in `src/a2a/extensions.ts:335`*

```typescript
(extension: AgentExtension) => { valid: boolean; error?: string; }
```

Validate an A2A extension declaration for correctness.

**Parameters**

- `extension` — The AgentExtension to validate

**Returns**: Object with `valid` boolean and optional `error` message

**Examples**

```typescript
const { valid, error } = validateExtensionDeclaration(ext);
if (!valid) {
  console.error('Invalid extension:', error);
}
```

### `extensionNegotiationMiddleware()`

*Defined in `src/a2a/extensions.ts:492`*

```typescript
(options: ExtensionNegotiationMiddlewareOptions) => RequestHandler
```

Express middleware for A2A extension negotiation.

**Parameters**

- `options` — Middleware configuration with extensions and enforcement settings

**Returns**: Express RequestHandler that performs extension negotiation

**Examples**

```typescript
app.use(extensionNegotiationMiddleware({
  extensions: agentCard.capabilities.extensions,
  enforceRequired: true,
}));
```

### `discoveryMiddleware()`

*Defined in `src/discovery.ts:730`*

```typescript
(config: DiscoveryConfig, options?: DiscoveryMiddlewareOptions) => RequestHandler
```

Create Express middleware for serving A2A Agent Card.

**Parameters**

- `config` — Discovery configuration (A2A v1.0 format)
- `options` — Middleware options for path routing and caching

**Returns**: Express RequestHandler that serves the Agent Card

**Examples**

```typescript
import express from "express";
import { discoveryMiddleware } from "@cleocode/lafs/discovery";

const app = express();

app.use(discoveryMiddleware({
  agent: {
    name: "my-lafs-agent",
    description: "A LAFS-compliant agent with A2A support",
    version: "1.0.0",
    url: "https://api.example.com",
    capabilities: {
      streaming: true,
      pushNotifications: false,
      extensions: []
    },
    defaultInputModes: ["application/json", "text/plain"],
    defaultOutputModes: ["application/json"],
    skills: [
      {
        id: "envelope-processor",
        name: "Envelope Processor",
        description: "Process LAFS envelopes",
        tags: ["lafs", "envelope", "validation"],
        examples: ["Validate this envelope", "Process envelope data"]
      }
    ]
  }
}));
```

### `discoveryFastifyPlugin()`

*Defined in `src/discovery.ts:901`*

```typescript
(fastify: unknown, options: { config: DiscoveryConfig; path?: string; }) => Promise<void>
```

Fastify plugin for A2A Agent Card discovery.

**Parameters**

- `fastify` — Fastify instance
- `options` — Plugin options containing `config` and optional `path`

**Returns**: Promise that resolves when the plugin is registered

**Examples**

```typescript
import Fastify from "fastify";
import { discoveryFastifyPlugin } from "@cleocode/lafs/discovery";

const app = Fastify();
app.register(discoveryFastifyPlugin, {
  config: { agent: { name: "my-agent", ... } },
});
```

### `createEnvelope()`

*Defined in `src/envelope.ts:331`*

```typescript
(input: CreateEnvelopeInput) => LAFSEnvelope
```

Create a fully validated LAFS envelope from a success or error input.

**Parameters**

- `input` — Discriminated union of success or error input data.

**Returns**: A complete `LAFSEnvelope` ready for serialization.

**Examples**

```ts
import { createEnvelope } from '@cleocode/lafs';

const envelope = createEnvelope({
  success: true,
  result: { items: [] },
  meta: { operation: 'tasks.list', requestId: 'req-1' },
});
```

### `parseLafsResponse()`

*Defined in `src/envelope.ts:483`*

```typescript
<T = unknown>(input: unknown, options?: ParseLafsResponseOptions) => T
```

Parse and unwrap a raw LAFS envelope, returning the result or throwing on error.

**Parameters**

- `input` — Raw value expected to be a valid `LAFSEnvelope`.
- `options` — Parsing options controlling error-code validation.

**Returns**: The `result` field of the envelope cast to `T`.

**Throws**

- LafsError When the envelope indicates failure (`success=false`).
- Error When the envelope is structurally invalid or   `requireRegisteredErrorCode` is `true` and the code is unregistered.

**Examples**

```ts
import { parseLafsResponse } from '@cleocode/lafs';

interface TaskList { items: Task[] }
const tasks = parseLafsResponse<TaskList>(rawEnvelope);
```

### `resolveFieldExtraction()`

*Defined in `src/fieldExtraction.ts:96`*

```typescript
(input: FieldExtractionInput) => FieldExtractionResolution
```

Resolve field extraction flags into a validated configuration.

**Parameters**

- `input` — The field extraction flag inputs

**Returns**: The resolved extraction configuration with mvi level and source

**Throws**

- `LAFSFlagError` When both `fieldFlag` and `fieldsFlag` are set.

**Examples**

```ts
const resolution = resolveFieldExtraction({ fieldsFlag: 'id,title' });
// => { fields: ['id', 'title'], mvi: 'minimal', mviSource: 'default', expectsCustomMvi: true }
```

### `extractFieldFromResult()`

*Defined in `src/fieldExtraction.ts:160`*

```typescript
(result: LAFSEnvelope["result"], field: string) => unknown
```

Extract a named field from a LAFS result object.

**Parameters**

- `result` — The envelope result value (object, array, or null)
- `field` — The field name to extract

**Returns**: The extracted value, or `undefined` if not found at any level

**Examples**

```ts
const result = { task: { id: 'T1', title: 'Fix bug' } };
extractFieldFromResult(result, 'title'); // => 'Fix bug'
```

### `extractFieldFromEnvelope()`

*Defined in `src/fieldExtraction.ts:206`*

```typescript
(envelope: LAFSEnvelope, field: string) => unknown
```

Extract a named field from an envelope's result.

**Parameters**

- `envelope` — The LAFS envelope to extract from
- `field` — The field name to extract

**Returns**: The extracted value, or `undefined` if not found

**Examples**

```ts
const value = extractFieldFromEnvelope(envelope, 'title');
```

### `applyFieldFilter()`

*Defined in `src/fieldExtraction.ts:241`*

```typescript
(envelope: LAFSEnvelope, fields: string[]) => LAFSEnvelope
```

Filter result fields in a LAFS envelope to the requested subset.

**Parameters**

- `envelope` — The LAFS envelope whose result will be filtered
- `fields` — Array of field names to retain in the result

**Returns**: A new envelope with the filtered result and `_meta.mvi` set to `'custom'`

**Examples**

```ts
const filtered = applyFieldFilter(envelope, ['id', 'title']);
// filtered.result contains only 'id' and 'title' fields
// filtered._meta.mvi === 'custom'
```

### `resolveFlags()`

*Defined in `src/flagResolver.ts:118`*

```typescript
(input: UnifiedFlagInput) => UnifiedFlagResolution
```

Resolve all flags across both layers and validate cross-layer semantics.

**Parameters**

- `input` — Combined format and field extraction flags

**Returns**: The unified resolution containing format, fields, and any cross-layer warnings

**Throws**

- `LAFSFlagError` When format or field layer flags conflict.

**Examples**

```ts
const result = resolveFlags({ human: true, field: 'title' });
// result.format => { format: 'human', source: 'flag', quiet: false }
// result.fields => { field: 'title', mvi: 'minimal', ... }
// result.warnings => ['Cross-layer: --human + --field "title". ...']
```

### `createLafsArtifact()`

*Defined in `src/a2a/bridge.ts:417`*

```typescript
(envelope: LAFSEnvelope) => Artifact
```

Create a LAFS envelope artifact for A2A.

**Parameters**

- `envelope` — LAFS envelope to wrap as an artifact

**Returns**: A2A Artifact containing the envelope as a DataPart

**Examples**

```typescript
const envelope = createEnvelope({
  success: true,
  result: { data: '...' },
  meta: { operation: 'analysis.run' }
});

const artifact = createLafsArtifact(envelope);
task.artifacts.push(artifact);
```

### `createTextArtifact()`

*Defined in `src/a2a/bridge.ts:450`*

```typescript
(text: string, name?: string) => Artifact
```

Create a text artifact.

**Parameters**

- `text` — Text content for the artifact
- `name` — Display name for the artifact

**Returns**: A2A Artifact containing the text as a TextPart

**Examples**

```typescript
const artifact = createTextArtifact('Hello, world!', 'greeting');
```

### `createFileArtifact()`

*Defined in `src/a2a/bridge.ts:485`*

```typescript
(fileUrl: string, mediaType: string, filename?: string) => Artifact
```

Create a file artifact.

**Parameters**

- `fileUrl` — URI pointing to the file resource
- `mediaType` — MIME type of the file (e.g., `application/pdf`)
- `filename` — Optional display filename for the artifact

**Returns**: A2A Artifact containing the file reference as a FilePart

**Examples**

```typescript
const artifact = createFileArtifact(
  'https://example.com/report.pdf',
  'application/pdf',
  'report.pdf',
);
```

### `isExtensionRequired()`

*Defined in `src/a2a/bridge.ts:537`*

```typescript
(agentCard: AgentCard, extensionUri: string) => boolean
```

Check if an extension is required in an Agent Card.

**Parameters**

- `agentCard` — Agent Card to inspect
- `extensionUri` — URI of the extension to check

**Returns**: True if the extension is declared as required

**Examples**

```typescript
if (isExtensionRequired(agentCard, LAFS_EXTENSION_URI)) {
  console.log('LAFS extension is mandatory for this agent');
}
```

### `getExtensionParams()`

*Defined in `src/a2a/bridge.ts:562`*

```typescript
(agentCard: AgentCard, extensionUri: string) => Record<string, unknown> | undefined
```

Get extension parameters from an Agent Card.

**Parameters**

- `agentCard` — Agent Card to inspect
- `extensionUri` — URI of the extension to look up

**Returns**: The extension's params object, or undefined if not found

**Examples**

```typescript
const params = getExtensionParams(agentCard, LAFS_EXTENSION_URI);
if (params?.supportsTokenBudgets) {
  // Enable budget tracking
}
```

### `isValidTransition()`

*Defined in `src/a2a/task-lifecycle.ts:88`*

```typescript
(from: TaskState, to: TaskState) => boolean
```

Check if a transition from one state to another is valid.

**Parameters**

- `from` — Current task state
- `to` — Desired target state

**Returns**: True if the transition is allowed by the state machine

**Examples**

```typescript
if (!isValidTransition('submitted', 'completed')) {
  throw new Error('Cannot go directly from submitted to completed');
}
```

### `isTerminalState()`

*Defined in `src/a2a/task-lifecycle.ts:109`*

```typescript
(state: TaskState) => boolean
```

Check if a state is terminal (no further transitions allowed).

**Parameters**

- `state` — Task state to check

**Returns**: True if the state is terminal

**Examples**

```typescript
if (isTerminalState(task.status.state)) {
  console.log('Task has reached a final state');
}
```

### `isInterruptedState()`

*Defined in `src/a2a/task-lifecycle.ts:129`*

```typescript
(state: TaskState) => boolean
```

Check if a state is interrupted (paused awaiting input).

**Parameters**

- `state` — Task state to check

**Returns**: True if the state indicates the task is waiting for external input

**Examples**

```typescript
if (isInterruptedState(task.status.state)) {
  promptUserForInput(task);
}
```

### `attachLafsEnvelope()`

*Defined in `src/a2a/task-lifecycle.ts:742`*

```typescript
(manager: TaskManager, taskId: string, envelope: LAFSEnvelope) => Task
```

Attach a LAFS envelope as an artifact to an A2A task.

**Parameters**

- `manager` — TaskManager instance managing the task
- `taskId` — ID of the task to attach the envelope to
- `envelope` — LAFS envelope to attach as an artifact

**Returns**: Deep clone of the updated task with the new artifact

**Examples**

```typescript
const envelope: LAFSEnvelope = { success: true, result: { data: 'ok' }, error: null, _meta: meta };
const updated = attachLafsEnvelope(manager, 'task-1', envelope);
```

### `streamTaskEvents()`

*Defined in `src/a2a/streaming.ts:204`*

```typescript
(bus: TaskEventBus, taskId: string, options?: StreamIteratorOptions) => AsyncGenerator<TaskStreamEvent>
```

Build an async iterator for real-time task stream events.

**Parameters**

- `bus` — TaskEventBus to subscribe to
- `taskId` — ID of the task to stream events for
- `options` — Iterator options including timeout

**Returns**: Async generator yielding task stream events

**Examples**

```typescript
for await (const event of streamTaskEvents(bus, 'task-1', { timeoutMs: 5000 })) {
  console.log('Received:', event);
}
```

### `createJsonRpcRequest()`

*Defined in `src/a2a/bindings/jsonrpc.ts:190`*

```typescript
(id: string | number, method: string, params?: Record<string, unknown>) => JsonRpcRequest
```

Create a JSON-RPC 2.0 request object.

**Parameters**

- `id` — Client-assigned request identifier
- `method` — The RPC method name to invoke
- `params` — Optional named parameters for the method call

**Returns**: A fully formed `JsonRpcRequest` object

**Examples**

```ts
const req = createJsonRpcRequest(1, 'tasks/get', { id: 'abc' });
// { jsonrpc: '2.0', id: 1, method: 'tasks/get', params: { id: 'abc' } }
```

### `createJsonRpcResponse()`

*Defined in `src/a2a/bindings/jsonrpc.ts:220`*

```typescript
(id: string | number | null, result: unknown) => JsonRpcResponse
```

Create a JSON-RPC 2.0 success response.

**Parameters**

- `id` — Identifier matching the originating request, or `null` for notifications
- `result` — The return value of the invoked method

**Returns**: A fully formed `JsonRpcResponse` object

**Examples**

```ts
const res = createJsonRpcResponse(1, { status: 'ok' });
// { jsonrpc: '2.0', id: 1, result: { status: 'ok' } }
```

### `createJsonRpcErrorResponse()`

*Defined in `src/a2a/bindings/jsonrpc.ts:250`*

```typescript
(id: string | number | null, code: number, message: string, data?: Record<string, unknown>) => JsonRpcErrorResponse
```

Create a JSON-RPC 2.0 error response.

**Parameters**

- `id` — Identifier matching the originating request, or `null` for notifications
- `code` — Numeric error code (standard or A2A-specific)
- `message` — Short human-readable description of the error
- `data` — Optional additional structured error data

**Returns**: A fully formed `JsonRpcErrorResponse` object

**Examples**

```ts
const err = createJsonRpcErrorResponse(1, -32001, 'Task not found');
// { jsonrpc: '2.0', id: 1, error: { code: -32001, message: 'Task not found' } }
```

### `createA2AErrorResponse()`

*Defined in `src/a2a/bindings/jsonrpc.ts:286`*

```typescript
(id: string | number | null, errorType: A2AErrorType, message: string, data?: Record<string, unknown>) => JsonRpcErrorResponse
```

Create an A2A-specific JSON-RPC error response by error type name.

**Parameters**

- `id` — Identifier matching the originating request, or `null` for notifications
- `errorType` — The A2A error type name (e.g. `"TaskNotFound"`)
- `message` — Short human-readable description of the error
- `data` — Optional additional structured error data

**Returns**: A fully formed `JsonRpcErrorResponse` with the resolved A2A error code

**Examples**

```ts
const err = createA2AErrorResponse(1, 'TaskNotFound', 'No task with id xyz');
// error.code === -32001
```

### `validateJsonRpcRequest()`

*Defined in `src/a2a/bindings/jsonrpc.ts:318`*

```typescript
(input: unknown) => { valid: boolean; errors: string[]; }
```

Validate the structure of a JSON-RPC request.

**Parameters**

- `input` — The unknown value to validate

**Returns**: An object with `valid` indicating success and `errors` listing any violations

**Examples**

```ts
const { valid, errors } = validateJsonRpcRequest({ jsonrpc: '2.0', id: 1, method: 'tasks/get' });
// valid === true, errors === []
```

### `isA2AError()`

*Defined in `src/a2a/bindings/jsonrpc.ts:367`*

```typescript
(code: number) => boolean
```

Check if a numeric error code is an A2A-specific error.

**Parameters**

- `code` — The numeric JSON-RPC error code to check

**Returns**: `true` if the code falls within the A2A error range

**Examples**

```ts
isA2AError(-32001); // true  (TaskNotFound)
isA2AError(-32700); // false (standard ParseError)
```

### `createGrpcStatus()`

*Defined in `src/a2a/bindings/grpc.ts:286`*

```typescript
(errorType: A2AErrorType, message: string, metadata?: Record<string, string>) => GrpcStatus
```

Create a gRPC Status object for an A2A error type.

**Parameters**

- `errorType` — The A2A error type name (e.g. `"TaskNotFound"`)
- `message` — Human-readable error message
- `metadata` — Optional key-value metadata to include in the ErrorInfo detail

**Returns**: A fully formed `GrpcStatus` object with ErrorInfo details

**Examples**

```ts
const status = createGrpcStatus('TaskNotFound', 'No task with id xyz');
// { code: 5, message: '...', details: [{ reason: 'TASK_NOT_FOUND', domain: 'a2a-protocol.org' }] }
```

### `createProblemDetails()`

*Defined in `src/a2a/bindings/http.ts:143`*

```typescript
(errorType: A2AErrorType, detail: string, extensions?: Record<string, unknown>) => ProblemDetails
```

Create an RFC 9457 Problem Details object for an A2A error.

**Parameters**

- `errorType` — The A2A error type name (e.g. `"TaskNotFound"`)
- `detail` — Human-readable explanation specific to this occurrence
- `extensions` — Optional additional members to include in the response

**Returns**: A fully formed `ProblemDetails` object

**Examples**

```ts
const problem = createProblemDetails('TaskNotFound', 'No task with id xyz');
// { type: 'https://a2a-protocol.org/errors/task-not-found', title: 'Task Not Found', status: 404, detail: '...' }
```

### `createLafsProblemDetails()`

*Defined in `src/a2a/bindings/http.ts:183`*

```typescript
(errorType: A2AErrorType, lafsError: LAFSError, requestId?: string) => ProblemDetails
```

Create an RFC 9457 Problem Details object bridging A2A error types with LAFS error data.

**Parameters**

- `errorType` — The A2A error type name (e.g. `"InvalidAgentResponse"`)
- `lafsError` — The LAFS error object to extract extension fields from
- `requestId` — Optional request identifier used as the `instance` field

**Returns**: A `ProblemDetails` object with LAFS extension fields

**Examples**

```ts
const problem = createLafsProblemDetails('InvalidAgentResponse', {
  code: 'E_AGENT_RESPONSE',
  message: 'Upstream agent returned invalid JSON',
  retryable: true,
  retryAfterMs: 5000,
}, 'req-123');
```

### `buildUrl()`

*Defined in `src/a2a/bindings/http.ts:225`*

```typescript
(endpoint: HttpEndpoint, params?: Record<string, string>) => string
```

Build a URL by substituting path parameters.

**Parameters**

- `endpoint` — HTTP endpoint definition from `HTTP_ENDPOINTS`
- `params` — Path parameter values keyed by name (without leading colon)

**Returns**: The resolved URL path string with parameters substituted

**Examples**

```ts
const url = buildUrl(HTTP_ENDPOINTS.GetTask, { id: 'task-42' });
// '/tasks/task-42'
```

### `parseListTasksQuery()`

*Defined in `src/a2a/bindings/http.ts:285`*

```typescript
(query: Record<string, string | undefined>) => ListTasksQueryParams
```

Parse camelCase query parameters for the ListTasks endpoint.

**Parameters**

- `query` — Raw query parameter map from the HTTP request

**Returns**: A typed `ListTasksQueryParams` object with coerced values

**Examples**

```ts
const params = parseListTasksQuery({ contextId: 'ctx-1', limit: '10' });
// { contextId: 'ctx-1', limit: 10 }
```

### `getErrorCodeMapping()`

*Defined in `src/a2a/bindings/index.ts:98`*

```typescript
(errorType: A2AErrorType) => ErrorCodeMapping
```

Get the complete error code mapping for a given A2A error type.

**Parameters**

- `errorType` — The A2A error type name (e.g. `"TaskNotFound"`)

**Returns**: The `ErrorCodeMapping` with JSON-RPC, HTTP, and gRPC codes

**Throws**

- Error if the error type is not a known A2A error type

**Examples**

```ts
const mapping = getErrorCodeMapping('TaskNotFound');
// { jsonRpcCode: -32001, httpStatus: 404, httpTypeUri: '...', grpcStatus: 'NOT_FOUND', grpcCode: 5 }
```

### `parseA2AVersionHeader()`

*Defined in `src/a2a/bindings/index.ts:146`*

```typescript
(headerValue: string | undefined) => string[]
```

Parse the `a2a-version` header into an array of version strings.

**Parameters**

- `headerValue` — The raw `a2a-version` header value, or `undefined` if absent

**Returns**: An array of version strings extracted from the header

**Examples**

```ts
parseA2AVersionHeader('1.0, 2.0'); // ['1.0', '2.0']
parseA2AVersionHeader(undefined);   // []
```

### `negotiateA2AVersion()`

*Defined in `src/a2a/bindings/index.ts:172`*

```typescript
(requestedVersions: string[]) => string | null
```

Negotiate an A2A protocol version from the client's requested versions.

**Parameters**

- `requestedVersions` — Array of version strings requested by the client

**Returns**: The negotiated version string, or `null` if no common version exists

**Examples**

```ts
negotiateA2AVersion(['1.0', '2.0']); // '1.0'
negotiateA2AVersion([]);              // '1.0' (default)
negotiateA2AVersion(['3.0']);          // null
```

### `circuitBreakerMiddleware()`

*Defined in `src/circuit-breaker/index.ts:537`*

```typescript
(config: CircuitBreakerConfig) => (_req: unknown, res: { status: (code: number) => { json: (body: unknown) => void; }; }, next: () => void) => Promise<void>
```

Create an Express middleware that wraps downstream handlers with a circuit breaker.

**Parameters**

- `config` — Circuit breaker configuration for the middleware instance

**Returns**: An Express-compatible middleware function

**Examples**

```typescript
app.use('/external-api', circuitBreakerMiddleware({
  name: 'external-api',
  failureThreshold: 5
}));
```

### `healthCheck()`

*Defined in `src/health/index.ts:116`*

```typescript
(config?: HealthCheckConfig) => (_req: unknown, res: { status: (code: number) => { json: (body: unknown) => void; }; }) => Promise<void>
```

Health check middleware for Express applications.

**Parameters**

- `config` — Optional health check configuration

**Returns**: An Express-compatible middleware function that serves the health endpoint

**Examples**

```typescript
import express from 'express';
import { healthCheck } from '@cleocode/lafs/health';

const app = express();

// Basic health check
app.use('/health', healthCheck());

// Custom health checks
app.use('/health', healthCheck({
  checks: [
    async () => ({
      name: 'database',
      status: await checkDatabase() ? 'ok' : 'error'
    })
  ]
}));
```

### `createDatabaseHealthCheck()`

*Defined in `src/health/index.ts:203`*

```typescript
(config: { checkConnection: () => Promise<boolean>; name?: string; }) => HealthCheckFunction
```

Create a health check function that verifies database connectivity.

**Parameters**

- `config` — Database check configuration
- `` — config.checkConnection - Async function returning `true` if the database is reachable
- `` — config.name - Display name for this check in health output

**Returns**: A `HealthCheckFunction` suitable for use in `HealthCheckConfig.checks`

**Examples**

```typescript
const dbCheck = createDatabaseHealthCheck({
  checkConnection: async () => await db.ping()
});

app.use('/health', healthCheck({
  checks: [dbCheck]
}));
```

### `createExternalServiceHealthCheck()`

*Defined in `src/health/index.ts:248`*

```typescript
(config: { name: string; url: string; timeout?: number; }) => HealthCheckFunction
```

Create a health check function that probes an external HTTP service.

**Parameters**

- `config` — External service check configuration
- `` — config.name - Display name for this check in health output
- `` — config.url - URL to probe for health status
- `` — config.timeout - Request timeout in milliseconds

**Returns**: A `HealthCheckFunction` suitable for use in `HealthCheckConfig.checks`

**Examples**

```typescript
const apiCheck = createExternalServiceHealthCheck({
  name: 'payment-api',
  url: 'https://api.payment.com/health',
  timeout: 5000
});
```

### `livenessProbe()`

*Defined in `src/health/index.ts:297`*

```typescript
() => (_req: unknown, res: { status: (code: number) => { json: (body: unknown) => void; }; }) => void
```

Liveness probe -- a minimal check confirming the process is running.

**Returns**: An Express-compatible middleware function

**Examples**

```typescript
app.get('/health/live', livenessProbe());
```

### `readinessProbe()`

*Defined in `src/health/index.ts:324`*

```typescript
(config?: { checks?: HealthCheckFunction[]; }) => (_req: unknown, res: { status: (code: number) => { json: (body: unknown) => void; }; }) => Promise<void>
```

Readiness probe -- verifies the service can accept traffic.

**Parameters**

- `config` — Optional configuration with custom health check functions

**Returns**: An Express-compatible async middleware function

**Examples**

```typescript
app.get('/health/ready', readinessProbe({
  checks: [dbCheck, cacheCheck]
}));
```

### `wrapMCPResult()`

*Defined in `src/mcpAdapter.ts:275`*

```typescript
(mcpResult: CallToolResult, operation: string, budget?: number) => LAFSEnvelope
```

Wrap an MCP tool result in a LAFS envelope.

**Parameters**

- `mcpResult` — The raw MCP CallToolResult
- `operation` — The operation name (tool name)
- `budget` — Optional token budget for response truncation

**Returns**: LAFS-compliant envelope with `success`, `result`, and `error` fields

**Examples**

```typescript
import { wrapMCPResult } from "@cleocode/lafs";

const envelope = wrapMCPResult(mcpToolResult, "tasks.list", 500);
if (envelope.success) {
  console.log(envelope.result);
}
```

### `createAdapterErrorEnvelope()`

*Defined in `src/mcpAdapter.ts:366`*

```typescript
(message: string, operation: string, category?: LAFSError["category"]) => LAFSEnvelope
```

Create a LAFS error envelope for MCP adapter errors.

**Parameters**

- `message` — Human-readable error message
- `operation` — The operation being performed when the error occurred
- `category` — Error category (defaults to `"INTERNAL"`)

**Returns**: LAFS envelope with `success: false` and the error payload

**Examples**

```typescript
import { createAdapterErrorEnvelope } from "@cleocode/lafs";

const errorEnvelope = createAdapterErrorEnvelope(
  "MCP server unreachable",
  "tasks.list",
  "TRANSIENT",
);
```

### `isTextContent()`

*Defined in `src/mcpAdapter.ts:421`*

```typescript
(content: unknown) => content is TextContent
```

Type guard to check if content is MCP TextContent.

**Parameters**

- `content` — Unknown value to check

**Returns**: `true` if the value is a valid MCP TextContent object

**Examples**

```typescript
if (isTextContent(item)) {
  console.log(item.text);
}
```

### `parseMCPTextContent()`

*Defined in `src/mcpAdapter.ts:447`*

```typescript
(content: TextContent) => unknown
```

Parse MCP text content as JSON if possible.

**Parameters**

- `content` — MCP TextContent to parse

**Returns**: Parsed JSON value, or the raw text string if JSON parsing fails

**Examples**

```typescript
const data = parseMCPTextContent(textContent);
```

### `projectEnvelope()`

*Defined in `src/mviProjection.ts:49`*

```typescript
(envelope: LAFSEnvelope, mviLevel?: MVILevel) => Record<string, unknown>
```

Project an envelope to the declared MVI verbosity level.

**Parameters**

- `envelope` — The full LAFS envelope to project
- `mviLevel` — Override MVI level; falls back to `envelope._meta.mvi`, then `'standard'`

**Returns**: A plain object containing only the fields appropriate for the resolved MVI level

**Examples**

```ts
const minimal = projectEnvelope(envelope, 'minimal');
// minimal contains only: success, _meta (requestId, contextVersion), result/error
```

### `estimateProjectedTokens()`

*Defined in `src/mviProjection.ts:180`*

```typescript
(projected: Record<string, unknown>) => number
```

Estimate token count for a projected envelope.

**Parameters**

- `projected` — The projected envelope object to estimate

**Returns**: The estimated token count based on JSON serialization length

**Examples**

```ts
const tokens = estimateProjectedTokens(projectEnvelope(envelope, 'minimal'));
// tokens ~= Math.ceil(JSON.stringify(projected).length / 4)
```

### `lafsErrorToProblemDetails()`

*Defined in `src/problemDetails.ts:96`*

```typescript
(error: LAFSError, requestId?: string) => LafsProblemDetails
```

Convert a LAFSError to an RFC 9457 Problem Details object.

**Parameters**

- `error` — The LAFS error to convert
- `requestId` — Optional request ID to set as the `instance` field

**Returns**: An RFC 9457-compliant `LafsProblemDetails` object

**Examples**

```typescript
import { lafsErrorToProblemDetails } from "@cleocode/lafs";

const pd = lafsErrorToProblemDetails(envelope.error!, envelope._meta.requestId);
// pd.status === 400, pd.type === "https://lafs.dev/errors/v1/E_VALIDATION"
```

### `gracefulShutdown()`

*Defined in `src/shutdown/index.ts:89`*

```typescript
(server: Server, config?: GracefulShutdownConfig) => void
```

Enable graceful shutdown for an HTTP server.

**Parameters**

- `server` — The Node.js HTTP server to manage
- `config` — Optional shutdown configuration

**Examples**

```typescript
import express from 'express';
import { gracefulShutdown } from '@cleocode/lafs/shutdown';

const app = express();
const server = app.listen(3000);

gracefulShutdown(server, {
  timeout: 30000,
  signals: ['SIGTERM', 'SIGINT'],
  onShutdown: async () => {
    console.log('Shutting down...');
    await db.close();
  }
});
```

### `isShuttingDown()`

*Defined in `src/shutdown/index.ts:206`*

```typescript
() => boolean
```

Check whether a shutdown sequence is currently in progress.

**Returns**: `true` if the server is shutting down, `false` otherwise

**Examples**

```typescript
if (isShuttingDown()) {
  return; // skip expensive work
}
```

### `getShutdownState()`

*Defined in `src/shutdown/index.ts:226`*

```typescript
() => ShutdownState
```

Get a snapshot of the current shutdown state.

**Returns**: A copy of the current `ShutdownState`

**Examples**

```typescript
const state = getShutdownState();
console.log(`Connections: ${state.activeConnections}`);
```

### `forceShutdown()`

*Defined in `src/shutdown/index.ts:245`*

```typescript
(exitCode?: number) => void
```

Terminate the process immediately without waiting for connections to drain.

**Parameters**

- `exitCode` — Process exit code

**Examples**

```typescript
forceShutdown(1);
```

### `shutdownMiddleware()`

*Defined in `src/shutdown/index.ts:265`*

```typescript
() => (_req: unknown, res: { status: (code: number) => { json: (body: unknown) => void; }; }, next: () => void) => void
```

Express middleware that rejects requests with 503 while the server is shutting down.

**Returns**: An Express-compatible middleware function

**Examples**

```typescript
app.use(shutdownMiddleware());
```

### `waitForShutdown()`

*Defined in `src/shutdown/index.ts:297`*

```typescript
() => Promise<void>
```

Wait until a shutdown sequence begins.

**Returns**: A promise that resolves when shutdown has started

**Examples**

```typescript
await waitForShutdown();
```

## Classes

### `TokenEstimator`

*Defined in `src/tokenEstimator.ts:97`*

```typescript
typeof TokenEstimator
```

Character-based token estimator for JSON payloads.

**Examples**

```typescript
const estimator = new TokenEstimator({ charsPerToken: 4 });
const tokens = estimator.estimate({ name: "hello", items: [1, 2, 3] });
```

#### `options`

*Defined in `src/tokenEstimator.ts:99`*

```typescript
Required<TokenEstimatorOptions>
```

Resolved configuration with defaults applied

#### `estimate()`

*Defined in `src/tokenEstimator.ts:117`*

```typescript
(value: unknown) => number
```

Estimate tokens for any JavaScript value. Handles circular references, nested objects, arrays, and Unicode.

**Parameters**

- `value` — Any value to estimate

**Returns**: Estimated token count

#### `estimateJSON()`

*Defined in `src/tokenEstimator.ts:128`*

```typescript
(json: string) => number
```

Estimate tokens from a JSON string. More efficient if you already have the JSON string.

**Parameters**

- `json` — JSON string to estimate

**Returns**: Estimated token count

#### `estimateWithTracking()`

*Defined in `src/tokenEstimator.ts:144`*

```typescript
(value: unknown, seen: WeakSet<object>, depth: number) => number
```

Internal recursive estimation with circular reference tracking.

**Parameters**

- `value` — Value to estimate
- `seen` — WeakSet tracking visited objects for circular reference detection
- `depth` — Current recursion depth

**Returns**: Estimated token count for this value

#### `estimateArray()`

*Defined in `src/tokenEstimator.ts:217`*

```typescript
(arr: unknown[], seen: WeakSet<object>, depth: number) => number
```

Estimate tokens for an array.

**Parameters**

- `arr` — Array to estimate
- `seen` — WeakSet tracking visited objects
- `depth` — Current recursion depth

**Returns**: Estimated token count including brackets and separators

#### `estimateObject()`

*Defined in `src/tokenEstimator.ts:242`*

```typescript
(obj: Record<string, unknown>, seen: WeakSet<object>, depth: number) => number
```

Estimate tokens for a plain object.

**Parameters**

- `obj` — Object to estimate
- `seen` — WeakSet tracking visited objects
- `depth` — Current recursion depth

**Returns**: Estimated token count including braces, keys, colons, and separators

#### `canSerialize()`

*Defined in `src/tokenEstimator.ts:280`*

```typescript
(value: unknown) => boolean
```

Check if a value can be safely serialized (no circular refs).

**Parameters**

- `value` — Value to check

**Returns**: `true` if `JSON.stringify` succeeds without throwing

#### `safeStringify()`

*Defined in `src/tokenEstimator.ts:295`*

```typescript
(value: unknown) => string
```

Serialize a value to JSON with circular reference handling.

**Parameters**

- `value` — Value to serialize

**Returns**: JSON string with circular references replaced by `"[Circular]"`

#### `safeCopy()`

*Defined in `src/tokenEstimator.ts:315`*

```typescript
<T>(value: T) => T
```

Create a deep copy of a value with circular refs replaced by `"[Circular]"`.

**Parameters**

- `value` — Value to copy

**Returns**: Deep clone with all circular references replaced

### `LAFSFlagError`

*Defined in `src/flagSemantics.ts:42`*

```typescript
typeof LAFSFlagError
```

Error thrown when LAFS flag validation fails.

#### `code`

*Defined in `src/flagSemantics.ts:44`*

```typescript
string
```

The LAFS error code (e.g. `'E_FORMAT_CONFLICT'`).

#### `category`

*Defined in `src/flagSemantics.ts:46`*

```typescript
LAFSErrorCategory
```

The error category resolved from the error registry.

#### `retryable`

*Defined in `src/flagSemantics.ts:48`*

```typescript
boolean
```

Whether the operation that produced this error can be retried.

#### `retryAfterMs`

*Defined in `src/flagSemantics.ts:50`*

```typescript
number | null
```

Milliseconds to wait before retrying, or `null` if not applicable.

#### `details`

*Defined in `src/flagSemantics.ts:52`*

```typescript
Record<string, unknown>
```

Additional structured details about the error.

### `ComplianceError`

*Defined in `src/compliance.ts:117`*

```typescript
typeof ComplianceError
```

Error thrown when `assertCompliance` or `withCompliance` detects failures.

**Examples**

```ts
try {
  assertCompliance(envelope);
} catch (err) {
  if (err instanceof ComplianceError) {
    console.log(err.issues);
  }
}
```

#### `issues`

*Defined in `src/compliance.ts:119`*

```typescript
ComplianceIssue[]
```

The structured list of compliance issues that caused this error.

### `ExtensionSupportRequiredError`

*Defined in `src/a2a/extensions.ts:366`*

```typescript
typeof ExtensionSupportRequiredError
```

Error thrown when required A2A extensions are not supported by the client.

#### `code`

*Defined in `src/a2a/extensions.ts:368`*

```typescript
-32008
```

JSON-RPC error code for extension support required

#### `httpStatus`

*Defined in `src/a2a/extensions.ts:370`*

```typescript
400
```

HTTP status code returned for this error

#### `grpcStatus`

*Defined in `src/a2a/extensions.ts:372`*

```typescript
"FAILED_PRECONDITION"
```

gRPC status code equivalent

#### `missingExtensions`

*Defined in `src/a2a/extensions.ts:374`*

```typescript
string[]
```

URIs of the required extensions that the client did not provide

#### `toJSONRPCError()`

*Defined in `src/a2a/extensions.ts:395`*

```typescript
() => { code: number; message: string; data: Record<string, unknown>; }
```

Convert to JSON-RPC error object.

**Returns**: JSON-RPC error with code, message, and data containing missing extension URIs

#### `toProblemDetails()`

*Defined in `src/a2a/extensions.ts:412`*

```typescript
() => Record<string, unknown> & { agentAction: string; }
```

Convert to RFC 9457 Problem Details object with agent-actionable fields.

**Returns**: Problem Details object with `agentAction` field for agent-driven retry

#### `toLafsError()`

*Defined in `src/a2a/extensions.ts:433`*

```typescript
() => { code: string; message: string; category: "CONTRACT"; retryable: boolean; retryAfterMs: null; details: Record<string, unknown>; }
```

Convert to a LAFSError-compatible object.

**Returns**: LAFS-compatible error object with code, message, category, and details

### `LafsError`

*Defined in `src/envelope.ts:379`*

```typescript
typeof LafsError
```

Error subclass that carries the full `LAFSError` payload.

**Examples**

```ts
try {
  parseLafsResponse(envelope);
} catch (err) {
  if (err instanceof LafsError) {
    console.log(err.code, err.agentAction);
  }
}
```

#### `code`

*Defined in `src/envelope.ts:381`*

```typescript
string
```

Stable, machine-readable error code.

#### `category`

*Defined in `src/envelope.ts:383`*

```typescript
LAFSErrorCategory
```

High-level classification of the error.

#### `retryable`

*Defined in `src/envelope.ts:385`*

```typescript
boolean
```

Whether the operation can be retried without modification.

#### `retryAfterMs`

*Defined in `src/envelope.ts:387`*

```typescript
number | null
```

Suggested delay in milliseconds before retrying, or `null` if not applicable.

#### `details`

*Defined in `src/envelope.ts:389`*

```typescript
Record<string, unknown>
```

Arbitrary key-value pairs with additional context about the error.

#### `registered`

*Defined in `src/envelope.ts:391`*

```typescript
boolean
```

Whether this error code exists in the canonical error registry.

#### `agentAction`

*Defined in `src/envelope.ts:397`*

```typescript
LAFSAgentAction | undefined
```

Recommended action for the consuming agent.

#### `escalationRequired`

*Defined in `src/envelope.ts:403`*

```typescript
boolean | undefined
```

Whether the error requires human or higher-privilege intervention.

#### `suggestedAction`

*Defined in `src/envelope.ts:409`*

```typescript
string | undefined
```

Free-text description of a suggested recovery action.

#### `docUrl`

*Defined in `src/envelope.ts:415`*

```typescript
string | undefined
```

URL pointing to documentation about this error code.

### `LafsA2AResult`

*Defined in `src/a2a/bridge.ts:151`*

```typescript
typeof LafsA2AResult
```

Wrapper for A2A responses with LAFS envelope support.

#### `getA2AResult()`

*Defined in `src/a2a/bridge.ts:170`*

```typescript
() => SendMessageResponse
```

Get the raw A2A response.

**Returns**: The underlying SendMessageResponse object

#### `isError()`

*Defined in `src/a2a/bridge.ts:179`*

```typescript
() => boolean
```

Check if the result is an error response.

**Returns**: True if the response contains a JSON-RPC error

#### `getError()`

*Defined in `src/a2a/bridge.ts:188`*

```typescript
() => JSONRPCErrorResponse | null
```

Get error details if the result is an error.

**Returns**: The JSON-RPC error response, or null if the result is a success

#### `getSuccess()`

*Defined in `src/a2a/bridge.ts:200`*

```typescript
() => SendMessageSuccessResponse | null
```

Get the success result.

**Returns**: The success response, or null if the result is an error

#### `getTask()`

*Defined in `src/a2a/bridge.ts:216`*

```typescript
() => Task | null
```

Extract a Task from the response (if present).

**Returns**: The extracted Task, or null if not present or result is an error

#### `getMessage()`

*Defined in `src/a2a/bridge.ts:240`*

```typescript
() => Message | null
```

Extract a Message from the response (if present).

**Returns**: The extracted Message, or null if not present or result is an error

#### `hasLafsEnvelope()`

*Defined in `src/a2a/bridge.ts:259`*

```typescript
() => boolean
```

Check if the response contains a LAFS envelope.

**Returns**: True if a LAFS envelope was found in the task's artifacts

#### `getLafsEnvelope()`

*Defined in `src/a2a/bridge.ts:273`*

```typescript
() => LAFSEnvelope | null
```

Extract a LAFS envelope from A2A artifact.

**Returns**: The first LAFS envelope found, or null if none present

#### `getTokenEstimate()`

*Defined in `src/a2a/bridge.ts:301`*

```typescript
() => { estimated: number; budget?: number; truncated?: boolean; } | null
```

Get token estimate from LAFS envelope.

**Returns**: Token estimate object, or null if no envelope or no estimate present

#### `getTaskStatus()`

*Defined in `src/a2a/bridge.ts:317`*

```typescript
() => TaskStatus | null
```

Get the task status.

**Returns**: The task's current status, or null if no task is present

#### `getTaskState()`

*Defined in `src/a2a/bridge.ts:326`*

```typescript
() => TaskState | null
```

Get the task state.

**Returns**: The task's current state string, or null if no task is present

#### `isTerminal()`

*Defined in `src/a2a/bridge.ts:338`*

```typescript
() => boolean
```

Check if the task is in a terminal state.

**Returns**: True if the task is in a terminal state, false otherwise or if no task is present

#### `isInputRequired()`

*Defined in `src/a2a/bridge.ts:351`*

```typescript
() => boolean
```

Check if the task requires user input.

**Returns**: True if the task state is `input-required`

#### `isAuthRequired()`

*Defined in `src/a2a/bridge.ts:360`*

```typescript
() => boolean
```

Check if the task requires authentication.

**Returns**: True if the task state is `auth-required`

#### `getArtifacts()`

*Defined in `src/a2a/bridge.ts:369`*

```typescript
() => Artifact[]
```

Get all artifacts from the task.

**Returns**: Array of task artifacts, or empty array if no task or no artifacts

#### `isDataPart()`

*Defined in `src/a2a/bridge.ts:374`*

```typescript
(part: Part) => part is DataPart
```

Type guard: checks whether a Part is a DataPart by inspecting its `kind` field.

#### `isLafsEnvelope()`

*Defined in `src/a2a/bridge.ts:379`*

```typescript
(data: unknown) => boolean
```

Heuristic check: returns true if the data object looks like a LAFS envelope (has `$schema`, `_meta`, `success`).

### `InvalidStateTransitionError`

*Defined in `src/a2a/task-lifecycle.ts:144`*

```typescript
typeof InvalidStateTransitionError
```

Thrown when attempting an invalid state transition.

#### `taskId`

*Defined in `src/a2a/task-lifecycle.ts:146`*

```typescript
string
```

ID of the task that failed the transition

#### `fromState`

*Defined in `src/a2a/task-lifecycle.ts:148`*

```typescript
TaskState
```

State the task was in when the transition was attempted

#### `toState`

*Defined in `src/a2a/task-lifecycle.ts:150`*

```typescript
TaskState
```

State the task was being transitioned to

### `TaskImmutabilityError`

*Defined in `src/a2a/task-lifecycle.ts:175`*

```typescript
typeof TaskImmutabilityError
```

Thrown when attempting to modify a task in a terminal state.

#### `taskId`

*Defined in `src/a2a/task-lifecycle.ts:177`*

```typescript
string
```

ID of the task that cannot be modified

#### `terminalState`

*Defined in `src/a2a/task-lifecycle.ts:179`*

```typescript
TaskState
```

Terminal state the task is in

### `TaskNotFoundError`

*Defined in `src/a2a/task-lifecycle.ts:202`*

```typescript
typeof TaskNotFoundError
```

Thrown when a task is not found.

#### `taskId`

*Defined in `src/a2a/task-lifecycle.ts:204`*

```typescript
string
```

ID of the task that was not found

### `TaskRefinementError`

*Defined in `src/a2a/task-lifecycle.ts:225`*

```typescript
typeof TaskRefinementError
```

Thrown when a refinement/follow-up task references invalid parent tasks.

#### `referenceTaskIds`

*Defined in `src/a2a/task-lifecycle.ts:227`*

```typescript
string[]
```

IDs of the referenced tasks that caused the error

### `TaskManager`

*Defined in `src/a2a/task-lifecycle.ts:325`*

```typescript
typeof TaskManager
```

In-memory task manager implementing A2A task lifecycle.

#### `tasks`

*Defined in `src/a2a/task-lifecycle.ts:327`*

```typescript
Map<string, Task>
```

Map of task ID to Task object

#### `contextIndex`

*Defined in `src/a2a/task-lifecycle.ts:329`*

```typescript
Map<string, Set<string>>
```

Index mapping context ID to set of task IDs

#### `createTask()`

*Defined in `src/a2a/task-lifecycle.ts:348`*

```typescript
(options?: CreateTaskOptions) => Task
```

Create a new task in the submitted state.

**Parameters**

- `options` — Task creation options including context, metadata, and references

**Returns**: Deep clone of the newly created task

**Examples**

```typescript
const manager = new TaskManager();
const task = manager.createTask({ contextId: 'ctx-1', metadata: { source: 'api' } });
```

#### `createRefinedTask()`

*Defined in `src/a2a/task-lifecycle.ts:406`*

```typescript
(referenceTaskIds: string[], options?: Omit<CreateTaskOptions, "referenceTaskIds">) => Task
```

Create a refinement/follow-up task referencing existing task(s).

**Parameters**

- `referenceTaskIds` — IDs of parent tasks to reference
- `options` — Additional task creation options (excluding referenceTaskIds)

**Returns**: Deep clone of the newly created refinement task

**Examples**

```typescript
const refined = manager.createRefinedTask(['task-1'], { parallelFollowUp: true });
```

#### `getTask()`

*Defined in `src/a2a/task-lifecycle.ts:433`*

```typescript
(taskId: string) => Task
```

Get a task by ID.

**Parameters**

- `taskId` — ID of the task to retrieve

**Returns**: Deep clone of the requested task

**Throws**

- `TaskNotFoundError` if no task with the given ID exists

**Examples**

```typescript
const task = manager.getTask('some-task-id');
console.log(task.status.state);
```

#### `listTasks()`

*Defined in `src/a2a/task-lifecycle.ts:460`*

```typescript
(options?: ListTasksOptions) => ListTasksResult
```

List tasks with optional filtering and pagination.

**Parameters**

- `options` — Filtering and pagination options

**Returns**: Paginated result with tasks and optional next page token

**Examples**

```typescript
const result = manager.listTasks({ contextId: 'ctx-1', state: 'working', limit: 10 });
for (const task of result.tasks) {
  console.log(task.id, task.status.state);
}
```

#### `updateTaskStatus()`

*Defined in `src/a2a/task-lifecycle.ts:520`*

```typescript
(taskId: string, state: TaskState, message?: Message) => Task
```

Update task status. Enforces valid transitions and terminal state immutability.

**Parameters**

- `taskId` — ID of the task to update
- `state` — New state to transition to
- `message` — Optional message to attach to the status update

**Returns**: Deep clone of the updated task

**Throws**

- `InvalidStateTransitionError` if the transition is not valid
- `TaskImmutabilityError` if the task is in a terminal state
- `TaskNotFoundError` if the task does not exist

**Examples**

```typescript
const updated = manager.updateTaskStatus('task-1', 'working');
```

#### `addArtifact()`

*Defined in `src/a2a/task-lifecycle.ts:565`*

```typescript
(taskId: string, artifact: Artifact) => Task
```

Add an artifact to a task.

**Parameters**

- `taskId` — ID of the task to add the artifact to
- `artifact` — Artifact to add

**Returns**: Deep clone of the updated task

**Throws**

- `TaskImmutabilityError` if the task is in a terminal state
- `TaskNotFoundError` if the task does not exist

**Examples**

```typescript
const artifact = createLafsArtifact(envelope);
manager.addArtifact('task-1', artifact);
```

#### `addHistory()`

*Defined in `src/a2a/task-lifecycle.ts:601`*

```typescript
(taskId: string, message: Message) => Task
```

Add a message to task history.

**Parameters**

- `taskId` — ID of the task to add the message to
- `message` — Message to add to history

**Returns**: Deep clone of the updated task

**Throws**

- `TaskImmutabilityError` if the task is in a terminal state
- `TaskNotFoundError` if the task does not exist

**Examples**

```typescript
manager.addHistory('task-1', { role: 'agent', parts: [{ kind: 'text', text: 'Done' }] });
```

#### `cancelTask()`

*Defined in `src/a2a/task-lifecycle.ts:635`*

```typescript
(taskId: string) => Task
```

Cancel a task by transitioning to canceled state.

**Parameters**

- `taskId` — ID of the task to cancel

**Returns**: Deep clone of the canceled task

**Throws**

- `InvalidStateTransitionError` if cancellation is not valid from the current state
- `TaskImmutabilityError` if the task is already in a terminal state

**Examples**

```typescript
const canceled = manager.cancelTask('task-1');
```

#### `getTasksByContext()`

*Defined in `src/a2a/task-lifecycle.ts:653`*

```typescript
(contextId: string) => Task[]
```

Get all tasks in a given context.

**Parameters**

- `contextId` — Context ID to look up

**Returns**: Array of deep-cloned tasks in the context, or empty array if none

**Examples**

```typescript
const tasks = manager.getTasksByContext('ctx-1');
```

#### `isTerminal()`

*Defined in `src/a2a/task-lifecycle.ts:676`*

```typescript
(taskId: string) => boolean
```

Check if a task is in a terminal state.

**Parameters**

- `taskId` — ID of the task to check

**Returns**: True if the task is in a terminal state

**Throws**

- `TaskNotFoundError` if the task does not exist

**Examples**

```typescript
if (manager.isTerminal('task-1')) {
  console.log('Task is finished');
}
```

#### `resolveContextForReferenceTasks()`

*Defined in `src/a2a/task-lifecycle.ts:685`*

```typescript
(referenceTaskIds: string[] | undefined) => string | undefined
```

Derive a contextId from the first referenced task, if any reference tasks are provided.

#### `validateReferenceTasks()`

*Defined in `src/a2a/task-lifecycle.ts:700`*

```typescript
(referenceTaskIds: string[], contextId: string) => void
```

Validate that all referenced tasks exist and share the same contextId.

### `TaskEventBus`

*Defined in `src/a2a/streaming.ts:50`*

```typescript
typeof TaskEventBus
```

In-memory event bus for task lifecycle streaming events.

#### `history`

*Defined in `src/a2a/streaming.ts:52`*

```typescript
Map<string, TaskStreamEvent[]>
```

Map of task ID to ordered event history

#### `listeners`

*Defined in `src/a2a/streaming.ts:54`*

```typescript
Map<string, Set<StreamListener>>
```

Map of task ID to active listener callbacks

#### `publishStatusUpdate()`

*Defined in `src/a2a/streaming.ts:69`*

```typescript
(event: TaskStatusUpdateEvent) => void
```

Publish a task status update event.

**Parameters**

- `event` — Status update event to publish

**Examples**

```typescript
bus.publishStatusUpdate({ kind: 'status-update', taskId: 'task-1', status: { state: 'working', timestamp: new Date().toISOString() }, final: false });
```

#### `publishArtifactUpdate()`

*Defined in `src/a2a/streaming.ts:86`*

```typescript
(event: TaskArtifactUpdateEvent) => void
```

Publish a task artifact update event.

**Parameters**

- `event` — Artifact update event to publish

**Examples**

```typescript
bus.publishArtifactUpdate({ kind: 'artifact-update', taskId: 'task-1', artifact: myArtifact });
```

#### `publish()`

*Defined in `src/a2a/streaming.ts:104`*

```typescript
(event: TaskStreamEvent) => void
```

Publish a task stream event to all listeners and history.

**Parameters**

- `event` — Task stream event to publish

**Examples**

```typescript
bus.publish(statusUpdateEvent);
```

#### `subscribe()`

*Defined in `src/a2a/streaming.ts:137`*

```typescript
(taskId: string, listener: StreamListener) => () => void
```

Subscribe to events for a specific task.

**Parameters**

- `taskId` — ID of the task to subscribe to
- `listener` — Callback invoked for each event

**Returns**: Unsubscribe function that removes this listener

**Examples**

```typescript
const unsubscribe = bus.subscribe('task-1', (event) => {
  console.log('Event:', event);
});
// Later: unsubscribe();
```

#### `getHistory()`

*Defined in `src/a2a/streaming.ts:170`*

```typescript
(taskId: string) => TaskStreamEvent[]
```

Get the full event history for a task.

**Parameters**

- `taskId` — ID of the task to retrieve history for

**Returns**: Array of past events for the task, or empty array if none

**Examples**

```typescript
const events = bus.getHistory('task-1');
```

### `PushNotificationConfigStore`

*Defined in `src/a2a/streaming.ts:264`*

```typescript
typeof PushNotificationConfigStore
```

In-memory manager for async push-notification configs.

#### `configs`

*Defined in `src/a2a/streaming.ts:266`*

```typescript
Map<string, Map<string, PushNotificationConfig>>
```

Nested map of task ID to config ID to push notification config

#### `set()`

*Defined in `src/a2a/streaming.ts:283`*

```typescript
(taskId: string, configId: string, config: PushNotificationConfig) => void
```

Store a push-notification config for a task.

**Parameters**

- `taskId` — ID of the task the config applies to
- `configId` — Unique identifier for this config within the task
- `config` — Push notification configuration to store

**Examples**

```typescript
store.set('task-1', 'webhook-1', { url: 'https://example.com/hook', token: 'abc' });
```

#### `get()`

*Defined in `src/a2a/streaming.ts:304`*

```typescript
(taskId: string, configId: string) => PushNotificationConfig | undefined
```

Retrieve a push-notification config by task and config ID.

**Parameters**

- `taskId` — ID of the task
- `configId` — ID of the config to retrieve

**Returns**: The push notification config, or undefined if not found

**Examples**

```typescript
const config = store.get('task-1', 'webhook-1');
```

#### `list()`

*Defined in `src/a2a/streaming.ts:319`*

```typescript
(taskId: string) => PushNotificationConfig[]
```

List all push-notification configs for a task.

**Parameters**

- `taskId` — ID of the task to list configs for

**Returns**: Array of push notification configs, or empty array if none

**Examples**

```typescript
const configs = store.list('task-1');
```

#### `delete()`

*Defined in `src/a2a/streaming.ts:338`*

```typescript
(taskId: string, configId: string) => boolean
```

Delete a push-notification config.

**Parameters**

- `taskId` — ID of the task
- `configId` — ID of the config to delete

**Returns**: True if the config was found and removed, false otherwise

**Examples**

```typescript
const removed = store.delete('task-1', 'webhook-1');
```

### `PushNotificationDispatcher`

*Defined in `src/a2a/streaming.ts:395`*

```typescript
typeof PushNotificationDispatcher
```

Deliver task updates to registered push-notification webhooks.

#### `dispatch()`

*Defined in `src/a2a/streaming.ts:432`*

```typescript
(taskId: string, event: TaskStreamEvent) => Promise<PushNotificationDeliveryResult[]>
```

Dispatch a task event to all registered webhooks for the task.

**Parameters**

- `taskId` — ID of the task whose webhooks to dispatch to
- `event` — Task stream event to deliver

**Returns**: Array of delivery results, one per registered config

**Examples**

```typescript
const results = await dispatcher.dispatch('task-1', statusUpdateEvent);
for (const r of results) {
  if (!r.ok) console.error(`Failed to deliver to ${r.configId}: ${r.error}`);
}
```

#### `buildHeaders()`

*Defined in `src/a2a/streaming.ts:485`*

```typescript
(config: PushNotificationConfig) => Record<string, string>
```

Build HTTP headers for push notification delivery including auth tokens.

### `TaskArtifactAssembler`

*Defined in `src/a2a/streaming.ts:516`*

```typescript
typeof TaskArtifactAssembler
```

Applies append/lastChunk artifact deltas into task-local snapshots.

#### `artifacts`

*Defined in `src/a2a/streaming.ts:518`*

```typescript
Map<string, Map<string, Artifact>>
```

Map of task ID to map of artifact ID to assembled artifact snapshot

#### `applyUpdate()`

*Defined in `src/a2a/streaming.ts:537`*

```typescript
(event: TaskArtifactUpdateEvent) => Artifact
```

Apply an artifact update event to the assembled snapshot.

**Parameters**

- `event` — Artifact update event containing the delta

**Returns**: The merged artifact snapshot after applying the update

**Examples**

```typescript
const assembler = new TaskArtifactAssembler();
const merged = assembler.applyUpdate(artifactUpdateEvent);
```

#### `get()`

*Defined in `src/a2a/streaming.ts:569`*

```typescript
(taskId: string, artifactId: string) => Artifact | undefined
```

Get a specific assembled artifact by task and artifact ID.

**Parameters**

- `taskId` — ID of the task
- `artifactId` — ID of the artifact to retrieve

**Returns**: The assembled artifact snapshot, or undefined if not found

**Examples**

```typescript
const artifact = assembler.get('task-1', 'art-1');
```

#### `list()`

*Defined in `src/a2a/streaming.ts:584`*

```typescript
(taskId: string) => Artifact[]
```

List all assembled artifacts for a task.

**Parameters**

- `taskId` — ID of the task to list artifacts for

**Returns**: Array of assembled artifact snapshots, or empty array if none

**Examples**

```typescript
const artifacts = assembler.list('task-1');
```

#### `mergeArtifact()`

*Defined in `src/a2a/streaming.ts:589`*

```typescript
(prior: Artifact | undefined, event: TaskArtifactUpdateEvent) => Artifact
```

Merge a new artifact update event into an existing artifact snapshot, handling append semantics.

#### `withLastChunk()`

*Defined in `src/a2a/streaming.ts:616`*

```typescript
(metadata: Record<string, unknown> | undefined, lastChunk: boolean) => Record<string, unknown>
```

Inject the `a2a:last_chunk` marker into artifact metadata.

### `CircuitBreakerError`

*Defined in `src/circuit-breaker/index.ts:92`*

```typescript
typeof CircuitBreakerError
```

Error thrown when a circuit breaker rejects a call.

**Examples**

```typescript
try {
  await breaker.execute(() => fetch('/api'));
} catch (err) {
  if (err instanceof CircuitBreakerError) {
    console.log('Circuit open, using fallback');
  }
}
```

### `CircuitBreaker`

*Defined in `src/circuit-breaker/index.ts:134`*

```typescript
typeof CircuitBreaker
```

Circuit breaker for protecting against cascading failures.

**Examples**

```typescript
import { CircuitBreaker } from '@cleocode/lafs/circuit-breaker';

const breaker = new CircuitBreaker({
  name: 'external-api',
  failureThreshold: 5,
  resetTimeout: 30000
});

try {
  const result = await breaker.execute(async () => {
    return await externalApi.call();
  });
} catch (error) {
  if (error instanceof CircuitBreakerError) {
    console.log('Circuit breaker is open');
  }
}
```

#### `state`

*Defined in `src/circuit-breaker/index.ts:136`*

```typescript
CircuitState
```

Current circuit state.

#### `failures`

*Defined in `src/circuit-breaker/index.ts:139`*

```typescript
number
```

Total failure count since last reset.

#### `successes`

*Defined in `src/circuit-breaker/index.ts:142`*

```typescript
number
```

Total success count since last reset.

#### `lastFailureTime`

*Defined in `src/circuit-breaker/index.ts:145`*

```typescript
Date | undefined
```

Timestamp of the most recent failure.

#### `consecutiveSuccesses`

*Defined in `src/circuit-breaker/index.ts:148`*

```typescript
number
```

Consecutive successes since the last failure.

#### `totalCalls`

*Defined in `src/circuit-breaker/index.ts:151`*

```typescript
number
```

Lifetime call count.

#### `halfOpenCalls`

*Defined in `src/circuit-breaker/index.ts:154`*

```typescript
number
```

Number of calls made while in the HALF\_OPEN state.

#### `resetTimer`

*Defined in `src/circuit-breaker/index.ts:157`*

```typescript
NodeJS.Timeout | undefined
```

Timer handle for the scheduled OPEN-to-HALF\_OPEN transition.

#### `execute()`

*Defined in `src/circuit-breaker/index.ts:194`*

```typescript
<T>(fn: () => Promise<T>) => Promise<T>
```

Execute a function with circuit breaker protection.

**Parameters**

- `fn` — Async function to execute under circuit breaker protection

**Returns**: The result of invoking `fn`

**Examples**

```typescript
const result = await breaker.execute(async () => {
  return await fetch('https://api.example.com/data');
});
```

#### `getState()`

*Defined in `src/circuit-breaker/index.ts:241`*

```typescript
() => CircuitState
```

Get the current circuit breaker state.

**Returns**: The current `CircuitState`

**Examples**

```typescript
if (breaker.getState() === 'OPEN') {
  console.log('Circuit is open, requests will be rejected');
}
```

#### `getMetrics()`

*Defined in `src/circuit-breaker/index.ts:261`*

```typescript
() => CircuitBreakerMetrics
```

Get a snapshot of the circuit breaker's runtime metrics.

**Returns**: A `CircuitBreakerMetrics` snapshot

**Examples**

```typescript
const metrics = breaker.getMetrics();
console.log(`State: ${metrics.state}, Failures: ${metrics.failures}`);
```

#### `forceOpen()`

*Defined in `src/circuit-breaker/index.ts:286`*

```typescript
() => void
```

Manually open the circuit breaker, rejecting all subsequent calls.

**Examples**

```typescript
breaker.forceOpen();
console.log(breaker.getState()); // 'OPEN'
```

#### `forceClose()`

*Defined in `src/circuit-breaker/index.ts:304`*

```typescript
() => void
```

Manually close the circuit breaker and reset all counters.

**Examples**

```typescript
breaker.forceClose();
console.log(breaker.getState()); // 'CLOSED'
```

#### `onSuccess()`

*Defined in `src/circuit-breaker/index.ts:310`*

```typescript
() => void
```

Records a successful call and may transition from HALF\_OPEN to CLOSED.

#### `onFailure()`

*Defined in `src/circuit-breaker/index.ts:323`*

```typescript
() => void
```

Records a failed call and may trip the circuit to OPEN.

#### `transitionTo()`

*Defined in `src/circuit-breaker/index.ts:340`*

```typescript
(newState: CircuitState) => void
```

Transitions the circuit to the given state, resetting HALF\_OPEN call count when entering HALF\_OPEN.

#### `shouldAttemptReset()`

*Defined in `src/circuit-breaker/index.ts:350`*

```typescript
() => boolean
```

Returns `true` if enough time has elapsed since the last failure to attempt a reset.

#### `scheduleReset()`

*Defined in `src/circuit-breaker/index.ts:358`*

```typescript
() => void
```

Schedules a timer to transition from OPEN to HALF\_OPEN after the configured reset timeout.

#### `reset()`

*Defined in `src/circuit-breaker/index.ts:371`*

```typescript
() => void
```

Resets all failure/success counters and clears the pending reset timer.

### `CircuitBreakerRegistry`

*Defined in `src/circuit-breaker/index.ts:403`*

```typescript
typeof CircuitBreakerRegistry
```

Registry for managing multiple named circuit breakers.

**Examples**

```typescript
const registry = new CircuitBreakerRegistry();

registry.add('payment-api', {
  failureThreshold: 3,
  resetTimeout: 60000
});

const paymentBreaker = registry.get('payment-api');
```

#### `breakers`

*Defined in `src/circuit-breaker/index.ts:405`*

```typescript
Map<string, CircuitBreaker>
```

Internal map of circuit breaker name to instance.

#### `add()`

*Defined in `src/circuit-breaker/index.ts:423`*

```typescript
(name: string, config: Omit<CircuitBreakerConfig, "name">) => CircuitBreaker
```

Register a new circuit breaker with the given name and configuration.

**Parameters**

- `name` — Unique name for the circuit breaker
- `config` — Configuration options (name is set automatically)

**Returns**: The newly created `CircuitBreaker`

**Examples**

```typescript
const breaker = registry.add('user-service', { failureThreshold: 3 });
```

#### `get()`

*Defined in `src/circuit-breaker/index.ts:446`*

```typescript
(name: string) => CircuitBreaker | undefined
```

Retrieve a circuit breaker by name.

**Parameters**

- `name` — Name of the circuit breaker to look up

**Returns**: The matching `CircuitBreaker`, or `undefined` if not found

**Examples**

```typescript
const breaker = registry.get('payment-api');
if (breaker) {
  await breaker.execute(() => callPaymentApi());
}
```

#### `getOrCreate()`

*Defined in `src/circuit-breaker/index.ts:466`*

```typescript
(name: string, config: Omit<CircuitBreakerConfig, "name">) => CircuitBreaker
```

Retrieve an existing circuit breaker or create one if it does not exist.

**Parameters**

- `name` — Name of the circuit breaker
- `config` — Configuration to use if a new breaker must be created

**Returns**: The existing or newly created `CircuitBreaker`

**Examples**

```typescript
const breaker = registry.getOrCreate('cache-api', { failureThreshold: 10 });
```

#### `getAllMetrics()`

*Defined in `src/circuit-breaker/index.ts:491`*

```typescript
() => Record<string, CircuitBreakerMetrics>
```

Collect metrics from all registered circuit breakers.

**Returns**: A record mapping breaker names to their current metrics

**Examples**

```typescript
const allMetrics = registry.getAllMetrics();
for (const [name, metrics] of Object.entries(allMetrics)) {
  console.log(`${name}: ${metrics.state}`);
}
```

#### `resetAll()`

*Defined in `src/circuit-breaker/index.ts:511`*

```typescript
() => void
```

Force-close all registered circuit breakers, resetting their counters.

**Examples**

```typescript
registry.resetAll();
```

## Interfaces

### `TokenEstimatorOptions`

*Defined in `src/tokenEstimator.ts:24`*

```typescript
TokenEstimatorOptions
```

Configuration options for the token estimator.

**Examples**

```typescript
const opts: TokenEstimatorOptions = {
  charsPerToken: 3.5,
  maxDepth: 50,
};
```

#### `charsPerToken`

*Defined in `src/tokenEstimator.ts:29`*

```typescript
number | undefined
```

Characters per token ratio.

#### `maxDepth`

*Defined in `src/tokenEstimator.ts:35`*

```typescript
number | undefined
```

Maximum depth to traverse for circular reference detection.

#### `maxStringLength`

*Defined in `src/tokenEstimator.ts:41`*

```typescript
number | undefined
```

Maximum string length to process for Unicode grapheme counting.

### `Warning`

*Defined in `src/types.ts:39`*

```typescript
Warning
```

A non-fatal warning attached to a LAFS envelope's `_meta.warnings` array.

#### `code`

*Defined in `src/types.ts:41`*

```typescript
string
```

Machine-readable warning code (e.g. `"W_DEPRECATED_FIELD"`).

#### `message`

*Defined in `src/types.ts:43`*

```typescript
string
```

Human-readable description of the warning.

#### `deprecated`

*Defined in `src/types.ts:49`*

```typescript
string | undefined
```

Name of the deprecated field, parameter, or feature.

#### `replacement`

*Defined in `src/types.ts:55`*

```typescript
string | undefined
```

Recommended replacement for the deprecated item.

#### `removeBy`

*Defined in `src/types.ts:61`*

```typescript
string | undefined
```

Semver version or ISO date after which the deprecated item will be removed.

### `LAFSMeta`

*Defined in `src/types.ts:127`*

```typescript
LAFSMeta
```

Metadata block (`_meta`) embedded in every LAFS envelope.

#### `specVersion`

*Defined in `src/types.ts:129`*

```typescript
string
```

Semantic version of the LAFS protocol specification (e.g. `"1.0.0"`).

#### `schemaVersion`

*Defined in `src/types.ts:131`*

```typescript
string
```

Semantic version of the JSON Schema used for this envelope (e.g. `"1.0.0"`).

#### `timestamp`

*Defined in `src/types.ts:133`*

```typescript
string
```

ISO 8601 timestamp of when the envelope was created.

#### `operation`

*Defined in `src/types.ts:135`*

```typescript
string
```

Dot-delimited operation identifier (e.g. `"tasks.list"`).

#### `requestId`

*Defined in `src/types.ts:137`*

```typescript
string
```

Unique identifier for correlating this request/response pair.

#### `transport`

*Defined in `src/types.ts:139`*

```typescript
LAFSTransport
```

Transport protocol through which this envelope is delivered.

#### `strict`

*Defined in `src/types.ts:141`*

```typescript
boolean
```

When `true`, schema validation rejects unknown properties.

#### `mvi`

*Defined in `src/types.ts:143`*

```typescript
MVILevel
```

Minimum Viable Information level controlling field inclusion.

#### `contextVersion`

*Defined in `src/types.ts:145`*

```typescript
number
```

Monotonically increasing version of the agent's context ledger.

#### `sessionId`

*Defined in `src/types.ts:151`*

```typescript
string | undefined
```

Session identifier for correlating multi-step agent workflows.

#### `warnings`

*Defined in `src/types.ts:157`*

```typescript
Warning[] | undefined
```

Non-fatal warnings to surface to the consuming agent.

### `LAFSError`

*Defined in `src/types.ts:240`*

```typescript
LAFSError
```

Structured error payload returned in a failing LAFS envelope.

#### `code`

*Defined in `src/types.ts:242`*

```typescript
string
```

Stable, machine-readable error code (e.g. `"E_NOT_FOUND"`).

#### `message`

*Defined in `src/types.ts:244`*

```typescript
string
```

Human-readable description of the error.

#### `category`

*Defined in `src/types.ts:246`*

```typescript
LAFSErrorCategory
```

High-level classification of the error.

#### `retryable`

*Defined in `src/types.ts:248`*

```typescript
boolean
```

Whether the operation can be retried without modification.

#### `retryAfterMs`

*Defined in `src/types.ts:250`*

```typescript
number | null
```

Suggested delay in milliseconds before retrying, or `null` if not applicable.

#### `details`

*Defined in `src/types.ts:252`*

```typescript
Record<string, unknown>
```

Arbitrary key-value pairs with additional context about the error.

#### `agentAction`

*Defined in `src/types.ts:258`*

```typescript
LAFSAgentAction | undefined
```

Recommended action for the consuming agent to take.

#### `escalationRequired`

*Defined in `src/types.ts:264`*

```typescript
boolean | undefined
```

When `true`, the error requires human intervention or a higher-privilege agent.

#### `suggestedAction`

*Defined in `src/types.ts:270`*

```typescript
string | undefined
```

Free-text description of a suggested recovery action for the agent.

#### `docUrl`

*Defined in `src/types.ts:276`*

```typescript
string | undefined
```

URL pointing to documentation about this error code.

### `LAFSPageCursor`

*Defined in `src/types.ts:286`*

```typescript
LAFSPageCursor
```

Cursor-based pagination metadata.

#### `mode`

*Defined in `src/types.ts:288`*

```typescript
"cursor"
```

Discriminant identifying cursor-based pagination.

#### `nextCursor`

*Defined in `src/types.ts:290`*

```typescript
string | null
```

Opaque cursor for fetching the next page, or `null` when at the end.

#### `hasMore`

*Defined in `src/types.ts:292`*

```typescript
boolean
```

Whether additional pages exist beyond the current one.

#### `limit`

*Defined in `src/types.ts:298`*

```typescript
number | undefined
```

Maximum number of items per page.

#### `total`

*Defined in `src/types.ts:304`*

```typescript
number | null | undefined
```

Total number of items across all pages, or `null` if unknown.

### `LAFSPageOffset`

*Defined in `src/types.ts:315`*

```typescript
LAFSPageOffset
```

Offset-based pagination metadata.

#### `mode`

*Defined in `src/types.ts:317`*

```typescript
"offset"
```

Discriminant identifying offset-based pagination.

#### `limit`

*Defined in `src/types.ts:319`*

```typescript
number
```

Maximum number of items per page.

#### `offset`

*Defined in `src/types.ts:321`*

```typescript
number
```

Zero-based index of the first item in this page.

#### `hasMore`

*Defined in `src/types.ts:323`*

```typescript
boolean
```

Whether additional pages exist beyond the current one.

#### `total`

*Defined in `src/types.ts:329`*

```typescript
number | null | undefined
```

Total number of items across all pages, or `null` if unknown.

### `LAFSPageNone`

*Defined in `src/types.ts:339`*

```typescript
LAFSPageNone
```

Sentinel pagination mode indicating no pagination is applied.

#### `mode`

*Defined in `src/types.ts:341`*

```typescript
"none"
```

Discriminant indicating no pagination.

### `ContextLedgerEntry`

*Defined in `src/types.ts:360`*

```typescript
ContextLedgerEntry
```

A single entry in the context ledger recording one state mutation.

#### `entryId`

*Defined in `src/types.ts:362`*

```typescript
string
```

Unique identifier for this ledger entry.

#### `timestamp`

*Defined in `src/types.ts:364`*

```typescript
string
```

ISO 8601 timestamp of when the mutation occurred.

#### `operation`

*Defined in `src/types.ts:366`*

```typescript
string
```

Operation that produced this context change.

#### `contextDelta`

*Defined in `src/types.ts:368`*

```typescript
Record<string, unknown>
```

Key-value delta describing the context fields that changed.

#### `requestId`

*Defined in `src/types.ts:374`*

```typescript
string | undefined
```

Request identifier that triggered this entry, for tracing.

### `ContextLedger`

*Defined in `src/types.ts:386`*

```typescript
ContextLedger
```

Append-only ledger tracking context mutations across agent interactions.

#### `ledgerId`

*Defined in `src/types.ts:388`*

```typescript
string
```

Unique identifier for this ledger instance.

#### `version`

*Defined in `src/types.ts:390`*

```typescript
number
```

Monotonically increasing version incremented on each mutation.

#### `createdAt`

*Defined in `src/types.ts:392`*

```typescript
string
```

ISO 8601 timestamp of when the ledger was created.

#### `updatedAt`

*Defined in `src/types.ts:394`*

```typescript
string
```

ISO 8601 timestamp of the most recent mutation.

#### `entries`

*Defined in `src/types.ts:396`*

```typescript
ContextLedgerEntry[]
```

Ordered list of context mutations from oldest to newest.

#### `checksum`

*Defined in `src/types.ts:398`*

```typescript
string
```

Integrity checksum of the current ledger state.

#### `maxEntries`

*Defined in `src/types.ts:400`*

```typescript
number
```

Maximum number of entries retained before oldest entries are pruned.

### `LAFSEnvelope`

*Defined in `src/types.ts:413`*

```typescript
LAFSEnvelope
```

Top-level LAFS response envelope wrapping every operation result.

#### `$schema`

*Defined in `src/types.ts:415`*

```typescript
"https://lafs.dev/schemas/v1/envelope.schema.json"
```

JSON Schema URL identifying the envelope schema version.

#### `_meta`

*Defined in `src/types.ts:417`*

```typescript
LAFSMeta
```

Protocol and request metadata.

#### `success`

*Defined in `src/types.ts:419`*

```typescript
boolean
```

Whether the operation completed successfully.

#### `result`

*Defined in `src/types.ts:421`*

```typescript
Record<string, unknown> | Record<string, unknown>[] | null
```

Operation result payload, or `null` on failure.

#### `error`

*Defined in `src/types.ts:427`*

```typescript
LAFSError | null | undefined
```

Structured error payload, or `null` on success.

#### `page`

*Defined in `src/types.ts:433`*

```typescript
LAFSPage | null | undefined
```

Pagination metadata when the result is a paginated collection.

#### `_extensions`

*Defined in `src/types.ts:439`*

```typescript
Record<string, unknown> | undefined
```

Vendor or protocol extension data keyed by extension identifier.

### `FlagInput`

*Defined in `src/types.ts:450`*

```typescript
FlagInput
```

Input parameters for resolving the output format via flag semantics.

#### `requestedFormat`

*Defined in `src/types.ts:456`*

```typescript
"json" | "human" | undefined
```

Explicitly requested output format string.

#### `jsonFlag`

*Defined in `src/types.ts:462`*

```typescript
boolean | undefined
```

Whether the `--json` CLI flag was provided.

#### `humanFlag`

*Defined in `src/types.ts:468`*

```typescript
boolean | undefined
```

Whether the `--human` CLI flag was provided.

#### `projectDefault`

*Defined in `src/types.ts:474`*

```typescript
"json" | "human" | undefined
```

Project-level default output format from configuration.

#### `userDefault`

*Defined in `src/types.ts:480`*

```typescript
"json" | "human" | undefined
```

User-level default output format from configuration.

#### `tty`

*Defined in `src/types.ts:491`*

```typescript
boolean | undefined
```

When true, indicates the output is connected to an interactive terminal. If no explicit format flag or project/user default is set, TTY terminals default to `"human"` format while non-TTY (piped, CI, agents) defaults to `"json"` per the LAFS protocol.  CLI tools should pass `process.stdout.isTTY ?? false` here.

#### `quiet`

*Defined in `src/types.ts:497`*

```typescript
boolean | undefined
```

Suppress non-essential output for scripting. When true, only essential data is returned.

### `ConformanceReport`

*Defined in `src/types.ts:507`*

```typescript
ConformanceReport
```

Result of a LAFS conformance test run.

#### `ok`

*Defined in `src/types.ts:509`*

```typescript
boolean
```

`true` if all checks passed; `false` if any check failed.

#### `checks`

*Defined in `src/types.ts:511`*

```typescript
{ name: string; pass: boolean; detail?: string; }[]
```

Individual conformance check results.

### `TokenEstimate`

*Defined in `src/types.ts:558`*

```typescript
TokenEstimate
```

Token-count estimate attached to a budget-aware envelope.

#### `estimated`

*Defined in `src/types.ts:560`*

```typescript
number
```

Estimated token count of the envelope after any truncation.

#### `truncated`

*Defined in `src/types.ts:566`*

```typescript
boolean | undefined
```

Whether the result was truncated to fit within the budget.

#### `originalEstimate`

*Defined in `src/types.ts:572`*

```typescript
number | undefined
```

Original estimated token count before truncation, if truncation occurred.

### `LAFSMetaWithBudget`

*Defined in `src/types.ts:582`*

```typescript
LAFSMetaWithBudget
```

Extended metadata block that includes an optional token-budget estimate.

#### `_tokenEstimate`

*Defined in `src/types.ts:588`*

```typescript
TokenEstimate | undefined
```

Token-count estimate for budget tracking.

### `LAFSEnvelopeWithBudget`

*Defined in `src/types.ts:599`*

```typescript
LAFSEnvelopeWithBudget
```

LAFS envelope variant whose metadata includes token-budget estimates.

#### `_meta`

*Defined in `src/types.ts:601`*

```typescript
LAFSMetaWithBudget
```

Metadata block extended with token-budget estimation.

### `BudgetEnforcementResult`

*Defined in `src/types.ts:652`*

```typescript
BudgetEnforcementResult
```

Outcome of running budget enforcement on a LAFS envelope.

#### `envelope`

*Defined in `src/types.ts:654`*

```typescript
LAFSEnvelope
```

The envelope after budget enforcement (possibly truncated).

#### `withinBudget`

*Defined in `src/types.ts:656`*

```typescript
boolean
```

`true` if the estimated token count is within the allowed budget.

#### `estimatedTokens`

*Defined in `src/types.ts:658`*

```typescript
number
```

Estimated token count of the final envelope.

#### `budget`

*Defined in `src/types.ts:660`*

```typescript
number
```

Maximum allowed token count that was enforced.

#### `truncated`

*Defined in `src/types.ts:662`*

```typescript
boolean
```

Whether the envelope's result was truncated to fit the budget.

### `ConformanceProfiles`

*Defined in `src/conformanceProfiles.ts:18`*

```typescript
ConformanceProfiles
```

Schema for the conformance-profiles JSON file.

#### `version`

*Defined in `src/conformanceProfiles.ts:20`*

```typescript
string
```

Semantic version of the conformance-profiles schema.

#### `tiers`

*Defined in `src/conformanceProfiles.ts:22`*

```typescript
Record<ConformanceTier, string[]>
```

Mapping from tier name to the ordered list of check names in that tier.

### `RegistryCode`

*Defined in `src/errorRegistry.ts:11`*

```typescript
RegistryCode
```

A single entry in the LAFS error-code registry.

#### `code`

*Defined in `src/errorRegistry.ts:13`*

```typescript
string
```

The canonical LAFS error code (e.g., `"E_FORMAT_CONFLICT"`).

#### `category`

*Defined in `src/errorRegistry.ts:15`*

```typescript
string
```

Broad error category (e.g., `"client"`, `"server"`, `"auth"`).

#### `description`

*Defined in `src/errorRegistry.ts:17`*

```typescript
string
```

Human-readable description of when this error occurs.

#### `retryable`

*Defined in `src/errorRegistry.ts:19`*

```typescript
boolean
```

Whether the operation that produced this error is safe to retry.

#### `httpStatus`

*Defined in `src/errorRegistry.ts:21`*

```typescript
number
```

HTTP status code mapped to this error.

#### `grpcStatus`

*Defined in `src/errorRegistry.ts:23`*

```typescript
string
```

gRPC status string mapped to this error.

#### `cliExit`

*Defined in `src/errorRegistry.ts:25`*

```typescript
number
```

CLI exit code mapped to this error.

#### `agentAction`

*Defined in `src/errorRegistry.ts:30`*

```typescript
string | undefined
```

Suggested agent action from the registry (e.g., `"retry"`, `"abort"`).

#### `typeUri`

*Defined in `src/errorRegistry.ts:35`*

```typescript
string | undefined
```

RFC 9457 type URI for this error, used in Problem Details responses.

#### `docUrl`

*Defined in `src/errorRegistry.ts:40`*

```typescript
string | undefined
```

URL pointing to human-readable documentation for this error.

### `ErrorRegistry`

*Defined in `src/errorRegistry.ts:50`*

```typescript
ErrorRegistry
```

Top-level shape of the LAFS error-registry JSON file.

#### `version`

*Defined in `src/errorRegistry.ts:52`*

```typescript
string
```

Semantic version of the error-registry schema.

#### `codes`

*Defined in `src/errorRegistry.ts:54`*

```typescript
RegistryCode[]
```

All registered LAFS error codes.

### `FlagResolution`

*Defined in `src/flagSemantics.ts:25`*

```typescript
FlagResolution
```

Result of resolving output format flags.

#### `format`

*Defined in `src/flagSemantics.ts:27`*

```typescript
"json" | "human"
```

The resolved output format: `'json'` for machine-readable or `'human'` for human-readable.

#### `source`

*Defined in `src/flagSemantics.ts:29`*

```typescript
"flag" | "project" | "user" | "default"
```

Which configuration layer determined the format value.

#### `quiet`

*Defined in `src/flagSemantics.ts:31`*

```typescript
boolean
```

When true, suppress non-essential output for scripting.

### `StructuredValidationError`

*Defined in `src/validateEnvelope.ts:45`*

```typescript
StructuredValidationError
```

Structured representation of a single validation error from AJV.

#### `path`

*Defined in `src/validateEnvelope.ts:47`*

```typescript
string
```

JSON Pointer path to the property that failed validation (e.g., `"/_meta/mvi"`).

#### `keyword`

*Defined in `src/validateEnvelope.ts:49`*

```typescript
string
```

The AJV validation keyword that triggered the error (e.g., `"required"`, `"type"`).

#### `message`

*Defined in `src/validateEnvelope.ts:51`*

```typescript
string
```

Human-readable description of the validation failure.

#### `params`

*Defined in `src/validateEnvelope.ts:53`*

```typescript
Record<string, unknown>
```

Keyword-specific parameters from AJV (e.g., `{ missingProperty: "success" }`).

### `EnvelopeValidationResult`

*Defined in `src/validateEnvelope.ts:63`*

```typescript
EnvelopeValidationResult
```

Result of validating a value against the LAFS envelope JSON Schema.

#### `valid`

*Defined in `src/validateEnvelope.ts:65`*

```typescript
boolean
```

True when the input fully conforms to the envelope schema.

#### `errors`

*Defined in `src/validateEnvelope.ts:67`*

```typescript
string[]
```

Flattened human-readable error messages (empty when valid).

#### `structuredErrors`

*Defined in `src/validateEnvelope.ts:69`*

```typescript
StructuredValidationError[]
```

Structured error objects with path, keyword, and params (empty when valid).

### `EnvelopeConformanceOptions`

*Defined in `src/conformance.ts:24`*

```typescript
EnvelopeConformanceOptions
```

Options for configuring envelope conformance checking.

#### `tier`

*Defined in `src/conformance.ts:29`*

```typescript
ConformanceTier | undefined
```

The conformance tier to filter checks by.

### `ComplianceIssue`

*Defined in `src/compliance.ts:26`*

```typescript
ComplianceIssue
```

Describes a single compliance failure detected during enforcement.

#### `stage`

*Defined in `src/compliance.ts:28`*

```typescript
ComplianceStage
```

The pipeline stage that produced this issue.

#### `message`

*Defined in `src/compliance.ts:30`*

```typescript
string
```

A short, human-readable description of the failure.

#### `detail`

*Defined in `src/compliance.ts:35`*

```typescript
string | undefined
```

Additional diagnostic information about the failure.

### `EnforceComplianceOptions`

*Defined in `src/compliance.ts:45`*

```typescript
EnforceComplianceOptions
```

Options controlling which compliance stages are executed.

#### `checkConformance`

*Defined in `src/compliance.ts:50`*

```typescript
boolean | undefined
```

Whether to run envelope conformance checks after schema validation.

#### `checkFlags`

*Defined in `src/compliance.ts:55`*

```typescript
boolean | undefined
```

Whether to run flag conformance checks.

#### `flags`

*Defined in `src/compliance.ts:60`*

```typescript
FlagInput | undefined
```

Flag input to validate when `checkFlags` is enabled.

#### `requireJsonOutput`

*Defined in `src/compliance.ts:65`*

```typescript
boolean | undefined
```

When true, asserts that the resolved output format is JSON.

### `ComplianceResult`

*Defined in `src/compliance.ts:75`*

```typescript
ComplianceResult
```

Aggregated result of a full LAFS compliance run.

#### `ok`

*Defined in `src/compliance.ts:77`*

```typescript
boolean
```

True when every executed stage passes with zero issues.

#### `envelope`

*Defined in `src/compliance.ts:82`*

```typescript
LAFSEnvelope | undefined
```

The parsed envelope, present only when schema validation succeeds.

#### `validation`

*Defined in `src/compliance.ts:84`*

```typescript
EnvelopeValidationResult
```

Schema validation result from AJV.

#### `envelopeConformance`

*Defined in `src/compliance.ts:89`*

```typescript
ConformanceReport | undefined
```

Envelope conformance report, present when `EnforceComplianceOptions.checkConformance` is true.

#### `flagConformance`

*Defined in `src/compliance.ts:94`*

```typescript
ConformanceReport | undefined
```

Flag conformance report, present when `EnforceComplianceOptions.checkFlags` is true.

#### `issues`

*Defined in `src/compliance.ts:96`*

```typescript
ComplianceIssue[]
```

All issues collected across every executed stage.

### `DeprecationEntry`

*Defined in `src/deprecationRegistry.ts:23`*

```typescript
DeprecationEntry
```

A single deprecation rule in the registry.

**Examples**

```typescript
const entry: DeprecationEntry = {
  id: "meta-mvi-boolean",
  code: "W_DEPRECATED_META_MVI_BOOLEAN",
  message: "_meta.mvi boolean values are deprecated",
  deprecated: "1.0.0",
  replacement: "Use _meta.mvi as one of: minimal|standard|full|custom",
  removeBy: "2.0.0",
  detector: (env) => typeof env._meta.mvi === "boolean",
};
```

#### `id`

*Defined in `src/deprecationRegistry.ts:25`*

```typescript
string
```

Unique identifier for this deprecation rule

#### `code`

*Defined in `src/deprecationRegistry.ts:27`*

```typescript
string
```

Warning code emitted when detected

#### `message`

*Defined in `src/deprecationRegistry.ts:29`*

```typescript
string
```

Human-readable deprecation message

#### `deprecated`

*Defined in `src/deprecationRegistry.ts:31`*

```typescript
string
```

Version where the feature was deprecated

#### `replacement`

*Defined in `src/deprecationRegistry.ts:36`*

```typescript
string | undefined
```

Suggested replacement or migration path.

#### `removeBy`

*Defined in `src/deprecationRegistry.ts:38`*

```typescript
string
```

Version where the deprecated feature will be removed

#### `detector`

*Defined in `src/deprecationRegistry.ts:40`*

```typescript
(envelope: LAFSEnvelope) => boolean
```

Predicate that returns `true` when the envelope uses the deprecated feature

### `LafsExtensionParams`

*Defined in `src/a2a/extensions.ts:40`*

```typescript
LafsExtensionParams
```

LAFS extension parameters declared in Agent Card

#### `supportsContextLedger`

*Defined in `src/a2a/extensions.ts:42`*

```typescript
boolean
```

Whether the agent supports context ledger tracking

#### `supportsTokenBudgets`

*Defined in `src/a2a/extensions.ts:44`*

```typescript
boolean
```

Whether the agent supports token budget enforcement

#### `envelopeSchema`

*Defined in `src/a2a/extensions.ts:46`*

```typescript
string
```

URL of the JSON Schema for the LAFS envelope

#### `kind`

*Defined in `src/a2a/extensions.ts:51`*

```typescript
ExtensionKind | undefined
```

Classification of the extension's behavior.

### `ExtensionNegotiationResult`

*Defined in `src/a2a/extensions.ts:65`*

```typescript
ExtensionNegotiationResult
```

Result of extension negotiation between client and agent

#### `requested`

*Defined in `src/a2a/extensions.ts:67`*

```typescript
string[]
```

URIs requested by the client

#### `activated`

*Defined in `src/a2a/extensions.ts:69`*

```typescript
string[]
```

URIs that matched agent-declared extensions

#### `unsupported`

*Defined in `src/a2a/extensions.ts:71`*

```typescript
string[]
```

Requested URIs not declared by the agent (ignored per spec)

#### `missingRequired`

*Defined in `src/a2a/extensions.ts:73`*

```typescript
string[]
```

Agent-required URIs not present in client request

#### `activatedByKind`

*Defined in `src/a2a/extensions.ts:75`*

```typescript
Partial<Record<ExtensionKind, string[]>>
```

Activated extensions grouped by declared kind (when provided)

### `BuildLafsExtensionOptions`

*Defined in `src/a2a/extensions.ts:191`*

```typescript
BuildLafsExtensionOptions
```

Options for building the LAFS extension declaration

#### `required`

*Defined in `src/a2a/extensions.ts:196`*

```typescript
boolean | undefined
```

Whether the LAFS extension is required for all requests.

#### `supportsContextLedger`

*Defined in `src/a2a/extensions.ts:201`*

```typescript
boolean | undefined
```

Whether the agent supports context ledger tracking.

#### `supportsTokenBudgets`

*Defined in `src/a2a/extensions.ts:206`*

```typescript
boolean | undefined
```

Whether the agent supports token budget enforcement.

#### `envelopeSchema`

*Defined in `src/a2a/extensions.ts:211`*

```typescript
string | undefined
```

URL of the JSON Schema for the LAFS envelope.

### `BuildExtensionOptions`

*Defined in `src/a2a/extensions.ts:246`*

```typescript
BuildExtensionOptions
```

Options for building a generic A2A extension declaration

#### `uri`

*Defined in `src/a2a/extensions.ts:248`*

```typescript
string
```

Canonical URI identifying the extension

#### `description`

*Defined in `src/a2a/extensions.ts:250`*

```typescript
string
```

Human-readable description of what the extension provides

#### `required`

*Defined in `src/a2a/extensions.ts:255`*

```typescript
boolean | undefined
```

Whether the extension is required for all requests.

#### `kind`

*Defined in `src/a2a/extensions.ts:257`*

```typescript
ExtensionKind
```

Classification of the extension's behavior

#### `params`

*Defined in `src/a2a/extensions.ts:262`*

```typescript
Record<string, unknown> | undefined
```

Additional parameters to include in the extension declaration.

### `ExtensionNegotiationMiddlewareOptions`

*Defined in `src/a2a/extensions.ts:460`*

```typescript
ExtensionNegotiationMiddlewareOptions
```

Options for the extension negotiation middleware

#### `extensions`

*Defined in `src/a2a/extensions.ts:462`*

```typescript
AgentExtension[]
```

Agent-declared extensions to negotiate against

#### `enforceRequired`

*Defined in `src/a2a/extensions.ts:467`*

```typescript
boolean | undefined
```

Return 400 if required extensions are missing.

### `AgentProvider`

*Defined in `src/discovery.ts:44`*

```typescript
AgentProvider
```

A2A Agent Provider information.

**Examples**

```typescript
const provider: AgentProvider = {
  url: "https://example.com",
  organization: "Acme Corp"
};
```

#### `url`

*Defined in `src/discovery.ts:46`*

```typescript
string
```

Organization URL (must be a valid HTTPS URL)

#### `organization`

*Defined in `src/discovery.ts:48`*

```typescript
string
```

Organization name (human-readable label)

### `AgentCapabilities`

*Defined in `src/discovery.ts:68`*

```typescript
AgentCapabilities
```

A2A Agent Capabilities.

**Examples**

```typescript
const caps: AgentCapabilities = {
  streaming: true,
  pushNotifications: false,
  extendedAgentCard: false,
  extensions: []
};
```

#### `streaming`

*Defined in `src/discovery.ts:73`*

```typescript
boolean | undefined
```

Supports streaming responses.

#### `pushNotifications`

*Defined in `src/discovery.ts:78`*

```typescript
boolean | undefined
```

Supports push notifications.

#### `extendedAgentCard`

*Defined in `src/discovery.ts:83`*

```typescript
boolean | undefined
```

Supports extended agent card.

#### `extensions`

*Defined in `src/discovery.ts:88`*

```typescript
AgentExtension[] | undefined
```

Supported extensions declared by this agent.

### `AgentExtension`

*Defined in `src/discovery.ts:108`*

```typescript
AgentExtension
```

A2A Agent Extension declaration.

**Examples**

```typescript
const ext: AgentExtension = {
  uri: "https://lafs.dev/extensions/v1/lafs",
  description: "LAFS envelope protocol",
  required: false,
  params: { supportsContextLedger: true }
};
```

#### `uri`

*Defined in `src/discovery.ts:110`*

```typescript
string
```

Extension URI (globally-unique identifier)

#### `description`

*Defined in `src/discovery.ts:112`*

```typescript
string
```

Human-readable description of what the extension provides

#### `required`

*Defined in `src/discovery.ts:114`*

```typescript
boolean
```

Whether the extension is required for interoperability

#### `params`

*Defined in `src/discovery.ts:119`*

```typescript
Record<string, unknown> | undefined
```

Extension-specific parameters.

### `AgentSkill`

*Defined in `src/discovery.ts:140`*

```typescript
AgentSkill
```

A2A Agent Skill.

**Examples**

```typescript
const skill: AgentSkill = {
  id: "envelope-processor",
  name: "Envelope Processor",
  description: "Validates and processes LAFS envelopes",
  tags: ["lafs", "envelope"],
  examples: ["Validate this envelope"],
};
```

#### `id`

*Defined in `src/discovery.ts:142`*

```typescript
string
```

Skill unique identifier (kebab-case recommended)

#### `name`

*Defined in `src/discovery.ts:144`*

```typescript
string
```

Human-readable display name

#### `description`

*Defined in `src/discovery.ts:146`*

```typescript
string
```

Detailed description of what the skill does

#### `tags`

*Defined in `src/discovery.ts:148`*

```typescript
string[]
```

Keywords/tags for discovery and categorization

#### `examples`

*Defined in `src/discovery.ts:153`*

```typescript
string[] | undefined
```

Example prompts that demonstrate typical usage.

#### `inputModes`

*Defined in `src/discovery.ts:158`*

```typescript
string[] | undefined
```

Supported input modes (overrides agent-level `AgentCard.defaultInputModes`).

#### `outputModes`

*Defined in `src/discovery.ts:163`*

```typescript
string[] | undefined
```

Supported output modes (overrides agent-level `AgentCard.defaultOutputModes`).

### `SecurityScheme`

*Defined in `src/discovery.ts:182`*

```typescript
SecurityScheme
```

Security scheme for authentication (OpenAPI 3.0 style).

**Examples**

```typescript
const scheme: SecurityScheme = {
  type: "http",
  scheme: "bearer",
  bearerFormat: "JWT",
};
```

#### `type`

*Defined in `src/discovery.ts:184`*

```typescript
"http" | "apiKey" | "oauth2" | "openIdConnect"
```

Authentication type per OpenAPI 3.0

#### `description`

*Defined in `src/discovery.ts:189`*

```typescript
string | undefined
```

Human-readable description of the scheme.

#### `scheme`

*Defined in `src/discovery.ts:194`*

```typescript
string | undefined
```

HTTP auth scheme name (e.g., `"bearer"`).

#### `bearerFormat`

*Defined in `src/discovery.ts:199`*

```typescript
string | undefined
```

Bearer token format hint (e.g., `"JWT"`).

### `AgentCard`

*Defined in `src/discovery.ts:225`*

```typescript
AgentCard
```

A2A v1.0 Agent Card - Standard format for agent discovery.

**Examples**

```typescript
const card: AgentCard = {
  name: "my-agent",
  description: "A LAFS-compliant agent",
  version: "1.0.0",
  url: "https://api.example.com",
  capabilities: { streaming: false },
  defaultInputModes: ["application/json"],
  defaultOutputModes: ["application/json"],
  skills: [],
};
```

#### `$schema`

*Defined in `src/discovery.ts:230`*

```typescript
string | undefined
```

JSON Schema URL for validation.

#### `name`

*Defined in `src/discovery.ts:232`*

```typescript
string
```

Human-readable agent name

#### `description`

*Defined in `src/discovery.ts:234`*

```typescript
string
```

Detailed description of agent capabilities

#### `version`

*Defined in `src/discovery.ts:236`*

```typescript
string
```

Agent version (SemVer)

#### `url`

*Defined in `src/discovery.ts:238`*

```typescript
string
```

Base URL for A2A endpoints

#### `provider`

*Defined in `src/discovery.ts:243`*

```typescript
AgentProvider | undefined
```

Service provider information.

#### `capabilities`

*Defined in `src/discovery.ts:245`*

```typescript
AgentCapabilities
```

Agent capabilities declaration

#### `defaultInputModes`

*Defined in `src/discovery.ts:247`*

```typescript
string[]
```

Supported input content types (MIME types)

#### `defaultOutputModes`

*Defined in `src/discovery.ts:249`*

```typescript
string[]
```

Supported output content types (MIME types)

#### `skills`

*Defined in `src/discovery.ts:251`*

```typescript
AgentSkill[]
```

Agent skills/capabilities for discovery

#### `securitySchemes`

*Defined in `src/discovery.ts:256`*

```typescript
Record<string, SecurityScheme> | undefined
```

Security authentication schemes (keyed by scheme name).

#### `security`

*Defined in `src/discovery.ts:261`*

```typescript
Record<string, string[]>[] | undefined
```

Required security scheme references (OpenAPI 3.0 format).

#### `documentationUrl`

*Defined in `src/discovery.ts:266`*

```typescript
string | undefined
```

Documentation URL for the agent.

#### `iconUrl`

*Defined in `src/discovery.ts:271`*

```typescript
string | undefined
```

Icon URL for the agent.

### `Capability`

> **Deprecated**: Use `AgentSkill` instead.

*Defined in `src/discovery.ts:287`*

```typescript
Capability
```

Legacy capability descriptor.

#### `name`

*Defined in `src/discovery.ts:289`*

```typescript
string
```

Capability name

#### `version`

*Defined in `src/discovery.ts:291`*

```typescript
string
```

Capability version

#### `description`

*Defined in `src/discovery.ts:296`*

```typescript
string | undefined
```

Human-readable description.

#### `operations`

*Defined in `src/discovery.ts:298`*

```typescript
string[]
```

Supported operations

#### `optional`

*Defined in `src/discovery.ts:303`*

```typescript
boolean | undefined
```

Whether this capability is optional.

### `ServiceConfig`

> **Deprecated**: Use `AgentCard` instead.

*Defined in `src/discovery.ts:315`*

```typescript
ServiceConfig
```

Legacy service configuration.

#### `name`

*Defined in `src/discovery.ts:317`*

```typescript
string
```

Service name

#### `version`

*Defined in `src/discovery.ts:319`*

```typescript
string
```

Service version

#### `description`

*Defined in `src/discovery.ts:324`*

```typescript
string | undefined
```

Human-readable description.

### `EndpointConfig`

> **Deprecated**: Will be removed in v2.0.0.

*Defined in `src/discovery.ts:335`*

```typescript
EndpointConfig
```

Legacy endpoint configuration.

#### `envelope`

*Defined in `src/discovery.ts:337`*

```typescript
string
```

Envelope endpoint URL

#### `context`

*Defined in `src/discovery.ts:342`*

```typescript
string | undefined
```

Context endpoint URL.

#### `discovery`

*Defined in `src/discovery.ts:344`*

```typescript
string
```

Discovery endpoint URL

### `DiscoveryDocument`

> **Deprecated**: Use `AgentCard` instead.

*Defined in `src/discovery.ts:356`*

```typescript
DiscoveryDocument
```

Legacy discovery document format.

#### `$schema`

*Defined in `src/discovery.ts:358`*

```typescript
string
```

JSON Schema URL

#### `lafs_version`

*Defined in `src/discovery.ts:360`*

```typescript
string
```

LAFS specification version

#### `service`

*Defined in `src/discovery.ts:362`*

```typescript
ServiceConfig
```

Service configuration

#### `capabilities`

*Defined in `src/discovery.ts:364`*

```typescript
Capability[]
```

Declared capabilities

#### `endpoints`

*Defined in `src/discovery.ts:366`*

```typescript
EndpointConfig
```

Endpoint configuration

### `DiscoveryConfig`

*Defined in `src/discovery.ts:397`*

```typescript
DiscoveryConfig
```

Configuration for the discovery middleware (A2A v1.0 format).

**Examples**

```typescript
const config: DiscoveryConfig = {
  agent: {
    name: "my-agent",
    description: "Example",
    version: "1.0.0",
    url: "https://api.example.com",
    capabilities: { streaming: false },
    defaultInputModes: ["application/json"],
    defaultOutputModes: ["application/json"],
    skills: [],
  },
  cacheMaxAge: 3600,
};
```

#### `agent`

*Defined in `src/discovery.ts:402`*

```typescript
Omit<AgentCard, "$schema"> | undefined
```

Agent information (required for A2A v1.0; omit only with legacy `service`).

#### `baseUrl`

*Defined in `src/discovery.ts:407`*

```typescript
string | undefined
```

Base URL for constructing absolute URLs.

#### `cacheMaxAge`

*Defined in `src/discovery.ts:412`*

```typescript
number | undefined
```

Cache duration in seconds.

#### `schemaUrl`

*Defined in `src/discovery.ts:417`*

```typescript
string | undefined
```

Schema URL override.

#### `headers`

*Defined in `src/discovery.ts:422`*

```typescript
Record<string, string> | undefined
```

Optional custom response headers.

#### `autoIncludeLafsExtension`

*Defined in `src/discovery.ts:428`*

```typescript
boolean | { required?: boolean; supportsContextLedger?: boolean; supportsTokenBudgets?: boolean; } | undefined
```

Automatically include LAFS as an A2A extension in Agent Card. Pass `true` for defaults, or an object to customize parameters.

#### `service`

> **Deprecated**: Use `agent` instead.

*Defined in `src/discovery.ts:440`*

```typescript
ServiceConfig | undefined
```

Legacy service configuration.

#### `capabilities`

> **Deprecated**: Use `agent.skills` instead.

*Defined in `src/discovery.ts:446`*

```typescript
Capability[] | undefined
```

Legacy capabilities list.

#### `endpoints`

> **Deprecated**: Use `agent.url` and individual endpoints.

*Defined in `src/discovery.ts:452`*

```typescript
{ envelope: string; context?: string; discovery?: string; } | undefined
```

Legacy endpoint URLs.

#### `lafsVersion`

> **Deprecated**: Use `agent.version` instead.

*Defined in `src/discovery.ts:462`*

```typescript
string | undefined
```

Legacy LAFS version override.

### `DiscoveryMiddlewareOptions`

*Defined in `src/discovery.ts:480`*

```typescript
DiscoveryMiddlewareOptions
```

Discovery middleware options.

**Examples**

```typescript
const options: DiscoveryMiddlewareOptions = {
  path: "/.well-known/agent-card.json",
  enableEtag: true,
};
```

#### `path`

*Defined in `src/discovery.ts:485`*

```typescript
string | undefined
```

Primary path to serve Agent Card.

#### `legacyPath`

> **Deprecated**: Will be removed in v2.0.0.

*Defined in `src/discovery.ts:491`*

```typescript
string | undefined
```

Legacy path for backward compatibility.

#### `enableLegacyPath`

*Defined in `src/discovery.ts:496`*

```typescript
boolean | undefined
```

Enable legacy path support.

#### `enableHead`

*Defined in `src/discovery.ts:501`*

```typescript
boolean | undefined
```

Enable HEAD requests.

#### `enableEtag`

*Defined in `src/discovery.ts:506`*

```typescript
boolean | undefined
```

Enable ETag caching.

### `CreateEnvelopeMetaInput`

*Defined in `src/envelope.ts:41`*

```typescript
CreateEnvelopeMetaInput
```

Input for constructing the `_meta` block of a LAFS envelope.

#### `operation`

*Defined in `src/envelope.ts:43`*

```typescript
string
```

Dot-delimited operation identifier (e.g. `"tasks.list"`).

#### `requestId`

*Defined in `src/envelope.ts:45`*

```typescript
string
```

Unique identifier for correlating this request/response pair.

#### `transport`

*Defined in `src/envelope.ts:51`*

```typescript
LAFSTransport | undefined
```

Transport protocol for this envelope.

#### `specVersion`

*Defined in `src/envelope.ts:57`*

```typescript
string | undefined
```

LAFS spec version to stamp on the envelope.

#### `schemaVersion`

*Defined in `src/envelope.ts:63`*

```typescript
string | undefined
```

JSON Schema version to stamp on the envelope.

#### `timestamp`

*Defined in `src/envelope.ts:69`*

```typescript
string | undefined
```

ISO 8601 timestamp; auto-generated when omitted.

#### `strict`

*Defined in `src/envelope.ts:75`*

```typescript
boolean | undefined
```

Whether strict schema validation should be applied.

#### `mvi`

*Defined in `src/envelope.ts:81`*

```typescript
boolean | MVILevel | undefined
```

MVI level as a string, or `true` for `'minimal'` / `false` for `'standard'`.

#### `contextVersion`

*Defined in `src/envelope.ts:87`*

```typescript
number | undefined
```

Context ledger version the caller is operating against.

#### `sessionId`

*Defined in `src/envelope.ts:93`*

```typescript
string | undefined
```

Session identifier for multi-step workflow correlation.

#### `warnings`

*Defined in `src/envelope.ts:99`*

```typescript
import("./types.js").Warning[] | undefined
```

Non-fatal warnings to attach to the envelope metadata.

### `CreateEnvelopeSuccessInput`

*Defined in `src/envelope.ts:109`*

```typescript
CreateEnvelopeSuccessInput
```

Input for creating a successful LAFS envelope.

#### `success`

*Defined in `src/envelope.ts:111`*

```typescript
true
```

Discriminant marking this as a success input.

#### `result`

*Defined in `src/envelope.ts:113`*

```typescript
Record<string, unknown> | Record<string, unknown>[] | null
```

Operation result payload (object, array, or `null`).

#### `page`

*Defined in `src/envelope.ts:119`*

```typescript
import("./types.js").LAFSPage | null | undefined
```

Pagination metadata for collection results.

#### `error`

*Defined in `src/envelope.ts:125`*

```typescript
null | undefined
```

Must be `null` for success inputs; exists for type uniformity with error inputs.

#### `_extensions`

*Defined in `src/envelope.ts:131`*

```typescript
Record<string, unknown> | undefined
```

Vendor or protocol extension data.

#### `meta`

*Defined in `src/envelope.ts:133`*

```typescript
CreateEnvelopeMetaInput
```

Metadata input for constructing the envelope's `_meta` block.

### `CreateEnvelopeErrorInput`

*Defined in `src/envelope.ts:144`*

```typescript
CreateEnvelopeErrorInput
```

Input for creating a failing LAFS envelope.

#### `success`

*Defined in `src/envelope.ts:146`*

```typescript
false
```

Discriminant marking this as an error input.

#### `error`

*Defined in `src/envelope.ts:148`*

```typescript
Partial<LAFSError> & Pick<LAFSError, "code" | "message">
```

Partial error object; at minimum `code` and `message` are required.

#### `result`

*Defined in `src/envelope.ts:160`*

```typescript
Record<string, unknown> | Record<string, unknown>[] | null | undefined
```

Optional result payload to include alongside the error. For validation tools (linters, type checkers), the actionable data (what to fix, suggested fixes) IS the result even when the operation "fails". Setting this allows agents to access both the error metadata and the detailed result in a single response.  When omitted or null, the envelope emits `result: null` (default behavior).

#### `page`

*Defined in `src/envelope.ts:166`*

```typescript
import("./types.js").LAFSPage | null | undefined
```

Pagination metadata, if applicable even in error scenarios.

#### `_extensions`

*Defined in `src/envelope.ts:172`*

```typescript
Record<string, unknown> | undefined
```

Vendor or protocol extension data.

#### `meta`

*Defined in `src/envelope.ts:174`*

```typescript
CreateEnvelopeMetaInput
```

Metadata input for constructing the envelope's `_meta` block.

### `ParseLafsResponseOptions`

*Defined in `src/envelope.ts:449`*

```typescript
ParseLafsResponseOptions
```

Options for `parseLafsResponse`.

#### `requireRegisteredErrorCode`

*Defined in `src/envelope.ts:456`*

```typescript
boolean | undefined
```

When `true`, unregistered error codes cause an additional `Error` to be thrown instead of the normal `LafsError`.

### `FieldExtractionInput`

*Defined in `src/fieldExtraction.ts:26`*

```typescript
FieldExtractionInput
```

Input flags for the field extraction layer.

#### `fieldFlag`

*Defined in `src/fieldExtraction.ts:31`*

```typescript
string | undefined
```

`--field <name>`: extract a single field as plain text, discarding the envelope.

#### `fieldsFlag`

*Defined in `src/fieldExtraction.ts:37`*

```typescript
string | string[] | undefined
```

`--fields <a,b,c>`: filter result to these fields while preserving the envelope. Accepts a comma-separated string or an array of field names.

#### `mviFlag`

*Defined in `src/fieldExtraction.ts:43`*

```typescript
string | undefined
```

`--mvi <level>`: requested envelope verbosity level (client-requestable levels only). The `'custom'` level is server-set and not valid here.

### `FieldExtractionResolution`

*Defined in `src/fieldExtraction.ts:53`*

```typescript
FieldExtractionResolution
```

Resolved field extraction configuration.

#### `field`

*Defined in `src/fieldExtraction.ts:58`*

```typescript
string | undefined
```

When set, extract this field as plain text, discarding the envelope.

#### `fields`

*Defined in `src/fieldExtraction.ts:63`*

```typescript
string[] | undefined
```

When set, filter the result to these fields (envelope is preserved).

#### `mvi`

*Defined in `src/fieldExtraction.ts:65`*

```typescript
MVILevel
```

Resolved MVI level. Falls back to `'minimal'` when no valid flag is provided.

#### `mviSource`

*Defined in `src/fieldExtraction.ts:67`*

```typescript
"flag" | "default"
```

Which input determined the mvi value: `'flag'` when mviFlag was valid, `'default'` otherwise.

#### `expectsCustomMvi`

*Defined in `src/fieldExtraction.ts:73`*

```typescript
boolean
```

True when `fields` are requested, indicating the server SHOULD set `_meta.mvi = 'custom'` in the response per section 9.1. Separate from the client-resolved mvi level.

### `UnifiedFlagInput`

*Defined in `src/flagResolver.ts:22`*

```typescript
UnifiedFlagInput
```

Combined input for both format and field extraction layers.

#### `human`

*Defined in `src/flagResolver.ts:27`*

```typescript
boolean | undefined
```

Request human-readable output (`--human` flag).

#### `json`

*Defined in `src/flagResolver.ts:32`*

```typescript
boolean | undefined
```

Request JSON output (`--json` flag).

#### `quiet`

*Defined in `src/flagResolver.ts:37`*

```typescript
boolean | undefined
```

Suppress non-essential output for scripting (`--quiet` flag).

#### `requestedFormat`

*Defined in `src/flagResolver.ts:42`*

```typescript
"json" | "human" | undefined
```

Explicit format override, taking highest precedence in the format layer.

#### `projectDefault`

*Defined in `src/flagResolver.ts:47`*

```typescript
"json" | "human" | undefined
```

Project-level default format from configuration.

#### `userDefault`

*Defined in `src/flagResolver.ts:52`*

```typescript
"json" | "human" | undefined
```

User-level default format from configuration.

#### `tty`

*Defined in `src/flagResolver.ts:59`*

```typescript
boolean | undefined
```

TTY detection hint. When true, defaults to human format if no explicit format flag or project/user default is set. CLI tools should pass `process.stdout.isTTY ?? false`.

#### `field`

*Defined in `src/flagResolver.ts:64`*

```typescript
string | undefined
```

Extract a single field as plain text, discarding the envelope (`--field` flag).

#### `fields`

*Defined in `src/flagResolver.ts:70`*

```typescript
string | string[] | undefined
```

Filter result to these fields while preserving the envelope (`--fields` flag). Accepts a comma-separated string or an array of field names.

#### `mvi`

*Defined in `src/flagResolver.ts:75`*

```typescript
string | undefined
```

Requested MVI verbosity level (`--mvi` flag).

### `UnifiedFlagResolution`

*Defined in `src/flagResolver.ts:85`*

```typescript
UnifiedFlagResolution
```

Combined resolution result with cross-layer warnings.

#### `format`

*Defined in `src/flagResolver.ts:87`*

```typescript
FlagResolution
```

Resolved format layer from the format precedence chain.

#### `fields`

*Defined in `src/flagResolver.ts:89`*

```typescript
FieldExtractionResolution
```

Resolved field extraction layer from field/fields/mvi flags.

#### `warnings`

*Defined in `src/flagResolver.ts:91`*

```typescript
string[]
```

Warnings for cross-layer interactions (non-fatal, informational only).

### `LafsA2AConfig`

*Defined in `src/a2a/bridge.ts:53`*

```typescript
LafsA2AConfig
```

Configuration for LAFS A2A integration.

#### `defaultBudget`

*Defined in `src/a2a/bridge.ts:58`*

```typescript
{ maxTokens?: number; maxItems?: number; maxBytes?: number; } | undefined
```

Default token budget for all operations.

#### `envelopeResponses`

*Defined in `src/a2a/bridge.ts:70`*

```typescript
boolean | undefined
```

Whether to automatically wrap responses in LAFS envelopes.

#### `protocolVersion`

*Defined in `src/a2a/bridge.ts:75`*

```typescript
string | undefined
```

A2A protocol version to use.

#### `defaultExtensions`

*Defined in `src/a2a/bridge.ts:80`*

```typescript
string[] | undefined
```

Extension URIs to activate for all requests.

### `LafsSendMessageParams`

*Defined in `src/a2a/bridge.ts:91`*

```typescript
LafsSendMessageParams
```

Request parameters for sending messages.

#### `message`

*Defined in `src/a2a/bridge.ts:93`*

```typescript
{ role: "user" | "agent"; parts: Part[]; metadata?: Record<string, unknown>; }
```

Message content including role, parts, and optional metadata

#### `configuration`

*Defined in `src/a2a/bridge.ts:108`*

```typescript
MessageSendConfiguration | undefined
```

A2A configuration for this request.

#### `budget`

*Defined in `src/a2a/bridge.ts:113`*

```typescript
{ maxTokens?: number; maxItems?: number; maxBytes?: number; } | undefined
```

Token budget override for this request.

#### `extensions`

*Defined in `src/a2a/bridge.ts:125`*

```typescript
string[] | undefined
```

Extensions to activate for this request.

#### `contextId`

*Defined in `src/a2a/bridge.ts:130`*

```typescript
string | undefined
```

Context ID for multi-turn conversations.

#### `taskId`

*Defined in `src/a2a/bridge.ts:135`*

```typescript
string | undefined
```

Task ID for continuing existing task.

### `CreateTaskOptions`

*Defined in `src/a2a/task-lifecycle.ts:259`*

```typescript
CreateTaskOptions
```

Options for creating a new task

#### `contextId`

*Defined in `src/a2a/task-lifecycle.ts:264`*

```typescript
string | undefined
```

Context ID for grouping related tasks.

#### `metadata`

*Defined in `src/a2a/task-lifecycle.ts:269`*

```typescript
Record<string, unknown> | undefined
```

Arbitrary metadata to attach to the task.

#### `referenceTaskIds`

*Defined in `src/a2a/task-lifecycle.ts:274`*

```typescript
string[] | undefined
```

IDs of parent tasks this task refines or follows up on.

#### `parallelFollowUp`

*Defined in `src/a2a/task-lifecycle.ts:279`*

```typescript
boolean | undefined
```

Whether this follow-up can run in parallel with its references.

### `ListTasksOptions`

*Defined in `src/a2a/task-lifecycle.ts:283`*

```typescript
ListTasksOptions
```

Options for listing tasks

#### `contextId`

*Defined in `src/a2a/task-lifecycle.ts:288`*

```typescript
string | undefined
```

Filter tasks by context ID.

#### `state`

*Defined in `src/a2a/task-lifecycle.ts:293`*

```typescript
TaskState | undefined
```

Filter tasks by current state.

#### `limit`

*Defined in `src/a2a/task-lifecycle.ts:298`*

```typescript
number | undefined
```

Maximum number of tasks to return.

#### `pageToken`

*Defined in `src/a2a/task-lifecycle.ts:303`*

```typescript
string | undefined
```

Cursor token for pagination (last seen task ID).

### `ListTasksResult`

*Defined in `src/a2a/task-lifecycle.ts:307`*

```typescript
ListTasksResult
```

Paginated result from listTasks

#### `tasks`

*Defined in `src/a2a/task-lifecycle.ts:309`*

```typescript
Task[]
```

Array of tasks matching the query

#### `nextPageToken`

*Defined in `src/a2a/task-lifecycle.ts:314`*

```typescript
string | undefined
```

Token for fetching the next page of results.

### `StreamIteratorOptions`

*Defined in `src/a2a/streaming.ts:176`*

```typescript
StreamIteratorOptions
```

Options for the stream task events async iterator

#### `timeoutMs`

*Defined in `src/a2a/streaming.ts:181`*

```typescript
number | undefined
```

Timeout in milliseconds before the iterator yields control.

### `PushNotificationDeliveryResult`

*Defined in `src/a2a/streaming.ts:353`*

```typescript
PushNotificationDeliveryResult
```

Result of delivering a push notification to a single webhook

#### `configId`

*Defined in `src/a2a/streaming.ts:355`*

```typescript
string
```

Identifier of the config that was dispatched to

#### `ok`

*Defined in `src/a2a/streaming.ts:357`*

```typescript
boolean
```

Whether the delivery succeeded

#### `status`

*Defined in `src/a2a/streaming.ts:362`*

```typescript
number | undefined
```

HTTP status code from the webhook response.

#### `error`

*Defined in `src/a2a/streaming.ts:367`*

```typescript
string | undefined
```

Error message if delivery failed.

### `JsonRpcRequest`

*Defined in `src/a2a/bindings/jsonrpc.ts:112`*

```typescript
JsonRpcRequest
```

A JSON-RPC 2.0 request object.

#### `jsonrpc`

*Defined in `src/a2a/bindings/jsonrpc.ts:114`*

```typescript
"2.0"
```

JSON-RPC protocol version, always `"2.0"`

#### `id`

*Defined in `src/a2a/bindings/jsonrpc.ts:116`*

```typescript
string | number
```

Client-assigned request identifier

#### `method`

*Defined in `src/a2a/bindings/jsonrpc.ts:118`*

```typescript
string
```

The RPC method name to invoke

#### `params`

*Defined in `src/a2a/bindings/jsonrpc.ts:123`*

```typescript
Record<string, unknown> | undefined
```

Named parameters for the method call.

### `JsonRpcResponse`

*Defined in `src/a2a/bindings/jsonrpc.ts:133`*

```typescript
JsonRpcResponse
```

A JSON-RPC 2.0 success response object.

#### `jsonrpc`

*Defined in `src/a2a/bindings/jsonrpc.ts:135`*

```typescript
"2.0"
```

JSON-RPC protocol version, always `"2.0"`

#### `id`

*Defined in `src/a2a/bindings/jsonrpc.ts:137`*

```typescript
string | number | null
```

Identifier matching the originating request, or `null` for notifications

#### `result`

*Defined in `src/a2a/bindings/jsonrpc.ts:139`*

```typescript
unknown
```

The return value of the invoked method

### `JsonRpcErrorResponse`

*Defined in `src/a2a/bindings/jsonrpc.ts:149`*

```typescript
JsonRpcErrorResponse
```

A JSON-RPC 2.0 error response object.

#### `jsonrpc`

*Defined in `src/a2a/bindings/jsonrpc.ts:151`*

```typescript
"2.0"
```

JSON-RPC protocol version, always `"2.0"`

#### `id`

*Defined in `src/a2a/bindings/jsonrpc.ts:153`*

```typescript
string | number | null
```

Identifier matching the originating request, or `null` for notifications

#### `error`

*Defined in `src/a2a/bindings/jsonrpc.ts:155`*

```typescript
{ code: number; message: string; data?: Record<string, unknown>; }
```

Error descriptor containing code, message, and optional data

### `GrpcServiceMethod`

*Defined in `src/a2a/bindings/grpc.ts:132`*

```typescript
GrpcServiceMethod
```

Descriptor for a single gRPC service method.

#### `request`

*Defined in `src/a2a/bindings/grpc.ts:134`*

```typescript
string
```

Protobuf request message type name

#### `response`

*Defined in `src/a2a/bindings/grpc.ts:136`*

```typescript
string
```

Protobuf response message type name

#### `streaming`

*Defined in `src/a2a/bindings/grpc.ts:138`*

```typescript
boolean
```

Whether the method uses server-side streaming

### `GrpcStatus`

*Defined in `src/a2a/bindings/grpc.ts:237`*

```typescript
GrpcStatus
```

gRPC Status object for A2A errors.

#### `code`

*Defined in `src/a2a/bindings/grpc.ts:239`*

```typescript
GrpcStatusCode
```

Numeric gRPC status code

#### `message`

*Defined in `src/a2a/bindings/grpc.ts:241`*

```typescript
string
```

Human-readable error message

#### `details`

*Defined in `src/a2a/bindings/grpc.ts:246`*

```typescript
GrpcErrorInfo[] | undefined
```

Structured error details (typically `google.rpc.ErrorInfo` entries).

### `GrpcErrorInfo`

*Defined in `src/a2a/bindings/grpc.ts:256`*

```typescript
GrpcErrorInfo
```

Equivalent of `google.rpc.ErrorInfo` for structured gRPC error details.

#### `reason`

*Defined in `src/a2a/bindings/grpc.ts:258`*

```typescript
string
```

UPPER\_SNAKE\_CASE reason string identifying the error

#### `domain`

*Defined in `src/a2a/bindings/grpc.ts:260`*

```typescript
string
```

Error domain (e.g. `"a2a-protocol.org"`)

#### `metadata`

*Defined in `src/a2a/bindings/grpc.ts:265`*

```typescript
Record<string, string> | undefined
```

Additional key-value metadata for the error.

### `ProblemDetails`

*Defined in `src/a2a/bindings/http.ts:111`*

```typescript
ProblemDetails
```

RFC 9457 Problem Details object.

#### `type`

*Defined in `src/a2a/bindings/http.ts:113`*

```typescript
string
```

URI reference identifying the problem type

#### `title`

*Defined in `src/a2a/bindings/http.ts:115`*

```typescript
string
```

Short human-readable summary of the problem

#### `status`

*Defined in `src/a2a/bindings/http.ts:117`*

```typescript
number
```

HTTP status code for this occurrence

#### `detail`

*Defined in `src/a2a/bindings/http.ts:119`*

```typescript
string
```

Human-readable explanation specific to this occurrence

### `ListTasksQueryParams`

*Defined in `src/a2a/bindings/http.ts:246`*

```typescript
ListTasksQueryParams
```

Parsed query parameters for the ListTasks endpoint.

#### `contextId`

*Defined in `src/a2a/bindings/http.ts:251`*

```typescript
string | undefined
```

Filter tasks by context identifier.

#### `state`

*Defined in `src/a2a/bindings/http.ts:256`*

```typescript
string | undefined
```

Filter tasks by state (e.g. `"submitted"`, `"working"`).

#### `limit`

*Defined in `src/a2a/bindings/http.ts:261`*

```typescript
number | undefined
```

Maximum number of tasks to return.

#### `pageToken`

*Defined in `src/a2a/bindings/http.ts:266`*

```typescript
string | undefined
```

Pagination token from a previous response.

### `ErrorCodeMapping`

*Defined in `src/a2a/bindings/index.ts:29`*

```typescript
ErrorCodeMapping
```

Complete error code mapping across all three transports.

#### `jsonRpcCode`

*Defined in `src/a2a/bindings/index.ts:31`*

```typescript
number
```

JSON-RPC numeric error code (e.g. `-32001`)

#### `httpStatus`

*Defined in `src/a2a/bindings/index.ts:33`*

```typescript
number
```

HTTP response status code (e.g. `404`)

#### `httpTypeUri`

*Defined in `src/a2a/bindings/index.ts:35`*

```typescript
string
```

RFC 9457 Problem Details type URI

#### `grpcStatus`

*Defined in `src/a2a/bindings/index.ts:37`*

```typescript
string
```

gRPC status name (e.g. `"NOT_FOUND"`)

#### `grpcCode`

*Defined in `src/a2a/bindings/index.ts:39`*

```typescript
number
```

gRPC numeric status code (e.g. `5`)

### `CircuitBreakerConfig`

*Defined in `src/circuit-breaker/index.ts:20`*

```typescript
CircuitBreakerConfig
```

Configuration options for a `CircuitBreaker` instance.

#### `name`

*Defined in `src/circuit-breaker/index.ts:22`*

```typescript
string
```

Unique identifier for this circuit breaker, used in log messages and metrics.

#### `failureThreshold`

*Defined in `src/circuit-breaker/index.ts:28`*

```typescript
number | undefined
```

Number of failures required to trip the circuit from CLOSED to OPEN.

#### `resetTimeout`

*Defined in `src/circuit-breaker/index.ts:34`*

```typescript
number | undefined
```

Milliseconds to wait before transitioning from OPEN to HALF\_OPEN.

#### `halfOpenMaxCalls`

*Defined in `src/circuit-breaker/index.ts:40`*

```typescript
number | undefined
```

Maximum number of trial calls allowed while in the HALF\_OPEN state.

#### `successThreshold`

*Defined in `src/circuit-breaker/index.ts:46`*

```typescript
number | undefined
```

Consecutive successes required in HALF\_OPEN to close the circuit.

### `CircuitBreakerMetrics`

*Defined in `src/circuit-breaker/index.ts:50`*

```typescript
CircuitBreakerMetrics
```

Snapshot of runtime metrics for a `CircuitBreaker`.

#### `state`

*Defined in `src/circuit-breaker/index.ts:52`*

```typescript
CircuitState
```

Current state of the circuit breaker.

#### `failures`

*Defined in `src/circuit-breaker/index.ts:55`*

```typescript
number
```

Total number of recorded failures since the last reset.

#### `successes`

*Defined in `src/circuit-breaker/index.ts:58`*

```typescript
number
```

Total number of recorded successes since the last reset.

#### `lastFailureTime`

*Defined in `src/circuit-breaker/index.ts:64`*

```typescript
Date | undefined
```

Timestamp of the most recent failure, if any.

#### `consecutiveSuccesses`

*Defined in `src/circuit-breaker/index.ts:67`*

```typescript
number
```

Number of consecutive successes since the last failure.

#### `totalCalls`

*Defined in `src/circuit-breaker/index.ts:70`*

```typescript
number
```

Total number of calls made through this circuit breaker.

### `HealthCheckConfig`

*Defined in `src/health/index.ts:20`*

```typescript
HealthCheckConfig
```

Configuration for the `healthCheck` middleware.

#### `path`

*Defined in `src/health/index.ts:25`*

```typescript
string | undefined
```

URL path at which the health endpoint is mounted.

#### `checks`

*Defined in `src/health/index.ts:31`*

```typescript
HealthCheckFunction[] | undefined
```

Array of custom health check functions to run on each request.

### `HealthCheckResult`

*Defined in `src/health/index.ts:44`*

```typescript
HealthCheckResult
```

Result of an individual health check.

#### `name`

*Defined in `src/health/index.ts:46`*

```typescript
string
```

Human-readable name identifying this check.

#### `status`

*Defined in `src/health/index.ts:49`*

```typescript
"error" | "ok" | "warning"
```

Outcome status of the check.

#### `message`

*Defined in `src/health/index.ts:55`*

```typescript
string | undefined
```

Optional descriptive message providing additional detail.

#### `duration`

*Defined in `src/health/index.ts:61`*

```typescript
number | undefined
```

Execution duration of the check in milliseconds.

### `HealthStatus`

*Defined in `src/health/index.ts:65`*

```typescript
HealthStatus
```

Aggregated health status returned by the health endpoint.

#### `status`

*Defined in `src/health/index.ts:67`*

```typescript
"healthy" | "degraded" | "unhealthy"
```

Overall service health derived from individual check results.

#### `timestamp`

*Defined in `src/health/index.ts:70`*

```typescript
string
```

ISO-8601 timestamp of when the health check was performed.

#### `version`

*Defined in `src/health/index.ts:73`*

```typescript
string
```

LAFS package version.

#### `uptime`

*Defined in `src/health/index.ts:76`*

```typescript
number
```

Server uptime in seconds since the middleware was initialised.

#### `checks`

*Defined in `src/health/index.ts:79`*

```typescript
HealthCheckResult[]
```

Individual check results.

### `LafsProblemDetails`

*Defined in `src/problemDetails.ts:29`*

```typescript
LafsProblemDetails
```

RFC 9457 Problem Details with LAFS extensions.

**Examples**

```typescript
const pd: LafsProblemDetails = {
  type: "https://lafs.dev/errors/v1/E_VALIDATION",
  title: "E_VALIDATION",
  status: 400,
  detail: "Invalid input",
  retryable: false,
};
```

#### `type`

*Defined in `src/problemDetails.ts:31`*

```typescript
string
```

URI reference identifying the problem type

#### `title`

*Defined in `src/problemDetails.ts:33`*

```typescript
string
```

Short human-readable summary (typically the error code)

#### `status`

*Defined in `src/problemDetails.ts:35`*

```typescript
number
```

HTTP status code for this error

#### `detail`

*Defined in `src/problemDetails.ts:37`*

```typescript
string
```

Human-readable explanation of the specific occurrence

#### `instance`

*Defined in `src/problemDetails.ts:42`*

```typescript
string | undefined
```

URI reference identifying the specific occurrence (typically the request ID).

#### `retryable`

*Defined in `src/problemDetails.ts:44`*

```typescript
boolean
```

Whether the operation that caused this error can be retried

#### `agentAction`

*Defined in `src/problemDetails.ts:49`*

```typescript
string | undefined
```

Recommended agent action (e.g., `"retry"`, `"escalate"`).

#### `retryAfterMs`

*Defined in `src/problemDetails.ts:54`*

```typescript
number | undefined
```

Suggested delay in milliseconds before retrying.

#### `escalationRequired`

*Defined in `src/problemDetails.ts:59`*

```typescript
boolean | undefined
```

Whether the error requires human escalation.

#### `suggestedAction`

*Defined in `src/problemDetails.ts:64`*

```typescript
string | undefined
```

Human-readable suggestion for resolving the error.

#### `docUrl`

*Defined in `src/problemDetails.ts:69`*

```typescript
string | undefined
```

Documentation URL for more information.

### `GracefulShutdownConfig`

*Defined in `src/shutdown/index.ts:12`*

```typescript
GracefulShutdownConfig
```

Configuration for the `gracefulShutdown` handler.

#### `timeout`

*Defined in `src/shutdown/index.ts:17`*

```typescript
number | undefined
```

Maximum time in milliseconds to wait for in-flight requests before forcing exit.

#### `signals`

*Defined in `src/shutdown/index.ts:23`*

```typescript
NodeJS.Signals[] | undefined
```

POSIX signals that trigger a graceful shutdown.

#### `onShutdown`

*Defined in `src/shutdown/index.ts:29`*

```typescript
(() => Promise<void> | void) | undefined
```

Callback invoked at the start of shutdown, before the server stops accepting connections.

#### `onClose`

*Defined in `src/shutdown/index.ts:35`*

```typescript
(() => Promise<void> | void) | undefined
```

Callback invoked after all connections have closed (or the timeout elapsed).

### `ShutdownState`

*Defined in `src/shutdown/index.ts:39`*

```typescript
ShutdownState
```

Snapshot of the current shutdown state.

#### `isShuttingDown`

*Defined in `src/shutdown/index.ts:41`*

```typescript
boolean
```

Whether a shutdown sequence is currently in progress.

#### `activeConnections`

*Defined in `src/shutdown/index.ts:44`*

```typescript
number
```

Number of TCP connections still open.

#### `shutdownStartTime`

*Defined in `src/shutdown/index.ts:50`*

```typescript
Date | undefined
```

Timestamp when the shutdown sequence began.

## Types

### `LAFSTransport`

*Defined in `src/types.ts:9`*

```typescript
LAFSTransport
```

Transport protocol used to deliver a LAFS envelope.

### `LAFSErrorCategory`

*Defined in `src/types.ts:19`*

```typescript
LAFSErrorCategory
```

Classification category for a LAFS error.

### `MVILevel`

*Defined in `src/types.ts:73`*

```typescript
MVILevel
```

Minimum Viable Information level controlling envelope verbosity.

### `LAFSAgentAction`

*Defined in `src/types.ts:176`*

```typescript
LAFSAgentAction
```

Recommended action an LLM agent should take in response to an error.

### `LAFSPage`

*Defined in `src/types.ts:351`*

```typescript
LAFSPage
```

Discriminated union of all supported pagination modes.

### `BudgetEnforcementOptions`

*Defined in `src/types.ts:533`*

```typescript
BudgetEnforcementOptions
```

Options controlling token-budget enforcement behaviour.

### `MiddlewareFunction`

*Defined in `src/types.ts:614`*

```typescript
MiddlewareFunction
```

Middleware function that transforms a LAFS envelope.

**Parameters**

- `envelope` — The envelope to transform.

**Returns**: The transformed envelope, optionally as a `Promise`.

### `NextFunction`

*Defined in `src/types.ts:626`*

```typescript
NextFunction
```

Continuation function passed to `BudgetMiddleware` to invoke the next middleware in the chain.

**Returns**: The envelope produced by the downstream handler.

### `BudgetMiddleware`

*Defined in `src/types.ts:640`*

```typescript
BudgetMiddleware
```

Middleware function for token-budget enforcement with chain delegation.

**Parameters**

- `envelope` — The envelope entering this middleware stage.
- `next` — Continuation to invoke the next middleware in the chain.

**Returns**: The (possibly transformed) envelope.

### `ConformanceTier`

*Defined in `src/conformanceProfiles.ts:10`*

```typescript
ConformanceTier
```

Named conformance tier indicating the breadth of checks applied.

### `TransportMapping`

*Defined in `src/errorRegistry.ts:64`*

```typescript
TransportMapping
```

A transport-specific status value resolved from the error registry.

### `ComplianceStage`

*Defined in `src/compliance.ts:17`*

```typescript
ComplianceStage
```

Identifies which stage of the compliance pipeline produced an issue.

### `ComplianceMiddleware`

*Defined in `src/compliance.ts:298`*

```typescript
ComplianceMiddleware
```

Middleware signature for intercepting LAFS envelopes in a pipeline.

**Parameters**

- `envelope` — The envelope entering this middleware.
- `next` — Callback that invokes the next middleware or terminal handler.

**Returns**: The (possibly transformed) envelope to pass upstream.

### `ExtensionKind`

*Defined in `src/a2a/extensions.ts:61`*

```typescript
ExtensionKind
```

Classification of an A2A extension's behavior.

### `CreateEnvelopeInput`

*Defined in `src/envelope.ts:184`*

```typescript
CreateEnvelopeInput
```

Discriminated union of success and error inputs for `createEnvelope`.

### `TaskStreamEvent`

*Defined in `src/a2a/streaming.ts:24`*

```typescript
TaskStreamEvent
```

Union type of task stream events emitted by the event bus.

### `PushTransport`

*Defined in `src/a2a/streaming.ts:377`*

```typescript
PushTransport
```

Transport function for sending HTTP requests to push-notification webhooks.

### `JsonRpcMethod`

*Defined in `src/a2a/bindings/jsonrpc.ts:45`*

```typescript
JsonRpcMethod
```

Union of all valid JSON-RPC method string values from `JSONRPC_METHODS`

### `A2AErrorType`

*Defined in `src/a2a/bindings/jsonrpc.ts:100`*

```typescript
"TaskNotFound" | "TaskNotCancelable" | "PushNotificationNotSupported" | "UnsupportedOperation" | "ContentTypeNotSupported" | "InvalidAgentResponse" | "AuthenticatedExtendedCardNotConfigured" | "ExtensionSupportRequired" | "VersionNotSupported"
```

Union of A2A error type key names from `JSONRPC_A2A_ERROR_CODES`

### `GrpcStatusCode`

*Defined in `src/a2a/bindings/grpc.ts:62`*

```typescript
GrpcStatusCode
```

Numeric gRPC status code value (0-16)

### `GrpcStatusName`

*Defined in `src/a2a/bindings/grpc.ts:64`*

```typescript
"NOT_FOUND" | "INTERNAL" | "INVALID_ARGUMENT" | "ABORTED" | "RESOURCE_EXHAUSTED" | "UNAVAILABLE" | "FAILED_PRECONDITION" | "OK" | "CANCELLED" | "UNKNOWN" | "DEADLINE_EXCEEDED" | "ALREADY_EXISTS" | "PERMISSION_DENIED" | "OUT_OF_RANGE" | "UNIMPLEMENTED" | "DATA_LOSS" | "UNAUTHENTICATED"
```

String name of a gRPC status code (e.g. `"OK"`, `"NOT_FOUND"`)

### `HttpEndpoint`

*Defined in `src/a2a/bindings/http.ts:51`*

```typescript
HttpEndpoint
```

Union of all HTTP endpoint descriptor objects from `HTTP_ENDPOINTS`

### `CircuitState`

*Defined in `src/circuit-breaker/index.ts:17`*

```typescript
CircuitState
```

Represents the three possible states of a circuit breaker.

### `HealthCheckFunction`

*Defined in `src/health/index.ts:41`*

```typescript
HealthCheckFunction
```

A function that performs a single health check.

## Variables

### `defaultEstimator`

*Defined in `src/tokenEstimator.ts:355`*

```typescript
TokenEstimator
```

Global token estimator instance with default settings.

### `MVI_LEVELS`

*Defined in `src/types.ts:89`*

```typescript
ReadonlySet<MVILevel>
```

Immutable set of all valid `MVILevel` values.

**Examples**

```ts
if (MVI_LEVELS.has(input)) {
  // input is a valid MVILevel
}
```

### `AGENT_ACTIONS`

*Defined in `src/types.ts:199`*

```typescript
ReadonlySet<LAFSAgentAction>
```

Immutable set of all valid `LAFSAgentAction` values.

**Examples**

```ts
if (AGENT_ACTIONS.has(action)) {
  // action is a valid LAFSAgentAction
}
```

### `LAFS_EXTENSION_URI`

*Defined in `src/a2a/extensions.ts:24`*

```typescript
"https://lafs.dev/extensions/envelope/v1"
```

Canonical LAFS extension URI

### `A2A_EXTENSIONS_HEADER`

*Defined in `src/a2a/extensions.ts:27`*

```typescript
"A2A-Extensions"
```

Canonical A2A Extensions header per spec Section 3.2.6

### `LAFS_SCHEMA_URL`

*Defined in `src/envelope.ts:32`*

```typescript
"https://lafs.dev/schemas/v1/envelope.schema.json"
```

Canonical JSON Schema URL for the LAFS v1 envelope.

**Examples**

```ts
import { LAFS_SCHEMA_URL } from '@cleocode/lafs';
console.log(LAFS_SCHEMA_URL);
// => 'https://lafs.dev/schemas/v1/envelope.schema.json'
```

### `CATEGORY_ACTION_MAP`

*Defined in `src/envelope.ts:245`*

```typescript
Record<LAFSErrorCategory, LAFSAgentAction>
```

Default agent action for each error category.

**Examples**

```ts
import { CATEGORY_ACTION_MAP } from '@cleocode/lafs';
const action = CATEGORY_ACTION_MAP['RATE_LIMIT']; // => 'wait'
```

### `TERMINAL_STATES`

*Defined in `src/a2a/task-lifecycle.ts:25`*

```typescript
ReadonlySet<TaskState>
```

States from which no further transitions are possible

### `INTERRUPTED_STATES`

*Defined in `src/a2a/task-lifecycle.ts:33`*

```typescript
ReadonlySet<TaskState>
```

States where the task is paused awaiting external input

### `VALID_TRANSITIONS`

*Defined in `src/a2a/task-lifecycle.ts:39`*

```typescript
ReadonlyMap<TaskState, ReadonlySet<TaskState>>
```

Valid state transitions (adjacency map). Terminal states have empty outgoing sets.

### `JSONRPC_METHODS`

*Defined in `src/a2a/bindings/jsonrpc.ts:19`*

```typescript
{ readonly SendMessage: "message/send"; readonly SendStreamingMessage: "message/stream"; readonly GetTask: "tasks/get"; readonly ListTasks: "tasks/list"; readonly CancelTask: "tasks/cancel"; readonly SubscribeToTask: "tasks/resubscribe"; readonly SetTaskPushNotificationConfig: "tasks/pushNotificationConfig/set"; readonly GetTaskPushNotificationConfig: "tasks/pushNotificationConfig/get"; readonly ListTaskPushNotificationConfig: "tasks/pushNotificationConfig/list"; readonly DeleteTaskPushNotificationConfig: "tasks/pushNotificationConfig/delete"; readonly GetExtendedAgentCard: "agent/getAuthenticatedExtendedCard"; }
```

All JSON-RPC method names defined by the A2A protocol.

### `JSONRPC_STANDARD_ERROR_CODES`

*Defined in `src/a2a/bindings/jsonrpc.ts:58`*

```typescript
{ readonly ParseError: -32700; readonly InvalidRequest: -32600; readonly MethodNotFound: -32601; readonly InvalidParams: -32602; readonly InternalError: -32603; }
```

Standard JSON-RPC 2.0 error codes.

### `JSONRPC_A2A_ERROR_CODES`

*Defined in `src/a2a/bindings/jsonrpc.ts:78`*

```typescript
{ readonly TaskNotFound: -32001; readonly TaskNotCancelable: -32002; readonly PushNotificationNotSupported: -32003; readonly UnsupportedOperation: -32004; readonly ContentTypeNotSupported: -32005; readonly InvalidAgentResponse: -32006; readonly AuthenticatedExtendedCardNotConfigured: -32007; readonly ExtensionSupportRequired: -32008; readonly VersionNotSupported: -32009; }
```

A2A-specific error codes (-32001 through -32009).

### `GRPC_STATUS_CODE`

*Defined in `src/a2a/bindings/grpc.ts:24`*

```typescript
{ readonly OK: 0; readonly CANCELLED: 1; readonly UNKNOWN: 2; readonly INVALID_ARGUMENT: 3; readonly DEADLINE_EXCEEDED: 4; readonly NOT_FOUND: 5; readonly ALREADY_EXISTS: 6; readonly PERMISSION_DENIED: 7; readonly RESOURCE_EXHAUSTED: 8; readonly FAILED_PRECONDITION: 9; readonly ABORTED: 10; readonly OUT_OF_RANGE: 11; readonly UNIMPLEMENTED: 12; readonly INTERNAL: 13; readonly UNAVAILABLE: 14; readonly DATA_LOSS: 15; readonly UNAUTHENTICATED: 16; }
```

Standard gRPC status codes (numeric values 0-16).

### `A2A_GRPC_STATUS_CODES`

*Defined in `src/a2a/bindings/grpc.ts:77`*

```typescript
Record<"TaskNotFound" | "TaskNotCancelable" | "PushNotificationNotSupported" | "UnsupportedOperation" | "ContentTypeNotSupported" | "InvalidAgentResponse" | "AuthenticatedExtendedCardNotConfigured" | "ExtensionSupportRequired" | "VersionNotSupported", "NOT_FOUND" | "INTERNAL" | "INVALID_ARGUMENT" | "ABORTED" | "RESOURCE_EXHAUSTED" | "UNAVAILABLE" | "FAILED_PRECONDITION" | "OK" | "CANCELLED" | "UNKNOWN" | "DEADLINE_EXCEEDED" | "ALREADY_EXISTS" | "PERMISSION_DENIED" | "OUT_OF_RANGE" | "UNIMPLEMENTED" | "DATA_LOSS" | "UNAUTHENTICATED">
```

Maps A2A error types to gRPC status names.

### `A2A_GRPC_ERROR_REASONS`

*Defined in `src/a2a/bindings/grpc.ts:100`*

```typescript
Record<"TaskNotFound" | "TaskNotCancelable" | "PushNotificationNotSupported" | "UnsupportedOperation" | "ContentTypeNotSupported" | "InvalidAgentResponse" | "AuthenticatedExtendedCardNotConfigured" | "ExtensionSupportRequired" | "VersionNotSupported", string>
```

UPPER\_SNAKE\_CASE error reasons without "Error" suffix.

### `A2A_GRPC_ERROR_DOMAIN`

*Defined in `src/a2a/bindings/grpc.ts:119`*

```typescript
"a2a-protocol.org"
```

Error domain for A2A gRPC errors.

### `GRPC_SERVICE_METHODS`

*Defined in `src/a2a/bindings/grpc.ts:148`*

```typescript
Record<string, GrpcServiceMethod>
```

gRPC service method definitions for the A2A protocol.

### `GRPC_METADATA_VERSION_KEY`

*Defined in `src/a2a/bindings/grpc.ts:216`*

```typescript
"a2a-version"
```

gRPC metadata key for A2A protocol version.

### `GRPC_METADATA_EXTENSIONS_KEY`

*Defined in `src/a2a/bindings/grpc.ts:224`*

```typescript
"a2a-extensions"
```

gRPC metadata key for activated A2A extensions.

### `HTTP_ENDPOINTS`

*Defined in `src/a2a/bindings/http.ts:22`*

```typescript
{ readonly SendMessage: { readonly method: "POST"; readonly path: "/message:send"; }; readonly SendStreamingMessage: { readonly method: "POST"; readonly path: "/message:stream"; }; readonly GetTask: { readonly method: "GET"; readonly path: "/tasks/:id"; }; readonly ListTasks: { readonly method: "GET"; readonly path: "/tasks"; }; readonly CancelTask: { readonly method: "POST"; readonly path: "/tasks/:id:cancel"; }; readonly SubscribeToTask: { readonly method: "GET"; readonly path: "/tasks/:id:subscribe"; }; readonly SetTaskPushNotificationConfig: { readonly method: "POST"; readonly path: "/tasks/:id/pushNotificationConfig"; }; readonly GetTaskPushNotificationConfig: { readonly method: "GET"; readonly path: "/tasks/:id/pushNotificationConfig"; }; readonly ListTaskPushNotificationConfig: { readonly method: "GET"; readonly path: "/tasks/:id/pushNotificationConfig:list"; }; readonly DeleteTaskPushNotificationConfig: { readonly method: "DELETE"; readonly path: "/tasks/:id/pushNotificationConfig/:configId"; }; readonly GetExtendedAgentCard: { readonly method: "GET"; readonly path: "/agent/authenticatedExtendedCard"; }; }
```

HTTP+JSON endpoint definitions for each A2A operation.

### `A2A_HTTP_STATUS_CODES`

*Defined in `src/a2a/bindings/http.ts:64`*

```typescript
Record<"TaskNotFound" | "TaskNotCancelable" | "PushNotificationNotSupported" | "UnsupportedOperation" | "ContentTypeNotSupported" | "InvalidAgentResponse" | "AuthenticatedExtendedCardNotConfigured" | "ExtensionSupportRequired" | "VersionNotSupported", number>
```

Maps A2A error types to HTTP status codes.

### `A2A_ERROR_TYPE_URIS`

*Defined in `src/a2a/bindings/http.ts:87`*

```typescript
Record<"TaskNotFound" | "TaskNotCancelable" | "PushNotificationNotSupported" | "UnsupportedOperation" | "ContentTypeNotSupported" | "InvalidAgentResponse" | "AuthenticatedExtendedCardNotConfigured" | "ExtensionSupportRequired" | "VersionNotSupported", string>
```

RFC 9457 Problem Details type URIs for A2A errors.

### `A2A_ERROR_MAPPINGS`

*Defined in `src/a2a/bindings/index.ts:79`*

```typescript
ReadonlyMap<"TaskNotFound" | "TaskNotCancelable" | "PushNotificationNotSupported" | "UnsupportedOperation" | "ContentTypeNotSupported" | "InvalidAgentResponse" | "AuthenticatedExtendedCardNotConfigured" | "ExtensionSupportRequired" | "VersionNotSupported", ErrorCodeMapping>
```

Precomputed cross-binding error mapping for all 9 A2A error types.

### `SUPPORTED_A2A_VERSIONS`

*Defined in `src/a2a/bindings/index.ts:120`*

```typescript
readonly ["1.0"]
```

Supported A2A protocol versions.

### `DEFAULT_A2A_VERSION`

*Defined in `src/a2a/bindings/index.ts:128`*

```typescript
"1.0"
```

Default A2A protocol version used when none is requested.

### `PROBLEM_DETAILS_CONTENT_TYPE`

*Defined in `src/problemDetails.ts:138`*

```typescript
"application/problem+json"
```

Content-Type for RFC 9457 Problem Details responses.
