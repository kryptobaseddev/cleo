---
title: "cleo inject"
description: "Inject/prepare tasks for TodoWrite format (session start)"
icon: "arrows-rotate"
---

{/* AUTO-GENERATED by generate-command-docs.sh - DO NOT EDIT MANUALLY */}


# cleo inject

Inject/prepare tasks for TodoWrite format (session start)

<Tip>Usually called via 'sync --inject'</Tip>

<Info>**Agent Relevance**: High - Frequently used by LLM agents</Info>

## Synopsis

```bash
cleo inject [OPTIONS]
```

## Options

| Option | Description |
|--------|-------------|
| `--format` | Output format: `text`, `json` |
| `--quiet` | Suppress output, return only essential data |
| `--dry-run` | Preview changes without applying |
| `--focused-only` | Only process focused task |

<Note>This command supports JSON output via `--json` flag for LLM agent integration.</Note>

## Examples

<CodeGroup>

```bash Basic Usage
cleo inject
```

```bash JSON Output
cleo inject --json
```

</CodeGroup>

## Exit Codes

| Code | Meaning | Recoverable |
|------|---------|:-----------:|
| `0` | Success | No |
| `3` | File operation failure | No |

## See Also

- [Commands Index](/commands/commands) - Full command reference
