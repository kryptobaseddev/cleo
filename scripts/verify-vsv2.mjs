#!/usr/bin/env node
// Placeholder verifier for EPIC Verifier Substrate v2 (TBD task ID)
// Will be backfilled with AC-derived stub via `cleo verify backfill <taskId>`
console.error(
  'Placeholder verifier — run `cleo verify backfill <taskId>` to generate real checks from AC',
);
process.exit(1);
