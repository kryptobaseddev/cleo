---
title: "cleo sequence"
description: "Inspect and manage task ID sequence (show/check/repair)"
icon: "hashtag"
---

{/* AUTO-GENERATED by generate-command-docs.sh - DO NOT EDIT MANUALLY */}


# cleo sequence

Inspect and manage task ID sequence (show/check/repair)

<Tip>Part of ID Integrity System - verify sequence before adding tasks</Tip>

<Info>**Agent Relevance**: High - Frequently used by LLM agents</Info>

## Synopsis

```bash
cleo sequence <show|check|repair> [OPTIONS]
```

## Subcommands

| Subcommand | Description |
|------------|-------------|
| `show` | Run `cleo sequence show --help` for details |
| `check` | Run `cleo sequence check --help` for details |
| `repair` | Run `cleo sequence repair --help` for details |

## Options

| Option | Description |
|--------|-------------|
| `--format` | Output format: `text`, `json` |
| `--json` | Force JSON output |
| `--human` | Force human-readable output |
| `--quiet` | Suppress output, return only essential data |
| `--verbose` | Show detailed output |

<Note>This command supports JSON output via `--json` flag for LLM agent integration.</Note>

## Examples

<CodeGroup>

```bash Basic Usage
cleo sequence
```

```bash JSON Output
cleo sequence --json
```

</CodeGroup>

## Exit Codes

| Code | Meaning | Recoverable |
|------|---------|:-----------:|
| `0` | Success | No |
| `4` | Resource not found | No |
| `6` | Validation error | No |
| `20` | Checksum mismatch | **Yes** |
| `22` | ID collision | **Yes** |

## See Also

- [Commands Index](/commands/commands) - Full command reference
- [validate](/commands/validate) - Check integrity
- [backup](/commands/backup) - Create backups
