---
id: t10306
tasks: [T10306]
kind: docs
summary: "amend ADR-068 charter — 12-row inventory + nested-nexus flag"
---

docs(T10306): amend ADR-068 charter — 12-row inventory + nested-nexus flag + signaldock-project historical

ADR-068 (CLEO Database Charter) is amended in place to reflect the verified
database substrate. T10305 shipped the runtime SSoT
(`packages/core/src/store/db-inventory.json`, re-exported as `DB_INVENTORY`
from `@cleocode/contracts`) with 12 entries; this changeset closes the
governance-overlay drift by extending ADR-068 from the original 9 rows to
the verified 12.

Concretely:

- Adds rows 10 (`skills.db` global), 11 (`global-brain`), and 12
  (`global-tasks`) — the three audit-discovered entries beyond ADR-068's
  original 9.
- Marks row 4 (`signaldock-project`) historical post-ADR-037 (merged into
  `conduit.db`); preserves the row for legacy backup detection.
- Adds a "Known structural bugs" subsection flagging the nested
  `~/.local/share/cleo/nexus/{nexus,signaldock}.db` duplicates for
  resolution under T10321 (E4-T2 BAN vs ADOPT ADR).
- Clarifies blob+manifest unification — they are ONE file at
  `<projectRoot>/.cleo/blobs/manifest.db` (row 7), not two.
- Cross-references the runtime SSoT in the inventory preamble so future
  readers know the governance overlay (this ADR) and the machine-readable
  SSoT (`DB_INVENTORY`) MUST stay in lockstep.
- Adds an `## Amendment History` section at the bottom for future
  in-place amendments to be traceable.
- Updates the `openCleoDb` role list (drops `signaldock-project`, adds
  `skills`).

Status remains `accepted` — this is an amendment, not a supersession.
Provenance cites T10305 + the audit research doc
`sg-brain-db-resilience-deep-audit-2026-05-23`.

Saga: T10281. Epic: T10282.
