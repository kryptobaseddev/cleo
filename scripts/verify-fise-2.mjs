#!/usr/bin/env node
// Placeholder verifier for FISE-2 CANT validateSpawnRequest (TBD task ID)
// Backfill via `cleo verify backfill <taskId>` once task ID assigned
console.error('Placeholder verifier — run `cleo verify backfill <taskId>` to generate real checks from AC');
process.exit(1);
